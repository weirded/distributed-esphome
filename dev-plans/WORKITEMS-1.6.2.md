# Work Items — 1.6.2

Theme: **Install paths users can trust.** 1.6.2 started as "Honest Gold" — a release that would walk the remaining Bronze+Silver+Gold quality-scale rules to `done`/`exempt` and flip the manifest tier. Mid-cycle, real-user-blocked install-path bugs surfaced (#82 add-on install from fresh HAOS, #83 standalone Docker Web UI auth regression, #84 `wifi.domain` dropped from OTA address resolution). Those got priority. The rest of the Gold work — the quality-scale rule walk, the TEST-AUDIT blind-spot closure, the hassfest tier-flip gate — re-homed to `WORKITEMS-1.6.3.md` under the original "Honest Gold" theme; 1.6.2 now ships the install-path unblocks plus the hardening that shipped alongside them (bundle discipline, ESPHome-authoritative config resolution, standalone-HA independence, worker log visibility).

Read first, in order: `dev-plans/WORKITEMS-1.6.3.md` (the deferred Honest Gold work — some of 1.6.2's prose still references it), `dev-plans/archive/WORKITEMS-1.6.1.md` §SS, §QS, §PR (where the deferrals originated).

Scope rule: **install-path unblocks + the hardening already shipped alongside them + worker log visibility (WL.*, real user pain).** Every open workitem either (a) fixes a shipped-but-broken path (install, auth, OTA address), (b) adds a regression guard for the paths we just fixed, (c) honestly restates a user-facing claim the code doesn't back, or (d) closes a first-setup operational gap users have hit. Everything else is 1.6.3.

---

## TP — Truth-in-claims (undo the pretend)

1.6.1 publishes three claims the code doesn't fully back: AppArmor-confined, silver-tier integration, all-tests-honest. This section pulls reality up to the claim where cheap, rewrites the claim to match reality where not, and leaves nothing quietly asymmetric. 1.6.2 handles the AppArmor claim (TP.1 + TP.2) and the CHANGELOG retrospective (TP.4). The quality-scale tier-flip portion of the original TP.3 moved to 1.6.3 along with the QS.* items it depends on; TP.3 here is scoped to the two doc-cleanup clauses that stand on their own.

- [ ] **TP.1 Rewrite SECURITY.md's SS.1 paragraph.** Current wording reads as a hardening improvement; the profile is attached-but-permissive (`file, capability, network, signal, dbus, unix, mount, pivot_root, ptrace,` all unqualified = functionally unconfined). Lift the honest caveat from `WORKITEMS-1.6.1.md` SS.1 ("permissive-but-attached buys the security-star credit today; tightening is tracked as future work") up into SECURITY.md proper. Don't ship a second release claiming SS.1 confined anything.

- [ ] **TP.2 Actually tighten the AppArmor profile (SS.1b).** Minimum viable narrow rules that don't break PlatformIO/penv/git/pip: `deny /etc/shadow* rw,`, `deny /run/secrets/** rw,`, `deny /proc/*/mem rw,`, `deny @{PROC}/sys/kernel/** w,`. Drop unqualified `ptrace,` (replace with `deny ptrace,` or remove — nothing in our stack ptraces anything). Verify with hass-4 smoke 17/17 green. Unblocks TP.1 from reading as "SECURITY.md now admits SS.1 is a no-op" → "SECURITY.md describes the first-pass confinement that landed." Prereq: CI.2 lands first so narrowing attempts don't burn deploy cycles to find breakage.

- [ ] **TP.3 `quality_scale.yaml` doc cleanup (the two clauses that don't require a tier flip).** (a) The header comment still says "manifest.json's `quality_scale: silver`" — the manifest now says `bronze`; fix the header. (b) `runtime-data` says "migration … is planned when the HA minimum is bumped past 2024.11" — the HA minimum *is* past 2024.11 now; the deferral expired. Restate the real reason it's deferred (or drop the hedge entirely and leave the status as-is for 1.6.3 to migrate as part of QS.G6). The original clauses (c) + (d) — every Bronze+Silver+Gold rule must appear `done`/`exempt` and the tier flip to `gold` — are inseparable from the QS.* work that moved to 1.6.3; tracked as the 1.6.3 release-blocker gate there.

- [ ] **TP.4 CHANGELOG.md retrospective entry at the top of 1.6.2.** Short "Corrections to 1.6.1" section opening the release notes. Users upgrading from 1.6.1 need to know: (a) the AppArmor profile they thought confined the add-on didn't — now it partly does (post-TP.2); (b) the "silver" integration-quality claim from 1.6.1 was unenforced — 1.6.2 honestly retreats to `bronze` in the manifest (the `gold` tier-flip moves to 1.6.3 where hassfest validation can back it); (c) the TR.* items closed paths that shipped broken; (d) remote workers now receive only the files needed for the target being compiled, not the whole config directory (BD.*). No marketing copy — user-centric plain language.

*(Former TP.4 "PARALLEL_UPDATES = 0 on every entity platform" is dropped — it's already landed in all five platforms: `sensor.py:49`, `binary_sensor.py:33`, `button.py:32`, `number.py:32`, `update.py:34`. Kept as a confirmed-done entry under QS.S below.)*

---

## TR — Real-bug remedies (flagged in PR #80 review, merged anyway)

Each of these was explicitly called out before merge. Fixing them here rather than pretending they don't exist.

- [ ] **TR.1 Fix bug #3's disconnect filter.** `ha-addon/server/device_poller.py::_AioesphomeapiDisconnectFilter` mutates `record.levelno` and returns `True`. Per Copilot's review (PR #80 comment), `Logger.callHandlers` already chose the ERROR handler before the filter ran — so the record still ships at ERROR, just with a misleading `DEBUG` label. Fix: return `False` to drop the matching record (simplest) or re-emit on a DEBUG-level logger under a different name and return `False` on the original (keeps the information). Regression test in `tests/test_device_poller.py` using pytest's `caplog` fixture: assert the "disconnect request failed" record does not surface at `ERROR`-level capture.

- [ ] **TR.2 Fix `mdns_advertiser.py` malformed-URL + dead conditional.** `ha-addon/server/mdns_advertiser.py:62` → `base_url = "http://:8765"` when `_primary_ipv4()` returns None. Either skip registration entirely on the None branch (advertising no-IP serves no one) or omit `base_url` from properties. Line 83's `socket.gethostname() if socket.gethostname() else None` is dead code — `gethostname()` always returns a non-empty string; guard against `"localhost"` specifically (which is what actually breaks mDNS resolution). Both covered by a new test file in 1.6.3's HT.5.

- [x] **TR.3** *(1.6.2-dev.4)* **Fix the `async_step_reconfigure` bugs.** `ha-addon/custom_integration/esphome_fleet/config_flow.py`: (1) fallback branch to `update_entry + reload + abort` dropped — `async_update_reload_and_abort` is always available at the add-on's declared HA minimum of 2024.11; (2) `self.context["entry_id"]` → `.get()` with early abort; (3) `assert self._reconfigure_entry is not None` replaced with `async_abort(reason="reconfigure_unknown_entry")` + matching translation key in `strings.json` + `translations/en.json`. Discovered while landing HT.7: **TR.3.4** — the helper `async_update_reload_and_abort` is `@callback` (synchronous) in HA 2024.11+ but the code `await`-ed it, raising `TypeError: object dict can't be used in 'await' expression` on every successful reconfigure. That had shipped in 1.6.1 too; dropped the `await`. HT.7 covers all four paths as regression guards. Manifest `homeassistant` minimum declaration tracked separately in 1.6.3's QS.G10.

- [ ] **TR.4 Connect Worker modal bash branch — add `--network host`.** `ha-addon/ui/src/components/ConnectWorkerModal.tsx::buildDockerCmd`'s bash branch is missing `--network host` that the compose branch has. Any user copy-pasting the output onto a LAN with ESP devices outside docker's default bridge gets a worker that can't OTA. Add the flag (same place as `--restart`). Regression guard via 1.6.3's HT.6 snapshot test.

- [ ] **TR.5 Dockerfile Debian-assertion guard.** `ha-addon/Dockerfile` currently detects Supervisor's silent build.yaml `@sha256:` regex rejection only because the downstream `apt-get install` fails on the Alpine fallback. One-line guard before the apt-get block:
  ```dockerfile
  RUN . /etc/os-release && [ "$ID" = "debian" ] || \
      (echo "ERROR: unexpected base '$ID' — Supervisor probably rejected build.yaml; aborting" >&2; exit 1)
  ```
  Turns a silent misbuild into a grep-able layer-0 failure. Documented in `ha-addon/build.yaml`'s comment; still worth the explicit guard against Dockerfile drift.

- [x] **TR.6** *(1.6.2-dev.4)* **Fix the reauth assert sibling to TR.3.** `async_step_reauth` now `.get()`s the entry_id and aborts with `reauth_unknown_entry` when the lookup returns None — same defensive shape as TR.3.2. `async_step_reauth_confirm`'s `assert self._reauth_entry is not None` replaced with `async_abort(reason="reauth_unknown_entry")`; the token-update path now uses `async_update_reload_and_abort` for symmetry with reconfigure. Added `reauth_unknown_entry` translation key to `strings.json` + `translations/en.json`. HT.11 covers the path as a regression guard.

---

## IM — Install-path modernization (bug #82)

Bug #82 (1.6.1): Supervisor fails to install the add-on with `Image docker.io/library/docker:29.3.1-cli does not exist`. Root cause is upstream: Supervisor's `addons/build.py` picks the builder image as `docker:<HOST_DOCKER_VER>-cli` (`self.sys_docker.info.version`) and runs `docker buildx` inside it. The image DOES exist on Docker Hub (pushed 2026-04-03, actively pulled today); the pull failed silently (Supervisor's high-level `images.pull` swallows errors) and the generic `does not exist` message fires when the subsequent `containers.create` can't find it locally. Most probable triggers: Docker Hub anonymous rate limit (100/6h/IP) or transient Hub/egress hiccup — the pull wrapper surfaces both identically.

We're the only piece forcing Supervisor into the local-build path (`build.yaml` present + no `image:` key in `config.yaml`). Publishing prebuilt multi-arch add-on images and flipping `config.yaml` to `image:` moves every install to a single `docker pull` of our own GHCR image and eliminates the whole failure class. Side-effects that pay down existing debt:
- Kills the `build.yaml deprecated` Supervisor warning (cluster with TR.5, #8, SS.4).
- Unblocks real `@sha256:` digest pinning for the add-on's base image (Supervisor's `build_from` regex rejects digests silently; a GHCR image tag does not).
- Replaces 3–8 min of local `apt-get` + `pip install` + toolchain compile on the user's HAOS box with a seconds-long pull.
- Folds the TR.5 Debian-assertion guard into "belt-and-suspenders for the Supervisor-fallback-build path" rather than the primary defense.

This is hardening, not a feature — fits the 1.6.2 theme.

- [x] **IM.1** *(1.6.2-dev.6)* **`publish-addon.yml` — multi-arch add-on image to GHCR.** Landed. Matrix-per-arch workflow (`amd64` + `aarch64`, each pushed as a single-arch image so Supervisor's `{arch}` substitution resolves correctly), builds `ha-addon/Dockerfile` with `--build-arg BUILD_ARCH` matching Supervisor's production build semantics, pushes to `ghcr.io/weirded/<arch>-addon-esphome-dist-server:<VERSION>` plus `:latest` on `main` / `:develop` on `develop`. Carries the same E.10 cosign + SC.2 CycloneDX attestation sequence as `publish-server.yml`. IM.4 verification (fresh install from GHCR on hass-4, not via `push-to-hass-4.sh`) still pending as release-blocker gate.

- [x] **IM.2** *(1.6.2-dev.6)* **`ha-addon/config.yaml` → add `image:` key.** Landed. `image: ghcr.io/weirded/{arch}-addon-esphome-dist-server` added; Supervisor resolves `{arch}` and appends `:<version>` from the same file. `build.yaml` retained so Supervisor can still fall back to a local build on air-gapped boxes or when GHCR is unreachable. Removal scheduled for 1.7 after a full release cycle on the prebuilt path proves stable.

- [x] **IM.3** *(1.6.2-dev.6)* **Closed the base-image digest gap.** Bumped `ARG BUILD_FROM` in `ha-addon/Dockerfile` and the client `FROM` in `ha-addon/client/Dockerfile` from the stale `sha256:233de…` to the current multi-arch index digest `sha256:92c262…` (Python 3.11.15-slim-trixie, verified via `docker buildx imagetools inspect`). Under the prebuilt-image path (IM.1 + IM.2), Supervisor no longer overrides `ARG BUILD_FROM`, so this digest IS the production base — the PR #80 "pin isn't applied" caveat is closed for production. `dev-plans/SECURITY_AUDIT.md` §F-13/SS.4 updated: server production path **FIXED**, Supervisor-driven local-build fallback remains **partial** (upstream regex still rejects `@sha256:…` in `build.yaml`) and is explicitly scoped as secondary.

- [ ] **IM.4 First-install verification on hass-4 (not via `push-to-hass-4.sh`).** Before tag: remove `local_esphome_dist_server` from hass-4, then install the non-local `esphome_dist_server` add-on via the add-on store UI. Confirm Supervisor log shows `Pulling image ghcr.io/weirded/amd64-addon-esphome-dist-server:1.6.2-dev.<N>` and NO `Running command docker buildx build` and NO `docker:<x.y.z>-cli` pull. Capture the log. This is the only direct regression anchor for bug #82 — the dev loop's local-build path exercises a different Supervisor codepath and cannot prove the user-facing install works. Lands as part of SD.2 release-blocker gate. (Note: HT.13's `INSTALL_MODE=ghcr` real-VM run on 1.6.2-dev.12 already confirmed Supervisor does pull the prebuilt image end-to-end on a fresh HAOS — IM.4 is the human sign-off on hass-4 specifically.)

- [x] **IM.5** *(1.6.2-dev.6)* **Kept `push-to-hass-4.sh` working post-IM.2.** Added a `sed -i '/^image:/d' distributed-esphome/config.yaml` step on the remote host immediately after the tarball extracts, so the dev loop's Supervisor install still takes the local-build path (the bumped `-dev.N` tag won't exist on GHCR at push time; Actions takes ~5–8 min to publish). The committed repo `config.yaml` keeps the `image:` key intact for production pulls. Inline comment in the script explains the why and points at this workitem.

- [x] **IM.6** *(1.6.2-dev.6)* **User-facing docs landed.** `ha-addon/DOCS.md` carries the "prebuilt image from GHCR" one-paragraph note under Getting Started. `ha-addon/CHANGELOG.md` opens the 1.6.2 section with a user-language bullet under Bug fixes describing the symptom and the switch to prebuilt images.

---

## BD — Bundle discipline (cut over to `esphome bundle`)

Status quo before BD: `ha-addon/server/scanner.py:267–289` — `create_bundle()` did `base.rglob("*")` across `/config/esphome/` with filters only for macOS `._*` and `.DS_Store`. Every job claim shipped **the entire config directory** to the claiming worker — `.git/` (history + remote URLs + any wired-up push credentials), full `secrets.yaml` (every device's Wi-Fi, API keys, noise-PSKs — not just what the current target references), unreferenced YAMLs and packages, plus any `.esphome/` / `.pioenvs/` / `.pio/` caches if a user ever ran `esphome compile` in-place on the HA host.

ESPHome 2026.4+ ships `ConfigBundleCreator` (`esphome/bundle.py`, landed 2026-04-07) that does exactly the right thing: walks the validated config, ships only referenced files, filters `secrets.yaml` to the keys the bundled target actually references, emits a `manifest.json`. `.git/` and cross-device secrets don't ship by construction.

Scope-fit for 1.6.2: `.git/` + full `secrets.yaml` on every claim was a **latent secret-exfiltration vector** — a fleet operator adding a worker on a friend's Docker host effectively granted that friend read access to every device's secrets and the fleet's git history. Fits the "close shipped-but-broken paths" rule.

Approach: clean cut-over, no dispatcher, no opt-in, no fallback. Raise the ESPHome floor to 2026.4.0, call `ConfigBundleCreator`, delete the rglob path. Bundling is a server↔worker implementation detail the user never sees — the only user-visible surface is "ESPHome 2026.4+ required" in the release notes.

- [x] **BD.1** *(1.6.2-dev.15)* **Replace `scanner.create_bundle()` with `ConfigBundleCreator`.** Landed. `create_bundle(config_dir, target)` calls `_full_validate_config(target)` and passes the result to `esphome.bundle.ConfigBundleCreator`, returning `BundleResult.data`. Rglob loop + `._`/`.DS_Store` filters deleted. Caller in `api.py` updated to pass `job.target`, and the bundle call moved to `run_in_executor` so the heavy validate-then-bundle path doesn't block the event loop mid-claim. Worker side byte-identical on the wire. **Additional fix landing with BD.1:** `_full_validate_config` now calls `CORE.reset()` before every validation — ESPHome's `unique_ids` / `loaded_integrations` / `component_ids` are process-global and leak across successive validations on our 67-target fleet, causing false "Duplicate entity" errors on later targets. `esphome compile` CLI runs one target per process so never hits this; our server runs many sequentially and must mimic the fresh-process contract explicitly. Without the reset, BD.1 would have hit the no-fallback path for most targets on first boot.

- [x] **BD.2** *(1.6.2-dev.15)* **Raise ESPHome floor to 2026.4.0.** Landed. `constants.MIN_ESPHOME_VERSION = "2026.4.0"` is the authoritative floor; `scanner.ensure_esphome_installed` refuses any older version with an ERROR log + `_esphome_install_failed = True` so the UI banner surfaces the problem instead of hanging. `.github/workflows/compile-test.yml` bumped `ESPHOME_VERSION` to `2026.4.2` with an inline comment pointing at `MIN_ESPHOME_VERSION`. `ha-addon/DOCS.md` Getting Started section documents the floor and the refusal behavior. Version-compare uses a local `_version_tuple` helper (dotted-ints, strips `-rc1`/`.dev0` suffixes after the numeric prefix) to avoid pulling in `packaging.version`.

- [x] **BD.3** *(1.6.2-dev.15)* **Regression tests.** Landed in `tests/test_scanner.py`. Rewrote the pre-BD bundle test section (which assumed whole-dir shipping) around the new target-specific shape. Eight tests total: `test_bundle_is_tar_gz`, `test_bundle_ships_the_target_yaml`, `test_bundle_paths_are_relative`, `test_bundle_includes_manifest` (ConfigBundleCreator emits `manifest.json` at tree root), `test_bundle_omits_unrelated_targets` (BD.3.3 — bundle for device1 must not ship device2.yaml), `test_bundle_omits_unrelated_package_files` (same shape for `packages/` includes), `test_bundle_excludes_git_dir` (BD.3.1 — `.git/config` with a fake `ghp_SUPERSECRETPAT` push URL + a loose-object file, assert `.git` absent from archive), `test_bundle_filters_secrets_to_referenced_keys` (BD.3.2 — secrets.yaml contains `wifi_ssid`, `wifi_password`, `other_device_api_key: OTHER-DEVICE-PSK-MUST-NOT-LEAK`, `unused_backdoor_password: ALSO-MUST-NOT-LEAK`; assert the referenced pair is present in the bundled secrets.yaml and the two unreferenced ones are not), and `test_bundle_raises_on_validation_error` (documents the intentional no-fallback behavior: a YAML that fails full validation cannot be dispatched until fixed).

- [x] **BD.4** *(1.6.2-dev.15)* **Docs.** `SECURITY.md` updated in two places: the "§ What is not in scope" bullet about `secrets.yaml` shipping to every worker now describes the 1.6.2 filtering behavior, and the Residual posture section gains a "Cycle deltas for 1.6.2" entry documenting the full pre-BD leak (`.git/` with remote URLs + push credentials, cross-device secrets, PlatformIO caches) and the ConfigBundleCreator cut-over. `ha-addon/CHANGELOG.md` gains a user-language "Smaller changes" bullet ("Remote workers now receive only the files needed for the target being compiled..."). `ha-addon/DOCS.md` Getting Started documents the ESPHome 2026.4+ floor. `dev-plans/SECURITY_AUDIT.md` untouched — the audit file doesn't currently enumerate the pre-BD leak as a finding; SECURITY.md's delta section is the canonical public record.

---

## EH — ESPHome-delegation (stop hand-rolling what ESPHome already provides)

The original EH section audited four places where we re-implemented ESPHome functionality. Only EH.2 landed in 1.6.2 — along the #84 fix that switched `_resolve_esphome_config` to ESPHome's full validator. EH.1 (idedata for firmware-artifact discovery), EH.3 (`esphome.const` imports), and EH.4 (version-detection cleanup) moved to 1.6.3 with the rest of the latent-fragility hardening.

- [x] **EH.2** *(1.6.2-dev.11)* **Validated-config substrate as source of truth for device metadata.** Landed. `_resolve_esphome_config` now attempts `esphome.config.validate_config(config, None, skip_external_update=True)` first — which injects every field ESPHome's component schemas compute, including the domain-aware `wifi.use_address` that closes #84. Falls back to the legacy `do_packages_pass` + `do_substitution_pass` pipeline on validation errors so targets with typos elsewhere in the file still populate the Devices tab. `_extract_metadata` reads from the same dict shape as before, so every field it derives (friendly_name, has_web_server, network_type, etc.) now comes from the fully-validated config when available. The original "read `storage.json`" framing was strictly worse: storage.json only exists post-compile on the worker, not on the server, so reading it would have required a new worker→server archival flow — whereas running the validator server-side is one API call with no new data flow and gives us the same (or stricter) source of truth. Covered by the tests landing alongside #84; no `storage.json` reader was added. `get_device_address` updated to preserve the existing `_static_ip` source label when the validator promotes `manual_ip.static_ip` into `use_address`, and to keep `mdns_default` for the validator-default `{name}.local` case (so the thread-only / wifi-no-domain UI tooltips stay accurate).

---

## SI — Standalone independence (make Home Assistant optional)

ESPHome Fleet ships as an HA add-on (primary path) and as a standalone Docker image (supported — the repo ships `docker-compose.yml`; #83 closed the standalone Web UI auth UX). The standalone path works today but carried latent coupling to HA runtime: Supervisor-injected env vars, the HA entity poller, Ingress-header assumptions, `/data/options.json` loaders, and the custom-integration installer all expected "HA is present." SI.1's audit found almost everything already fell back cleanly — one hard C-case (device-restart HA-fallback returning 500) plus nine sites that degraded but emitted noisy per-site "skipped — no SUPERVISOR_TOKEN" DEBUG lines with no clear startup signal that standalone mode was in effect. SI.2 closed the C-case + added a clean startup banner; SI.3 documented the feature matrix.

Scope-fit: rule (a) — the `docker-compose.yml` path was shipped-but-brittle. Pairs with TP (truth-in-claims): a `docker-compose.yml` that ships broken edges is a claim the code doesn't fully back.

- [x] **SI.1** *(1.6.2-dev.18)* **Audit HA coupling; produce `dev-plans/HA-COUPLING-AUDIT.md`.** Landed. 14 HA-coupled sites audited and verified against current code. Summary: **13 already A/B** (graceful or degrades cleanly — `#83`'s `require_ha_auth=false` default + HTML 401 closed the last previously-known hard-block) and **one borderline C** (device-restart HA-fallback returned 500 when no Supervisor token; body diagnostic but status code misleading). Workitem's original C-list turned out smaller than expected: the HA entity poller is not a spinloop (returns early if no `SUPERVISOR_TOKEN`; the `while True` only runs under HA), the custom-integration installer already returns a documented `"skipped_no_parent"` shape, and the bearer-middleware's path-4 default is already pass-through since `#83`. Non-finding: the "HA Core WebSocket client" listed in the original workitem text doesn't exist — the server's WebSockets are inbound only. Audit document contains the full per-site matrix + a feature matrix copy-pasted into README's standalone section.

- [x] **SI.2** *(1.6.2-dev.18)* **Close SI.1's C-bucket + polish passive-HA log lines.** Landed. Added `helpers.ha_mode()` / `helpers.is_standalone()` (auto-detects via `SUPERVISOR_TOKEN`, optional `HA_MODE=standalone|addon` env override). Startup banner: `on_startup` now logs a single INFO line — `"Running in standalone mode (no HA Supervisor detected)"` or `"Running as HA add-on (Supervisor detected)"` — so operators grep one place to confirm which coupling is active instead of reading nine downstream "skipped — no SUPERVISOR_TOKEN" lines. `supervisor_discovery.register_discovery` promoted its no-token log from DEBUG to INFO so standalone operators see a clear `"Skipping Supervisor auto-discovery (standalone mode — no SUPERVISOR_TOKEN)"` line; `unregister_discovery` left silent (symmetric no-op, not useful). `ui_api.restart_device` fallback returns **HTTP 503** (not 500) when Supervisor is absent, with an added `"hint"` field naming the standalone-mode cause and pointing at the physical-button / Web UI alternative. No other code changes — the audit confirmed the rest is already graceful.

- [x] **SI.3** *(1.6.2-dev.18)* **Docs pass for the standalone path.** Landed. `README.md` §"As a standalone Docker container" gained a "What works and what doesn't without Home Assistant" subsection listing (i) the startup auto-detection + `HA_MODE` env override, (ii) full feature set available in standalone (compile queue, workers, device poller + mDNS, OTA, firmware archive, git versioning, editor, schedules, Web UI), (iii) the four unavailable-without-HA features (auto-discovery, Devices-tab HA connectivity column, Restart-button HA-service fallback, Supervisor-driven ESPHome version auto-detection — falls back to PyPI latest-stable), (iv) configuration differences (`/data/settings.json` + Settings drawer; `require_ha_auth=false` default with `#83`'s HTML 401 page available behind the toggle). Links out to `dev-plans/HA-COUPLING-AUDIT.md` for the full per-site matrix. `ha-addon/DOCS.md` deliberately untouched per the workitem's guidance (add-on-specific docs, don't pollute).

---

## WL — Worker log visibility

The Workers tab shows worker state (online, parallel slots, current jobs) but no way to view the worker's own log output. Compile-job logs stream back and are viewable in Queue-History, but everything *outside* a compile job — startup errors, heartbeat failures, ESPHome install failures, OTA retries on a remote LAN, the "worker won't register" class of first-setup bug — is visible only on the host running the worker. For local workers (spawned inside the server add-on container) the output is merged into the add-on log alongside server noise; filtering to just the local worker's lines requires greppery. For remote workers on friend's-Docker-host / Raspberry-Pi / remote-office deployments, the operator has no visibility without SSH-ing to the worker's host — and by the time they're SSH-ing, they're already past the "is this thing working?" stage.

Scope-fit: rule (d) — shipping workers without a way to see why one's misbehaving is a first-setup operational break every first-time-remote-worker user hits. Kept in 1.6.2 (even though it's a new user-visible capability) because real users are currently stuck on "is this thing working?" with no recourse. Reuses the existing compile-log transport shape + xterm viewer so the new-capability surface is narrow.

Design approved 2026-04-23 — full spec at `dev-plans/designs/2026-04-23-worker-log-visibility.md`. Key constraint: **logs only traverse worker → server while a UI dialog is open.** Signalling rides on the existing heartbeat response (additive `stream_logs: Optional[bool]` — no `PROTOCOL_VERSION` bump per PY-6). Presence signal is per-worker WS subscriber count on the server; when count is 0, heartbeat response tells worker to stop pushing. When the dialog is open, worker pushes at 1 Hz to a dedicated endpoint that mirrors the compile-log transport line-for-line. The UI reuses `LogModal` parametrized over a tagged `source` union (`job` | `worker`) — one viewer, two callers.

- [x] **WL.1** *(1.6.2-dev.21)* **Worker-side log-tail ring buffer.** `ha-addon/client/client.py` gains a `LogCaptureHandler` (`logging.Handler` subclass) attached to the root logger, teeing every formatted log record into a bounded `collections.deque(maxlen=2000)` + monotonic byte-offset counter. Buffer is process-lifetime only; no disk persistence. Thread-safe via `threading.Lock` — the handler fires from main, heartbeat, and each job-slot thread. Always on (cheap 2 MB ceiling) so the backlog is ready whenever the server asks for it. Does not replace the existing stdout `StreamHandler` — lines still go to stdout for host-side tailing. Coverage: `tests/test_worker_log_buffer.py` (overflow drops oldest, `drain_since` advances byte-offset correctly, doesn't suppress stdout, 50×50 concurrent append+drain under contention).

- [x] **WL.2** *(1.6.2-dev.21)* **Pull-when-watched push protocol + server broker.** (a) Protocol: extend `ha-addon/server/protocol.py` (+ byte-identical client copy — PY-6) with `HeartbeatResponse.stream_logs: Optional[bool]` and a new `WorkerLogAppend(offset: int, lines: str)` model mirroring the shape of `JobLogAppend`. All additive; `PROTOCOL_VERSION` stays at 1. (b) Server: new `ha-addon/server/worker_log_broker.py` owning per-`client_id` bounded deque (2000 lines), `_next_offset` dict for dedupe/restart detection (offset went backwards → inject ANSI dim `--- worker restarted ---` separator, reset), WS subscriber sets, and a 1 h eviction task scheduled after the last unsubscribe. Broker is **independent** of the `Worker` registry — separation of concerns. (c) Endpoints: `POST /api/v1/workers/{client_id}/logs` (worker bearer, body cap 4× `MAX_LOG_BYTES`, calls `broker.append`), `GET /ui/api/workers/{client_id}/logs` (HA-ingress trust, plain-text hydration snapshot), WS `/ui/api/workers/{client_id}/logs/ws` (HA-ingress trust, server fans out `append()` writes to open subscribers). (d) Heartbeat handler at `api.py:151-186` adds `resp.stream_logs = broker.is_watched(client_id)`. (e) Worker loop gains a daemon pusher thread; spawned/exited on heartbeat `stream_logs` transitions, drains the ring buffer at 1 Hz via `POST /api/v1/workers/{id}/logs`. On 5xx the thread retries next tick with the same `acked_offset` (lines never drop on happy path). Coverage: `tests/test_worker_log_broker.py` (subscribe/unsubscribe math, `is_watched` flips at 0↔1, 1 h eviction via injected clock, restart detection on backwards offset, oversized-body rejection), `tests/test_worker_log_protocol.py` (round-trip with/without `stream_logs`, `WorkerLogAppend` round-trip, older-peer `extra="ignore"` compatibility).

- [x] **WL.3** *(1.6.2-dev.21)* **UI log viewer + Workers-tab dropdown + e2e coverage.** (a) Parametrize `ha-addon/ui/src/components/LogModal.tsx` — existing `jobId: string` prop becomes `source: { kind: 'job'; jobId } | { kind: 'worker'; workerId }`; URL builder and "Download" filename key off `source.kind`. Update `QueueTab.tsx` callsite in the same commit (no deprecation wrapper — one contract, not two). (b) `WorkersTab.tsx` gets a per-row dropdown menu with a "View logs" item; menu `open` state lifted to the parent keyed on `client_id`, matching `QueueTab.tsx`'s `downloadMenuOpenJobId` pattern (bugs #2 / #71 — 1 Hz SWR poll would otherwise tear the menu down mid-click). Dialog opens `<LogModal source={{ kind: 'worker', workerId }} />`. Initial hydration pulls the server buffer; a dim "Waiting for worker to start streaming… (up to 10 s)" hint renders inside xterm until the first push-frame arrives. (c) Coverage: `ha-addon/ui/e2e/worker-logs-dialog.spec.ts` (mocked Playwright — open dropdown, click View logs, xterm hydrates with seeded snapshot, simulated WS push appends live lines, Download produces a file with `content-disposition: attachment; filename="worker-<hostname>-<iso-ts>.log"`, dropdown-open-state survives a 1 Hz mocked SWR poll).

---

## QS — Quality Scale (confirmed-done entries only)

The full quality-scale rule walk — QS.B1 brands submission, QS.S1 test coverage, QS.G1–G10 gold-tier rules, QS.P1 strict-typing scoping — moved to `dev-plans/WORKITEMS-1.6.3.md`. 1.6.2 keeps only the two factual confirmations that don't require work.

- [x] **QS.S-confirm `PARALLEL_UPDATES` already landed on every entity platform.** Verified: `sensor.py:49`, `binary_sensor.py:33`, `button.py:32`, `number.py:32`, `update.py:34` — all `PARALLEL_UPDATES = 0` with the coordinator-throttle rationale in the comment. Former TP.4 is satisfied. Nothing to do; mention in release notes and leave the `quality_scale.yaml:parallel-updates` entry as `done`.

- [x] **QS.P-confirm `async-dependency` + `inject-websession` already landed.** Verified: only external I/O is `aiohttp` against the add-on's HTTP API; `coordinator.py` uses `async_get_clientsession(hass)` not a fresh `aiohttp.ClientSession`. Nothing to do. If we ever claim Platinum, these two are free.

---

## HT — Honest testing (install-path regression guards)

1.6.2 keeps the three HT items that already landed (HT.1, HT.7, HT.11 — all real-flow tests wired to the TR.* fixes) plus the HT.13 family and HT.14 (regression guards for the install paths we just fixed). The broader TEST-AUDIT-1.6.1 blind-spot closure — HT.2 (reseed-consumer invariant), HT.3 (static-IP fixture suite), HT.4 (static-IP OTA prod guard), HT.5 (mDNS advertiser coverage), HT.6 (Connect Worker modal snapshot), HT.8 (git-versioning concurrency), HT.9 (firmware-storage concurrency), HT.10 (protocol cross-version), HT.12 (integration coverage measurement) — moved to 1.6.3.

- [x] **HT.1** *(1.6.2-dev.3)* **Unskip IT.2 — real-hass lifecycle tests.** Landed in `tests/test_integration_setup.py` — three tests under a real `hass` fixture: happy-path setup+unload, `setup → unload → setup` reload cycle (CR.12 class regression guard), and first-poll-failure → `ConfigEntryState.SETUP_RETRY` → reload → `LOADED` recovery path. Wiring: symlink the integration into each test's `hass.config.config_dir/custom_components/` (the `hass` fixture's per-test tmpdir — the old repo-root symlink was in the wrong place), pop `DATA_CUSTOM_COMPONENTS` so HA re-scans, patch `EsphomeFleetCoordinator._async_update_data` + `EventStreamClient._run` to silence HTTP + WebSocket attempts during setup, and session-warm pycares' singleton shutdown thread so `verify_cleanup`'s thread-leak check doesn't trip on first-test lazy-start. PY-10 invariant becomes load-bearing instead of cosmetic; prereq for 1.6.3's QS.S1 (≥95% coverage).

- [x] **HT.7** *(1.6.2-dev.4)* **Real-flow test for `async_step_reconfigure` (post-TR.3).** Landed in `tests/test_integration_reconfigure_flow.py` — four tests under a real `hass` fixture: (a) form renders with current URL, (b) valid input aborts with `reconfigure_successful` + entry data updated, (c) invalid URL returns form error `invalid_url`, (d) unknown `entry_id` aborts with `reconfigure_unknown_entry`. The missing-entry_id path isn't exercisable through `flow.async_init` (HA 2025.1+ rejects it at the framework layer via `report_usage`), noted in-file; the defensive `.get()` remains as belt-and-suspenders. Landing these tests caught the TR.3.4 `await`-a-sync-callback bug noted in TR.3 above.

- [x] **HT.11** *(1.6.2-dev.4)* **Real-flow test for `async_step_reauth` (post-TR.6).** Landed in `tests/test_integration_reauth_flow.py` — four tests under a real `hass` fixture: (a) form renders with current URL placeholder, (b) valid token aborts with `reauth_successful` + entry data updated, (c) empty/whitespace token returns form error `token_required`, (d) unknown `entry_id` aborts with `reauth_unknown_entry`. Same framework-layer caveat as HT.7 — HA 2025.1+ rejects missing-entry_id at `flow.async_init`, so the defensive lookup in code is belt-and-suspenders. The four tests share `_integration_test_fixtures.py` with HT.1/HT.7 (pycares thread warmup + custom_components install + network mock).

- [ ] **HT.13** *(scripts landed 1.6.2-dev.10; first real-VM `ghcr` run verified 1.6.2-dev.12; smoke-suite portability still outstanding)* **— automated HaOS install regression guard.** Analogue of `push-to-hass-4.sh` for the fresh-HAOS path, modeled on `ha-outback-mate3/scripts/`'s Proxmox-qga pipeline (same VMID default 106, same deterministic-MAC scheme so two projects sharing a Proxmox node pick distinct VMIDs, not distinct pipelines). Landed as four helpers under `scripts/haos/` plus a top-level orchestrator at `push-to-haos.sh`:
  - `scripts/haos/provision-vm.sh` — creates HAOS VM on a Proxmox host over SSH (`qm create` + disk import + EFI + resize + start); tagged `distributed-esphome,test`.
  - `scripts/haos/onboard.sh` — drives `/api/onboarding/*` to create admin user, finish core_config/analytics/integration, mint a long-lived access token into `$HOME/.config/distributed-esphome/haos-token`.
  - `scripts/haos/install-addon.sh` — tars `ha-addon/`, scps to the Proxmox host, chunked `pvesh agent/file-write` into the guest, reassembles under `/mnt/data/supervisor/addons/local/esphome_dist_server/`, drives Supervisor via `docker exec hassio_cli ha apps install|update|rebuild`, then `ha core restart` so the bundled custom integration reloads. **Two install modes:** `INSTALL_MODE=local` (default) strips `image:` from `config.yaml` on the guest and exercises the Supervisor local-build path (same shape as `push-to-hass-4.sh`); `INSTALL_MODE=ghcr` keeps the key and exercises the GHCR-pull path — the direct regression anchor for bug #82 per IM.1/IM.2.
  - `scripts/haos/teardown-vm.sh` — `qm stop` + `qm destroy --purge`, guarded by a typed-VMID confirmation unless `FORCE=1`.
  - `push-to-haos.sh` (repo root) — top-level orchestrator: reads the add-on token from `/data/settings.json` via qga, waits for `/ui/api/server-info` to report the bumped version, runs `npm run test:e2e:hass-4` with `HASS4_URL` + `HASS4_ADDON_TOKEN` pointed at the VM.

  **First real-VM run (1.6.2-dev.12, `INSTALL_MODE=ghcr`):** fresh HAOS 17.2 VMID 106 → provision + onboard + install. `docker image inspect` on the running container confirms the add-on is `ghcr.io/weirded/amd64-addon-esphome-dist-server:1.6.2-dev.12@sha256:db520d5c…` — Supervisor pulled the prebuilt image rather than invoking a local `docker buildx`; bug #82's install path is verified end-to-end. Three in-script bugs surfaced + fixed in the same dev.N: (1) SSH `MaxAuthTries` / fail2ban lockout after ~15 sequential `ssh pve` calls — mitigated via `ControlMaster=auto` + `ControlPersist` opened once per script; (2) stale `/tmp/deaddon.*` chunks from a failed previous run could concatenate onto the next tarball — script now `rm -f` the chunks before each push; (3) `ha_cli "core restart" 120` was passing `120` as a positional arg to `ha core restart` instead of setting `HA_CLI_TIMEOUT` — fixed to use the env var like every other long-running call.

  **Remaining gap — smoke-suite portability.** 4 of the `e2e-hass-4` Playwright tests assume `cyd-office-info` exists under `/config/esphome/` and fail on blank HAOS (`devices tab loads and shows the target device`, `incremental build`, `pinned bulk compile`, `schedule fires` — all `target not found`). HT.13a + HT.13b + HT.13c close the gap.

  **Known non-blocking:** chunked `pvesh file-write` push takes ~5 min for a 6.3 MB tarball (~160 serial REST calls). Not a bug — the qga bandwidth is what it is. Fine for a pre-release gate that runs once per release, not fine for per-turn loops — `push-to-hass-4.sh` stays the dev-loop tool.

- [ ] **HT.13a** *(seed mechanism landed 1.6.2-dev.19; two orthogonal blockers surfaced — see HT.13b + HT.13c)* **— seed a minimal ESPHome fixture fleet on the HAOS VM.** Picked the "seed a real fleet" path over "split specs into a portable subset" — every `e2e-hass-4` spec is already `HASS4_TARGET`-env-overridable, so pointing them at a **different real CYD** that hass-4's own smoke never touches is strictly more informative than carving out a no-fleet subset. Landed:
  - `scripts/haos/seed-fleet.sh` — new. `ssh hass-4 "tar cf - -C /usr/share/hassio/homeassistant/esphome <files>"` → stream → scp to Proxmox → chunked `pvesh file-write` → `tar xf` under `/mnt/data/supervisor/homeassistant/esphome/`. Reuses install-addon.sh's SSH-ControlMaster + stale-chunk-cleanup patterns. Default fleet: `cyd-world-clock.yaml garage-door-big.yaml .common.yaml secrets.yaml` — the minimum set that covers the 4 previously-failing specs plus the parallel-compile sub-test's hardcoded `garage-door-big.yaml`.
  - `push-to-haos.sh` — gained a §1.5 seed step (gated on `SKIP_SEED=1` for iteration) and a `HASS4_TARGET=cyd-world-clock.yaml` default on the Playwright invocation. `cyd-world-clock` lives on the LAN at `192.168.225.71` — hass-4's smoke compiles `cyd-office-info` on a physically different CYD, so the two runs don't race on one ESP.
  - Real-VM result (1.6.2-dev.17, blank HAOS, INSTALL_MODE=local): 5 passed → **11 passed** after seeding. The entire "target not found" class of failure is gone. `ls /mnt/data/supervisor/homeassistant/esphome/` on the guest returns all 4 fixture files, owner `root:root`, sized correctly.

  Remaining 4 failures on that run are orthogonal to HT.13a:
  - 2 × "version mismatch" — spec compared `addon_version` to the local `ha-addon/VERSION` file, which a parallel process bumped mid-run (dev.17→dev.18 during the 18-min Playwright suite). Pure race; no fleet-seed relationship. HT.13c closes this by committing the fleet to the repo (removes the cross-process window).
  - 2 × "compile timeout 10 min" — the fresh-HAOS add-on's lazy-install of ESPHome never completes because no HA ESPHome add-on is installed on the VM; `/ui/api/server-info` returns `esphome_install_status: "installing"` indefinitely, so the worker can't claim any compile job. Root cause + fix shape tracked as HT.13b.

- [ ] **HT.13b Bootstrap the ESPHome runtime on a fresh HAOS VM.** `ha-addon/server/scanner.py:ensure_esphome_installed` normally reads the version reported by the HA ESPHome add-on to seed `/data/esphome-versions/<ver>/`. A fresh HAOS VM created by `scripts/haos/provision-vm.sh` has no HA ESPHome add-on installed, so detection returns `null` and the lazy install never fires — confirmed on the HT.13a real-VM run: `GET /ui/api/server-info → {"esphome_install_status":"installing","esphome_server_version":"installing"}` for an hour. `POST /ui/api/esphome-version {"version":"2026.4.2"}` sets the selected version but the install still didn't progress in a 7-min poll window; investigate whether the install hook is wired to run on explicit `set_esphome_version` or only on the detect-from-HA path. Fix shape: `push-to-haos.sh` gains a §1.75 step that POSTs an explicit version (env-var `HAOS_ESPHOME_VERSION`, default pinned to whatever `compile-test.yml` uses) and polls `/ui/api/server-info.esphome_install_status` until `ready` (cap at 10 min; fail loudly with the last status string and the add-on log tail on timeout). This is the *real* regression anchor for HT.13 — without it, every compile test on HAOS silently never gets a worker.

- [ ] **HT.13c Commit a scrubbed fleet fixture under `tests/fixtures/haos-fleet/`.** HT.13a's `seed-fleet.sh` pulls live from `ssh hass-4`. That keeps the fixture in sync with the CYD that actually exists on the LAN, but means `push-to-haos.sh` only works from a machine with SSH reach to hass-4. Commit a scrubbed version of the 4-file fleet (wifi secrets replaced with dummy values; static IPs genericized) to `tests/fixtures/haos-fleet/` and teach `seed-fleet.sh` a `FLEET_SOURCE=repo|hass-4` knob. Unblocks future CI ports of HT.13, and keeps the current hass-4 path as an opt-in for real-device runs. Depends on HT.13b landing first — otherwise CI runs just trip over the ESPHome-install-stuck issue.

- [x] **HT.14** *(1.6.2-dev.20)* **`scripts/push-to-standalone.sh` — automated standalone-Docker install regression guard.** Landed. Ships a repo-root `docker-compose.yml` (the standalone deployment surface; workitem assumed one existed, now it does) plus four helpers under `scripts/standalone/` (`deploy.sh`, `seed-fleet.sh`, `bootstrap-esphome.sh`, `teardown.sh`) and a top-level `push-to-standalone.sh` orchestrator that mirrors `push-to-haos.sh`'s flow. Verified end-to-end against `docker-optiplex-5` on 2026-04-23: mocked Playwright suite 125/125 green, plus 11/11 standalone-portable e2e-hass-4 specs (direct-port-auth ×9 for #82/#83, pinned-bulk-compile ×1, schedule-fires ×1). The intended target host was `docker-pve` but its sshd accepts the TCP handshake and never emits a banner (broken on the host itself; reproducible from LAN peers, not a laptop-routing issue) — `STANDALONE_HOST` is env-overridable so both hosts can be exercised. Regression guard picked up bug #104 on its first real run (missing `git` in `Dockerfile.standalone`, filed + fixed in the same turn). Excluded specs (`ha-services`, `cyd-office-info`, `incremental-build`) via `--grep-invert` so new standalone-safe specs land automatically. HT.13b-equivalent install bootstrap is baked in (`bootstrap-esphome.sh`: POST explicit version, poll `esphome_install_status` to `ready`) — will lift into the HAOS orchestrator when HT.13b itself lands. CI wiring left as follow-up (needs GHCR-reachable runner with SSH reach to a test host); call it out in SD.2's release-blocker gate.

---

## CI — Automate the catches (install-path guards only)

1.6.2 keeps the two CI jobs that guard the install paths and the AppArmor claim — the Dockerfile buildx smoke (CI.1) prevents broken Dockerfiles from landing on main and then silently failing in the publish workflows, and the AppArmor syntax+load smoke (CI.2) gives TP.2's narrowing attempts a real feedback loop so we're not deploying to hass-4 Sunday and watching what breaks. The broader CI automation — ESPHome version matrix (CI.3), hassfest quality-scale validator (CI.4), skip-ratio invariant (CI.5), coverage ratchet (CI.6) — moved to 1.6.3 alongside the quality-scale work they support.

- [ ] **CI.1 `build.yml` workflow — Dockerfile buildx build on every PR.** Runs `docker buildx build --load` on `ha-addon/Dockerfile` and `ha-addon/client/Dockerfile`. Doesn't publish; just asserts the build succeeds. Closes "broken Dockerfile lands on main and only fails in `publish-{server,client}.yml` after merge, when `develop` already advertises the fix." ~3–4 min extra per push; cheap insurance.

- [ ] **CI.2 `apparmor.yml` workflow — profile syntax + load smoke.** New workflow: `apt-get install apparmor-utils`, run `apparmor_parser -N` against `ha-addon/apparmor.txt` to syntax-check, then `docker build` the add-on image and run the container with the profile loaded (`--security-opt apparmor=esphome_dist_server`) — assert the container reaches a healthcheck endpoint. Doesn't prove confinement works; proves the profile loads and doesn't break boot. **Prereq for any meaningful SS.1 tightening (TP.2)** — otherwise the feedback loop for every narrowing attempt is "deploy to hass-4 Sunday and watch what breaks."

---

## SD — Scope discipline

- [ ] **SD.2 Release-blocker gate pre-tag (1.6.2-scope).** Before tagging `v1.6.2`, every one of the following must be true:
  1. `dev-plans/RELEASE_CHECKLIST.md`'s security-docs cross-check passes (no stale claims).
  2. IM.4 manual install-from-GHCR verified on hass-4 (direct regression anchor for bug #82 — the dev loop's local-build path exercises a different Supervisor codepath).
  3. HT.13 green end-to-end — scripts landed, `INSTALL_MODE=ghcr` real-VM run confirms Supervisor pulls the prebuilt image, HT.13b + HT.13c landed so compile tests actually complete on blank HAOS.
  4. HT.14 green end-to-end on a clean standalone-Docker host.
  5. TR.1 / TR.2 / TR.4 / TR.5 landed.
  6. TP.1 / TP.3 / TP.4 landed (SECURITY.md honest, quality_scale.yaml header + runtime-data hedge fixed, CHANGELOG retrospective written).
  7. TP.2 AppArmor narrowing landed with CI.2 green on at least one PR.
  8. WL.1 + WL.2 + WL.3 landed with coverage green.
  9. `scripts/check-invariants.sh` — all rules (PY-1..10, UI-1..7, E2E-1) green.
  10. `ha-addon/CHANGELOG.md` accurately describes what users see changing from 1.6.1 → 1.6.2 (install path via prebuilt image, standalone auth UX, `wifi.domain` now honored, worker logs now visible in the UI, bundle discipline).

  Dropped from the original Gold-grade SD.2: the hassfest-at-gold requirement, the ≥95% coverage threshold, the brands-PR gate, the TEST-AUDIT-1.6.1 Top-5 blind-spot closure — those re-home to 1.6.3's SD.2.

---

## Open Bugs & Tweaks

### Carried forward from 1.6.1

*(none yet — 1.6.1 closed with bugs #1–#22 all addressed. Any post-tag regression against `v1.6.1` lands here as a numbered bug.)*

### New in 1.6.2

- [x] **#82** *(1.6.2-dev.6)* — Add-on install fails with `Image docker.io/library/docker:29.3.1-cli does not exist` on fresh HAOS boxes. Filed 2026-04-21 (tremebundo, 1.6.1). Root cause upstream: Supervisor's `addons/build.py` picks the builder image as `docker:<HOST_DOCKER_VER>-cli` and runs `docker buildx` locally on every install; the pull of that builder image fails when Docker Hub is rate-limiting or briefly unreachable. Closed via IM.1 (publish-addon.yml → GHCR), IM.2 (`image:` key in `config.yaml`), IM.3 (digest pin applies in production), IM.5 (dev loop keeps the local-build path), and IM.6 (user docs). IM.4 (first-install verification on hass-4 from GHCR, not via the dev rsync) remains a release-blocker gate — code is in place; direct regression anchor is a manual pre-tag install.

- [x] **#83** *(1.6.2-dev.6)* — Standalone Docker Web UI broken by mandatory HA Bearer auth. Filed 2026-04-21 (Isidorosis + confirmed by hiltctn, 1.6.1). Regression 1.5.x → 1.6.x: AU.7 flipped `require_ha_auth` default to `true` — correct for the add-on path (HA Ingress short-circuits before the flag is read) but hard-broke the standalone `docker-compose` path. Closed via two paired changes landed together:

  1. **Default flipped back to opt-in.** `AppSettings.require_ha_auth` default is `False` in `settings.py`; existing installs that set `true` in `/data/settings.json` keep their value. AU.7 rationale block in `ha_auth.py` docstring + the `SECURITY_AUDIT.md` F-03 entry rewritten to reflect the new default and the threat-model reasoning. Settings-drawer help text reframed from "leave on unless…" to "defaults to off; turn it on if the direct port is reachable from an untrusted network." `AppSettings().require_ha_auth is False` is a regression-guard test in `test_ha_auth.py`; `test_settings.py` expectations updated.

  2. **HTML 401 page for browsers.** `ha_auth.py:_prefers_html()` content-negotiates on `Accept` — machine clients (`application/json`, `*/*`, or missing header) keep the existing JSON 401 body for worker/integration/curl scripting, while browsers (`text/html` prefer) get `_HTML_401_PAGE`: a self-contained styled page (inline CSS, no external deps, adapts to light/dark OS theme) explaining both recovery paths — provide a bearer token, or open the web UI via Home Assistant Ingress and disable the flag in Settings → Authentication. The standalone-Docker flow is called out explicitly (env var + `/data/settings.json` edit). Regression tests in `test_ha_auth.py` cover both content paths and the default-Accept "don't leak SPA content" contract. `direct-port-auth.spec.ts` toggles the flag on for the duration of the describe block (so the smoke continues to exercise the 401 contract) and asserts the HTML variant renders when `Accept: text/html` is sent.

  Changelog entry added at the top of the 1.6.2 section under Bug fixes — user-visible phrasing, no workitem IDs.

- [x] **#84** *(1.6.2-dev.11)* — `wifi.domain` / `ethernet.domain` ignored when resolving OTA target; worker pushes to `<name>.local` instead of `<name><domain>`. Filed 2026-04-22 (bakerkj, 1.6.1). ESPHome's wifi component injects `use_address = CORE.name + config[CONF_DOMAIN]` during *validation* (`esphome/components/wifi/__init__.py:342`, default `.local` at `:376`), so the canonical resolved string lives in `config["wifi"]["use_address"]` once validation completes. Our `ha-addon/server/scanner.py::_resolve_esphome_config` stopped one step short before the fix — it ran `load_yaml` + `do_packages_pass` + `do_substitution_pass` but never triggered the wifi/ethernet validators, so `use_address` was never injected. `get_device_address` then hand-rolled a three-tier waterfall (`use_address` → `manual_ip.static_ip` → `f"{device_name}.local"`) that quietly dropped `domain`. Symptom on bakerkj's box: target with `wifi: {domain: .my-domain.com}` → server handed the worker `device-name.local` → OTA failed at `Error resolving IP address of ['device-name.local']`. Same waterfall was the single source of truth for the dashboard IP column, so the fix closes the OTA failure AND the dashboard IP column AND the integration device poller's poll target in a single place.

  **Landed (1.6.2-dev.11):** `_resolve_esphome_config` in `ha-addon/server/scanner.py` now runs `esphome.config.validate_config(config, None, skip_external_update=True)` as the primary resolver; the substitution-only pipeline stays as the fallback on any validation error (typos elsewhere in the file, stricter schemas, etc.) and logs at WARNING with the first error message so operators can see which target degraded. `get_device_address` still returns the existing source-label set (`wifi_use_address` / `wifi_static_ip` / `mdns_default` / etc.) — preferred over inventing a new `_validated` label that every UI caller would need to understand. Kept the legacy labels accurate by adding two heuristics: (1) when `use_address` matches `manual_ip.static_ip`, report `_static_ip` (validator-promoted static IP); (2) when `use_address` matches `{name}.local`, report `mdns_default` (validator's default injection with no user-configured domain). This preserves every existing address-source UI rendering while the domain-aware path closes bakerkj's bug. New regression fixture `tests/fixtures/esphome_configs/wifi_domain.yaml` + three new tests in `tests/test_scanner.py` (`test_wifi_domain_fixture_resolves_address`, `test_static_ip_fixture_keeps_static_ip_source_label`, `test_get_device_address_validated_use_address_from_static_ip`). 1.6.3's HT.3 and HT.4 remain for the broader parametrized oracle suite + e2e-hass-4 smoke. EH.2 (ESPHome-authoritative metadata) landed on the same substrate — see EH section.

- [ ] **#85** Small UI tweak — let's add a "Clear Selected" to the Clear menu on the Queue page.

- [ ] **#86** Handle this on an install that has no ESPHome Builder: `2026-04-23 07:59:12,140 WARNING scanner: Config dir /config/esphome does not exist or is not a directory`.

- [x] **#104** *(1.6.2-dev.20)* — `Dockerfile.standalone` missing `git` → git-versioning silently disabled in standalone Docker deployments. Surfaced while running HT.14 against a fresh compose stack on docker-optiplex-5: server startup logged `ERROR git_versioning: git binary not found on PATH; auto-versioning disabled` + traceback from `git init -b main`, after which every config save silently skipped history. Ordinary add-on installs weren't affected because `ha-addon/Dockerfile` has the right apt layer (`gcc libffi-dev libssl-dev git iputils-ping`); `Dockerfile.standalone` shipped with only `gcc libffi-dev libssl-dev`. Fix: align the apt layer with `ha-addon/Dockerfile` so both images install `git` and `iputils-ping`. Direct HT.14 find — the regression guard did its job on its first real run.

 - [ ] 87 it seems we regressed on the authentication, the HAOS install is not giving me a 401 when I access it directly.

- [ ] **#105** — Fresh HAOS add-on install gets stuck on "Installing ESPHome …" forever with no user recourse. Observed on a fresh HAOS VM (no HA ESPHome builder add-on installed, `SUPERVISOR_TOKEN` present) running 1.6.2-dev.17: `/ui/api/server-info` stays at `esphome_install_status: "installing"` indefinitely; the add-on log is a firehose of `"ESPHome still installing — skipping config resolution"` with no `"Installing ESPHome …"` line ever appearing. Two paired gaps, one user-visible: (a) `main.py:_install_esphome_background` has a PyPI-latest-stable fallback that only runs when `SUPERVISOR_TOKEN` is unset — on HAOS it early-returns and defers to `pypi_version_refresher`, which only triggers an install when the HA ESPHome builder add-on is installed. On fresh HAOS with no builder add-on, nothing ever fires. (b) Secondary recovery path: the UI version picker also fails to trigger an install — `POST /ui/api/esphome-version` (`ui_api.py:set_esphome_version_handler`) updates `_selected_esphome_version` but doesn't schedule `ensure_esphome_installed`. Fix shape: (1) drop the `SUPERVISOR_TOKEN` early-return in `_install_esphome_background` so fresh HAOS installs auto-bootstrap from PyPI latest stable; the refresher still upgrades to the builder add-on's pinned version if one shows up later (idempotent under VersionManager's LRU cache). (2) `set_esphome_version_handler` schedules `ensure_esphome_installed(version)` in an executor, mirroring `reinstall_esphome` at `ui_api.py:1447-1454`. Closes HT.13a's "compile timeout 10 min" blocker and HT.13b's latent hypothesis — the `push-to-haos.sh` orchestrator-level poll becomes a thin assertion rather than a workaround. 