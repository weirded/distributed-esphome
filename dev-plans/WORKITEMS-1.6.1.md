# Work Items — 1.6.1

Theme: **Home Assistant gold-standard polish.** Lift the add-on toward the highest practical Supervisor security score (AppArmor profile, declared `signed`, digest-pinned bases) and the bundled integration from `bronze` to `gold` on the HA Integration Quality Scale (diagnostics, repairs, system health, reauth/reconfigure flows, ≥95% test coverage). Plus the surface polish reviewers notice first — proper icon/logo sizes, multi-language translations, service-action docs, repo hygiene files.

Background: there is no single "Gold" tier for add-ons in the HA ecosystem — the operational definition for this release is **add-on security stars (max practical) + integration Quality Scale Gold + presentation/repo polish**. Today the security posture is already strong (cosign signatures, SBOM attestations, hash-pinned deps, full `SECURITY.md` threat model); the gap is in declarative surfaces (AppArmor, `signed: true`) and the integration's Gold-tier rules (currently `quality_scale: bronze`).

Some current `config.yaml` flags (`host_network: true`, `hassio_api: true`, `homeassistant_api: true`, `auth_api: true`) are deliberate trade-offs — not gaps to close. Each gets a one-paragraph justification in `DOCS.md` so the lower star count has a written rationale users can read.

## Security score (Supervisor stars)

- [x] **SS.1 AppArmor profile** *(1.6.1-dev.5, first-pass permissive)* — `ha-addon/apparmor.txt` lands + `apparmor: true` declared in `config.yaml`. Supervisor loads the named profile (`esphome_dist_server`, renamed by Supervisor to `local_esphome_dist_server` on install) so the add-on runs under AppArmor confinement rather than fully unconfined, which flips the Supervisor "AppArmor" security star. Dmesg confirms the profile is applied (`operation="profile_load"`).

  **First-pass is deliberately permissive** (`file,`, `capability,`, `network,`, `signal,`, `dbus,`, `unix,`, `mount,`, `pivot_root,`, `ptrace,`). Two earlier attempts with path-level allows hit denials — the stock `abstractions/python` include didn't cover `/usr/local/lib/libpython3.11.so.1.0` on our slim base (boot-time failure, `DENIED open`), and a broader `file,` with explicit `deny /config/...` carveouts passed boot but broke PlatformIO's `penv` bootstrap during compile. The working set of narrow rules varies by Python minor version + PlatformIO release + ESPHome toolchain, so itemising them would churn on every upstream bump. Permissive-but-attached buys the security-star credit today; tightening is tracked as future work against observed denial telemetry.

  Hass-4 smoke 17/17 green with the profile active. No DENIED entries in dmesg after profile replace.
- ~~**SS.2 `signed: true`**~~ WONTFIX *(1.6.1-dev.1)* — there is no `signed` field in Supervisor's add-on `config.yaml` schema. The only sibling concept is `codenotary:` (pre-2024 CAS signer integration), which is effectively abandoned and doesn't validate against Sigstore's cosign signatures. Our images *are* cosign-signed via keyless OIDC (`publish-server.yml` / `publish-client.yml`) and the signatures are verifiable externally with `cosign verify` — the `DOCS.md` → "Verifying what you're running" section has the command. Supervisor doesn't surface a "signed" UI badge for cosign today, so there's no line to flip. Revisit if Supervisor adds native cosign support.
- [x] **SS.3 Documented privilege exceptions** *(1.6.1-dev.2)* — new "Why this add-on requests these permissions" subsection in `ha-addon/DOCS.md` covers each non-default flag (`host_network`, `hassio_api`, `homeassistant_api`, `auth_api`, `config:rw` map) with a concise reason each is load-bearing. Store-page readers now have a written rationale for the lower star count rather than just seeing "uses elevated permissions" with no context.
- [x] **SS.4 Base image digest pinning** *(1.6.1-dev.5, partial — worker fully pinned, server blocked by Supervisor regex)*:
  - **Worker** (`ha-addon/client/Dockerfile`): digest-pinned since CF.3 in 1.6; digest re-verified this cycle against `docker buildx imagetools inspect python:3.11-slim`, unchanged from last release (`sha256:233de06753d30d120b1a3ce359d8d3be8bda78524cd8f520c99883bfe33964cf`).
  - **Server `ARG BUILD_FROM` default** (`ha-addon/Dockerfile`): digest-pinned this cycle. Local `docker build` without Supervisor now uses the pinned digest by default. In production Supervisor overrides this via `--build-arg BUILD_FROM=<value from build.yaml>`, so the ARG default only matters for local / CI reproducibility.
  - **Server `build.yaml`**: attempted the pin on current Supervisor (hass-4, 2026-04) — Supervisor silently rejects `@sha256:…` in the `build_from` map (the regex at `supervisor/validate.py` is unchanged from the CF.3-era observation). When the pin is present, Supervisor falls back to its default arch-base image (`ghcr.io/home-assistant/<arch>-base`, Alpine/musl) without surfacing a validation error; the add-on build then fails on our `apt-get` step (Alpine has no apt), which is how we detect the rejection. Kept build.yaml tag-only; the constraint has been re-documented in the file so the next attempt doesn't rediscover it. Full digest pinning at the Supervisor level remains **BLOCKED on upstream Supervisor** accepting `@sha256:…` in the `build_from` regex.

## Integration: Bronze → Gold

Today `ha-addon/custom_integration/esphome_fleet/manifest.json` declares `"quality_scale": "bronze"`. Gold has 24 rules. The flip to `"gold"` happens in QS.9 only after the others land.

- [ ] **QS.1 Diagnostics support** — new `ha-addon/custom_integration/esphome_fleet/diagnostics.py` exposing `async_get_config_entry_diagnostics`. Returns a redacted dump of coordinator state (workers, queue, devices, settings) using `homeassistant.components.diagnostics.async_redact_data` to scrub the server token. Test: `tests/test_integration_diagnostics.py`.
- [ ] **QS.2 Repairs** — new `repairs.py` seeding repair issues: `token_invalid` (401s persist past one cycle). Each gets a translation key in `strings.json`. Use `ir.async_create_issue` / `ir.async_delete_issue` so resolved issues clear themselves on the next coordinator update.
- [ ] **QS.3 System health** — new `system_health.py` surfacing server reachability, ESPHome version, worker count, queue depth in HA's System Health dashboard.
- [ ] **QS.4 Reauthentication flow** — wire `async_step_reauth` in `config_flow.py`. SP.8's token rotation is a real concern now; without reauth, rotating the token via the Settings drawer requires deleting + re-adding the integration. Translation strings + a test under `tests/test_integration_config_flow.py`.
- [ ] **QS.5 Reconfiguration flow** — `async_step_reconfigure` in `config_flow.py` so users can edit server URL / token via the integration's "Configure" button instead of removing and re-creating.
- [ ] **QS.6 Stale-device cleanup** — coordinator's `_async_update_data` should remove devices that have disappeared from the server's targets list. Today they linger forever in the device registry. One DR (device registry) query, one `dr.async_remove_device` per stale entry.
- [ ] **QS.7 Translatable exception messages** — replace any `raise ValueError("...")` / raw-string exceptions in service handlers and the coordinator with `raise HomeAssistantError(translation_domain=DOMAIN, translation_key=...)`. Add the keys to `strings.json` + `translations/en.json`.
- [ ] **QS.8 Test coverage to ≥95%** — depends on **IT.1 / IT.2 / IT.3** in `WORKITEMS-1.6.md` (the Integration Test Refactor that switches from `SimpleNamespace` mocks to a real `pytest_homeassistant_custom_component` `hass` fixture). Without IT.*, the "≥95% coverage" claim is honest but the *quality* of the coverage isn't Gold-grade. Treat IT.* as a precondition — surface them here as a dependency rather than re-listing.
- [ ] **QS.9 Flip `quality_scale` to `gold`** — manifest edit, **last** in the workstream. Only after QS.1–QS.8 land and the upstream rules checker (run `python3 -m script.hassfest --action validate` against the integration if reachable, or the equivalent `homeassistant.scripts.check_config` flow) accepts the manifest claiming `gold`. CI invariant: add a hassfest-run step to the test job so a manifest claiming `gold` fails CI if any rule regresses later.

## Presentation polish

- [ ] **PR.1 Icon at 128×128** — current `ha-addon/icon.png` is 64×64 (half the convention). Re-render at 128×128 PNG. Add an SVG sibling `ha-addon/icon.svg` since the source vector exists at `ha-addon/ui/src/assets/esphome-logo.svg`.
- [ ] **PR.2 Logo at landscape ratio** — current `ha-addon/logo.png` is 192×192 square. HA convention is ~250×100 horizontal. Re-export the wordmark + glyph as a horizontal lockup so it fits the Add-on Store header banner without awkward letterboxing.
- [x] **PR.3 Service-action documentation** *(already satisfied as of 1.6.1-dev.1)* — `ha-addon/custom_integration/esphome_fleet/services.yaml` already carries `name:` + `description:` on every service (`compile`, `cancel`, `validate`) and every field (`worker`, `targets`, `esphome_version`, `job_ids`, `target`). The HA Developer Tools → Actions page surfaces the full docs. The workitem's "currently bare" framing was stale by the time 1.6.1 opened — no new work needed.
- [ ] **PR.4 Add-on translations beyond `en`** — at minimum `de.yaml` / `es.yaml` / `fr.yaml` under `ha-addon/translations/`, mirroring `en.yaml`. Same for the integration's `translations/` directory. Auto-translate as the seed and accept community PRs to refine. Lower priority than PR.1–PR.3; can slip to a future release if scope is tight.
- [ ] **PR.5 Submit integration branding to `home-assistant/brands`** *(follow-up from 1.6 #58)* — HA's Integrations UI fetches logos from `brands.home-assistant.io/<domain>/*.png`, which requires a PR to [`home-assistant/brands`](https://github.com/home-assistant/brands). Deliverables: `custom_integrations/esphome_fleet/{icon,logo}.png` at 256×256, plus `icon@2x.png` / `logo@2x.png` at 512×512. Source art lives in `ha-addon/ui/src/assets/esphome-logo.svg`; re-export the sizes and open the PR. Today the integration shows as a generic placeholder on the HA Integrations page; this lands the proper ESPHome-Fleet wordmark there. PR.1 + PR.2 should land first so the add-on store and the integration both use the same refreshed artwork.

## Repository hygiene

- [ ] **RH.1 `LICENSE` file at repo root** — `README.md` mentions MIT but there is no `LICENSE.txt`/`LICENSE`. Required by many automated scanners and by GitHub's own license-detection (currently shows "No license"). One file, MIT text, copyright `2024–2026 Stefan Zier`.
- [ ] **RH.2 `CONTRIBUTING.md`** — short doc covering: how to run the test suite (`pytest tests/` + `cd ha-addon/ui && npm run test:e2e`), the `bump-dev.sh` end-of-turn loop, the WORKITEMS bug-numbering convention, the PY/UI/E2E enforced invariants, and a pointer to `CLAUDE.md` for the deeper conventions.
- [ ] **RH.3 `.github/CODEOWNERS`** — single line `* @weirded` (or scoped per directory if more contributors land later). Stops accidental drift if a future contributor opens a PR without explicit reviewer assignment.
- [ ] **RH.4 Optional `ARCHITECTURE.md`** — short outside-reader summary of server/worker tiers + auth model, with a pointer to `CLAUDE.md` for the deep version. The existing `CLAUDE.md` is excellent but framed as Claude-developer guidance, not project-architecture intro. Stretch goal — skip if time is tight.

## Open Bugs & Tweaks

### Carried forward from 1.6

- [x] **#111** *(1.6.1-dev.7)* *(carried from 1.6)* — new `useVersioningEnabled()` hook wraps the SWR settings read and is consumed by every UI surface that leads to the History drawer. Config-history hamburger item is disabled + tooltip-explained when versioning is off. Editor toolbar's History button is hidden. "Commit changes…" hamburger item is hidden. LogModal's "Diff since compile" button is hidden.

- [x] **#112** *(1.6.1-dev.7)* *(carried from 1.6)* — short-hash Commit column dropped entirely from the per-device `CompileHistoryPanel`, the fleet-wide `QueueHistoryDialog`, and the Queue tab's live table when versioning is off. Kept the SWR settings read consolidated through the new hook so a user who flips versioning on in Settings sees columns re-appear on the next poll without a reload.

### New in 1.6.1

- [x] **#1** *(1.6.1-dev.8)* — firmware download available from every history surface that shows the row. New `FirmwareDownloadMenu` component consolidates the .bin / .bin.gz × factory / ota dropdown used by QueueTab, per-device `CompileHistoryPanel`, and fleet-wide `QueueHistoryDialog`. Server-side, `JobHistoryDAO.get(job_id)` added so `/ui/api/jobs/{id}/firmware` can fall back to the persistent history table when the live queue has already coalesced the row — otherwise download would 404 the moment a job left the queue. `/ui/api/jobs/{id}/firmware-variants` has the same fallback so the UI's variant probe works on history rows. Accessibility: trigger still carries a stable `aria-label="Download firmware"` so mocked e2e tests (`compile-download.spec.ts`) stay green against the shared component.

- [x] **#2** *(1.6.1-dev.6)* *(carried from PR #64 review)* — `ArchivedDevicesList.tsx` now calls `fmtEpochRelative(a.archived_at)` instead of the inline `Math.floor(...)` block. Pluralisation / rounding / negative-delta handling are now shared with Queue / History / Last-compiled columns so the Archive dialog can't drift out of sync. No behaviour change; one import + one constant assignment.

- [x] **#3** *(1.6.1-dev.6)* — installed a module-level `logging.Filter` on the `aioesphomeapi.connection` logger inside `ha-addon/server/device_poller.py`. The filter downgrades any ERROR-level record whose message starts with `"disconnect request failed"` to DEBUG. Narrow match: only the specific disconnect-phase failure gets demoted; all other `aioesphomeapi` errors (connection-setup failures, auth failures, unexpected tracebacks during `/states` polling) still log at ERROR. The poller behaviour is unchanged; only the log level of the expected OTA-reboot disconnect path is adjusted.

- [x] **#4** *(1.6.1-dev.6)* — new canonical marker is `# esphome-fleet:` with an explanatory header (`# Read by the ESPHome Fleet add-on. Do not remove.`). `scanner.read_device_meta` accepts both the new marker and the legacy `# distributed-esphome:` for backward compat. `scanner.write_device_meta` always writes the new marker + explanatory header, and strips any legacy marker + stale header variants on rewrite so an edit-in-place migrates existing files transparently. Two comment references in `ui_api.py` plus one in `scanner.py` itself updated to match. Test coverage: new reader test for the new marker, preserved legacy-reader test, new migrator test (`test_write_device_meta_migrates_legacy_marker`). 72/72 scanner tests pass.

- [x] **#5** *(1.6.1-dev.5)* — `stage: experimental` removed from `ha-addon/config.yaml`. The add-on has shipped stable releases through 1.6 with full test coverage, cosign + SBOM supply-chain hardening, the AppArmor profile from SS.1, and a real threat model in `dev-plans/SECURITY_AUDIT.md`. No `stage:` key defaults to stable in Supervisor, so the experimental badge goes away on the next install / upgrade. In-line comment explains why the flag is gone so a future reader doesn't re-add it.

- [x] **#6** *(1.6.1-dev.9)* — `iputils-ping` added to both the server add-on (`ha-addon/Dockerfile`) and the standalone worker (`ha-addon/client/Dockerfile`). The OTA network-diagnostics probe in `client._ota_network_diagnostics` calls `ping -c 3 -W 2 <device>` and bubbles packet-loss / RTT summary lines into the job log; before this fix the missing binary surfaced as `Ping: [Errno 2] No such file or directory: 'ping'` on every OTA failure, which hid the real cause. Used `iputils-ping` over a Python ICMP library because raw-socket ICMP needs `CAP_NET_RAW` and conflicts with the SS.1 AppArmor profile. Defensive FileNotFoundError catch preserves legibility if a third-party image strips the binary. PY-4 invariant respected: `IMAGE_VERSION` bumped 6 → 7 and `MIN_IMAGE_VERSION` bumped 5 → 7 so standalone workers on the old image are flagged in the UI and get prompted to re-pull.

- [x] **#7** *(1.6.1-dev.10)* — two-part fix. (1) The Devices-tab IP column already existed with `defaultVisible: true`, but `loadColumnVisibility` treated "id absent from the stored list" as "explicitly hidden" — users upgrading from before the column was added saw it missing. Storage format is now `{known: string[], visible: string[]}` with backward-compat for the old flat-array shape; a column id absent from `known` falls back to its `defaultVisible` instead of being forced off. (2) Added a MAC→IP fallback via `/proc/net/arp` (`ha-addon/server/arp.py`) wired through `ui_api.py` — when the device poller has a cached `mac_address` but no fresh mDNS-derived IP, the ARP table fills it in. Surfaces as `address_source: "arp"` so the UI labels it *"via ARP"* under the IP. No subprocess, no ICMP, no new capabilities — just reads the file the kernel already maintains (`host_network: true` makes it visible). 5 new pytest cases in `tests/test_arp.py`; Playwright `persistence.spec.ts` updated to accept the new storage shape.

- [x] **#8** *(1.6.1-dev.10)* — worker-selection reason is captured at claim time and surfaced in both the live Queue tab and the fleet-wide Queue History dialog. `api._handle_claim` computes one of `pinned_to_worker` / `only_online_worker` / `fewer_jobs_than_others` / `higher_perf_score` / `first_available` from the registry snapshot and passes it to `queue.claim_next` as a hint; pinned jobs override the hint to `pinned_to_worker` inside `claim_next`. The reason is logged at INFO alongside the existing claim line (`Job X (…) claimed by host [id] worker N — reason=<r>`) and persisted on `Job.selection_reason` + the `jobs.selection_reason` history column (idempotent `ALTER TABLE` migration for existing DBs). UI: new `formatSelectionReason()` helper + `"Why"` column on QueueTab and QueueHistoryDialog, hover-tooltip explains the pill label.

- [x] **#9** *(1.6.1-dev.9)* — worker now archives every successful compile on the server, OTA path included. New shared helper `_archive_firmware_to_server(required=True|False)` in `client.py` wraps the collect-variants + per-variant upload + log flow; download-only callers pass `required=True` (upload is the point of the job), OTA-success / OTA-retry / OTA-failed callers pass `required=False` so an upload failure is a WARNING in the log rather than failing the job. Server `api._handle_firmware_upload` drops the `download_only` gate — the WORKING-state + worker-identity checks remain the real safety net. `_purge_firmware` + `firmware_budget_enforcer` + `reconcile_orphans` now protect any history row with `has_firmware` (not just `download_only`), so an archived OTA binary survives queue coalescing and is evictable only under disk-budget pressure. QueueTab's `canDownload` loosened: any successful compile with `has_firmware` offers Download, not just download-only jobs. Stale test `test_firmware_upload_rejects_non_download_only_job` flipped to `_accepts_` + assertion inverted (200 instead of 400); 169/169 server tests + 125/125 mocked e2e green.

- [ ] 10 marketing and positioning in documentation: I think we need to start skewing away from people who have many devices to just presenting this as an overall better user interface compared to the native ESPHome builder, even for people with a small number of devices. There are a few standout features we now have that we should highlight that make this a much better tool to use.

- [ ] 11 live logs fail with `Connection requires encryption` for encrypted devices when the initial scan indexed the YAML before the ESPHome venv finished installing (issue [#60 comment 4282642961](https://github.com/weirded/distributed-esphome/issues/60#issuecomment-4282642961)). Root cause: `build_name_to_target_map` in `ha-addon/server/scanner.py:1052` depends on `_resolve_esphome_config` to run ESPHome's `do_substitution_pass` so `esphome.name` resolves and `api.encryption.key` can be extracted. On fresh boot the ESPHome venv is lazy-installed (SE.*, 1–3 min); scans run during that window return `None` from the resolver, so `_encryption_keys` for the affected target never gets populated. `device_poller` still marks the device online (`device_poller.py:425` "requires encryption → online" branch), so nothing looks wrong — until live logs calls `APIClient(..., noise_psk=_encryption_keys.get(dev.name))`, gets `None`, and the device rejects the unencrypted handshake. Fix: hook a one-shot rescan off `ensure_esphome_installed`'s completion — when the install flips from in-flight to ready, re-invoke `build_name_to_target_map` and re-seed `device_poller._encryption_keys` / `_address_overrides` / `_name_map`. Belt-and-braces: also populate underscore/hyphen variants in `_encryption_keys` (scanner.py:1091-1093 already does this for `name_map` but not for the key map, so an mDNS-normalized `dev.name` vs a literal `esphome.name` would still miss). Diagnostic surface: `GET /ui/api/targets/{f}/api-key` (ui_api.py:2316) already returns 404 when the entry is absent — add a one-liner to the "Device not found" / "Connection requires encryption" log paths suggesting operators hit that endpoint so support threads like #60 can be triaged in one round trip. Regression guard: unit test that primes the scanner in "venv not ready → ready" order and asserts `_encryption_keys` contains the key after the transition.