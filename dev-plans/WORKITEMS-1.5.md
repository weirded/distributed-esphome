# Work Items — 1.5.0

Theme: **UI quality + rebrand + HA native integration.** Harden the frontend (split the DevicesTab god component, close accessibility gaps, clean up the API layer, backfill e2e coverage), rebrand the product to **ESPHome Fleet** (user-facing names + integration domain), and make ESPHome Fleet a first-class HA citizen with native services, update entities, and mDNS discovery.

> **Release numbering note (#70):** This release was developed as `1.4.1-dev.N` through dev.72. Renamed to 1.5.0 late in the cycle because the scope grew past a normal patch release (HA native integration, ESPHome unbundling, security hardening). Existing `*(1.4.1-dev.N)*` completion stamps inside this file refer to the actual build tags that were produced during development — they are kept as-is so Docker tags and git SHAs remain discoverable.

## UI Cleanup

- [x] ~~**QS.1**~~ WONTFIX *(1.4.1-dev.5 audit)* — `src/lib/utils.ts` exports `cn()` (tailwind-merge + clsx), which is imported by 7 shadcn components (`ui/dialog.tsx`, `ui/badge.tsx`, `ui/button.tsx`, `ui/checkbox.tsx`, `ui/dropdown-menu.tsx`, `ui/select.tsx`, `ui/input.tsx`). The original grep in this item missed the `@/lib/utils` alias path. Keeping the file.
- [x] **QS.2** *(1.4.1-dev.5)* — `aria-label` (+ `aria-pressed` on the toggles) added to the 5 icon-only buttons:
  - App.tsx theme toggle (☀/☾) — `aria-label` + `aria-pressed={theme==='light'}`
  - App.tsx streamer mode (👁/🔒) — `aria-label` + `aria-pressed={streamerMode}`
  - DevicesTab row hamburger (⋮) — already added in QS.16 (`aria-label="More actions"`)
  - DevicesTab column picker (⚙) — `aria-label="Toggle columns"`
  - EsphomeVersionDropdown refresh (↻) — already added during PR #54 review
- [x] **QS.3** *(1.4.1-dev.5)* — Converted `<span onClick>` to `<button type="button">` for Secrets, theme toggle, streamer toggle in App.tsx. SortHeader already landed in QS.21.
- [x] **QS.4** *(1.4.1-dev.5)* — Replaced `data.key!` non-null assertion in `getApiKey()` with an explicit null-check that throws `Error('Server did not return an API key')`. Callers (DeviceContextMenu) now get a meaningful message instead of crashing downstream on `clipboard.writeText(undefined)`.
- [x] **QS.5** *(1.4.1-dev.5)* — Device interface already had `compile_target?: string | null` (the referenced line had moved); added the JSDoc block clarifying the Device-vs-Target distinction and what `compile_target: null` means (unmanaged device, no matching YAML).
- [x] **QS.6** *(1.4.1-dev.5)* — Dropped the custom `JSON.stringify`-based `deepCompare`. SWR's default stable-hash compare already prevents re-renders when polled data is structurally unchanged, and the custom version was O(n) per tick + broke on undefined/circular + hid legitimate key-order differences.
- [x] **QS.7** *(1.4.1-dev.5)* — Replaced the five `onError: () => {}` silent swallows with a `logSwrError(key)` helper that does `console.error('SWR', key, err)`. Each SWR key (`serverInfo`, `versions`, `workers`, `devices`, `queue`) now bubbles errors to the console with its identity attached. Stretch (top-of-page banner when `serverInfo` SWR has an error) not implemented — deferred to WORKITEMS tbd if we actually see errors in the logs.

## API Layer Cleanup

- [x] **QS.8** *(1.4.1-dev.12)* — Added `parseResponse<T>(response, errorTag)` and `expectOk(response, errorTag)` helpers at the top of `api/client.ts`. Both delegate to a shared `_readError` that tries `response.json().error` first, then falls back to `<errorTag> (HTTP <status>)`. ~30 boilerplate blocks (`if (!r.ok) { const data = await r.json().catch(...); throw new Error(...); }`) collapsed into single helper calls. File size unchanged in lines but the redundant pattern is gone — one place to fix the error path if it changes.
- [x] **QS.9** *(1.4.1-dev.12)* — Named response interfaces at the top of `api/client.ts`: `CompileResponse`, `CancelResponse`, `RetryResponse`, `RemoveResponse`, `ClearResponse`, `ScheduleResponse`, `SaveTargetResponse`, `CreateTargetResponse`, `RenameTargetResponse`, `ApiKeyResponse`, `JobLogResponse`, `ValidateResponse`, `SecretKeysResponse`, `EsphomeSchemaResponse`. Replaced inline `as { enqueued: number }` and similar casts. Wire contract is now self-documenting and exported types can be imported by tests/components.
- [x] **QS.10** *(1.4.1-dev.12)* — `getTargets`, `getDevices`, `getWorkers`, `getQueue`, `getServerInfo`, `getEsphomeVersions`, `getTargetContent`, `getJobLog`, `getArchivedConfigs` all now route through `parseResponse<T>` so server-provided `error` strings propagate up instead of being swallowed by a generic `"Failed to fetch X"`. Stretch goal (top-of-page error banner on serverInfo SWR error) deferred — `logSwrError` from QS.7 already lands the error in the console.

## Component Hygiene

- [x] **QS.11** *(1.4.1-dev.13)* — New `components/ui/label.tsx` exporting `<Label>` for the field-label pattern (block, uppercase, tracking-wide, muted). Replaced 10 hand-rolled `<label className="block text-[11px] font-medium uppercase tracking-wide text-[var(--text-muted)] mb-1">…</label>` instances across `ConnectWorkerModal` (8), `UpgradeModal` (2), and `DeviceTableModals` (1). `htmlFor` prop forwarded for accessibility — wired `htmlFor="rename-device-name"` on the rename modal.
- [x] **QS.12** *(1.4.1-dev.13)* — `RenameModal` now uses the existing `<Input>` wrapper instead of a raw `<input style={{...}}>`. The inline style block (`width: 100%, padding: 8px 12px, background: var(--surface2), border: 1px solid var(--border), borderRadius: var(--radius), color: var(--text), fontSize: 14`) is gone — `Input` already encodes the canonical look. Also swapped the inline-styled `<label>` and `<p>` to use Tailwind utilities for the small typography tweaks.
- [x] **QS.13** *(1.4.1-dev.13)* — New `components/ui/button-group.tsx` exporting `<ButtonGroup>` — a segmented-button wrapper that strips child borders/radii via descendant selectors and re-adds the segment dividers. Replaced the inline `<div style={{ display: 'flex', gap: 0, border: ..., borderRadius: ..., overflow: 'hidden' }}>` with two children that each had `style={{ borderRadius: 0, border: 'none' }}` overrides in `ConnectWorkerModal`'s shell toggle. The `ScheduleModal` mode-toggle the workitem mentioned was already gone (folded into `UpgradeModal`'s radio buttons in 1.4.0); only one current user, but the component is in place for future segmented controls.
- [x] **QS.14** *(1.4.1-dev.17)* — Audit and convert inline `style={{ ... }}` to Tailwind. Migrated ~100 inline styles across the high-leverage tab/column files: `useDeviceColumns.tsx` (32), `WorkersTab.tsx` (21), `QueueTab.tsx` (18), `DevicesTab.tsx` (18), `SchedulesTab.tsx` (13). Common patterns folded: `fontSize: 10/11/12` → `text-[Npx]`, `color: 'var(--…)'` → `text-[var(--…)]`, `display: 'inline-flex', alignItems: 'center', gap: N` → `inline-flex items-center gap-N`, `fontFamily: 'monospace'` → `font-mono`, `cursor: 'pointer'` → `cursor-pointer`, `fontStyle: 'italic'` → `italic`. Only genuinely dynamic styles kept inline (computed colors from runtime state, conditional opacity).
- [x] **QS.15** *(1.4.1-dev.16)* — **Decision: Lucide everywhere.** Documented in CLAUDE.md under Design Judgment: *"All UI icons come from lucide-react. No emoji glyphs (🕐 📅 📌), no HTML entities (`&#8942;`, `&#9881;`), no custom SVGs inline."* Migration touches 10 files. All emoji status indicators become colored Lucide icons (🕐→Clock, 📅→Calendar, 📌→Pin, 🔒/👁→EyeOff/Eye, ☀/☾→Sun/Moon, ✓/✗→Check/X, ●→Circle filled). All HTML entities become Lucide (`⋮`→MoreVertical, `⚙`→Settings2, `↻`→RotateCw, `↓`→Download, `↗`→ExternalLink, `▲/▼`→ChevronUp/Down in the shared SortHeader — cascades to all 11 sortable columns). Each icon-only button retains its `aria-label`; icons that carry semantic meaning beyond decoration (status, pin, trigger kind) wrap in a `<span title="…">` so hover still reveals context. Lucide 1.7.0 was already a dep via shadcn primitives; no new install.

## DevicesTab Split

The current `DevicesTab.tsx` is **1,173 lines with 24 hooks** and an ESLint disable for missing deps. Splitting it unblocks the 1.5 tag/group-by work that touches the same file.

- [x] **QS.16** *(1.4.1-dev.2)* — Replaced hand-rolled `DeviceMenu` with shadcn `DropdownMenu`. Dropped the manual positioning logic (`translateX(-100%)`, viewport-flip math, click-catching backdrop). New `components/devices/DeviceContextMenu.tsx`; placement, focus trap, click-outside, Escape, and keyboard nav now come from Radix.
- [x] **QS.17** *(1.4.1-dev.2)* — Extracted the 369-line columns `useMemo` into `components/devices/useDeviceColumns.tsx`. Removed the `// eslint-disable-next-line react-hooks/exhaustive-deps`. DevicesTab.tsx 1,001 → 631 lines.
- [x] **QS.18** *(1.4.1-dev.2)* — Extracted bulk Actions dropdown + bulk schedule modal into `components/devices/DeviceTableActions.tsx`. Owns its own `bulkScheduleOpen` state.
- [x] **QS.19** *(1.4.1-dev.2)* — Moved `RenameModal` + `DeleteModal` to `components/devices/DeviceTableModals.tsx`. RenameModal re-exported so existing App.tsx imports keep working.
- [x] **QS.20** *(1.4.1-dev.2)* — Memoized `handlePin`/`handleUnpin` in DevicesTab and `handleCompile`/`handleOpenUpgradeModal`/`handleDeleteDevice`/`handleRenameDevice`/`switchTab` in App.tsx so `useDeviceColumns`' dep array actually caches across SWR polls.
- [x] **QS.21** *(1.4.1-dev.2)* — Extracted shared `SortHeader` to `components/ui/sort-header.tsx`. Click target is a real `<button>` (semantic HTML); the `<th>` gets `aria-sort` via a `getAriaSort()` helper. Cascaded to all 11 sortable columns across Devices, Queue, Schedules.

## EditorModal + Utils Split

- [x] **QS.22** *(1.4.1-dev.15)* — New `components/editor/` submodule splitting the ~230 lines of Monaco glue out of EditorModal.tsx:
  - `yamlValidation.ts` — `ESPHOME_SCHEMA`, `collectSyntaxMarkers`, `validateYaml`, `validateYamlDebounced`, `loadComponentList`, + the `_componentList` / `_validationTimer` module-level state.
  - `completionProvider.ts` — `getConfigVars`, `findParentComponent`, `currentLineIndent`, `COMMON_SUB_KEYS`, plus `registerEsphomeCompletionProvider` (idempotent — gated on an internal flag so re-mounts don't duplicate) and `setEsphomeVersion` for the async provider to read the current version.
  - `monacoSetup.ts` — orchestrator `setupEsphomeEditor(editor, monaco)` that wires everything. EditorModal's `handleEditorDidMount` shrank to ~10 lines: store refs, call the orchestrator, add the dirty-decoration change listener. EditorModal.tsx 619 → 319 lines (-48%).
- [x] **QS.23** *(1.4.1-dev.15)* — Split `src/utils.ts` into `utils/format.ts` (timeAgo, stripYaml, fmtDuration, haDeepLink), `utils/cron.ts` (formatCronHuman), `utils/jobState.ts` (isJob* + getJobBadge). Old `src/utils.ts` kept as a barrel re-export so existing `from '../utils'` callsites work with zero change; new code should import from the submodule.
- [x] **QS.24** *(1.4.1-dev.15)* — Dropped the unused `onRename` prop from `EditorModal.Props` and the `void _onRename` compat line. Also removed the `onRename={…}` passed from `App.tsx` (the handler it wrapped was never called).

## Tests and Safety Net

- [x] **QS.25** *(1.4.1-dev.21)* — Add missing e2e coverage. Now fully spans the originally-listed scope. Initial batch (dev.18) shipped PT.1 / PT.3 / PT.5 / PT.6 / PT.7 / PT.8. Follow-ups in dev.19 added PT.2 (schedule-modal) and PT.4 (bulk-schedule). dev.21 closes the rest of the QS.25 enumeration with three new mocked spec files: `rename-delete.spec.ts` (5 tests — rename POST with `new_name` payload, delete archive flow, "Delete Permanently" two-step confirmation), `worker-actions.spec.ts` (5 tests — Clean Cache POST, Remove worker DELETE, parallel-jobs slot control debounce + payload, Connect Worker modal opens), `persistence.spec.ts` (5 tests — column visibility hides + survives reload + writes `device-columns` to localStorage, theme toggle round-trips via `theme` localStorage key, theme defaults to dark when no value, `showUnmanaged` round-trips).
- [x] **QS.26** *(1.4.1-dev.18)* — Add React Error Boundary around `<App />`. New `components/ErrorBoundary.tsx` class component wired into `main.tsx` between `<StrictMode>` and `<App />`. `getDerivedStateFromError` flips a state flag; the fallback UI is a centred card with the error message, a Reload button (window.location.reload), and a Dismiss button so transient render errors don't require a full reload. Stack trace forwarded to `console.error` for browser devtools / HA add-on log capture.
- [x] **QS.27 Optional polish** *(1.4.1-dev.55)* — All five sub-items landed:
  - `ConnectWorkerModal` consolidated 8× `useState` → `useReducer` with typed `FormState` + `{ type: 'set', field, value }` action. One dispatch replaces 8 fan-out effects; the `preset` pre-population path + docker-command derivation both read from one source of truth.
  - `address_source` is now a `type AddressSource = 'mdns' | 'mdns_default' | 'wifi_use_address' | 'ethernet_use_address' | 'openthread_use_address' | 'wifi_static_ip' | 'ethernet_static_ip'` union in `types/index.ts`, imported by `DevicesTab` + `useDeviceColumns`. Stale cases (`static`, `override_yaml`, `override_static`, `override_use_address`) dropped.
  - `LogModal` `setInterval` now carries a "why the 1 Hz tick exists" docstring — re-runs `timeAgo` every second so the elapsed counter ticks smoothly instead of stair-stepping with SWR's 1 Hz poll.
  - Per-tab sort state persists via a new `utils/persistState.ts` `usePersistedState` helper. Keys: `devices-sort`, `queue-sort`, `workers-sort`, `schedules-sort`. Survives page reloads + add-on redeploys.
  - URL query param `?tab=<name>` now deep-links to the tab. `_initialTab()` reads it on mount with sessionStorage fallback; `switchTab` calls `history.replaceState` so copy-paste/browser-back work. Invalid values fall through to the default.

## Playwright Coverage Backfill

### Mocked tests (`ha-addon/ui/e2e/`)

- [x] **PT.1** *(1.4.1-dev.18)* `pin-unpin.spec.ts` — 6 tests: pinned-device pin icon visible (with title="Pinned to 2026.2.0"), unpinned-device has no pin icon, hamburger on pinned shows "Unpin version (2026.2.0)" item, hamburger on unpinned shows "Pin to current version", clicking Unpin fires DELETE on the pin endpoint with the right target, clicking Pin fires POST.
- [x] **PT.2** *(1.4.1-dev.19)* `schedule-modal.spec.ts` — 6 tests: per-row Upgrade button opens modal in Now mode (heading "Upgrade — …", confirm button "Upgrade"); Schedules-tab Edit opens modal in Scheduled mode (heading "Schedule Upgrade — …", confirm button "Save Schedule"); Now/Scheduled radio toggle re-titles + re-labels confirm; saving a recurring schedule fires POST /schedule with `{cron: "0 2 * * *", tz: …}` for the right target; saving one-time fires POST /schedule/once; "Remove existing schedule" button (visible because garage-door has a schedule in fixtures) fires DELETE /schedule.
- [x] **PT.3** *(1.4.1-dev.18)* `schedules-tab.spec.ts` — 8 tests: lists scheduled devices (garage-door + office), tab badge shows count "2", search filters rows, recurring cell shows humanized cron ("Daily 03:00 · Active"), one-time cell shows "Once: …" prefix, Remove Selected disabled when no rows checked, Remove Selected fires DELETE for each checked row, Edit opens the Upgrade modal.
- [x] **PT.4** *(1.4.1-dev.19)* `bulk-schedule.spec.ts` — 5 tests: Schedule Selected and Remove Schedule from Selected items disabled with no row checked; selecting devices enables Schedule Selected; Schedule Selected opens the modal in scheduleOnly mode with "Schedule Upgrade — N devices" heading and Now radio hidden; saving a bulk schedule fires POST /schedule once per selected target; Remove Schedule from Selected fires DELETE /schedule for each scheduled target only (skipping unscheduled rows).
- [x] **PT.5** *(1.4.1-dev.18)* `queue-extras.spec.ts` — 5 tests: Triggered column shows "User" / "Recurring" cells, successful job uses Rerun label (not Retry), failed job uses Retry (not Rerun), Cancelled badge renders for cancelled jobs, Clear Succeeded sends `{states:['success']}` (no 'cancelled') to the clear endpoint.
- [x] **PT.6** *(1.4.1-dev.18)* `modal-sizing.spec.ts` — 4 tests: editor modal fits within viewport at 1024×768 + 1920×1080; log modal fits within viewport at 1024×768 + 1920×1080. Measures `[role="dialog"]` `getBoundingClientRect().right/bottom` against `window.innerWidth/innerHeight`. Regression guard for the "modal overflows the viewport" class of bugs.
- [x] **PT.7** *(1.4.1-dev.18)* `button-consistency.spec.ts` — 3 tests: Queue toolbar buttons have identical height, Schedules toolbar buttons have identical height, Workers tab's Connect Worker button has the canonical 28px height. (The Devices toolbar already had this test in `devices.spec.ts`.)
- [x] **PT.8** *(1.4.1-dev.18)* `cancel-new-device.spec.ts` — 2 tests: cancelling a brand-new device (Escape on the editor) fires DELETE on the `.pending.<name>.yaml` stub (regression guard for #42); closing an existing already-saved target does NOT fire DELETE (sanity check that the cleanup branch only triggers for unsaved entries in `unsavedNewTargetsRef`).

### Prod tests (`ha-addon/ui/e2e-hass-4/`)

- [x] **PT.9** *(1.4.1-dev.19)* `schedule-fires.spec.ts` — Sets a `schedule_once` 8 seconds in the future on cyd-office-info via API, polls the queue for a new job tagged `scheduled: true, schedule_kind: 'once'`, asserts the target's `schedule_once` clears after the fire, and cancels the resulting job to avoid burning a real compile cycle. Includes idempotent before-each cleanup so a previous failed run can't poison subsequent runs.
- [x] **PT.10** *(1.4.1-dev.21)* `incremental-build.spec.ts` — Two consecutive cyd-office-info compiles pinned to local-worker (shared `.pioenvs` cache). Asserts `second.duration_seconds ≤ 1.20 × first.duration_seconds` (env-tunable via `SPEEDUP_THRESHOLD`). Walked the threshold down then back up: original spec called for 0.5 (≥50% speedup), reality is tighter — three healthy back-to-back runs measured ratios of 0.76, 0.83, and 0.88, which is too noisy for any sub-1.0 threshold. Honest about what it catches: the realistic regression is "PlatformIO redownloads its package cache" → ratio ≫ 1.5. Ratio is logged so trends can still be eyeballed across CI runs.
- [x] **PT.11** *(1.4.1-dev.19)* `pinned-bulk-compile.spec.ts` — Posts a single-target bulk `/ui/api/compile` with an explicit `esphome_version` (chosen from `available[]` to differ from `selected`) and asserts the resulting queue job carries that exact version in `esphome_version`. Cancels the job after the assertion — the test verifies the wire-level pin survival, not actual compile output.

### Fixture updates

- [x] **PT.12** *(1.4.1-dev.18)* — Update `e2e/fixtures.ts`. Bedroom-light gained `pinned_version: '2026.2.0'`; garage-door gained a recurring `0 3 * * *` schedule (UTC, enabled); new `office.yaml` target with a `schedule_once` 24h in the future; queue gained job-006 (cancelled, living-room) and job-007 (scheduled recurring success, garage-door). New mock routes: `/pin`, `/schedule`, `/schedule/once`, `/schedule/toggle`, `/schedule-history`, `/meta`, `/workers/*/clean`. Existing smoke + queue-search tests adjusted with `.first()` where the new fixtures introduced duplicate rows.

## Rebrand: ESPHome Fleet

The product has grown beyond its original "distribute ESPHome compiles to faster machines" identity — 1.4 added fleet-scale management (version pinning, scheduled upgrades, device tags, Schedules tab). The current name "Distributed ESPHome" overweights the compile mechanism and underweights the actual user value. Rebrand to **"ESPHome Fleet"**.

**Ordering:** This section executes **before** the HA Native Integration section below. The integration's `DOMAIN` determines service names, entity IDs, events, and config entries — all of which are scripted against by users in automations. Changing the domain post-launch breaks every user's config. Doing the rebrand first lets us ship the integration with the `esphome_fleet` domain from day one.

**Scope: user-facing strings + integration domain.** Since HI.* ships in the same release with no users yet, we can also rename the in-progress integration domain (`distributed_esphome` → `esphome_fleet`) at zero migration cost. The rest stays the same — no breaking changes for existing add-on users.

**What stays the same:** repo name (`weirded/distributed-esphome`), Docker image names (`esphome-dist-server`, `esphome-dist-client`), add-on slug (`esphome_dist_server`), Python module names, logger names, and the YAML comment marker (`# distributed-esphome:` — parsed from users' existing YAML files; changing it would be a user-data migration).

- [x] **RB.1** *(1.4.1-dev.28)* — `config.yaml`: `name` → `"ESPHome Fleet"`, `panel_title` → `"ESPHome Fleet"` (was `"ESPH Distributed"`), `description` rewritten to highlight all four pillars (distributed compile, version pinning, scheduled OTA upgrades, unified UI) instead of overweighting compile.
- [x] **RB.2** *(1.4.1-dev.28)* — UI header span `Distributed Build` → `Fleet` (so the header chain now reads `ESPHome [logo] Fleet v1.4.1-dev.N`). `ui/index.html` `<title>` → `ESPHome Fleet`. Also flipped three Playwright fixtures that asserted on `getByText('Distributed Build')` to `getByText('Fleet', { exact: true })` so e2e didn't regress on the rename.
- [x] **RB.3** *(1.4.1-dev.28)* — `README.md`: heading `# Distributed ESPHome` → `# ESPHome Fleet`, opening tagline rewritten to lead with fleet management (bulk ops, scheduled OTA upgrades, version pinning, distributed compile, modern UI), image alt text + ASCII diagram (`ESPH Distributed` → `ESPHome Fleet`), install instructions (`**ESPHome Distributed Build Server**` → `**ESPHome Fleet**`), sidebar reference (`**ESPH Distributed**` → `**ESPHome Fleet**`).
- [x] **RB.4** *(1.4.1-dev.28)* — `DOCS.md`: heading `# ESPHome Distributed Build Server` → `# ESPHome Fleet`, sidebar reference + opening blurb rewritten with same framing as the README so the in-add-on docs match the public README.
- [x] **RB.5** *(1.4.1-dev.28)* — `repository.json`: `"name": "Distributed ESPHome"` → `"name": "ESPHome Fleet"` (HA add-on store display name for the repo entry).
- [x] **RB.6** *(1.4.1-dev.28)* — `CHANGELOG.md`: new 1.4.1 section opens with a user-facing rebrand callout: *"Same add-on, same Docker images, no migration needed — just a new name that better describes what the tool does."* Identifies what changes (sidebar, store name, browser tab, logs) and what doesn't (repo, image names, slug).
- [x] **RB.7** *(1.4.1-dev.28)* — `CLAUDE.md` project overview rewritten to lead with `ESPHome Fleet (internally: distributed-esphome) manages fleets of ESPHome devices — offloads compilation to remote workers, schedules upgrades, pins versions per device, and organizes devices via tags.` Added an explicit naming-convention paragraph so future Claude turns know which surfaces stay `distributed_esphome` / `esphome-dist-*`.
- [x] **RB.8** *(1.4.1-dev.28)* — Server `Dockerfile` `io.hass.name` + `io.hass.description` labels updated (description now mirrors the new config.yaml description). Startup script echo + `server/main.py` startup/shutdown INFO logs + module docstring all reference "ESPHome Fleet". `docker-compose.worker.yml` header comment updated. *PY-4 not triggered:* the invariant scope is the **client** (worker) Dockerfile (`ha-addon/client/Dockerfile`), and that file is unchanged. The server-Dockerfile labels are metadata only — no `COPY` content changed, so neither `IMAGE_VERSION` nor `MIN_IMAGE_VERSION` needs to bump.

**Verification:** deploy to hass-4 and confirm: HA sidebar reads "ESPHome Fleet", add-on page header reads "ESPHome Fleet", UI top nav reads `ESPHome [logo] Fleet v1.4.1-dev.N`, browser tab title reads "ESPHome Fleet", `ha addons logs` shows "Starting ESPHome Fleet" on restart, and the GitHub README renders with the new heading.

## HA Native Integration

Custom integration that makes ESPHome Fleet a first-class HA citizen: native services callable from automations, `update` entities on the HA dashboard, and zero-config discovery via mDNS. Auto-installed by the add-on on startup (files copied to `/config/custom_components/`, user confirms via the Integrations UI). Integration domain is `esphome_fleet` (set in RB, above — all identifiers below use the new domain from day one).

**Auto-install mechanism:** The add-on already maps `homeassistant_config` (for reading ESPHome YAMLs). Change to `read_only: false` so we can write to `/config/custom_components/`. On startup, an s6 script compares the bundled integration version against what's installed and copies if newer. Then calls the Supervisor API to reload custom components. This is the same pattern used by other community add-ons — not an official API, but widely used and stable.

**Discovery:** The server advertises `_esphome-fleet._tcp` via mDNS. The integration's `manifest.json` declares a `zeroconf` matcher. HA shows "ESPHome Fleet discovered" → user clicks Configure → one confirmation screen → done. Falls back to manual URL entry if mDNS isn't working.

- [x] **HI.1** *(1.4.1-dev.35)* — Integration scaffold at `custom_integration/esphome_fleet/`: `__init__.py` (async_setup/setup_entry/unload_entry), `manifest.json` (domain `esphome_fleet`, integration_type `hub`, iot_class `local_polling`, zeroconf matcher `_esphome-fleet._tcp.local.`), `config_flow.py` (manual URL + mDNS zeroconf with confirm screen), `const.py`, `strings.json`, `translations/en.json`, `services.yaml`. Structure adapted from Ardumine's [PR #57](https://github.com/weirded/distributed-esphome/pull/57) with the post-rebrand domain and name — credited in commit. All user-visible strings read "ESPHome Fleet"; all identifiers are `esphome_fleet`.
- [x] **HI.2** *(1.4.1-dev.35)* — Three HA services registered globally per-hass (not per-entry) so they show up in the automation editor immediately:
  - `esphome_fleet.compile` — top-level `target:` device selector (picks target YAMLs), optional `worker` device selector (picks a build worker to pin the job to), optional `targets` list (`list | "all" | "outdated"`) as the text-only alternative, optional `esphome_version`. Calls `POST /ui/api/compile`. (Field set evolved through #63 → #65 → #66; see those entries for the reasoning.)
  - `esphome_fleet.cancel` — `job_ids` (list). Calls `POST /ui/api/cancel`.
  - `esphome_fleet.validate` — `target`. Calls `POST /ui/api/validate`.
  - `services.yaml` defines each with field selectors so the automation editor gives autocomplete + validation. Voluptuous schemas in `services.py` validate inputs before the API call. Services are registered on first config-entry setup and torn down when the last entry is removed.
- [x] **HI.3** *(1.4.1-dev.36)* — `update.py` registers one `TargetFirmwareUpdate(CoordinatorEntity, UpdateEntity)` per managed target. `installed_version` = `target.running_version`; `latest_version` = `target.pinned_version` (if set) else the coordinator's `esphome_versions.selected`. `async_install()` calls `POST /ui/api/compile` with a single-target list (and optional ESPHome version override from the install dialog), then kicks the coordinator to refresh so the queue-depth sensor reflects the new pending job in ~1s. HA's Update card picks up "Update available" + Install button with zero extra UI work. `UpdateEntityFeature.INSTALL` set; `has_entity_name = True` so the per-device card shows just "Firmware".
- [x] **HI.4** *(1.4.1-dev.36)* — `sensor.py` ships three sensor types:
  - `QueueDepthSensor` — global, attached to the hub device. `native_value` = count of jobs whose state is `pending` or `working`. Unit: "jobs", `SensorStateClass.MEASUREMENT`, `EntityCategory.DIAGNOSTIC`.
  - `TargetFirmwareVersionSensor` — one per managed target. State = `target.running_version`. Diagnostic category so it doesn't clutter the primary UI.
  - `WorkerActiveJobsSensor` — one per worker. State = count of WORKING queue entries whose `assigned_client_id` matches. Same diagnostic treatment as queue-depth.
  Dynamic-add pattern: a coordinator listener re-scans on every update tick and `async_add_entities` only for targets/workers not already seen (tracked per-config-entry in a `seen: set[str]`).
- [x] **HI.5** *(1.4.1-dev.36)* — `binary_sensor.py` — one `WorkerOnlineBinarySensor` per worker. `is_on` = `worker.online`; `device_class = CONNECTIVITY` so HA picks up the right icon/label. Same dynamic-add pattern as the sensors.
- [x] **HI.6** *(1.4.1-dev.37)* — `EsphomeFleetCoordinator` now tracks per-job `{id → state}` across polls (`_last_job_states` dict) and fires `esphome_fleet_compile_complete` on the HA bus whenever a job transitions from a non-terminal state into one of `success`/`failed`/`timed_out`/`cancelled`. Event payload: `{job_id, target, state, duration_seconds, esphome_version, worker_hostname, worker_id, scheduled, schedule_kind}`. Jobs already terminal on the first poll don't fire (startup snapshots can contain last-night's history); transitions within the terminal set are ignored (metadata-only updates). Stale job ids drop out of the tracker when they leave the queue so the dict doesn't grow unbounded. Server push channel deliberately avoided — HA-side coordinator diffing keeps the server stateless w.r.t. HA.
- [x] **HI.7** *(1.4.1-dev.37)* — New `ha-addon/server/mdns_advertiser.py`. `FleetAdvertiser` registers a `ServiceInfo` for `_esphome-fleet._tcp.local.` against its own `AsyncZeroconf` instance, carrying `version` (from `/app/VERSION`), `base_url` (from primary IPv4 + configured port), and `protocol: "1"` as TXT properties. Wired into `on_startup` after `device_poller.start()` and torn down in `on_shutdown`. Matches the HA integration's zeroconf config-flow matcher (`_esphome-fleet._tcp.local.` in manifest.json) so discovery is one-click: HA sees the record, shows "ESPHome Fleet discovered", user confirms the pre-filled URL.
- [x] **HI.8** *(1.4.1-dev.37)* — New `ha-addon/server/integration_installer.py` with `install_integration(source_dir, destination_dir)`. Called at the top of `on_startup`: reads `manifest.json.version` from `/app/custom_integration/esphome_fleet/` (bundled by the Dockerfile's new `COPY custom_integration/`) and from `/config/custom_components/esphome_fleet/`. If destination is missing or older, it `rmtree`s + `copytree`s the source and logs one INFO line telling the user to open Settings → Devices & Services. Returns a status literal (`installed`/`updated`/`unchanged`/`skipped_no_source`/`skipped_no_parent`/`failed`) so future callers can gate on it. Deliberately NOT calling any Supervisor reload endpoint — the private ones are fragile; one INFO line + user-initiated HA restart is the safer path. Also: the `custom_integration/` tree moved from repo root to `ha-addon/custom_integration/` so it's inside the add-on build context. 5 new pytest tests cover fresh install, unchanged, update-replaces-old, missing-source, unparseable-manifest.
- [x] **HI.9** *(1.4.1-dev.37)* — `config.yaml`: `map: - config:rw` → `map: - homeassistant_config:rw`. Old key was the deprecated shorthand; new key is the canonical one per HA Supervisor docs. Gives HI.8's installer write access to `/config/custom_components/esphome_fleet/`. **Not** a PY-4 trigger: PY-4's scope is the **worker** Dockerfile (`ha-addon/client/Dockerfile`), not server-side config.yaml edits, so neither `IMAGE_VERSION` nor `MIN_IMAGE_VERSION` needs to bump.
- [x] **HI.10** *(1.4.1-dev.35)* — `EsphomeFleetCoordinator` in `coordinator.py` subclasses HA's `DataUpdateCoordinator[dict[str, Any]]`, polls the add-on's `/ui/api/server-info`, `/targets`, `/devices`, `/workers`, `/queue`, and `/esphome-versions` every 30s (`DEFAULT_POLL_INTERVAL_SECONDS` in `const.py`). Uses `async_get_clientsession(hass)` for HA's shared session. Exposes an `async_post_json(path, payload)` helper that services piggyback on for compile/cancel/validate. `async_config_entry_first_refresh()` in `async_setup_entry` makes the first poll blocking so a broken `base_url` surfaces as a setup error (not a silent no-data integration).
- [x] **HI.11** *(1.4.1-dev.36)* — `device.py` exports three `DeviceInfo` builders: `hub_device_info(entry_id, base_url)` for the add-on itself, `target_device_info(target, entry_id)` for each managed YAML (name falls back through friendly_name → device_name → filename stem; model = `"platform · board"` when known; sw_version = running_version; `suggested_area` from target.area; `via_device` chains to the hub), and `worker_device_info(worker, entry_id)` for each build worker (name = `"hostname (worker)"`, model = `"cpu · os"` when system_info is populated, sw_version = client_version). `__init__.py` pre-registers all three kinds via `device_registry.async_get_or_create` on setup AND on every coordinator update so newly-added targets/workers show up in HA's device registry without a restart. Identifiers: `("esphome_fleet", f"hub:{entry_id}")` / `"target:{filename}"` / `"worker:{client_id}"` — stable across friendly-name changes.
- [x] **HI.12** *(1.4.1-dev.39)* — Four new test modules, 37 new tests:
  - `tests/test_integration_config_flow.py` (14) — parametric `_normalize_base_url` cases: http/https, trailing slash, whitespace, IPv6 brackets → pass; paths/queries/fragments/non-http → reject.
  - `tests/test_integration_device_info.py` (9) — `hub_device_info`, `target_device_info` (friendly-name → device-name → filename-stem fallback; platform·board model; suggested_area copy-through; via_device chain), `worker_device_info` (hostname suffix, cpu·os model, empty-system-info fallback).
  - `tests/test_integration_coordinator_events.py` (6) — HI.6 state-transition logic: first-poll doesn't fire on terminal snapshots; pending→success fires with full payload; working→failed fires; terminal→terminal (metadata-only) doesn't re-fire; same-state across polls doesn't fire; disappeared jobs drop from the `_last_job_states` tracker so it doesn't grow unbounded.
  - `tests/test_integration_services.py` (7) — HI.2 handlers: compile with minimal payload, compile with version+worker, compile with "all" string sentinel, cancel payload, validate payload, `_first_coordinator` raises `HomeAssistantError` on empty domain data.
  - `tests/test_integration_installer.py` (already landed in HI.8) — 5 tests round out the installer coverage.
  Tests use `homeassistant` import (in CI deps) + stdlib mocks instead of `pytest-homeassistant-custom-component` — the HA test plugin's autouse `verify_cleanup` + `pytest_socket` fixtures break every existing server/client test that opens an aiohttp loopback. Plain `homeassistant` is lightweight enough and sufficient for exercising the integration's wire contract. Conftest gains a third sys.path entry for `ha-addon/custom_integration/`; CI's pip install gains the `homeassistant` package. Full pytest: 398 → 435.

## HA User Authentication

ESPHome Builder (the stock dashboard) has `auth_api: true` in its add-on `config.yaml`. We don't. That single flag gives an add-on access to Supervisor's `/auth` endpoint, which lets us actually *identify* HA users (not just "they passed HA Ingress") and validate their credentials on direct-port access.

Currently, `/ui/api/*` is unauthenticated by design — we trust HA Ingress (F-03 in the security audit is explicitly WONTFIX for this reason). With `auth_api`, F-03 becomes a real fix rather than an accepted risk: direct-port `:8765` can require a valid HA token, and we can attribute every mutation to the HA user who initiated it.

Scope: additive. Ingress-trusted access keeps working unchanged. Direct-port access gains an auth requirement. Action attribution surfaces in the server log + queue entries. **No new login UI** — for Ingress, the user is already logged into HA; for direct-port, we return 401 + a clean `WWW-Authenticate` challenge and document that direct-port clients must send `Authorization: Bearer <HA-long-lived-access-token>`.

- [x] **AU.1** *(1.4.1-dev.57)* — `auth_api: true` added to `ha-addon/config.yaml`. Gives the add-on access to Supervisor's `/auth` endpoint so it can validate HA-user tokens on direct-port calls. Config-only change; no PY-4 trigger.
- [x] **AU.2** *(1.4.1-dev.57)* — `ha_auth_middleware` in new `ha-addon/server/ha_auth.py`, wired after the worker-tier `auth_middleware`. Three paths (in order): (1) Supervisor peer trust — request from `172.30.32.2` gets `request["ha_user"]` from `X-Remote-User-Name`/`X-Remote-User-Id` headers; (2) Bearer token — `POST http://supervisor/auth` with the token, parse the response to `{name, id, is_admin}`; (3) neither → pass through (legacy) OR 401 + `WWW-Authenticate: Bearer realm="ESPHome Fleet"` when `require_ha_auth` is on. The bearer path uses the add-on's own `SUPERVISOR_TOKEN` to authenticate to Supervisor; the user's token is the body payload.
- [x] **AU.3** *(1.4.1-dev.57)* — `require_ha_auth: bool` add-on option (default `false`), plumbed through `AppConfig`. When enabled, direct-port `/ui/api/*` calls must carry a valid HA Bearer. Ingress-tunneled requests continue to work because `X-Ingress-Path` arrives via the trusted Supervisor peer. `SECURITY_AUDIT.md` F-03 updated from WONTFIX to FIXED.
- [x] **AU.4** *(1.4.1-dev.57)* — Mutation attribution. New `_who(request)` helper in `ui_api.py` appends `" by <name>"` to mutation log lines when `request["ha_user"]` was resolved. Applied to: compile enqueue, cancel, pin, unpin, meta update, schedule set/remove/toggle/once, save content, delete, rename — 11 handlers in total. Unauthenticated requests continue to log unchanged (no trailing "by …").
- ~~**AU.5**~~ REMOVED — Admin-only gating for destructive actions. Struck: this is a home-lab tool used by 1-2 people; role-based access control adds complexity without value for the target audience.
- [x] **AU.6** *(1.4.1-dev.57)* — 11 new tests in `tests/test_ha_auth.py`: Supervisor-peer trust (w/ and w/o user headers), bearer-valid / bearer-invalid paths, `require_ha_auth=true` 401s, non-`/ui/api/` bypass, Supervisor-peer precedence over bearer, `_validate_bearer_with_supervisor` returns None without `SUPERVISOR_TOKEN`, parses user from 200 response, and returns None on non-200. `DOCS.md` gains a "Direct-Port API Access" section with the long-lived-access-token + `require_ha_auth` + `curl` walkthrough.

## Server Performance

- [x] **SP.1** *(1.4.1-dev.7)* — Added `compression_middleware` to the aiohttp app that calls `response.enable_compression()` on any `StreamResponse` that's not a WebSocket and doesn't already have `Content-Encoding`. aiohttp honors the client's `Accept-Encoding` so it's a no-op on clients that don't send gzip, and skips already-compressed responses. Works for every `/ui/api/*` JSON response, static JS/CSS assets, and the INDEX_HTML template — all uncompressed before. On a 50-device `/ui/api/targets` response (~40-50 KB) this is a ~5× shrink, and on the 1 Hz polls for workers/devices/queue it adds up fast over mobile/VPN links.
- [x] **SP.2** *(1.4.1-dev.7)* — `/ui/api/queue` now strips `log` from **every** job in the list response, not just pending/working. Previously terminal jobs carried up to 512 KB of log each in the 1 Hz poll; 10 finished jobs = ~5 MB/s of redundant payload on steady-state (the log modal already fetches individually). Frontend: QueueTab's `hasLog` changed from `job.log || inProgress` to `job.state !== 'pending'` (terminal jobs still show the Log button because the modal lazy-loads via `/ui/api/jobs/{id}/log`). LogModal's terminal-job path now also calls `startPolling(jobId)` instead of writing `currentJob.log` directly — does one full HTTP fetch and stops as soon as `finished: true`.
- [x] **SP.3** *(1.4.1-dev.7, already fixed + small cleanup)* — The referenced `app.get` vs `app["_rt"]` key mismatch was already fixed in 1.4.0 (commit `3ac6ded`). Live `ha addons logs` verification: the repeated INFO messages I saw were all during the push-to-hass-4 restart window (add-on restarts ~10 s apart during deploy); in steady state the logs fire exactly once per startup and then stay silent until the actual HA ESPHome add-on version changes. Small cleanup nonetheless: demoted `scanner.set_esphome_version`'s own INFO log to DEBUG since the three callers (on_startup, pypi_version_refresher, `/ui/api/esphome-version` POST handler) already log their own INFO with better context ("Active ESPHome version: X", "…detected: X", "…changed to X via UI"), and the generic helper was just duplicating the message at startup.

## Dependency Updates

Triage and merge the 8 open Dependabot PRs. Group into low-risk auto-merge, medium-risk CI-verify, and high-risk human review per the v1.3.1 release-checklist pattern.

### Low-risk — merge on green CI

- [x] **DU.1** *(1.4.1-dev.23)* — `globals` 17.4.0 → 17.5.0 (ui devDep). Applied directly in develop via `npm install globals@^17.5.0 --legacy-peer-deps` (PR #53 was against main; bringing the bumps to develop instead so they ride the next release with everything else). 99-test mocked Playwright suite + tsc -b clean.
- [x] **DU.2** *(1.4.1-dev.23)* — `typescript-eslint` 8.57.0 → 8.58.2 (ui devDep). Same path as DU.1; tsc -b clean. (Patch range, no rule changes affecting our codebase.)
- [x] **DU.3** *(1.4.1-dev.23)* — `lucide-react` 1.7.0 → 1.8.0 (ui dep, minor). Same path as DU.1. QS.15 already standardized us on Lucide so this is the canonical icon-library bump. 99-test mocked Playwright suite still green; spot-checked DevicesTab + WorkersTab + ScheduleTab icons render correctly.

### Medium-risk — merge after full Playwright + smoke test

- [x] **DU.4** *(1.4.1-dev.24)* — `@base-ui/react` 1.3.0 → 1.4.0. Applied directly in develop (PR #49 was against main; closed). Validated against the full 99-test mocked Playwright suite — every shadcn wrapper that exercises Base-UI primitives (Dialog, DropdownMenu, Select, Checkbox via the schedule + persistence + worker-actions specs) round-trips cleanly. Hass-4 9-test suite next.
- [x] **DU.5** *(1.4.1-dev.24)* — `@types/node` 24.12.0 → 25.6.0 (devDep, major, type-only). Applied directly in develop. tsc -b clean across both `tsconfig.app.json` and `tsconfig.node.json` projects — no new type errors in `vite.config.ts` or anywhere else. Bundle output unchanged (type-only bump).

### High-risk — human review required

- [x] **DU.6** *(1.4.1-dev.23)* — `docker/build-push-action` v6 → v7 (actions, major). Bumped in `publish-client.yml` and `publish-server.yml`. Will be validated by the GHCR push workflows on the dev.23 commit; failure would be visible immediately in the GH Actions UI and easy to revert.
- [x] **DU.7** *(1.4.1-dev.23)* — `docker/login-action` v3 → v4 (actions, major). Bumped in both publish workflows. Same registry/username/password inputs as v3 per release notes; same dev.23 GHCR run validates.
- [x] **DU.8** *(1.4.1-dev.23)* — `actions/checkout` v4 → v6 (actions, major, two versions jumped). Bumped in `ci.yml` (×4), `compile-test.yml` (×2), and both publish workflows. ubuntu-latest runners have Node 24 already so v6's runtime requirement is satisfied. dev.23 CI run validates.

### Process

- [x] **DU.9** *(1.4.1-dev.24)* — No-op. All 8 bumps were npm or GitHub Actions; no Python `requirements.txt` deps changed, so `scripts/refresh-deps.sh` doesn't need a rerun.
- [x] **DU.10** *(1.4.1-dev.24)* — Moot. Bumps were applied directly to develop in batches (no parallel dependabot rebases needed); the dependabot PRs against main were closed with a pointer to the develop commits.

## Supply Chain Hardening

Close the remaining open items from the refreshed `SECURITY_AUDIT.md` supply-chain section. Ordered small-to-large; the first two are quick wins that tighten the build pipeline without touching the runtime. The last two are larger pieces of infrastructure work.

- [x] **SC.1** *(1.4.1-dev.58)* — SHA-pin GitHub Actions (F-19). Replaced every `uses: <org>/<repo>@v<N>` in `.github/workflows/*.yml` with `@<40-char-sha> # v<N>.<M>.<P>` — 29 references across `ci.yml`, `compile-test.yml`, `publish-client.yml`, `publish-server.yml`. New SC.1 rule in `scripts/check-invariants.sh` asserts every non-local `uses:` line ends in a 40-char lowercase hex SHA; sanity-verified by flipping one back to `@v6` and watching the rule fire. Dependabot understands this form and will open PRs bumping both the SHA and the trailing-comment version as new tags land.

- [x] **SC.2** *(1.4.1-dev.60)* — SBOM generation + cosign attestation. Both `publish-client.yml` and `publish-server.yml` now run `anchore/sbom-action@e22c389 # v0.24.0` after the docker build to generate a CycloneDX JSON, attach it as a workflow artifact named `esphome-dist-{client,server}-<version>.sbom.json`, and bind it to the image digest via `cosign attest --type cyclonedx --predicate <sbom.json> <image>@<digest>`. `DOCS.md` gains a "Verifying the SBOM" subsection with the `cosign verify-attestation` → `jq` pipe that prints the component inventory. Verifiable, reproducible component listing for every release; no separate artifact store required.

- [ ] **SC.3 Worker pip install hash-pinning (F-18)** — ship pre-generated hash-pinned constraints files per supported ESPHome version inside the worker Docker image. Each time an ESPHome release lands, our release process regenerates `esphome-constraints/<version>.txt` via `pip-compile --generate-hashes --strip-extras --output-file=<version>.txt <version>-input.txt` (where `<version>-input.txt` is just `esphome==<version>`). Worker-time `version_manager.ensure_version()` invokes `pip install --require-hashes -c esphome-constraints/<version>.txt esphome==<version>`. **If a requested version has no shipped constraints file**, refuse the install with a clear user-facing error (*"ESPHome version X is not covered by a hash-pinned constraints file. Upgrade this worker, or use a version listed in `esphome-constraints/`."*) rather than silently falling back to unverified resolution. Medium scope, and it adds a release-process step: whenever `esphome` ships a new release, we regenerate the constraints + rebuild the worker image. A scheduled GH Action that opens a PR weekly with newly-generated constraints for any recently-published ESPHome version covers the automation. Closes the last open item from the 1.3.1 supply-chain workstream (item 1).

- [ ] ~~**SC.4**~~ — **WONTFIX**. Worker source-code auto-update is intentional core functionality; requiring `docker pull` + restart on every worker for each `-dev.N` bump is too much friction for the home-lab use case this project targets. The shared bearer token between server and workers already establishes a high-trust relationship — a compromised token means every job bundle + OTA is attacker-controlled either way, so removing *just* the source-code overlay doesn't meaningfully shrink the attack surface. Signing the bundle (option b) is theoretically cleaner but the key-rotation complexity isn't worth the marginal gain given the trust-boundary analysis. The SC.4 removal briefly landed in 1.4.1-dev.60 and was rolled back in dev.62 after user feedback. `SECURITY_AUDIT.md` F-02 rationale updated to reflect this deliberate choice. Revisit only if the deployment model shifts from home-lab to multi-tenant.

## Diagnostics: scanner + device-IP resolution logging

Issue #60 surfaced a class of user-facing bug where a YAML fails to resolve silently and the device shows "Checking" forever in the Devices tab with no usable error. Root cause: `_resolve_esphome_config` catches every exception at DEBUG level, and DEBUG is filtered out in production. This section closes the observability gap along the full IP-resolution pipeline — scanner, device poller, live-logs lookup — so users (and maintainers helping them) can trace exactly *how* Fleet tried to reach a given device and where it gave up. Pure logging changes; no behavior change, no API change, no new deps.

- [x] **DL.1** *(1.4.1-dev.58)* — Scanner resolve failures promoted to WARNING. `_resolve_esphome_config`'s generic except now logs `WARNING: Could not resolve config for <target>: <ExceptionType> (<msg>) — stack trace at DEBUG` plus the full traceback at DEBUG. Previously visible only at DEBUG (issue #60).
- [x] **DL.2** *(1.4.1-dev.58)* — Address waterfall logged per target. After `get_device_address` in `build_name_to_target_map`, the scanner emits `INFO: Target <file> → device <name> at <addr> (source=<src>)` once per scan per target. Narrows live-logs "Device not found" reports to one of three buckets immediately.
- [x] **DL.3** *(1.4.1-dev.58)* — `DevicePoller.update_compile_targets` promoted the proactive-create log from DEBUG to INFO and added a matching line for the fill-in-IP branch (covers devices created from mDNS before scanner had an address to offer).
- [x] **DL.4** *(1.4.1-dev.58)* — `ws_device_log` dumps the poller's current device list as INFO before sending the user-facing "Device not found" error. Operators can compare the failing target against the poller's known-devices list in a single log scroll.
- [x] **DL.5** *(1.4.1-dev.58)* — New `tests/test_scanner.py::test_resolve_failure_logs_warning`: supplies a YAML with an unresolvable `!secret`, calls `_resolve_esphome_config`, asserts `caplog` captures a WARNING record mentioning the target filename. Regression guard for DL.1.
- [ ] **DL.6** — Follow up on issue #60 once dev.58 is deployed. Ask `jrentsch` to upgrade and share the new WARNING / address-waterfall / poller-dump output. External; not a code task.


## Server ESPHome Unbundling

Bugs #51 (reverted) and #56 (PyObjC leak, twice) are symptoms of the same root: the server bakes a pinned `esphome==X.Y.Z` into its Docker image, so every version bump re-resolves a huge transitive graph with platform-conditional sharp edges. Bug #20 (ESPHome 2026.4.0 API break) is a sibling — the pinned server was blind to the upstream API change until we tried to bump. Patching each class one at a time is losing ground.

Full unbundling — ship the server with **no** ESPHome baked in, install the right version at first boot into `/data/esphome-versions/<ver>/` via the existing `VersionManager` (`ha-addon/client/version_manager.py`) — eliminates all three classes at the root. Infrastructure is already in the repo: `VersionManager` is the worker's version-install engine, and `ui_api.validate_config` already uses it for pinned devices. This section generalizes that pattern to the default server path.

By default the server tracks the HA ESPHome add-on's version (already surfaced by `pypi_version_refresher._ha_addon_esphome_version`) with a fallback to a floor constant, so users' server-side scanner and `esphome config` validate always match the version they actually expect.

**First-boot UX:** 3–5 minutes where the UI is functional but thin (no autocomplete, scanner metadata empty, validate returns 503). A top-of-app banner shows "Installing ESPHome X.Y.Z…". Subsequent restarts reuse the installed venv — no re-install.

- [x] **SE.1** *(1.4.1-dev.66)* — Dropped `esphome>=2024.1.0` from `ha-addon/server/requirements.txt`; regenerated `requirements.lock` via `scripts/refresh-deps.sh`. **800 lines (~50 transitive deps) fell out of the lockfile**, `aioesphomeapi` (standalone, not ESPHome itself) retained. Server Docker image is ~200 MB smaller. The lazy-install path from SE.2 becomes the primary code path; the scanner/components/validate degradation paths from SE.4–SE.6 ensure the UI stays functional during the first-boot install window.
- [x] **SE.2** *(1.4.1-dev.64)* — `scanner.ensure_esphome_installed(version)` blocking function + module globals (`_server_esphome_venv`, `_server_esphome_bin`, `_esphome_ready`, `_esphome_install_failed`). Calls `VersionManager(versions_base=Path("/data/esphome-versions"), max_versions=5).ensure_version(version)`, activates the venv, fires the ready event. `main.on_startup` schedules it as a background task via `asyncio.create_task(loop.run_in_executor(None, ensure_esphome_installed, version))` so the 1–3 min first-boot install never blocks aiohttp startup. Graceful degradation: on VersionManager exception / missing site-packages / import failure, flips `_esphome_install_failed` and returns cleanly. Shutdown cancels the task alongside the other background loops.
- [x] **SE.3** *(1.4.1-dev.64)* — `scanner._activate_esphome_venv(venv_path)` prepends `<venv>/lib/python{M.N}/site-packages` to `sys.path`. Resolves `M.N` from `sys.version_info` (VersionManager creates venvs with `sys.executable`, so they always match). Idempotent — re-activating a venv whose site-packages is already on `sys.path` is a no-op. Returns False on malformed venvs instead of raising.
- [x] **SE.4** *(1.4.1-dev.65)* — `_resolve_esphome_config` early-return when `_esphome_ready` is clear and the bundled `esphome` package isn't importable either. Logs INFO `ESPHome still installing — skipping config resolution for <target>` and returns `None`. Downstream callers (device_poller, `/ui/api/targets`) already fall back to `yaml.safe_load` on a `None` return, so friendly_name stays raw `${...}` for a couple of seconds until the venv is ready. Also hooked `pypi_version_refresher`: when the HA add-on's ESPHome version changes, schedule `ensure_esphome_installed(new_version)` in the executor so the server's venv tracks the add-on automatically (VersionManager cache-hits on already-installed versions).
- [x] **SE.5** *(1.4.1-dev.65)* — `ui_api.get_esphome_components` (the autocomplete endpoint) walks `<venv>/lib/python{M.N}/site-packages/esphome/components/` directly when the venv is ready, falling back to `esphome.loader.__file__` for the pre-SE.1 bundled state, and returning an empty list on hard failure. Autocomplete briefly off during first-boot install, then self-populates.
- [x] **SE.6** *(1.4.1-dev.65)* — `ui_api.validate_config` now defaults `esphome_bin` to `_server_esphome_bin` when the venv is ready, falling back to bare `"esphome"` for the pre-SE.1 / test-harness state. When no pin is set AND the venv isn't ready AND no `esphome` binary is on PATH, returns HTTP 503 with `{"success": false, "output": "ESPHome still installing, please retry in a moment"}` so the UI can surface a retry banner instead of crashing. Pinned-device branch unchanged — it still runs `VersionManager.ensure_version(pin)` independently.
- [x] **SE.7** *(1.4.1-dev.64)* — `scanner._get_installed_esphome_version()` now prefers the venv binary: when `_esphome_ready` is set it shells out to `<venv>/bin/esphome version`, parses the `Version: X.Y.Z` line, and caches the result until `set_esphome_version` fires (which busts the cache). Falls back to `importlib.metadata.version("esphome")` for the test harness and the pre-SE.1 transitional state. Returns `"installing"` mid-flight (ready event clear, failure flag clear) and `"unknown"` on terminal failure. One `subprocess.run` per version change, not per poll — the 1 Hz endpoints are cheap.
- [x] **SE.8** *(1.4.1-dev.66)* — `/ui/api/server-info` gained `esphome_install_status: "installing" | "ready" | "failed"` + `esphome_server_version: str`. New `POST /ui/api/esphome/reinstall` endpoint retries the install in the background. New `EsphomeInstallBanner` React component renders a sticky top-of-app strip during install/failed states; Retry button on failure wired to the reinstall endpoint. SWR tightens `serverInfo` polling to 3 s during `installing` and 30 s otherwise.
- [x] **SE.9** *(1.4.1-dev.66)* — `tests/test_esphome_unbundle.py` covers the lazy-install contract end-to-end: SE.3 venv activation (happy path, idempotency, malformed venv), SE.2 install (happy, VersionManager exception, module-globals wiring), SE.7 version lookup (venv binary + cache, cache-bust on `set_esphome_version`, installing vs unknown states), SE.4 degradation (`_resolve_esphome_config` returns None when no bundled esphome and venv not ready), and SE.8 server-info status transitions (ready/installing/failed) + reinstall endpoint. The existing `test_scanner.py::test_get_esphome_version_returns_unknown_when_not_installed` was updated to set `_esphome_install_failed=True` (previously the test ran with neither flag set, which post-SE.7 now correctly returns `"installing"`).


- [x] **SE.10** *(1.4.1-dev.66)* — CLAUDE.md's "Architecture → Server → scanner.py" bullet rewritten to describe the lazy-install lifecycle. Users see the install banner on first boot; subsequent restarts are instant because VersionManager cache-hits. No Dockerfile change needed — the Dockerfile already installs from the lockfile only, which now omits ~50 packages.


## Firmware Download (pulled forward from 1.5)

Let users compile a target **without** the OTA flash step, then download the resulting firmware binary from the Queue tab. Two primary use cases: first-flash (USB flashing a new ESP that hasn't been OTA-adopted yet) and disaster recovery (device is unreachable but you still want the latest build in hand).

**Mode selection.** Chosen in the UpgradeModal via a third radio alongside the existing "Now" / "Scheduled" pair — or, equivalently, a post-mode sub-toggle once the user picks "Now". Proposed UX: two radios inside "Now" mode labeled **"Compile + OTA"** (default, current behavior) and **"Compile + Download"**. When the second is chosen, the modal's Confirm button reads "Compile & Download" and the enqueued job carries a new `download_only: bool = true` flag. Scheduled mode always runs OTA — download-only scheduled runs don't have a clear use case and can be re-evaluated later.

**Lifecycle.** Binary lifetime is tied to the **queue item**, not the target. The worker POSTs the compiled firmware back to the server; the server writes it to `/data/firmware/{job_id}.bin`; the server deletes the file when the user clears the job from the queue (individual Clear, bulk Clear Succeeded/Finished/All, or the existing per-target-coalescing cleanup in `enqueue()`). Retrying a downloaded-only job is a fresh compile — the previous binary's storage is released when its job entry disappears. No TTL / age-based cleanup — consistent with #18's "users clear explicitly" stance.

**Download surface.** A **Download** button appears on Queue-tab rows when `job.state == 'success' AND job.download_only AND job.has_firmware == true`. Clicking streams the file as `{target-stem}.bin` with appropriate `Content-Disposition`. No device-row entry point — the concept only makes sense in a "this specific compile" context, which lives in the queue.

- [x] **FD.1** *(1.4.1-dev.42)* Protocol plumbing — `download_only: bool = False` added to the `JobAssignment` pydantic model in BOTH `ha-addon/server/protocol.py` and `ha-addon/client/protocol.py` (byte-identical per PY-6). `Job` dataclass in `job_queue.py` gains `download_only` + `has_firmware` (persisted in `to_dict` / `from_dict` with backward-compatible defaults so existing `/data/queue.json` files load without migration).
- [x] **FD.2** *(1.4.1-dev.42)* Enqueue accepts the flag — `/ui/api/compile` handler reads `body.download_only`, `JobQueue.enqueue(..., download_only=...)` wires it through to the created `Job`. Per-target coalescing from #23 still applies (tested). `validate_only` mutual exclusion is moot: `/ui/api/compile` doesn't accept `validate_only` at all; the validate endpoint is a separate path.
- [x] **FD.3** *(1.4.1-dev.42)* UpgradeModal UI — added a `nowAction: 'ota' | 'download'` sub-toggle with two radios ("Compile + OTA" / "Compile + Download (no OTA)") that only renders in Now mode. Scheduled mode hides them. Confirm button label swaps between "Upgrade" and "Compile & Download". `App.tsx` handler passes the flag through to `compile()`. Toast verb switches to "Compile-and-download queued for {target}" on download paths. Pin-update logic suppressed when download-only is selected (device state isn't changing).
- [x] **FD.4** *(1.4.1-dev.42)* Worker skips OTA + uploads binary — `client.py` reads `download_only` from the assignment and takes a new branch: `esphome compile <target>` (no OTA), then `_locate_firmware_binary()` walks `.esphome/build/<device>/.pioenvs/<device>/` picking `firmware.factory.bin` (ESP32) preferentially over `firmware.bin` (ESP8266), then `_upload_firmware()` POSTs the raw bytes via the new `post_bytes()` helper with a 10-minute timeout and 3-retry backoff. All `.py`-auto-updatable; no `IMAGE_VERSION` bump. Job log gets both the located path and the final byte count so ESPHome-layout shifts are diagnosable from the UI's Log modal.
- [x] **FD.5** *(1.4.1-dev.42)* Server receives + stores firmware — new `POST /api/v1/jobs/{id}/firmware` (worker tier; Bearer auth via existing middleware). Reads the raw body (not multipart — the worker just POSTs bytes), rejects 404 on unknown jobs, 400 on non-download-only, 400 on empty body, 409 on race (job moved out of WORKING during upload — the on-disk file is deleted in that branch), 500 on save failure, 200 on success. Storage at `/data/firmware/{job_id}.bin` via `firmware_storage.save_firmware`. `JobQueue.mark_firmware_stored()` flips `has_firmware` + persists — only when state is WORKING and the job was enqueued download-only.
- [x] **FD.6** *(1.4.1-dev.42)* Server serves firmware — new `GET /ui/api/jobs/{id}/firmware` (UI tier — HA Ingress trust). Uses `web.FileResponse` so aiohttp streams the file without buffering (tiny for ESP but future-proof for larger binaries). `Content-Disposition: attachment; filename="{target-stem}-{short-job-id}.bin"`. 404 when `has_firmware` is False or when the file is missing on disk (logs a warning in the latter case — indicates flag/filesystem drift).
- [x] **FD.7** *(1.4.1-dev.42)* Lifecycle cleanup — `JobQueue.remove_jobs` and `clear` both call a new `_purge_firmware(ids)` helper after mutating the queue; `enqueue`'s per-target stale-cleanup also purges (so "Compile + Download" a target twice in a row doesn't leak the first binary). `load()` calls `firmware_storage.reconcile_orphans(self._jobs.keys())` once at startup to sweep anything whose job no longer exists (crash recovery). Helpers resolve `DEFAULT_FIRMWARE_DIR` at call time (not import time) so pytest's monkeypatch works.
- [x] **FD.8** *(1.4.1-dev.42)* Queue-tab Download button — plain anchor styled via `buttonVariants({ variant: 'secondary', size: 'sm' })` with `href="./ui/api/jobs/{id}/firmware"` + `download` attr. Rendered only when `job.state === 'success' && job.download_only && job.has_firmware`. Click is a regular browser download — no modal, no toast. OTA-mode rows and pending/working rows don't render it.
- [x] **FD.9** *(1.4.1-dev.42)* Tests — 17 new pytest: 10 in `test_firmware_storage.py` (save/delete/read/reconcile/helpers); 7 in `test_queue.py` (flag persistence across reload, `mark_firmware_stored` state gating, remove+clear+per-target-coalescing firmware purge); 4 in `test_api.py` (POST /firmware success + rejection paths); 5 in `test_ui_api.py` (compile accepts download_only, GET /firmware streams + 404 paths). New fixture job-008 + `**/ui/api/jobs/*/firmware` mock route in `e2e/fixtures.ts`. New `e2e/compile-download.spec.ts` (5 tests) for UpgradeModal toggle + Queue-tab Download button. Full pytest: 435 → 461 (+26); mocked Playwright: 102 → 107 (+5). Hass-4 smoke gets the full round-trip verification on deploy.

## Code Review Follow-ups

Review of `develop` → `main` diff (147 commits, 141 files, +11,854 / −3,025 LOC) captured in `dev-plans/CODE_REVIEW-develop-vs-main.md`. Items below are the actionable findings translated into workitems, ordered by the review's priority punch list (CR.1–CR.7) followed by the rest grouped by area. Each item cites the review section + concrete file:line for executors.

Decisions already made (see CR.2, CR.3, CR.26):
- **Multi-instance** — declare `single_config_entry: true`, don't add an entry-picker.
- **Integration auth** — wire Supervisor bearer token so `require_ha_auth=true` doesn't break the integration.
- **Integration tests** — accept mock-heavy state for 1.5; deferred refactor (rename + pytest-hass rewrite) goes to 1.6.

### Punch list (pre-release — ideally land all before tagging 1.5.0)

- [ ] **CR.1** *(release prep)* — Rewrite `CHANGELOG.md` for 1.5.0 (§6.1). One paragraph per workstream: FD (firmware download), AU (HA user auth), SE (ESPHome unbundling — call out 1–3 min first-boot install so users don't panic), SC (SBOM + SHA pinning + cosign), HI (HA native integration — services, entities, WebSocket events, device registry merge), QS (UI refactor + a11y), DO/device-registry cleanup, bug fixes. Current "Rebrand" one-liner is misleadingly understates the release. Per CLAUDE.md guidance: 90% user-visible, 10% internal, only changes relative to last public release.
- [ ] **CR.2** *(integration)* — Declare single-instance in the integration manifest. `ha-addon/custom_integration/esphome_fleet/manifest.json` gains `"single_config_entry": true`. HA then refuses a 2nd entry, matching the current `_first_coordinator` assumption. Remove the implicit multi-instance foot-gun (§2.4). Also audit `services.py:76,128,176,184` — the `_first_coordinator` helper stays but its docstring should say "single-entry by contract, see manifest." No service-picker work required per the chosen direction.
- [ ] **CR.3** *(integration)* — Wire Supervisor bearer token from integration to add-on (§2.5). `coordinator.py:133-148::_get_json` gains an `Authorization: Bearer <token>` header sourced from the Supervisor-minted token (HA Core passes this via `hass.config.api.api_password` or the `SUPERVISOR_TOKEN` env the integration is loaded into — pick the HA-canonical path). `const.py::CONF_TOKEN` becomes real: config flow captures it on the manual step, Supervisor-discovered flows get it from the discovery payload. Add-on side: ensure `/ui/api/*` accepts this bearer when `require_ha_auth=true`. Result: flipping `require_ha_auth=true` no longer silently breaks the integration. Document the auth flow in `DOCS.md` and `SECURITY.md`.
- [ ] **CR.4** *(server — bug)* — Remove the dead write in `ha-addon/server/job_queue.py:604-606` (§2.1). Lines 604–606 assign `JobState.TIMED_OUT` and then immediately overwrite with `JobState.PENDING`. Either drop line 604 (the retry path is really "TIMED_OUT never observed; just re-enqueue") or promote TIMED_OUT to a real intermediate state with its own persistence + event. Pick the drop — simpler, matches observed intent.
- [ ] **CR.5** *(UI — bug class)* — Fix three error-swallowing functions in `ha-addon/ui/src/api/client.ts` (§2.9): `validateConfig` (307–318) wraps `r.json()` in try/catch before returning the bespoke error shape; `getSecretKeys` (320–325) and `getEsphomeSchema` (327–331) stop returning `[]` on `!r.ok` and instead toast + log via the existing SWR/error path. Functions that "silently degrade" on 401/500 cause category-3 UX bugs (UI appears to work, feature off).
- [ ] **CR.6** *(tests)* — Replace both `page.waitForTimeout()` calls in E2E (§4): `e2e/cancel-new-device.spec.ts:70` and `e2e-hass-4/cyd-office-info.spec.ts:237`. The prod-smoke one gates every deploy; a flake there wastes cycles. Swap for `expect.poll(...)` or deterministic DOM-state waits. Also add new invariant **E2E-1** in `check-invariants.sh`: no `waitForTimeout` in `e2e/*.spec.ts` or `e2e-hass-4/*.spec.ts` — fails CI on any occurrence.
- [ ] **CR.7** *(integration — UX)* — Demote entity category on hub/state sensors (§3.5). `sensor.py:111` (queue depth), `binary_sensor.py:58` (worker online), `OnlineDevicesSensor`, `TotalDevicesSensor`, `OutdatedDevicesSensor`, `TotalSlotsSensor` — all currently `EntityCategory.DIAGNOSTIC`, meaning HA hides them from the default Lovelace picker and automation triggers. These are exactly the things users dashboard on. Drop the category (default = primary) on state sensors; keep DIAGNOSTIC only for things that are genuinely diagnostic (version strings, build dates).

### Server bugs / hygiene

- [ ] **CR.8** *(server — bug)* — `ha-addon/server/ha_auth.py:110-120` returns a ghost-user dict (`{"name": None, "id": None, "is_admin": None}`) when Supervisor's `/auth` returns HTTP 200 with an unparseable body (§2.3). Change to `return None` so an unparseable response fails auth instead of silently attaching `request["ha_user"] = {name: None}` — that shape shows up in audit logs as "action by None" six months later when someone needs evidence.
- [ ] **CR.9** *(server — PY-2)* — `ha-addon/server/scanner.py:207-214`: the `subprocess.run([_server_esphome_bin, "version"], ...)` call doesn't log the command line before execution, violating PY-2 (§2.2). Low direct impact (it's a version probe) but the invariant exists precisely so we never debug "what did the subprocess actually run?" blind. Add `logger.info("Running: %s", cmd)` before the call. Also tighten `scripts/check-invariants.sh`'s PY-2 check so "module has a logger" isn't enough — grep for a `logger.` call within ~10 lines before every `subprocess.run`/`Popen`, fail if absent.
- [ ] **CR.10** *(server — installer)* — Make `ha-addon/server/integration_installer.py:137-141` atomic (§2.10). Current pattern is `shutil.rmtree(dest); shutil.copytree(src, dest)` — a crash between the two leaves `/config/custom_components/esphome_fleet/` gone until the next successful start. Use the standard "copy to `.new`, `os.replace` over old" pattern. Also `_patch_manifest_version` needs atomic write (tempfile + `os.replace` instead of open+write). Add `ignore=shutil.ignore_patterns("__pycache__", "*.pyc")` to `copytree` so future source-tree changes don't leak cache dirs into user HA config.
- [ ] **CR.11** *(security doc)* — Update `SECURITY.md` (§5.1–5.5) to call out: (a) `require_ha_auth=false` default trade-off and how CR.3 changes it, (b) Supervisor-IP trust path in `ha_auth.py:138-147` — fine on HA OS, spoofable on bare-Docker deployments on a shared bridge network; document explicitly, (c) `integration_installer` writes to `/config/custom_components/` — the add-on has r/w access to user HA config, (d) firmware-upload trust assumption (no checksum; workers are trusted), (e) `supervisor_discovery.py` SUPERVISOR_TOKEN handling — verify no exception-dump path logs the header. Also re-assess F-03 in `SECURITY_AUDIT.md`: "fixed" is optimistic while `require_ha_auth` defaults off and the integration breaks when it's on. Mark as PARTIAL until CR.3 ships.

### Integration polish

- [ ] **CR.12** *(integration — bug)* — Fix `ha-addon/custom_integration/esphome_fleet/__init__.py:76` fire-and-forget unload (§2.6): `entry.async_on_unload(lambda: hass.async_create_task(event_stream.stop()))` decouples unload completion from WS teardown. Change to `entry.async_on_unload(event_stream.stop)` — `async_on_unload` accepts a coroutine function directly; HA will await it as part of unload. Eliminates the rapid-reload WS-leak race.
- [ ] **CR.13** *(integration — perf)* — Parallel GETs in the coordinator (§2.8). `coordinator.py:61-67` runs six `/ui/api/*` fetches sequentially every 30 s. Wrap in `asyncio.gather` — cuts wall time to ~1× RTT and halves server-side handler load. Trivial change, ~10 lines.
- [ ] **CR.14** *(integration — perf)* — Diff-skip the device-registry listener (§2.7). `__init__.py:60-65`'s listener runs `_register_devices()` every coordinator tick (30 s) regardless of change. Track the last-seen identifier set; skip the body when identifiers are unchanged. Avoids keeping `async_get_or_create` and `async_entries_for_config_entry` hot on idle systems.
- [ ] **CR.15** *(integration — manifest)* — Round out `ha-addon/custom_integration/esphome_fleet/manifest.json` (§3.1): add `"quality_scale": "bronze"` (or higher if you feel it's earned), add `"loggers": ["esphome_fleet"]` so users can enable debug via HA UI. Audit `"version"` — today `"0.1.0"` is hardcoded and `integration_installer._patch_manifest_version` overwrites it at install. Contributors who hand-install from the repo get a permanent `v0.1.0`. Source-of-truth should be `VERSION` file + installer; document the mechanism in a comment right above the version field OR remove the field entirely (core-bundled integrations don't require it).
- [ ] **CR.16** *(integration — config flow)* — Add connectivity probe to `config_flow.py:62-81` user step (§3.3). Before creating the entry, do a 3 s `GET /ui/api/server-info` against the normalized URL; on failure, return a schema error ("Could not reach server at that URL"). Prevents the "entry created, immediately shows red, user can't tell why" UX. Supervisor-discovery path can skip the probe since Supervisor already vetted the URL.
- [ ] **CR.17** *(integration — UX)* — Wire the `EsphomeInstallBanner` retry button (§3.4). The banner renders for all three `esphome_install_status` states but the `'failed'` branch doesn't connect to a recovery flow. `reinstallEsphome()` already exists in `api/client.ts:114-117`. Hook it up: failed-state banner shows a "Retry install" button; click fires the reinstall endpoint, flips the banner back to `'installing'`, and the existing SWR poll refreshes when status transitions. Re-open SE.8 in its section with a note pointing at CR.17.
- [ ] **CR.18** *(integration — hygiene)* — Add module docstring to `ha-addon/custom_integration/esphome_fleet/_discovery.py` (§3.6) — the 32 lines copied from Ardumine PR #57 have zero explanation of what they do. Either inline into `config_flow.py` or give the file a real docstring. Also add a `README.md` in the integration dir (§3.7) — two paragraphs explaining what the subtree is, how it's installed (via `integration_installer` from the add-on), and why `esphome_fleet` as the domain despite the surrounding repo using `distributed_esphome`.

### Documentation + invariants

- [ ] **CR.19** *(docs)* — Add a "First boot takes 1–3 minutes" note in `ha-addon/DOCS.md` near the Getting Started section (§6.2). Explain the lazy-install behavior (SE.1–10) — the add-on downloads ESPHome into `/data/esphome-versions/` at first boot and the UI shows the `EsphomeInstallBanner`. Without this note users will think the add-on is hung on a fresh install.
- [ ] **CR.20** *(docs)* — Document the `require_ha_auth` UX trade-off in DOCS.md (§5.1). Before CR.3 ships: flipping to `true` breaks the HA integration. After CR.3: safe. Write the explanation that matches the as-shipped state at release time.
- [ ] **CR.21** *(docs)* — Document `app_config.py` precedence (§7.4). Server config now has three sources: `/data/options.json`, env vars, `app_config.py`. Precedence and merging order goes in the `app_config.py` module docstring — one source of truth.
- [ ] **CR.22** *(docs / architecture)* — Event bus + WebSocket SLA clarification (§7.1). (a) `event_bus.broadcast` docstring: change "from sync or async code" to "must be called on the aiohttp loop" — `put_nowait` is loop-affine; breaks silently if anyone moves mutation into a thread pool later. (b) Verify `coordinator.py:130` prunes `_last_job_states` on every tick — add a test if not. (c) Document the two SLAs separately: UI recovers a missed event in 1 s via SWR; HA integration recovers in up to 30 s via coordinator poll. Put this in the `event_bus.py` module docstring or a dedicated `dev-plans/ARCHITECTURE.md` stub.
- [ ] **CR.23** *(invariants)* — New invariants in `scripts/check-invariants.sh`:
  - **UI-6** — ban the `return [] on !r.ok` (or `return default` on error) pattern in `ha-addon/ui/src/api/client.ts`. Pairs with CR.5's fixes. Grep regex: any function in `api/*.ts` that has `if (!r.ok)` without throwing or toasting.
  - **E2E-1** — ban `page.waitForTimeout` in `e2e/*.spec.ts` and `e2e-hass-4/*.spec.ts`. Pairs with CR.6.
  - **HA-1** *(optional)* — ban `[class*="monaco"]`-style CSS-class-substring selectors in Playwright specs; they're known flake surfaces.
- [ ] **CR.24** *(supply chain — verification)* — Confirm final `ha-addon/server/requirements.lock` was regenerated inside the linux/amd64 Docker container via `scripts/refresh-deps.sh` (not a macOS host, §9.4). PY-9 added the grep-guard after bug #56; a quick `grep -E "pyobjc|appnope"` on the current lock should come back empty. One-line release-prep verification.

### Auditable / "confirm no regression"

- [ ] **CR.25** *(storage)* — Audit `firmware_storage.py` retention (§8). Confirm firmware files (`/data/firmware/*.bin`) are cleaned up via `_purge_firmware` on queue clear/coalesce AND that `reconcile_orphans` runs on startup. No time-based TTL is by design (#18) but we must not leak files whose job ids no longer exist.
- [ ] **CR.26** *(deferred to 1.6)* — Integration test-strategy follow-ups (§3.2 / §4). Placeholder in this release file pointing to the 1.6 items: (a) rename `tests/test_integration_*.py` to `test_integration_logic_*.py` to match what they actually test (helper-function unit tests), (b) add `pytest-homeassistant-custom-component` and rewrite tests under a real `hass` fixture. Links: `dev-plans/WORKITEMS-1.6.md` CR-deferred items. Referenced from here so the 1.5 release notes can honestly say "integration test story is queued for 1.6."

### Optional / user-decide

- [ ] **CR.27** *(repo hygiene — optional)* — Consider adding `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` to the repo (§9.1). Single-developer home-lab project today, so these are aspirational; the reviewer flagged them as missing. Queue as user-decides — if you don't want them, resolve this checkbox as WONTFIX with a note. If you do: `CONTRIBUTING.md` can crib heavily from `CLAUDE.md` (trim AI-specific sections), include the naming-split rationale from §7.2.
- [ ] **CR.28** *(repo hygiene — optional)* — GHCR retention policy (§6.5). 147 commits × the `bump-dev.sh`-per-turn pattern means `publish-client.yml` and `publish-server.yml` have generated a lot of container images. Add a retention rule: keep `:latest`, keep tagged stable releases, expire `-dev.*` tags older than 90 days. Alternatively gate `-dev.*` publishing to `main` + tags only. Queue as low-priority; won't bite until GHCR storage limits hit.

## Open Bugs & Tweaks

- [x] **#1** *(1.4.1-dev.25)* ([GitHub](https://github.com/weirded/distributed-esphome/issues/56)) — Top bar doesn't scroll on mobile (iOS). Fix: added `overflow-x: auto` to the `header` rule in `theme.css`. The header is already a `display: flex` container with `gap: 10px` and `whitespace-nowrap` items, so on narrow viewports the natural width exceeds the screen and items overflow off the right edge with no way to reach them. With `overflow-x: auto` the header becomes its own scroll container — sticky positioning still works, but the user can swipe horizontally inside it to reach Secrets / theme / streamer / version dropdown. Desktop is unchanged because there's no overflow on wide viewports. New regression test in `theme-responsive.spec.ts` runs at iPhone-SE width (320px), asserts `getComputedStyle(header).overflowX === 'auto'`, asserts `header.scrollWidth > clientWidth` (there's actually something to scroll), then `scrollIntoViewIfNeeded()` on the streamer toggle and asserts it lands in the viewport.
- [x] **#2** *(1.4.1-dev.3)* — Hamburger menu closed on every 1Hz SWR poll. Regression from QS.16: the new shadcn `DropdownMenu` lived inside the row's actions cell, where re-mounts (triggered by columns memo invalidation on `activeJobsByTarget` and inline-arrow refs) tore down its internal open state. Fix: lifted open state to DevicesTab as `menuOpenTarget: string | null`, threaded through `useDeviceColumns` to `DeviceContextMenu` as controlled `open` + `onOpenChange` props. The state now survives any number of row remounts because it lives outside the row tree. Also updated two e2e tests to use `getByRole('menuitem')` since Radix's items are correctly typed as menuitems (was `getByRole('button')` matching the old hand-rolled `<button>` elements). 
- [x] **#3** *(1.4.1-dev.4)* — After the #2 fix the menu stayed open but the content visibly flashed on every 1Hz SWR poll. Cause: SWR hands us a fresh `target` object reference each poll (same values, new object), so `DeviceContextMenu` re-rendered unconditionally — Radix's overlay briefly re-mounted/animated each time. Fix: wrapped `DeviceContextMenu` in `React.memo` with a custom `propsEqual` that compares only the `Target` fields actually read in render (`target`, `has_restart_button`, `has_api_key`, `pinned_version`) plus `open`, and treats function props as always-equal (identity changes don't affect behavior because they close over the same underlying handlers). The menu now renders once and stays rendered across polls. Also fixed the `e2e-hass-4/cyd-office-info.spec.ts` live-logs smoke test to use `getByRole('menuitem')` instead of `getByRole('button')` (same Radix-menuitem fix as the two mocked tests in #2). 
- [x] **#4** *(1.4.1-dev.6)* — Even after #3's `React.memo` on `DeviceContextMenu`, the overlay visibly twitched while the menu was open. Likely root cause: the actions-cell body was still an inline arrow inside `useDeviceColumns` that React reconciled on every poll. Even though the memo'd menu itself was skip-rendered, the surrounding `<Button>Upgrade</Button><Button>Edit</Button>` siblings re-evaluated every tick with fresh inline `onClick` closures and a fresh `style={{...}}` object literal, which nudged the trigger's bounding box enough for Base UI's Positioner to re-fire. Fix: extracted the cell body into a dedicated `React.memo`'d `ActionsCell` component (`components/devices/ActionsCell.tsx`) with a tight prop compare (target fields actually read, `inFlight`, `menuOpen`); moved the inline `style` to a module-level const so the `<div>` style prop is referentially stable; added `data-[state=closed]:!animate-none` to the DropdownMenu popup so any residual state cycle is invisible instead of animating. Also updated the four `header span[title*="..."]` selectors in mocked e2e tests (theme + streamer toggles) to `header button[...]` to match the `<span>`→`<button>` conversion landed in QS.3.
- [x] **#5** *(1.4.1-dev.10)* — AssertionError flood from SP.1's compression middleware. Three iterations to get right:
  - **dev.7 (original)** — `response.enable_compression()` called on any non-WebSocket StreamResponse. Tripped `aiohttp/web_response.py:451 assert self._payload_writer is not None` via `FileResponse` (routes.add_static), which has its own Range/cache/compression pipeline incompatible with enable_compression().
  - **dev.8** — narrowed to `type(response) is web.Response` (excludes FileResponse). Moved the assertion to line 818 (`assert self._body is not None`) — fires on `web.Response(status=204)` returned by worker-API deregister/cancel-assignment endpoints.
  - **dev.9** — short-circuit on status 204/304 and None/empty bodies. Cleared the asserts. Replaced by an `UserWarning: Synchronous compression of large response bodies (46306540 bytes)` — the worker job-claim endpoint returns a JobAssignment with a base64-encoded tarball of the entire config dir (~46 MB).
  - **dev.10 (final)** — restricted middleware to `/ui/api/*` paths. Worker tier (`/api/v1/*`) runs worker↔server on a LAN and shouldn't block the event loop gzipping tarballs. UI-tier compression is the actual SP.1 goal anyway. Log now clean.
- [x] **#6** *(already fixed by #2 + #4)* — CI failure monitor's first catches on develop. All four already resolved or superseded:
  - [CI #24408342243](https://github.com/weirded/distributed-esphome/actions/runs/24408342243) failure on `44f8445` (QS.16-21 DevicesTab split bundle rebuild) — 2 mocked Playwright failures in `create-device.spec.ts` ("duplicate device" flow). Old selectors targeted the hand-rolled context menu; fixed in bug #2 which moved tests to `getByRole('menuitem')`.
  - [CI #24410409557](https://github.com/weirded/distributed-esphome/actions/runs/24410409557) failure on `41817f6` (QS.1-QS.7 UI hygiene) — 5 mocked Playwright failures on theme toggle and streamer mode tests. Old selectors matched `header span[title*="..."]`; fixed in bug #4 which updated them to `header button[...]` after the `<span onClick>`→`<button>` conversion from QS.3.
  - [CI #24413471559](https://github.com/weirded/distributed-esphome/actions/runs/24413471559) and [CI #24413738329](https://github.com/weirded/distributed-esphome/actions/runs/24413738329) — two `ESPHome Compile Tests` **cancelled** runs on `adb9348` and `a0703c7` (SP.1-3 server perf). Cancelled by `concurrency.cancel-in-progress` when follow-up commits landed; no real failure to fix.
- [x] **#7** *(1.4.1-dev.11, partial)* — First Supervisor warning (regex mismatch) fixed by fully-qualifying the image path in `ha-addon/build.yaml`: `python:3.11-slim` → `docker.io/library/python:3.11-slim`. Same image, but now satisfies the Supervisor's `^([a-zA-Z\-\.:\d{}]+/)*?([\-\w{}]+)/([\-\w{}]+)(:[\.\-\w{}]+)?$` regex. Also updated `ha-addon/Dockerfile` to `ARG BUILD_FROM=docker.io/library/python:3.11-slim` + `FROM ${BUILD_FROM}` so the build.yaml selection is actually honored (previously the hardcoded `FROM python:3.11-slim` ignored whatever Supervisor injected as `--build-arg`). The second warning (build.yaml deprecated) is **left as-is** — moving fully off build.yaml needs a glibc-based Python base image, and HA's ghcr.io base images are alpine/musl-only for Python. PlatformIO toolchains require glibc. Revisit when HA ships a Debian-Python base, or if we change our PlatformIO install strategy.
- [x] **#8** *(not-a-regression — hass-4 is stale)* — Log monitor flagged `UserWarning: Synchronous compression of large response bodies (46306540 bytes)` on hass-4. This is the SP.1 aiohttp warning from the worker job-claim endpoint gzipping a 46 MB config tarball — already fixed in #5 final (commit `585327f` scopes the compression middleware to `/ui/api/*` only, excluding `/api/v1/*` worker endpoints). The warning appears because `hass-4` is still running **1.4.0-dev.52** (pre-release), not the current `1.4.1-dev.10` develop. Will disappear on next `./push-to-hass-4.sh`.
- [x] **#9** *(1.4.1-dev.11, transient)* — hass-4 Supervisor Docker build cascade at 2026-04-14 10:48. The Supervisor tried to rebuild the add-on image `local/amd64-addon-esphome_dist_server:1.4.0-dev.52` and failed:
  > `ERROR [supervisor.docker.addon] Docker build failed for local/amd64-addon-esphome_dist_server:1.4.0-dev.52 (exit code 1)`
  > `#2 ERROR: failed to authorize: failed to fetch anonymous token: Get "https://auth.docker.io/token?scope=repository%3Alibrary%2Fpython%3Apull&service=registry.docker.io": dial tcp [2a06:98c1:3106::6812:2bb2]:443: connect: network is unreachable`
  > `ERROR [supervisor.addons.addon] Could not build image for app local_esphome_dist_server`
  
  Root cause: hass-4's Docker tried to pull `python:3.11-slim` over IPv6 and IPv6 routing to `auth.docker.io` was broken (or the host didn't have working v6 to that endpoint). Cascaded into `Ingress error: Cannot connect to host 172.30.32.1:8765`.
  
  **Resolved on its own** at 2026-04-14 ~10:59. Manually re-triggered `ha addons update` after the dev.11 push and the build succeeded immediately (Docker daemon retried, network probably fell back to IPv4, auth went through). 1.4.1-dev.11 came up clean. Closing as transient. If it recurs, the `/etc/docker/daemon.json` `{"ipv6": false}` knob is the standard workaround.
- [x] **#10** *(1.4.1-dev.14)* — Dropped `armhf`, `armv7`, `i386` from `config.yaml` `arch:` and from `build.yaml` `build_from:`. ESPHome compilation is effectively 64-bit-only now; no real-world worker users on those archs. Supervisor's "deprecated arch values" warning gone.
- [x] **#11** *(1.4.1-dev.14)* — Both `_log_poll_warning` callsites in `ha_entity_poller` (template API + states API) now pick the hint based on status class:
  - **5xx** → "HA Core may be restarting; will retry on the next poll" (the right remedy — wait for HA to come back)
  - **401/403** → "check homeassistant_api: true in config.yaml" (the original hint, kept where it's actually correct)
  - other → no hint, just the bare HTTP code
  Demote-to-debug behavior from 1.3.1 #5 unchanged.
- [x] **#12** *(transient)* — Supervisor `ERROR [supervisor.addons.manager] Version changed, use Update instead Rebuild` at 2026-04-14 10:58:29. Artifact of a Supervisor `ha app rebuild` call firing while the stored add-on version on disk was different from the Supervisor's cached version for the same slug — typically seen during the `./push-to-hass-4.sh` Reload/Update/Rebuild loop. No action needed; the script already handles this by retrying `ha app update` first, which is what eventually succeeded. Not a code bug.
- [x] **#13** *(transient — Docker Hub)* — `Publish Client Docker Image` workflow failed on `develop` @ `59a8301` ([run 24417779600](https://github.com/weirded/distributed-esphome/actions/runs/24417779600)) — the `Build and push` step's buildx output is dominated by an HTML error page (complete with base64-encoded Docker Hub logo), ending in `##[error]buildx failed with: <html...>`. The previous publish ([run 24417488903](https://github.com/weirded/distributed-esphome/actions/runs/24417488903) on `c61be24`, 6 minutes earlier) and the next run on develop both succeeded. Classic symptom of Docker Hub serving a 503/504 error page during base-image pull (probably `library/python:3.11-slim` — same dep as bug #9's hass-4 failure). The commit itself (`fix: drop deprecated archs (#10) + better 5xx hint (#11)`) didn't touch the Dockerfile, so this is pure transient flake. **Re-run the workflow** from the Actions UI (or it'll get retried naturally on next push) — no code fix. If this becomes a pattern, consider pinning `python:3.11-slim` to a digest or switching to the HA base image (`ghcr.io/home-assistant/...`) which would also close bug #7/#9. Two failures attributable to Docker Hub reliability issues in one day (the earlier hass-4 one at 10:48, this one at 19:06) is noise, not a trend — revisit if we see three in a week.
- [x] **#14** *(1.4.1-dev.27)* ([GitHub](https://github.com/weirded/distributed-esphome/issues/58)) — Version selection visibility. Real cause confirmed via a follow-up screenshot from the reporter: the dropdown popup's TOP edge was rendering BEHIND the sticky header. Header had `z-index: 100` in `theme.css`; Base UI's `DropdownMenuContent` portal sits at `z-50`, so the header beat the popup in the stacking war and the "ESPHome Version" label + search input appeared *under* the dark header strip. Fix: lowered `header { z-index }` from 100 → 30. Header still sits above scrolled content (which has no explicit z-index) but now correctly *below* dropdown popovers (z-50) and dialog overlays (z-50). Bonus side effect: dialog overlays now properly cover the header when a modal is open. (The dev.26 `DialogContent` `max-h` change is kept as a defensive measure for tall modals.) New regression test in `theme-responsive.spec.ts` opens the header dropdown and uses `document.elementsFromPoint()` at the popup's top-center pixel to assert the popup is in front of the header in the hit-test order.

- [x] **#15** *(1.4.1-dev.29)* — Sidebar + add-on page still showed "ESPHome Distributed Build Server" / "ESPH Distributed" after the RB.* rebrand shipped. Root cause was *not* the rebrand patch itself — it was that `push-to-hass-4.sh` had been silently falling through to `ha app rebuild --force` on every deploy for many turns. Background: `ha app update` returns non-zero as "No update available" if Supervisor hasn't yet rescanned the on-disk addon directory; the script's `update || rebuild --force || true` chain caught that and rebuilt instead. **`ha app rebuild` rebuilds the docker image but does NOT bump Supervisor's installed-version metadata** — and the addon's name / panel_title / description that HA shows in the sidebar all come from the *installed* config.yaml, not the on-disk one. So the running container was at dev.28 with the new name (the smoke tests passed against it) but Supervisor's metadata thought we were still at dev.17 with the old name. **Immediate fix:** ran `ha app update local_esphome_dist_server` manually on hass-4 — `version` jumped from 17 → 28, `name` updated to "ESPHome Fleet", sidebar refreshed without a reinstall. **Systemic fix:** rewrote the deploy loop in `push-to-hass-4.sh` (gitignored): use `ha store reload` (NOT `ha app reload` or `ha apps reload` — both no-op for the local addon directory; only `ha store reload` actually triggers the rescan) → poll `version_latest` until Supervisor sees the on-disk version → then call `ha app update` without the `|| rebuild --force` fallback (`set -e` now fails loudly if update doesn't take). The fallback to `rebuild` is now reached only when installed already equals target (intentional image refresh). Verified end-to-end: deploying dev.29 went CURRENT 28 → reload → wait → update → addons info reports `name: ESPHome Fleet, version: 1.4.1-dev.29` cleanly.

- [x] **#16** *(1.4.1-dev.30)* — Beta ordering was "backwards": `2026.4.0b2` showing above `2026.4.0b3` in the version list. Root cause: the nested `_version_key` helper inside `_fetch_pypi_versions` parsed the pre-release tag (`a`/`b`/`rc`) but threw away the number after it. `0b3` and `0b2` produced identical keys → Python's stable sort preserved PyPI's alphabetical input order, putting `b2` above `b3` in the descending sort. Subtler latent bug: segments without a pre-release suffix produced a *shorter* tuple, making stable releases sort *below* the pre-releases of the same base (a tuple that's a prefix of another compares less-than in Python). Fix: extracted `_esphome_version_key` to module scope with a uniform `(main_num, stage_rank, stage_num)` shape per segment — every key has the same length and the stage number (e.g. the `3` in `b3`) survives. 4 new unit tests in `test_main.py` cover b3 > b2, stable-outranks-pre-release, mixed order, and plain semver.
- [x] **#17** *(1.4.1-dev.30)* — Faster re-queue when a worker goes offline mid-job. Previously a WORKING job whose worker sleeps/crashes would sit stalled for the full `JOB_TIMEOUT` (600s default) before the 30s timeout-checker noticed. Fix: `JobQueue.check_timeouts()` now accepts an optional `is_worker_online: Callable[[str], bool]` and short-circuits any WORKING job whose assigned worker's last heartbeat is beyond the registry's `worker_offline_threshold` (default 30s). `timeout_checker` in `main.py` wires it to `registry.is_online(cid, threshold_secs=cfg.worker_offline_threshold)`. Re-queue preserves the existing retry-counter / MAX_RETRIES semantics; after too many offline re-queues the job still transitions to FAILED permanently with a log line identifying the cause (`"Permanently failed after 3 offline-worker requeues"`). Live compiles that genuinely take close to 10 minutes are unaffected because the liveness check only runs while the worker is *actually* silent on heartbeats — an active worker pinging every 10s never trips it. 3 new tests in `test_queue.py`: offline → immediate requeue, online → no-op, offline path honors MAX_RETRIES + emits the offline-specific failure message.

- [x] **#18** *(1.4.1-dev.30)* — Don't clear jobs from the queue automatically; persist across restarts/upgrades. Two auto-clear paths removed: (1) `load_from_disk` used to prune terminal jobs older than 1h on every startup; (2) `timeout_checker` used to call `prune_old_terminal(3600)` every ~5 minutes. Both deleted — the Queue tab is the user's record of what the system did, and time-based pruning meant overnight history vanished before the user could see it. Users clear explicitly via the Queue tab's Clear dropdown. `prune_old_terminal()` itself is kept on the class for explicit callers (none in-tree now but the method is stable API). **Persistence confirmation:** queue already persists to `/data/queue.json`; HA add-on `/data/` survives stop/start/restart/update/rebuild (uninstall is the only operation that wipes it, which is expected and user-driven). The startup log line now reads `"Loaded N jobs from /data/queue.json (persisted across restarts)"` so this is visible in `ha addons logs`. Per-target coalescing in `enqueue()` still clears stale terminal jobs *for the same target* when a new compile is enqueued — that's a deliberate UX choice (one row per device, not an ever-growing failure log) and distinct from time-based auto-pruning.

- [x] **#19** *(1.4.1-dev.32)* — After clicking the Refresh button in the header's ESPHome version dropdown, the list didn't immediately show newly-released versions. Root cause: the Refresh handler only called `mutateEsphomeVersions()`, which re-fetches `GET /ui/api/esphome-versions` — but that endpoint just returns the server's in-memory `esphome_available_versions` cache, which is only updated by the background `pypi_version_refresher` task on a 1-hour TTL. So the client was re-reading the same stale list. Fix: new `POST /ui/api/esphome-versions/refresh` endpoint that forces a synchronous PyPI fetch bypassing the TTL and returns the fresh list. New client helper `refreshEsphomeVersions()`; the header Refresh handler now calls it and hands the result directly to SWR via `mutateEsphomeVersions(fresh, false)` so the dropdown reflects the new versions without waiting for the next SWR tick. Toast now includes the total count ("ESPHome version list updated (N versions)") so the user can see the refresh actually pulled something.
- [x] **#21** *(1.4.1-dev.33)* — Worker submits result for a cancelled job. The race itself is benign (the user's intent was honored, the WARNING correctly surfaced wasted work, the result was discarded not merged), but the `Cancelled by user.` trail was invisible in the server log — so the `submit_result: job … in unexpected state CANCELLED` warning looked orphaned. Fix: `JobQueue.cancel()` now emits an INFO log per cancelled job, including the prior state and (when cancelling a WORKING job) a note that the worker may still be compiling. Example: `INFO job_queue: Cancelled job abc-123 (cyd-office-info.yaml) from state working — worker AI-MacBook-Pro may still be compiling`. Makes the `cancelled → later submit_result` sequence unambiguously explainable. Push-cancel to the worker (terminate esphome subprocess, skip OTA) deferred to 1.5 when the worker API gets a proper cancel endpoint.

- [x] **#20** *(1.4.1-dev.31)* — Not a flake after all. The three `test_scanner.py` failures on `8bd303f` turned out to be a real regression driven by ESPHome **2026.4.0** hitting PyPI sometime between the previous successful CI run and this one (our CI installs `esphome` unpinned). Two breaking API changes in `esphome.components.substitutions.do_substitution_pass`: (1) dropped the `ignore_missing` kwarg, (2) changed from in-place mutation to returning a new config. `scanner._resolve_esphome_config` caught the resulting `TypeError` silently via `except Exception` and returned `None`, so `build_name_to_target_map` produced empty dicts and the scanner tests asserted against nothing. Fix in `8f… 697b1ce`: call the new signature first (`config = do_substitution_pass(config, None)`) and fall back to the legacy form (`ignore_missing=True`, in-place mutation) for older ESPHomes still pinned in worker venvs. Reproduced locally by upgrading `esphome==2026.4.0` and confirmed the full 391-test suite stays green on both new and legacy ESPHomes. CI green on the follow-up commit.

- [x] **#22** *(1.4.1-dev.33)* — Devices table showed the raw `device_name` (title-cased from `esphome.name`) instead of the `friendly_name` for some devices after dev.31. Renumbered from the second #21 in the original list (bug number clash with the cancel-race bug above). Root cause: my dev.31 fix for the ESPHome 2026.4.0 scanner compat tried the *new* signature first (`do_substitution_pass(config, None)` with no kwargs). On the pinned `esphome==2026.3.3` in the server image, that signature IS accepted — but defaults to `ignore_missing=False`. Any YAML with an unresolved `${substitution}` (a common pattern — `friendly_name: ${area_name}` where `area_name` comes from a package the resolver couldn't find) raised `cv.Invalid`, which the outer `except Exception` in `_resolve_esphome_config` silently caught and returned None. The scanner then fell back to raw YAML parsing, which doesn't substitute — so `friendly_name` came back as the literal `${placeholder}` string and `device_name` survived only because it doesn't go through substitution the same way. Fix: flipped the try-order so `ignore_missing=True` (legacy, always tolerant) is tried first, and only when it raises `TypeError` (i.e. the kwarg is gone in new ESPHome) does it fall through to the new signature, which is already tolerant by design. New fixture `tests/fixtures/esphome_configs/unresolved_subs_device.yaml` (intentionally references an undefined `${pretty_name}`) and 2 new tests in `test_scanner.py` lock the regression. Verified on both esphome 2026.3.3 and 2026.4.0 — full 393-test pytest suite green on both.

- [x] **#23** *(1.4.1-dev.45)* — Download-only success row showed "OTA Pending" badge with no Clear / Rerun buttons. Root cause in `ui/src/utils/jobState.ts`: `isJobInProgress` treated any `state=success && ota_result !== 'success' && !== 'failed'` as still-in-progress (compile-done-but-OTA-pending semantics for normal jobs). Download-only jobs skip OTA entirely, so `ota_result` stays null — the predicate then classified them as in-progress → `isJobFinished` returned false → `isJobRetryable` returned false → Clear + Rerun omitted → `getJobBadge` fell into the fallback "OTA Pending" branch. Fix: three predicates + `getJobBadge` now short-circuit on `download_only` (and `validate_only`) so state=success is a proper terminal. Badge label: "Ready" (reads as "your binary is ready to download"). Working + pending variants also got dedicated labels ("Compiling" during work, "Download" while pending) so the row is explanatory throughout the lifecycle. Two new Playwright assertions in `compile-download.spec.ts` lock the badge text + Clear/Rerun button presence.

- [x] **#24** *(1.4.1-dev.45)* — Data-loss race: abandoned worker destroys successful firmware upload. Two of the three compounding bugs fixed; the third (push-cancel to abandoned worker) stays deferred to 1.5 per #21.
  
  **Fix (1) — non-atomic upload + state check** (server-side). `POST /api/v1/jobs/{id}/firmware` now runs the `state == WORKING` guard BEFORE calling `request.read()` / `save_firmware`. A stale late upload from an abandoned worker is rejected with `409 job_not_working` and the on-disk binary from the successful worker is untouched. The stale delete-after-overwrite path is structurally impossible now — we never write the bytes in the first place.
  
  **Fix (2) — worker identity check (security audit F-08).** Endpoint now reads `X-Client-Id` (same header used by `/jobs/next`) and rejects with `409 worker_identity_mismatch` when it doesn't equal `job.assigned_client_id`. `client.py`'s `post_bytes()` + `_upload_firmware()` thread the worker's `client_id` through so legitimate uploads carry the header. Broader F-08 coverage (submit_result, update_status) is a follow-up — this bug's blast radius was firmware specifically, and expanding to the whole worker API is out of scope for 1.4.1.
  
  **Deferred — push-cancel to abandoned worker (#21).** Still loses ~60s of compute when a worker is abandoned mid-job, but the user-visible data loss is gone. Revisit with the 1.5 remote-compilation work which needs a worker cancel endpoint anyway.
  
  Three new pytest regression tests in `test_api.py`: stale-worker upload post-success is rejected AND the good binary survives (the core data-loss scenario); unassigned-worker upload during WORKING is rejected with no file written; matching-assigned-worker upload succeeds. Full pytest: 461 → 464.

- [ ] **#25** — Icon in the HA integration picker.
  **Deferred to a follow-up release.** Home Assistant's *Add Integration* picker does NOT render `icon.png` from a custom integration — it only shows icons for integrations listed in the `home-assistant/brands` repo. Shipping a PR there takes weeks of review and requires hosting stable URLs. Tracking as a separate chore; a placeholder `icon.png` bundled in the integration dir will auto-show once the brands PR lands.

- [x] **#26** *(1.4.1-dev.47)* — Supervisor-discovery path: no URL prompt for local add-on.
  Added `"hassio": true` to `manifest.json` + `async_step_hassio` in `config_flow.py`. Server POSTs to `http://supervisor/discovery` on startup (`supervisor_discovery.py`) and DELETEs on shutdown. HA now surfaces the add-on as a *Discovered* integration with a one-click Confirm — the user never sees the URL prompt. Manual `async_step_user` remains as a fallback for people running the server standalone. Unique-id set from the resolved base URL so repeated discoveries dedupe cleanly against manual setups. Followed up in dev.47 by adding `discovery: [esphome_fleet]` to the add-on's `config.yaml` — Supervisor returned 403 without it.

- [x] **#27** *(1.4.1-dev.48)* — Merge targets with the native ESPHome integration device row.
  `/ui/api/targets` now returns `mac_address` (populated from the device poller's mDNS TXT / native-API lookup). The HA custom integration's `target_device_info` normalizes the MAC to HA's lower-case colon form and attaches it as `connections={(CONNECTION_NETWORK_MAC, mac)}` in `DeviceInfo`. HA's device registry sees the matching connection on the ESPHome integration's device and merges the two rows — our per-target entities (Firmware update, Schedule, Pinned version) now appear on the same device card as ESPHome's (running version, WiFi signal, sensors, etc.) instead of creating a duplicate device. When MAC is unknown (device offline and never polled), we fall back to the previous `identifiers={(DOMAIN, f"target:{filename}")}` keying so we never lose the entity.

- [x] **#28** *(1.4.1-dev.46)* — Drop redundant firmware-version sensor; expose Schedule + Pinning instead.
  Removed `TargetFirmwareVersionSensor` (the native ESPHome integration already reports running version). Added `TargetScheduleSensor` ("Once: 2026-04-20 18:30", cron expressions, "Paused", "No schedule") and `TargetPinnedVersionSensor` ("2025.9.3" or "Auto").

- [x] **#29** *(1.4.1-dev.46)* — Worker disk / CPU / memory sensors.
  Added five new worker sensors reading from `system_info`: `Disk usage` (%), `Disk free` (string), `CPU usage` (%), `CPU cores` (int), `Memory` (string). Kept the existing `Active jobs` sensor + `Online` connectivity binary sensor.

- [x] **#30** *(1.4.1-dev.46)* — HA integration version matches add-on version.
  `integration_installer.py` now reads `/app/VERSION` and rewrites `manifest.json`'s `version` field on copy. HA's *Installed integrations* list shows the same version as the add-on, so users can tell at a glance whether they're on the latest. Falls back to the hard-coded manifest version when VERSION is missing (dev checkouts / unit tests).

- [x] **#31** *(1.4.1-dev.46)* — Prettier discovery confirm dialog.
  Stripped the `._esphome-fleet._tcp.local.` mDNS service-type suffix from the discovered name before it hits the UI, and rewrote the dialog copy ("Home Assistant found an ESPHome Fleet server at http://…. Confirm or edit the URL to finish setup.") so it reads like a welcome screen rather than a debug dump.

- [x] **#32** *(1.4.1-dev.48)* — Target-device merge verified on hass-4.
  Inspected `/usr/share/hassio/homeassistant/.storage/core.device_registry` after the dev.48 deploy: 64 of 66 target devices now carry BOTH the Fleet and the native ESPHome integration's config-entry IDs on a single device row (example: `Office Info Display` has `config_entries=[01KP9VMTSTMB4RXW8AZK3D8BHS (fleet), 01K5F4REH60ZEFB2RQF4XB7K03 (esphome)]` with `connections=[[mac, 38:18:2b:84:a8:fc]]`). Fleet entities (`update.*_firmware`, `sensor.*_upgrade_schedule`, `sensor.*_pinned_esphome_version`) now appear on the same device card as ESPHome's entities. The 2 unmerged target devices are edge cases where the device poller hasn't observed the MAC yet (offline + never connected); they re-merge automatically on the next coordinator tick after MAC discovery. Initial impression of "not working" was a snapshot of pre-dev.48 state before HA Core picked up the updated integration code.

- [x] **#33** *(1.4.1-dev.49)* — Suppress duplicate "Discovered ESPHome Fleet" entries.
  The mDNS zeroconf flow and the Supervisor hassio flow resolve the same local add-on to different URLs (`http://local-esphome-dist-server:8765` vs. `http://192.168.x.x:8765`), so the unique-id check alone couldn't dedupe them. Hassio is the preferred path (zero user interaction) — zeroconf now aborts with `already_configured` when any Fleet entry exists AND aborts with `already_in_progress` when a hassio-sourced flow is already in the notification tray. Users with multiple servers can still add additional ones via the manual flow in Settings → Integrations.

- [x] **#34** *(1.4.1-dev.49)* — Rename "Schedule" entity to "Upgrade schedule".
  `TargetScheduleSensor._attr_name` changed to disambiguate from other "schedule" concepts that ESPHome devices expose. Entity ID migrates automatically (HA keys on `unique_id`, which didn't change).

- [x] **#35** *(1.4.1-dev.49)* — Pinned ESPHome version: display "None" when unpinned.
  `TargetPinnedVersionSensor.native_value` returns `"None"` (was `"Auto"`) when the target has no `pinned_version` set. Matches the UI's Pin column copy.

- [x] **#36** *(1.4.1-dev.50)* — Suppress duplicate zeroconf discovery under Supervisor.
  Under Supervisor, both hassio and zeroconf fire for the same local add-on but resolve to different URLs (internal hostname vs. LAN IP). Zeroconf now aborts unconditionally when `hassio` is in `hass.config.components`. Non-Supervisor installs keep zeroconf. The previous per-flow progress check (#33) was a timing race — hassio and zeroconf fire before either creates an entry.

- [x] **#37** *(1.4.1-dev.50)* — Device-targeted Compile + Validate actions.
  `esphome_fleet.compile` and `esphome_fleet.validate` services now support HA's standard `target:` device picker — select one or more managed devices from the UI and click "Perform action". The handler resolves device-registry IDs to YAML filenames via the `("esphome_fleet", "target:<filename>")` identifier. The old explicit `targets` / `target` fields remain as a fallback for automations. Worker/hub devices are rejected with a clear error.

- [x] **#38** *(1.4.1-dev.50)* — Compile action fix + tests.
  Made `targets` optional in the schema (was `vol.Required`; broke the service call when using device targeting). Added 4 new pytest cases: device-targeted compile resolves IDs, non-target device rejection, empty-call error message, device-targeted validate. Total service tests: 8 → 12.

- [x] **#39** *(1.4.1-dev.51)* — HA integration leaves stale devices when targets/workers are removed.
  `_register_devices` now builds the set of live identifiers each tick and compares against `dr.async_entries_for_config_entry`. Fleet-only stale devices get `async_remove_device`; merged devices (#27) get `async_update_device(remove_config_entry_id=...)` so the ESPHome integration's device row survives. Three new pytest cases: stale removal, merged detach, live-device retention. Device info tests: 13 → 16.

- [x] **#40** *(1.4.1-dev.51)* — Human-readable schedule entities + separate one-time entity.
  `TargetScheduleSensor` now shows human-readable cron (ported `formatCronHuman` from the UI's `utils/cron.ts`): "Daily 03:00 (America/Los_Angeles)", "Mon 08:30", "Every 2h", etc. Falls back to raw cron for patterns the formatter doesn't recognize. Added new `TargetScheduledOnceSensor` for one-time scheduled upgrades — shows "Apr 20, 2026 18:30" or "None". Both sensors are `EntityCategory.DIAGNOSTIC` and appear on the merged device card alongside ESPHome's native entities.

- [x] **#41** *(1.4.1-dev.54)* — Real-time entity state updates via WebSocket.
  New server-side event bus (`event_bus.py`) + `/ui/api/ws/events` WebSocket endpoint that broadcasts `queue_changed` / `workers_changed` / `targets_changed` / `devices_changed` events. Broadcasts piggy-back on existing persistence points: `JobQueue._persist`, `registry.register/remove/set_disabled`, `scanner.write_device_meta`, `DevicePoller._save_cache`, plus direct hooks on content-save / delete / rename / slot-change / cache-clean endpoints. The HA integration's `ws_client.py` opens a long-lived WebSocket with exponential-backoff reconnect (2s → 60s cap) and calls `coordinator.async_request_refresh()` on every event. The 30s coordinator poll stays enabled as a safety net — a missed event adds at most 30s of latency on one update. New `tests/test_event_bus.py` covers delivery, multi-subscriber fan-out, queue-full eviction, and payload passthrough (6 tests).

- [x] **#42** *(1.4.1-dev.52)* — Worker count + total build slots sensors on the hub device.
  `WorkerCountSensor` counts registered workers; `TotalSlotsSensor` sums `max_parallel_jobs` across all workers. Both are `MEASUREMENT` state class for HA history graphs.

- [x] **#43** *(1.4.1-dev.52)* — Selected ESPHome version sensor on the hub device.
  `SelectedEsphomeVersionSensor` reads from `coordinator.data["esphome_versions"]["selected"]`.

- [x] **#44** *(1.4.1-dev.52)* — Fleet version sensor on the hub device.
  `FleetVersionSensor` reads `addon_version` from `/ui/api/server-info`. Also visible in the hub device's info card.

- [x] **#45** *(1.4.1-dev.52)* — Clean cache button for workers.
  New `ButtonEntity` platform. One `WorkerCleanCacheButton` per worker — pressing it POSTs to `/ui/api/workers/{client_id}/clean`, which flags the worker to purge its build cache on the next heartbeat. Also usable as an HA action from automations.

- [x] **#46** *(1.4.1-dev.52)* — Total / online / outdated device count sensors on the hub device.
  Three new `MEASUREMENT` sensors: `TotalDevicesSensor`, `OnlineDevicesSensor`, `OutdatedDevicesSensor`. Count from `coordinator.data["targets"]` using the `online` and `needs_update` fields.

- [x] **#47** *(1.4.1-dev.52)* — Worker build-slot control via HA NumberEntity.
  New `NumberEntity` platform. One `WorkerSlotCountNumber` per worker (0–32, box mode). Setting the value POSTs to `/ui/api/workers/{client_id}/parallel-jobs`; the worker picks up the new count on its next heartbeat. Setting to 0 pauses the worker. Usable from automations (`number.set_value`). 

- [x] **#48** *(1.4.1-dev.53)* — Cyd-office-info validation failure: compare pin against actual binary, not tracked version.
  The YAML uses `sram1_as_iram: true` (added in ESPHome 2026.4.0). The Fleet add-on's container bundles ESPHome 2026.3.3 but `pypi_version_refresher` updates the server's "selected" version from the HA Supervisor's ESPHome add-on (2026.4.0). The validate endpoint's `if pin and pin != get_esphome_version()` short-circuited when the pin matched the tracked "selected" version, silently using the 2026.3.3 binary instead of installing 2026.4.0 via version_manager. Fixed by comparing the pin against the ACTUAL installed binary version (`_get_installed_esphome_version()`). Added a `pin_version: 2026.4.0` comment in cyd-office-info.yaml (via the existing `/ui/api/targets/.../pin` endpoint) so validation installs and uses 2026.4.0.

- [x] **#49** *(1.4.1-dev.55)* — Worker entities duplicated on HA/add-on restart.
  Workers persist their `client_id` to `/esphome-versions/.client_id` and re-send it as `existing_client_id` on re-register. Server's `register()` reused the ID only if the worker was already in `_workers` — on add-on restart, that dict is empty, so every re-registration silently minted a NEW UUID, leaving the old client_id's HA device orphaned (the #39 stale-cleanup would eventually reap it, but only after a scan cycle, and only if no entities still referenced the old ID). Fixed: `register()` now reuses `existing_client_id` unconditionally, creating a fresh `Worker` entry with the SAME id when the server doesn't remember it. Logs "Re-attached worker X (...) — server didn't know this client" in that path.

- [x] ~~**#51**~~ OBSOLETED by SE.1 *(1.4.1-dev.66)* — Always bundle latest public ESPHome in Docker container. Original ask was to auto-track upstream ESPHome releases in the server image. Superseded by the full Server ESPHome Unbundling workstream: the server no longer bundles ESPHome at all — it lazy-installs the version reported by the HA ESPHome add-on at first boot via `VersionManager`. No more lockfile churn on every ESPHome release; no more platform-marker class of bugs (#56). The original dev.55 → dev.56 revert attempt is preserved below for history.
  First attempt: added `--upgrade` to `pip-compile` in `refresh-deps.sh` to stop silent version pinning. This DID bump ESPHome 2026.3.3 → 2026.4.0, but also pulled `pyobjc-core==12.1` + three `pyobjc-framework-*` into the lockfile WITHOUT a `sys_platform == "darwin"` marker, breaking the linux/amd64 Docker build (`error: PyObjC requires macOS to build`). `--upgrade` re-resolves the full graph from scratch and apparently the fresh resolution of `bleak` (an `aioesphomeapi` transitive) pulled them in unconditionally. Reverted the lockfile + `--upgrade` flag. TODO: either add explicit platform-marker handling (`--markers` flag isn't a thing for pip-compile; may need `pip-compile-multi` or a post-process step that re-adds `; sys_platform == "darwin"` to pyobjc lines), or switch to uv/poetry which handle platform markers correctly. IMAGE_VERSION + MIN_IMAGE_VERSION stay at 4.

- [x] **#52** *(1.4.1-dev.55)* — ESPHome Web link in header.
  New pill-style link: "ESPHome Web ↗" opens `https://web.esphome.io/` in a new tab. Serial/USB flashing workaround for first-time provisioning or bricked devices with no OTA path.

- [x] **#53** *(1.4.1-dev.55)* — Compile/validate actions accept HA target-injected keys.
  When the user invoked the Compile action without picking a device, HA still passed `device_id: None` (plus `entity_id`, `area_id`, `floor_id`, `label_id`) in the service data. voluptuous rejected them as "extra keys not allowed". Added `_TARGET_KEYS` shared between `COMPILE_SCHEMA` and `VALIDATE_SCHEMA` declaring each as `vol.Optional` with `vol.Any(None, cv.string, [cv.string])`.

- [x] **#54** *(1.4.1-dev.55)* — Duplicated device comes up online without responding.
  `duplicate_device` now strips identity-pinning network fields from the clone: `wifi.use_address`, `wifi.manual_ip.static_ip`, `ethernet.use_address`, `openthread.use_address`. The clone inherited the source's IP; the poller connected to that address (still hosting the OLD device) and marked the duplicate online. Other fields (ssid, password, gateway) preserved. Three new tests; the former `preserves_use_address` test flipped to match the new behavior.

- [x] **#55** *(1.4.1-dev.55)* — Selected ESPHome version broadcast + hub sensors event-driven.
  `scanner.set_esphome_version` now broadcasts `targets_changed` on actual transitions so `SelectedEsphomeVersionSensor` updates in real time (previously only refreshed on the 30-s coordinator tick). The other hub sensors (Queue depth, Workers, Total slots, Total/Online/Outdated devices, Fleet version) were already event-driven via the coordinator-refresh path on any broadcast (#41).

- [x] **#57** *(1.4.1-dev.63)* — **All four CI workflows broken by SC.1 regex mishap.** Commit `eec0511` ("feat(SC.1, DL.1-5): SHA-pin GitHub Actions + diagnostic logging cluster") applied SHA pins across `ci.yml`, `compile-test.yml`, `publish-client.yml`, `publish-server.yml` but the replacement regex stripped newlines at comment-to-directive boundaries. Result: **25 malformed lines across 4 files** where two `uses:` directives (or one `uses:` plus a trailing `with:` / `name:`) got mashed onto a single line. Example:
  > `- uses: actions/checkout@de0fac2e... # v6.0.2- uses: actions/setup-python@a309ff8... # v6.2.0with:`
  
  GitHub can't parse the YAML → all four workflows fail with `"jobs": [], "conclusion": "failure"` at workflow-load time. Zero jobs ran on any of them.
  
  **Why the SC.1 invariant didn't catch it:** the new `scripts/check-invariants.sh` rule asserts every `uses: <org>/<repo>@...` line contains a 40-char lowercase hex SHA. On these malformed lines, SHAs *are* present (both of them) — the rule just doesn't detect "two `uses:` directives on one line" or "this YAML doesn't parse as valid GitHub workflow syntax."
  
  **Fix:**
  1. **Regenerate the 4 workflow files** with proper line breaks. The SHAs themselves look correct — just re-split the jammed lines.
  2. **Strengthen the invariant**: replace (or augment) the current SHA-regex check with `actionlint` (the canonical GitHub workflow validator). If actionlint isn't trivial to wire up, a simpler grep guard that rejects any line containing two `uses:` tokens (`grep -Pn '^.*\buses:.*\buses:' .github/workflows/*.yml`) catches the specific class of mistake this commit made.
  3. **Local preflight (optional belt-and-suspenders):** `scripts/check-invariants.sh` could parse each workflow with `python3 -c "import yaml; yaml.safe_load(open(f))"` to catch YAML-level corruption before CI rejects the workflow.
  
  Ship (1) + (2) in the same commit. (3) is bonus; overlaps with what actionlint already does.

  **Follow-up shipped *(1.4.1-dev.63)*.** (1) was already resolved in dev.59 via the line-by-line regex rewrite. (2) + (3) landed now as a new #57 rule in `scripts/check-invariants.sh`: a POSIX-safe `grep -E '(^|[^a-zA-Z_])uses:.*[^a-zA-Z_]uses:'` on every workflow rejects any line with two `uses:` tokens, plus an optional `yaml.safe_load` parse check that runs when PyYAML is importable in the current Python (CI has it; local Homebrew Python often doesn't, so that branch is gated and won't fail dev-box runs). Sanity-verified by planting a jammed line and watching both the primary grep and the invariant count (11 → 12) react.

- [x] **#56** *(1.4.1-dev.61)* — **PyObjC leak in server `requirements.lock` (second occurrence).** CI failed on `7b7c54c` ("fix: bugs #49, #51-55 + QS.27 polish + ESPHome 2026.4.0 bundled") with both the `audit` and `compile-server (*)` workflows erroring at `error: PyObjC requires macOS to build`. Root cause: when bundling ESPHome 2026.4.0, someone regenerated `ha-addon/server/requirements.lock` by running `pip-compile` directly on a Mac host instead of via the Docker-based `scripts/refresh-deps.sh`. `pyobjc-core==12.1` + `pyobjc-framework-cocoa==12.1` leaked in as macOS-only transitive deps (pulled in by zeroconf's platform-conditional deps when resolved on Darwin). Working tree has been re-generated cleanly; fix just needs to ship. **This is the same regression class as 1.3.1-dev.9**, which is exactly what `refresh-deps.sh`'s header comment warns about:
  > CRITICAL: lockfiles must be generated on the same platform the Dockerfiles install on (linux/amd64), otherwise platform-conditional transitive deps leak in.
  
  Follow-up guardrail — **option 1 shipped** *(1.4.1-dev.61)*: new rule in `scripts/check-invariants.sh` (#56) greps both `ha-addon/{server,client}/requirements.lock` for `pyobjc`, `pyobjc-core`, `pyobjc-framework-*`, or `appnope` package lines. Any hit fails CI with the remediation pointer: `bash scripts/refresh-deps.sh` (runs pip-compile inside the Docker linux/amd64 container that the Dockerfiles use). Sanity-verified by planting a fake pyobjc line in `requirements.lock` and watching the rule fire (9 → 11 total invariants, up from 10).

- [x] **#58** *(1.4.1-dev.62)* — **Yes, that was SC.4.** In 1.4.1-dev.60 the `/api/v1/client/code` endpoint was disabled (returned 410 Gone) and heartbeat responses stopped advertising `server_client_version`, so workers had no signal to auto-update themselves. The rationale was to close F-02 (supply-chain — server pushing arbitrary Python to every worker), but requiring manual `docker pull` on every worker each `-dev.N` bump is too much friction for the home-lab model. **Rolled back in dev.62.** F-02 is now marked PARTIAL (image-version gating from 1.3.0) with an updated rationale explaining why this is an intentional design choice — the shared bearer token already makes server ↔ worker a high-trust relationship, so a compromised token means full-fleet compromise either way.

- [x] **#59** *(transient — pioarduino CDN)* — `ESPHome Compile Tests` workflow failed on `develop` @ `fcbec87` ([run 24520008210](https://github.com/weirded/distributed-esphome/actions/runs/24520008210)) in the `compile-client (esp32s2-idf)` job. PlatformIO's package download of `scons-4.8.1.zip` from `github.com/pioarduino/registry` returned a 502 four times in a row (2s → 4s → 8s → 16s backoff, all failed) before exiting. Not a code issue — the commit (`revert(SC.4)`) doesn't touch compile or Dockerfiles, and the preceding and following runs on neighboring SHAs succeeded. Classic external-CDN flake, same class as #13 (Docker Hub 502/503) and #9 (Docker auth IPv6). No re-run kicked; the next commit on develop (`4bca65a docs(#58)`) will re-run compile tests anyway. If pioarduino 502s become a recurring pattern, consider pinning scons into a local cache step in `compile-test.yml`; two flakes in a month is noise, not a trend.

- [x] **#60** *(1.4.1-dev.67)* — `CI` workflow failed on `develop` @ `eb25e68` ([run 24525404736](https://github.com/weirded/distributed-esphome/actions/runs/24525404736)) in the `lint — mypy — server` job. One type error in `ha-addon/server/main.py:960`:
  > `error: Argument 1 to "create_task" of "AbstractEventLoop" has incompatible type "Future[None]"; expected "Generator[Any, None, Never] | Coroutine[Any, Any, Never]"  [arg-type]`
  
  Introduced by the SE.4 hook in `pypi_version_refresher`: the committed line wrapped `loop.run_in_executor(None, ensure_esphome_installed, new_detected)` in `loop.create_task(...)`, but `run_in_executor` returns a `Future[None]` — already scheduled on the executor, not a coroutine, so wrapping it in `create_task` is both a type error and a redundancy. Fix: drop the `create_task(...)` wrapper; the bare `run_in_executor` call is correct fire-and-forget. Fix is already in the working tree, queued for the next SE.* commit. No production impact — `create_task` on an already-scheduled Future is a cheap no-op at runtime, so the install still fires correctly on the running container; only mypy catches it.

- [x] **#61** *(1.4.1-dev.67)* — `CI` workflow failed on `develop` @ `b11e7e8` ([run 24525827827](https://github.com/weirded/distributed-esphome/actions/runs/24525827827)) in the `test — Run server + client tests` job. Four new SE.8 tests in `tests/test_esphome_unbundle.py` (lines 224, 241, 255, 269) imported via `from tests.test_ui_api import _make_ui_app`, but `tests/` has no `__init__.py` and isn't on sys.path — `conftest.py` only adds `ha-addon/{server,client,custom_integration}`, not the tests dir itself. Error on all 4: `ModuleNotFoundError: No module named 'tests'`. Fixed by changing to the sibling form `from test_ui_api import _make_ui_app`, matching how the rest of the suite references siblings via the conftest-configured rootdir. 15 SE tests green locally.

- [x] **#62** *(1.4.1-dev.68)* — HA integration worker entities missing. Root cause: each platform's `async_setup_entry` kept an in-memory `seen_workers` / `seen_targets` closure set to avoid double-adding entities on coordinator updates. When the #39 stale-cleanup removed a worker/target whose coordinator snapshot had briefly gone silent (server restart gap), the closure still contained that `client_id`, so subsequent `_discover` refreshes saw `in seen_*` → True → skip. Device was recreated on reconnect but entities were never re-added. Hit hass-4 during the SE workstream restart churn — 6 of 7 worker devices ended up with 0 entities. Fix: new `_discovery.entity_already_registered()` helper queries HA's entity registry by `unique_id`. Replaced the closure check in all 5 platforms (sensor, binary_sensor, update, button, number). Registry-backed check is self-healing across add/remove cycles. 

- [x] **#63** *(1.4.1-dev.68; superseded by #66 in 1.4.1-dev.70)* — Initial approach mixed target + worker devices in one picker with handler-side splitting. User found the UX confusing (#66). Replaced with two separate device-selector fields — see #66 below for the final shape.

- [x] **#64** *(1.4.1-dev.69)* — HA actions testing audit + additions. Audit of `tests/test_integration_services.py`: 14 existing unit tests covered handler transformation logic (device_id → targets/worker split, error paths). Gaps: schema validation, service-registry lifecycle, services.yaml parsability, end-to-end via HA's REST API. Added **8 new pytest cases** (schema accepts HA-injected keys, targets sentinels, dict-targets rejection, all-optional fields, cancel schema requires job_ids, register/unregister lifecycle, services.yaml parses + has expected `target: device: integration` blocks) + **new `e2e-hass-4/ha-services.spec.ts`** (3 tests that invoke `POST /api/services/esphome_fleet/*` directly against hass-4's REST API with an HA LLAT; self-skips when `HASS_TOKEN` isn't exported). Covers the end-to-end path users invoke from HA's Developer Tools → Services tab. Services tests: 14 → 25.

- [x] **#65** *(1.4.1-dev.70)* — Removed the free-text `worker_id` field from the `esphome_fleet.compile` action. The only supported way to pin a worker is now the typed device-selector (see #66). Dropped the schema key, the handler branch that read it, and the `services.yaml` field descriptor.

- [x] **#66** *(1.4.1-dev.70)* — Rewrote the `esphome_fleet.compile` action to use two separate device-selector fields instead of one mixed picker. `target:` at the top level now filters to `manufacturer: "ESPHome"` (YAML-managed target devices only), and a new `worker` field under `fields:` filters to `manufacturer: "ESPHome Fleet Worker"`. Required bumping worker `DeviceInfo.manufacturer` from "ESPHome Fleet" (shared with the hub) to "ESPHome Fleet Worker" so the selector filter is unambiguous. Handler gains `_resolve_worker_device_id(device_id) → client_id` that walks the device-registry `worker:<client_id>` identifier, and raises a clear HomeAssistantError if someone bypasses the selector filter with a non-worker device. Schema test `test_services_yaml_parses` extended to assert both filters and the absence of the old `worker_id`. Handler tests `test_compile_service_pins_worker_from_device_field` and `test_compile_service_raises_when_worker_field_is_not_worker` cover the new happy and defensive paths. Dropped the two obsolete mix-in tests with an explanatory comment.

- [x] **#67** *(1.4.1-dev.70)* — Audited the 150-commit diff from `v1.4.0..HEAD` for shims/aliases/compat paths introduced within this release cycle. Findings:
  - **#63 interim mixed picker** (`services.py::_resolve_device_ids` — a single picker that split targets + workers by identifier prefix): already removed in this same turn by #65/#66. ✅
  - **SE.1–SE.7 "pre-SE.1 fallback" paths** (`ui_api.py:113-128`, `ui_api.py:918-945`, `scanner.py:178/195/614`): comments mark these as "pre-SE.1 bundled package" fallbacks, but they are **also load-bearing for the test harness** — the host pytest env has `esphome` installed via pip, so without these fallbacks the `/ui/api/components`, `/ui/api/validate`, and `scanner._resolve_esphome_config` tests would all need explicit `_scanner._esphome_ready.set()` mocking. Net: keeping them is no code churn and no extra test boilerplate, vs. ripping them out is ~50 lines of test setup for no production behavior change. Left as-is.
  - **Every other `legacy` / `backward-compat` / `deprecated` marker in the codebase** (`api.py:240` legacy client routes, `ui_api.py:2023-2033` legacy worker aliases, `app_config.py:66` `client_offline_threshold` alias, `scheduler.py:197` UTC fallback for pre-#90 schedules, `main.py:598` `_legacy_schedule_checker_removed`): `git log -S` shows all of them were introduced in `v0.0.14` / `v0.0.51` / `v1.3.1` / `v1.4.0` (`#85-#87`). Genuine back-compat for publicly-shipped versions — not in-scope for this audit.
  - **Worker `DeviceInfo.manufacturer` rename** (from "ESPHome Fleet" → "ESPHome Fleet Worker" in this turn for #66): no back-compat alias added because `DeviceInfo.manufacturer` is authoritative on every coordinator refresh — HA updates the registry in place. No stale user-facing state. ✅
  - **Conclusion:** No actionable legacy cruft left over from 1.4.1. The only within-cycle shim (#63) was cleaned up by #65/#66 in the same turn.

- [x] **#68** *(1.4.1-dev.72)* — Disambiguated the Download buttons. Three call-sites were all labeled "Download" but did different things: `LogModal` and `DeviceLogModal` save the xterm buffer as a text file (the build/device log), and the Queue tab's per-row anchor downloads the compiled firmware `.bin` from `/ui/api/jobs/{id}/firmware`. Renamed to "Download log" (LogModal + DeviceLogModal) and "Download .bin" (QueueTab). Tooltips tightened at the same time ("Download build log as text file", "Download device log as text file", "Download compiled firmware binary (.bin)"). Updated `e2e/compile-download.spec.ts` to match the new anchor name. Mocked suite: 108 passed.

- [x] **#69** *(1.5.0-dev.74)* — Multi-format firmware download. ESP32 compiles now produce both `firmware.factory.bin` (full flash image, used for first USB/serial flash) and `firmware.bin` (smaller OTA-safe image); ESP8266 produces only the latter. Worker uploads each variant separately via the new `POST /api/v1/jobs/{id}/firmware/{variant}` route; server stores them side-by-side at `/data/firmware/{job_id}.{variant}.bin`. The Queue-tab Download button is now a shadcn `DropdownMenu` with one menu item per `(variant, raw|.gz)` combination (up to four rows on ESP32: Factory .bin / Factory .bin.gz / OTA .bin / OTA .bin.gz). Server endpoint `GET /ui/api/jobs/{id}/firmware?variant={factory|ota}&gz={0|1}` serves each; gzip compression happens on-the-fly with `Content-Encoding: identity` + `.bin.gz` filename so browsers save the literal compressed file. Per-job `firmware_variants: string[]` field on the queue JSON drives the dropdown so the UI doesn't need extra round-trips. Pre-#69 legacy blobs (`{job_id}.bin` on disk) surface as the single variant "firmware" so upgraded installs keep working for in-flight jobs. Legacy `POST /api/v1/jobs/{id}/firmware` route kept as a compat shim that stores under the "firmware" variant name; pre-#69 workers that hit it before auto-updating still upload successfully. `firmware_storage.py` fully reworked: `save_firmware(variant=…)`, `read_firmware(variant=…)`, `list_variants(job_id)`, variant-aware `reconcile_orphans`, and `delete_firmware` that now purges every variant (not just one) for a cleared job. 11 firmware-storage unit tests + 2 new ui_api tests (variant selection, gzip flag) for total 525→533 passing.

- [x] **#70** *(1.5.0-dev.73)* — Renamed the current release from 1.4.1 to 1.5.0 — scope (HA native integration, ESPHome unbundling, Fleet rebrand, security hardening) outgrew a patch version. Cascaded the future release files by one minor: `WORKITEMS-1.5.md` → `.6`, `.6` → `.7`, `.7` → `.8`, and `WORKITEMS-1.4.1.md` → `WORKITEMS-1.5.md`. Updated each file's H1 heading to match its new number. Bumped `ha-addon/VERSION` + `config.yaml` + `client.py` `CLIENT_VERSION` from `1.4.1-dev.72` to `1.5.0-dev.73` (continuous dev.N sequence so Docker-tag/git-SHA lookup stays predictable). Updated `CLAUDE.md`, `dev-plans/README.md`, `ha-addon/CHANGELOG.md` heading, `ha-addon/DOCS.md` (two "1.4.1 onward" mentions), `dev-plans/SECURITY_AUDIT.md` (F-03 "FIXED in 1.4.1" + WORKITEMS link). Historical dev-build tags inside WORKITEMS (e.g. `*(1.4.1-dev.55)*` incident markers, pre-existing completion stamps) left untouched — they refer to actual Docker images and git SHAs that exist under those labels. Added a callout at the top of `WORKITEMS-1.5.md` documenting the rename so anyone browsing the file history understands why dev.N stamps mention both 1.4.1 and 1.5.0.