# Work Items — 1.6.0

Theme: **Power-user fleet management.** Git-backed config version history, per-device compile history, archived-config recovery, key/value device tagging, disk budgeting, and declarative worker routing — the controls you need to run a growing fleet without losing track of what it's doing.

## Settings

Introduce an in-app Settings surface separate from Home Assistant's Supervisor-managed `options.json`. Reasons:

- Feature toggles (auto-commit on save, job history retention, disk budgets, future proxy config) don't belong in the Supervisor Configuration tab — they're product settings, not deployment plumbing. Editing via Supervisor also forces a full add-on restart per change, which is hostile for day-to-day toggles.
- Over time most user-facing configuration should live here rather than in the Supervisor options form. `options.json` keeps only the fields Supervisor genuinely needs (token, port, `require_ha_auth`).

**Storage model.** `/data/settings.json`, owned by the server. Atomic write (tempfile + `os.replace`). Loaded once into an in-memory singleton; every mutation rewrites the file and updates the singleton under a single asyncio lock.

**Bootstrap.** On first boot after the 1.6 upgrade, if `/data/settings.json` doesn't exist, seed it once from the current `options.json` values for fields that have moved (`job_history_retention_days`, `firmware_cache_max_gb`, `job_log_retention_days`). After that the file is the only source of truth — editing the moved field in Supervisor has no effect. Documented in `DOCS.md`.

**Immediacy.** Every setting change takes effect immediately. Server-side consumers read `get_settings().xxx` at decision time (not at startup), so the next scheduled retention run, the next git auto-commit, the next cache prune all see the new value on the next tick — no add-on restart. UI consumers subscribe via an SWR hook on `/ui/api/settings`; the drawer's `mutate()` after a PATCH propagates changes to other tabs on the next poll.

- [x] **SP.1 Settings store + DAO** *(1.6.0-dev.3)* — new `ha-addon/server/settings.py`. `AppSettings` dataclass with typed fields + defaults. Atomic load/save (tempfile + `os.replace`). Thread-safe (single `asyncio.Lock` around mutations). Fields landing in 1.6: `auto_commit_on_save: bool = True`, `job_history_retention_days: int = 365`, `firmware_cache_max_gb: float = 2.0`, `job_log_retention_days: int = 30`. Structured so new fields can be added in future releases without schema churn. Exposes a `get_settings()` accessor for live reads and an `update_settings(**partial)` coroutine for atomic mutation.
- [x] **SP.2 One-time import from options.json** *(1.6.0-dev.3)* — on first boot where `/data/settings.json` is absent, seed it from the current `options.json` values for any fields that have migrated. Log at INFO what was imported and from which key. Subsequent boots skip import entirely. Idempotent: if the file exists, even if empty-but-valid, do not re-import.
- [x] **SP.3 Settings REST API** *(1.6.0-dev.3)* — `GET /ui/api/settings` returns the full blob. `PATCH /ui/api/settings` accepts a partial JSON object, validates types and ranges per field, persists atomically, updates the in-memory singleton, and returns the full updated blob. UI-tier trust only (no worker access). Validation errors return `400` with the offending field name and reason.
- [x] **SP.4 Settings drawer** *(1.6.0-dev.3)* — gear icon in the app header (top right of the tab bar). Click opens a shadcn `Sheet` from the right. Drawer content is organized into sections (`Config versioning`, `Job history`, `Disk management`, `About`) so the shape scales as more settings land. Each setting is a form row: label, help text, input (`Switch` for bools, `Input type="number"` for numeric, text input for strings). Save-on-change — no bulk Save button; toast confirms persistence. Built on shadcn primitives so we can graduate to a full-page `/settings` route later without rewriting rows. Route-addressable deep link can follow in a later release.
- [x] **SP.5 Live-effect semantics verified end to end** *(1.6.0-dev.3)* — infrastructure floor proven by `test_get_settings_sees_update_immediately` + `test_patch_settings_persists_across_get`: a PATCH is observable via the live `get_settings()` accessor on the very next call. Downstream consumers (auto-commit hook, job-history retention task, disk-budget prune tasks) don't exist yet — when AV.2 / JH.3 / DM.4 land, each will wire through `get_settings()` at call time and add its own per-consumer live-effect test.
- [x] **SP.6 Tests** *(1.6.0-dev.3)* — unit: store round-trip; atomic write survives a simulated crash mid-write; PATCH validates types/ranges; one-time import runs exactly once; `get_settings()` sees PATCH mutations immediately. Integration: full PATCH round-trip through the UI API with validation errors. Mocked Playwright: open the drawer, flip auto-commit off, close drawer, reopen, verify persistence; numeric field rejects invalid input.
- [x] **SP.7 DOCS.md update** *(1.6.0-dev.3)* — new subsection under Configuration documenting the Settings drawer, the one-time import behavior, and the list of fields that have moved out of the Supervisor Configuration tab. Keep the Supervisor options section honest — annotate each moved field with "now in the in-app Settings drawer (1.6+)".
- [x] **SP.8 Move remaining Supervisor options into Settings** *(1.6.0-dev.8, hotfixed dev.9)* — `server_token`, `job_timeout`, `ota_timeout`, `worker_offline_threshold`, `device_poll_interval`, `require_ha_auth` all moved from `options.json` to `/data/settings.json`. Every consumer (`auth_middleware`, `ha_auth_middleware`, `JobQueue` timeout checker, `DevicePoller`, `WorkerRegistry.is_online`, Supervisor-discovery snippet, server-info endpoint, compile / retry / schedule enqueue paths) reads via `get_settings()` at call time, so drawer edits take effect on the next request or scheduled tick with no restart. Drawer adds four new sections (`Authentication`, `Timeouts`, `Polling`) plus a masked server-token input with show/hide + copy buttons. `config.yaml` no longer declares `options:` / `schema:` — the Supervisor Configuration tab is empty by design. `app_config.py` shrank to `config_dir` + `port` only. **Migration source-of-truth:** dev.8 only read `/data/options.json` for the import, which Supervisor empties once the schema is stripped — the dev.8 deploy on hass-4 lost the pre-1.6 token and every remote worker 401'd. Dev.9 fixes this by querying Supervisor's live HTTP API (`http://supervisor/addons/self/info`) first, then falling back to `/data/options.json`, then `/data/auth_token`, then auto-generate — Supervisor's own record of the user's options survives the schema strip, so upgrades now preserve token + timeouts intact. Token-rotation caveat on the local-worker subprocess (captures `SERVER_TOKEN` at spawn; restart-required) still applies. Tests: 17 unit tests including three explicit regression tests for the hass-4 incident (auto-heal via Supervisor API; options + token import from Supervisor when options.json is empty; silent no-op outside Supervisor).

## Auto-Versioning (local git, zero config)

Every save creates a git commit automatically. Users get per-file history, diff, and rollback without touching git or configuring anything. The config directory becomes a local git repo on first startup; existing git repos are left intact.

**Why git:** Diff is free (`git diff`), history is free (`git log --follow`), rollback is `git checkout <hash> -- <file>` + a new commit, Monaco already has a diff editor in the bundle, and the `git` binary ships in the HA add-on base image (zero image size cost). The alternative (copy-on-write snapshots + DIY diff) is more code and worse tooling.

- [x] **AV.1 Auto-init** *(1.6.0-dev.4, refined dev.5)* — on server startup, if `/config/esphome/` is not a git repo, run `git init` + `git add -A` + initial commit "Initial commit by distributed-esphome". If it's already a git repo (user set it up themselves), skip init entirely — including the `.gitignore`, which is left untouched so Pat-with-git's curated file isn't mutated. Fresh-init still writes `.gitignore` with `secrets.yaml` + `.esphome/` safety-net entries. Pre-existing repo with no resolvable `user.name`/`user.email` (nothing in repo/global/system config) gets a one-time Fleet identity fallback so auto-commits don't emit git's "configured automatically" warning.
- [x] **AV.2 Auto-commit on save** *(1.6.0-dev.4, refined dev.5, completed dev.10)* — editor save + **every other user-initiated write path** (pin / unpin, meta, schedule set / delete / toggle / once, rename, delete, archive restore) now triggers `git add --all -- <file>` + `git commit -m "<action>: <file>"` in a debounced async task. Rename commits both old and new paths so the history reads as a delete-of-old + add-of-new. Action label is specific per endpoint (e.g. `pin: bedroom.yaml`, `schedule: bedroom.yaml`) so `git log --author="HA User"` reads like an activity feed. **Gated on `settings.auto_commit_on_save`** (live-read at commit time). **Author respects the repo's own `user.name`/`user.email`**. Debounce window of 2s coalesces rapid actions. `.archive/` added to fresh-init gitignore so soft-deletions don't double-track. Non-blocking: each endpoint returns immediately, commits happen async.
- [x] **AV.3 History endpoint** *(1.6.0-dev.10)* — `GET /ui/api/files/{filename}/history` returns `[{hash, short_hash, date, author_name, author_email, message, lines_added, lines_removed}]` via `git log --follow --numstat`. The `--follow` flag tracks renames. Paginated via `limit` (default 50, max 500) + `offset`. Empty list (not an error) if the file has no commit history yet. Parser uses `\x1f` (unit separator) between fields — `\x1e` (record separator) breaks `str.splitlines()` in Python which treats it as a line boundary; the fix is tested.
- [x] **AV.4 Diff endpoint** *(1.6.0-dev.10)* — `GET /ui/api/files/{filename}/diff?from=<hash>&to=<hash>` returns `{"diff": "<unified diff>"}`. Works for any combination of hashes: both → direct diff; only `from` → diff against HEAD; only `to` → diff working tree against `to`; neither → diff working tree against HEAD. Hash values validated as 4–40 hex chars so injected shell metachars can't leak into `git diff` (tested).

**Build order (remaining).** AV.6 is the gateway that makes rollback and manual-commit actually usable — the endpoints for both (AV.5, AV.11) are built *with* the history panel so the Restore button and the "Commit these changes" affordance are live the moment the UI ships. AV.7 follows as its own pass. AV.8 folded into AV.6 — the panel IS the diff viewer. AV.9 stays deferred until the file-tree editor exists.

- [x] **AV.5 Rollback endpoint** *(1.6.0-dev.11)* — `POST /ui/api/files/{filename}/rollback` body `{hash}`. Runs `git checkout <hash> -- <filename>` to restore the working-tree file to that commit's content. **If `settings.auto_commit_on_save` is true**, follows up with `git add + git commit -m "revert: <file> to <short-hash>"` so the rollback is itself a recorded commit. When auto-commit is false, the working tree is left dirty — the user is expected to review + commit via AV.11. Returns `{content, committed, hash, short_hash}`. Invalidates the scanner config cache. Validates `hash` as 4-40 hex chars. Tests: 4 unit + 2 integration covering both toggle states, malformed hashes, and non-repo safety.

- [x] **AV.6 History + diff panel** *(1.6.0-dev.11, absorbs the old AV.8)* — the full-featured per-file history UX, opened from the Editor modal's toolbar ("History" button) and from the Devices-tab hamburger menu ("View history…"). One drawer/sheet scoped to one file:

  1. **Header row.** File name, commit count, and an auto-commit status chip (on/off + link to the Settings drawer). If there are uncommitted changes to this file, a yellow banner above the content reads *"You have uncommitted changes. [Commit…]"* and clicking the button opens AV.11's flow.

  2. **Compare selector.** A pill bar near the top: `From: [—] ↔ To: [Current]`. Both pills are dropdowns listing every commit plus "Current" (=working tree). Changing either side re-renders the diff below. Defaults: `From` = HEAD's parent (or the oldest available commit), `To` = Current — so opening the panel shows "what changed since the last committed state."

  3. **Diff viewer.** Monaco's built-in `DiffEditor` (already in the bundle, just not imported — this is what the retired AV.8 was going to do). Renders the unified diff between `From` and `To` via `GET /ui/api/files/{f}/diff?from=<hash>&to=<hash>`. Read-only, syntax-highlighted YAML, inline + side-by-side toggle. If the two sides are identical the editor shows a "no differences" empty state.

  4. **Commit list.** Newest-first table of commits (via AV.3), paginated with "Load more" at the bottom. Each row shows `short_hash · <action: filename> · <relative time> · +N/-M`. Author name appears only when not the Fleet identity. Click a row to set its commit as `To` and its parent as `From` (default: "show me what this commit changed"). Shift/cmd-click a second row to set `From` to that commit instead. Per-row actions on hover/menu:
     - **Set as From / Set as To** — explicit compare
     - **Restore this version** — calls AV.5 with a confirmation dialog; shows what rollback will do (one-line summary: "Restore to {short_hash} from {relative time}. This will create a new `revert:` commit." or "…this will replace your uncommitted changes." when auto-commit is off).

  5. **Empty state.** When there's no history yet: "No saved versions yet — your first edit will show up here."

  SWR-backed so a manual commit (AV.11) or a rollback refreshes the list without a full reload. Built on the shadcn `Sheet` primitive added in SP.4. Does NOT open inside the Editor modal (avoids a modal-in-modal situation); it's a separate drawer that can be opened with the Editor still underneath.

- [x] **AV.7 Config diff on compile** *(1.6.0-dev.15)* — every `queue.enqueue()` / `queue.retry()` call now stamps the job with `config_hash = git_versioning.get_head(config_dir)`. Job model serialises + round-trips the field; UI types pick it up. The Queue-tab Log modal renders a **"Diff since compile"** button on every non-validate job that has a `config_hash`; clicking opens the AV.6 History panel pre-set to `from = job.config_hash, to = "Current (working tree)"` so the user lands directly on "what's changed since this compile started." **Auto-commit-off handling:** we always stamp `HEAD` at enqueue time regardless of the toggle. When auto-commit is on, that hash is the committed state that got compiled; when it's off, it's whatever the user last committed — pairing it with the working-tree default on the right surface captures both committed and uncommitted deltas. From/To pickers still let the user flip either side to any hash. Button is hidden on jobs that pre-date AV.7 (no `config_hash`) and on validate-only jobs. Tests: 2 new Playwright specs (shortcut opens drawer with preset from-hash; button hidden on no-config-hash jobs) + existing queue tests exercise the extra field end-to-end.

- [x] **AV.11 Manual commit endpoint + UI** *(1.6.0-dev.11)* — the escape valve for when auto-commit is off (Pat-with-git who didn't want Fleet writing commits, but still needs a path to flush pending edits through the UI). Also useful even when auto-commit is on — covers externally-made edits (Samba, SSH) that don't go through our write endpoints.

  **Endpoint.** `POST /ui/api/files/{filename}/commit` body `{message?: string}`. Runs `git add --all -- <filename>` + `git commit -m "<message>" -- <filename>`. Returns `{committed: boolean, hash?: string, short_hash?: string, message?: string}` — `committed: false` with no hash when there was nothing to commit. Uses the same Settings-driven author chain as AV.2 (respects repo `user.name`/`user.email` if set; falls back to Settings). Returns 200 either way (empty commit is not an error). Default message when body has no `message`: `"save: <filename>" (manual)` — the trailing marker distinguishes hand-triggered commits from auto-commit entries in the log.

  **UI 1 — when there are uncommitted changes to a file.** Banner in the AV.6 history panel: *"You have uncommitted changes. [Commit…]"*. The button opens a compact prompt: message field (optional, placeholder defaults to the auto-message), Commit / Cancel. On success, the new commit appears at the top of the history list via SWR mutate.

  **UI 2 — auto-commit off education.** When `auto_commit_on_save` is false and the user saves in the editor, the editor's save toast carries an extra action: *"Saved. [Commit now]"* — one-click path to commit that specific save without leaving the editor. Absent when auto-commit is on (since it'd fire automatically within 2s anyway).

  Single-file scope for the MVP. A fleet-wide "Commit all pending changes" button can come later if multi-file manual commits become a real user need.

- [x] **AV.10 Tests (init + commit)** *(1.6.0-dev.4)* — `tests/test_git_versioning.py` covers: init on empty dir, init skips existing repo, gitignore append-only, tolerates missing dir / missing git binary, commit gated on `auto_commit_on_save`, debounce coalesces rapid writes to one commit, unrelated paths get separate commits, commit on non-repo dir is safe. Integration in `tests/test_ui_api.py`: save via editor API → `git log` shows `save: <file>`; toggle off → no new commit. History/diff/rollback/manual-commit tests follow in AV.5 / AV.11.

## Job History

Today the queue (`/data/queue.json`) IS the user's record of what the system did. Bug #18 removed time-based auto-pruning so history survives restarts, but per-target coalescing in `enqueue()` still clears *earlier* terminal jobs for a target whenever a new compile is enqueued (deliberate: one row per device on the Queue tab, not an ever-growing failure log). Net effect: users can only ever see the *latest* compile attempt per target, never the sequence of past attempts. Once they explicitly Clear the queue, even that disappears.

Add a persistent append-only job history that survives both coalescing AND explicit clears, powering: per-device "when did this last succeed?", fleet-wide success-rate rollups, compile-time trend lines, and audit trails of who-compiled-what-when. Separate from the live Queue (UI-wise and storage-wise), read-only from the user's perspective.

**Storage model:** SQLite (`/data/job-history.db`) rather than a growing JSON file. Per-job writes are cheap, indexed queries by target / timestamp / state are fast, and the file stays self-compacting. Add-on base image already has `sqlite3` as a stdlib builtin.

**Write model:** every time a job transitions to a terminal state (SUCCESS, FAILED, TIMED_OUT, CANCELLED), copy the finalized `Job` into the history DB. Coalescing still clears the queue entry but the history record stays. Explicit Clear on the Queue tab also leaves history intact.

- [ ] **JH.1 Schema + DAO** — new `ha-addon/server/job_history.py`. SQLite schema: `jobs(id TEXT PRIMARY KEY, target TEXT, state TEXT, triggered_by TEXT, trigger_detail TEXT, download_only BOOL, validate_only BOOL, pinned_version TEXT, esphome_version TEXT, assigned_client_id TEXT, submitted_at INTEGER, started_at INTEGER, finished_at INTEGER, duration_seconds REAL, compile_result TEXT, ota_result TEXT, config_hash TEXT, log_excerpt TEXT)`. `log_excerpt` is the last ~2 KB of the job log (not the full log — that's too big for an append-only DB). Indexed by `(target, finished_at DESC)` and `(finished_at DESC)`. DAO exposes `record_terminal(job)` + `query(target=None, state=None, since=None, limit=50, offset=0)`.
- [ ] **JH.2 Write hook** — `JobQueue.mark_result` / `timeout_checker` / `cancel` all funnel through a single `_transition_to_terminal(job, new_state)` helper today (verify); hook `job_history.record_terminal(job)` in that one place. Coalescing in `enqueue()` gets the same hook called right before the old entry is removed from `_jobs` (records the terminal state the removed job had at that moment). Idempotent on `id` — inserting the same job id twice is a no-op (ON CONFLICT DO NOTHING on the primary key).
- [ ] **JH.3 Retention** — `job_history_retention_days` lives in the new Settings store (see SP.*, default 365, max 3650, 0 = unlimited). Nightly background task in `main.py` reads the current value via `get_settings()` on each run (so edits in the Settings drawer take effect on the next run without restart) and deletes rows older than the retention window. Log the evict count at INFO. Exposed in the Settings drawer under `Job history`. Even at hundreds of compiles/day, the DB stays well under 100 MB at a year of retention.
- [ ] **JH.4 History endpoint** — `GET /ui/api/history?target=<file>&state=<SUCCESS|FAILED|…>&since=<epoch>&limit=50&offset=0` returns `[{job}]` ordered newest-first. `GET /ui/api/history/stats?target=<file>&window_days=30` returns a rollup `{total, success, failed, cancelled, timed_out, avg_duration_seconds, p95_duration_seconds, last_success_at, last_failure_at}`. UI-tier (HA Ingress trust); no worker-tier access.
- [ ] **JH.5 History drawer on device row** — new hamburger-menu entry "Compile history…" on each Devices-tab row. Opens a side drawer (shadcn `Sheet`) showing the most recent 50 entries for that target: compact table with columns `when / state badge / duration / triggered-by / ESPHome version`. Click a row → expands a log-excerpt panel. Paginates (load more) via `offset`. Empty state: "No history yet — first compile will appear here." Re-uses the same badge helpers as the Queue tab (`utils/jobState.ts`) for visual consistency.
- [ ] **JH.6 Per-device last-success indicator** — augment the existing Devices tab columns with a `Last compiled` column (toggleable, off by default): shows "X ago ✓" from the most recent SUCCESS in history, red "X ago ✗" if the most recent terminal was a FAILED/TIMED_OUT, "never" if no history. Served via a single rollup in `/ui/api/targets` (add `last_compile: {at, state}` to each target payload — one SQL query per target, or one batched JOIN). Avoids the "click through to see if anything ever worked" problem.
- [ ] **JH.7 Fleet-wide History tab (stretch)** — new top-level tab (after Schedules) listing all compile history across all targets, with filters for state / target / date range / triggered-by. Same table layout as Queue, but strictly read-only (no Retry / Clear / Cancel). Export-to-CSV button for offline analysis. Can slip to 1.6 if the per-device drawer (JH.5) turns out to be enough.
- [ ] **JH.8 Tests** — unit tests: terminal transition records a row, idempotent on duplicate id, coalescing emits a history row before eviction, retention task deletes old rows only. Integration tests: full compile round-trip leaves a SUCCESS row in history; the `/ui/api/history` endpoint returns it; cancelling a job leaves a CANCELLED row; `/ui/api/history/stats` rollup matches the rows. Mocked Playwright: open the history drawer on a device row, see entries, expand one to see the log excerpt.

## Worker Update Documentation

After the SC.4 revert (bug #58 in 1.4.1), workers update manually: user notices the stale badge in the Workers tab and runs `docker pull && docker restart` on the worker host. Two gaps today:

1. **`ha-addon/DOCS.md`** (`line 21`) mentions the stale-image badge but never tells the user *how* to remediate it. Zero guidance on keeping workers up to date.
2. **The stale-badge tooltip** (`WorkersTab.tsx::ImageStaleBadge`, lines 125–151) points users at the Connect Worker modal — which emits a full `docker run` command valid for new installs but overkill for "I just need to refresh an already-running container". The cheap path (`docker pull && docker restart <name>`) isn't surfaced in the UI.

**Why not auto-update:** evaluated and declined. Docker has no truly native auto-update for long-running containers. Third-party tools (Watchtower is in maintenance limbo; What's Up Docker is active but still third-party) add friction and dependencies we don't want to own. Docker-native alternatives (Compose + scheduled pull; Swarm services + `docker service update --force`) all require user-configured schedulers anyway. For a home-lab tool the right trade-off is: don't promise automation we haven't built — document the manual path clearly, keep the stale-badge safety net, and let users wire their own pull cadence into whatever host scheduler they already use.

- [ ] **WU.1 DOCS.md "Keeping workers up to date" section** — place after the existing Workers section in `ha-addon/DOCS.md` (near line 21). Covers: why it matters (protocol compatibility, CVE patches, `MIN_IMAGE_VERSION` gating), how to tell (stale badge + per-worker build version), refresh command (`docker pull ghcr.io/weirded/esphome-dist-client:latest && docker restart <name>`) with Windows/Mac Docker Desktop parity note, full re-install pointer to Connect Worker modal, Docker Compose variant (`docker compose pull && docker compose up -d`), one-paragraph aspirational pointer at community auto-update tools (Watchtower/wud/cron) with explicit "we don't endorse a specific one", optional cosign verification link pointing at SECURITY.md.
- [ ] **WU.2 Improve stale-badge tooltip** — `ha-addon/ui/src/components/WorkersTab.tsx:125-151`. Replace the "reinstall via Connect Worker modal" copy with the literal 2-line refresh command: `docker pull <image>` + `docker restart <containerName|hostname>`, plus "Details: DOCS → Keeping workers up to date". Fall back to `<container-name>` placeholder if `hostname` is missing. Stays a `title=` tooltip — no new modal.
- [ ] **WU.3 One-line README pointer** — under the Workers section of `README.md`: *"Workers don't auto-update — check the Workers tab for the stale-image badge and follow the DOCS instructions to refresh."* Keeps the README honest so users don't assume automation exists.

No Python changes, no protocol changes, no Dockerfile changes. Test footprint: optional one-line Playwright assertion in the existing `e2e/worker-actions.spec.ts` that the tooltip contains `docker pull`; skip if it doesn't fit the spec's shape.

## Integration Test Refactor (deferred from 1.5 CR.26)

The 1.5-cycle code review (`dev-plans/CODE_REVIEW-develop-vs-main.md` §3.2 / §4) flagged that `tests/test_integration_*.py` builds a `SimpleNamespace(data={DOMAIN: {...}})` and calls handler functions directly. That's unit tests of helper Python, not integration tests — they won't catch `async_on_unload` misuse, config-flow step bugs, service schema drift at HA load time, `_attr_unique_id` collisions, or `device_info` wiring errors. The user chose to accept this for 1.5 ship and queue the fix here.

- [ ] **IT.1** — Rename all mock-based integration test files to reflect reality. `tests/test_integration_services.py` → `test_integration_services_logic.py` (or `test_integration_helpers.py`). Apply the same rename to `test_integration_installer.py`, `test_integration_update_entities.py` etc. only where they are genuinely helper-level. Add a new invariant **PY-10** in `scripts/check-invariants.sh`: any `tests/test_integration_*.py` file must import `pytest_homeassistant_custom_component` — files that don't either rename to `_logic` or add the dep. Stops the credibility drift where the test name implies more than the content.
- [ ] **IT.2** — Add `pytest-homeassistant-custom-component` to dev dependencies; rewrite a first batch of integration tests under a real `hass` fixture. Priority targets: `async_setup_entry` / `async_unload_entry` flows (catches CR.12 class), config flow steps (manual + hassio + zeroconf discovery paths), service registration with schema validation, entity `unique_id` collision check across all managed devices, `device_info` assertion that Fleet entities merge onto the native ESPHome device row (bug #27/#32 regression guard). Leave logic-level tests alone — they still have value and don't need to move.
- [ ] **IT.3** — Add a contract test covering `/ui/api/*` endpoint shapes. Run a fake server fixture (aiohttp test client), issue every endpoint the UI + integration consume, `assert set(response_keys) == expected` against a schema defined in one place. Fails CI on drift between server JSON and TS types / integration dict accesses. Closes the "UI silently breaks when a server field is renamed" gap the reviewer called out.

## Carried forward from past releases

Surfaced by the 2026-04-16 archive sweep. Each item was deferred (or filed as a TODO) in an earlier release and never made it into a successor WORKITEMS file.

- [ ] **CF.1 Bundle Monaco locally** *(from 1.3.1 bug #15)* — `@monaco-editor/react` currently loads Monaco's runtime from `https://cdn.jsdelivr.net`, which forces `cdn.jsdelivr.net` into the CSP `script-src` / `style-src` / `font-src` / `connect-src` (see the rationale comment in `ha-addon/server/main.py:67-73`). Switch to `vite-plugin-monaco-editor` (or equivalent) so we can drop the CDN origin entirely and ship a fully self-hosted UI. Side benefits: editor works in air-gapped installs and survives a jsDelivr outage. Watch the bundle-size impact — Monaco is large; tree-shake to the YAML language only. Update `tests/test_security_headers.py` to assert the CDN is no longer in the CSP.
- [ ] **CF.2 UI archive viewer** *(from 1.2.0 #132)* — server endpoints already exist (`GET /ui/api/archive`, `POST /ui/api/archive/{f}/restore`, `DELETE /ui/api/archive/{f}`) and `getArchivedConfigs()` is wired in `api/client.ts`, but there is no UI consumer. Add an "Archive" surface — likely a section under Settings or a modal launched from the Devices toolbar — that lists archived YAML files with Restore + Delete-Permanently actions. Reuses the existing two-step delete-permanent confirmation pattern from the live device delete flow.
- [ ] **CF.3 Docker base image digest pinning** *(from 1.3.1 E.4)* — `ha-addon/Dockerfile` and `ha-addon/client/Dockerfile` currently pin `python:3.11-slim` by tag, not digest. Originally deferred because the HA add-on Supervisor injects `BUILD_FROM` and the chosen base must work across all supported architectures. With SHA-pinned actions (SC.1) and SBOM attestation (SC.2) shipped, this is the last unpinned link in the supply chain. Investigate whether the Supervisor `BUILD_FROM` override accepts a `@sha256:` digest; if so, pin both the local `ha-addon/build.yaml` `build_from` map and the worker `ha-addon/client/Dockerfile` `FROM` line. Refresh the digest on every release after verifying the new digest's CVE bulletin. PY-4 trigger when the worker image's base digest changes.

## Open Bugs & Tweaks

- [x] **1** *(1.6.0-dev.7)* Doc update. First-steps list in `DOCS.md` now includes an explicit "Restart Home Assistant" step so users know the bundled `esphome_fleet` custom integration only becomes active after HA Core restarts (it's auto-copied to `/config/custom_components/` on add-on boot, but Core loads integrations at startup). Also adds a note about restarting HA again after future add-on upgrades that change the integration.

- [x] **2** *(1.6.0-dev.7)* `DOCS.md` now has a full per-option table for the Supervisor Configuration tab (`token`, `job_timeout`, `ota_timeout`, `worker_offline_threshold`, `device_poll_interval`, `require_ha_auth`) — default + what it does for each. The Settings-drawer section picked up the new `git_author_name`/`git_author_email` row alongside the existing entries.

- [x] **3** *(1.6.0-dev.7)* `panel_icon` switched from `mdi:progress-wrench` to `mdi:chip` — same icon ESPHome Device Builder uses in the HA sidebar, so Fleet and Builder sit next to each other with matching visual vocabulary.

- [x] **4** *(1.6.0-dev.7)* Light-mode header now uses the themed `--surface`/`--text` variables (white bg, dark text) instead of the forced-dark override that existed to keep the old ESPHome wordmark readable. The new house-glyph logo (cyan/blue) reads fine on either background; chips/badges adapt via existing CSS vars. Verified visually via headless Playwright preview.

- [x] **#9** *(1.6.0-dev.12)* — Supervisor log spam ("Option 'X' does not exist in the schema") fixed by calling `POST /addons/self/options` with an empty payload once after the SP.8 migration, guarded by a `/data/.supervisor_options_cleared` marker so it runs at most once per install. Supervisor rewrites its cached options to match the (empty) schema and the warnings stop. Soft-fail on any API error — the warnings are cosmetic, not load-bearing.

- [ ] **8** — **WONTFIX-for-now** — attempted to remove `ha-addon/build.yaml` in dev.12 so Supervisor would stop warning about its deprecated-status, but the build immediately failed: without `build.yaml` Supervisor falls back to its own arch-default base (`ghcr.io/home-assistant/<arch>-base`, Alpine/musl), and our Dockerfile's `apt-get install gcc libffi-dev libssl-dev git` step (needed by PlatformIO's glibc toolchains) doesn't exist on Alpine. Proper fix options: (a) port the Dockerfile to apk + recompile every native dep for musl; (b) push a prebuilt glibc-Python base image to GHCR and point at it via a Supervisor `image:` key. Both are real work; the deprecation WARNING is cosmetic. `build.yaml` restored in dev.13 with an extended comment documenting the incident so we don't try again without a plan.

- [x] **#7** *(1.6.0-dev.12)* — `/api/v1/*` 401 WARNING spam fixed. `auth_middleware` now funnels each refusal through a new `_log_auth_failure()` helper that throttles per `(peer_ip, reason)` pair with a 60-second window: first refusal logs a full WARNING; subsequent refusals within the window increment a suppressed-count; when the window elapses, the next WARNING includes a `(N similar suppressed in last Ms)` tail so operators still see the pattern. Drops hass-4's ~14k/hour bearer-mismatch torrent to roughly one line per minute per distinct peer. Conftest clears the rate-limit state between tests so assertions on WARNING lines aren't suppressed by leftover state from a prior test.

- [x] **5** *(1.6.0-dev.12)* — light-mode button colors tuned up. The `success` and `warn` variants used to hardcode very dark hex values (`#14532d` / `#78350f`) that looked intentional against the dark app bg but jarringly inky in light mode (the Upgrade button read as "nearly black blob" on white). Moved the palette under CSS-var control — `--button-success-bg` / `--button-warn-bg` — with theme-aware values: dark mode keeps the deep colors for continuity, light mode gets saturated green/amber with dark foreground text.

- [x] **#6** *(1.6.0-dev.12)* — CI rename-test fix. `test_file_history_follows_renames` ran a raw `subprocess.run(["git", "commit", ...])` against a tmp repo but never set `user.name`/`user.email`; post-SP.8, `init_repo` no longer writes those into `.git/config` either (production uses `-c` overrides at commit time). Test now passes its own `-c user.name=Test -c user.email=test@example.com` to match that pattern.

- [x] **10** *(1.6.0-dev.12)* — History panel now shows Monaco's `DiffEditor` in proper **side-by-side mode** with YAML syntax highlighting instead of the unified-diff text blob. New server endpoint `GET /ui/api/files/{f}/content-at?hash=<sha>` returns file content at a specific commit (via `git show <hash>:<path>`) or working tree content when hash is omitted. HistoryPanel fetches both sides via two parallel SWR calls and feeds them as `original` / `modified` to the DiffEditor. From/To pickers re-arranged to a 2-col grid directly above the left/right panes so selection-to-pane alignment is unambiguous.

- [x] **11** *(1.6.0-dev.12, resolved by #10)* — The `__WORKING_TREE__` sentinel never escapes to the server anymore because bug #10's side-by-side diff rewrite replaced the single `/diff` call with two parallel `/content-at` calls. The UI converts `WORKING_TREE` to `null` / omits `hash` before hitting the API, so the server never sees the sentinel.

- [x] **12** *(1.6.0-dev.14)* — Side-by-side diff was silently collapsing to inline/unified view. Root cause: Monaco's `DiffEditor` has an `useInlineViewWhenSpaceIsLimited` option that defaults to `true` — when the pane width is below ~900px (the drawer was ~870px), it overrides `renderSideBySide: true` and flips to inline mode. Fix: pass `useInlineViewWhenSpaceIsLimited: false` so side-by-side is honored at any width. Verified via headless Playwright preview: the drawer now shows two Monaco editors (`.editor.original` + `.editor.modified`) with red/green inline change markers and YAML highlighting on both panes.

- [x] **13** *(1.6.0-dev.15)* — CI mypy errors fixed by refactoring `_parse_log_with_numstat` to accumulate per-commit counts into local `added_total` / `removed_total` ints, then assign them to the entry dict in one shot at the end of each commit block. Eliminates the `int(current["lines_added"])` read that tripped the `call-overload` error (since `current` is typed `dict[str, object]`). No `# type: ignore` comments needed. `mypy ha-addon/server/git_versioning.py` clean.

- [x] **14** *(1.6.0-dev.15)* — Diff always shows the older version on the left. HistoryPanel now has a silent normaliser that swaps From ↔ To whenever the user's selection would put the newer commit on the left. Uses the commit list's newest-first ordering plus a "working-tree is newest of all" rule for the `Current` sentinel. The row-click default already put parent→self (older→newer) in the correct slots; this just ensures manual picks via the dropdowns or Set-as-From/To buttons can't violate the invariant.

- [x] **15 (re-opened)** *(1.6.0-dev.17)* — `__WORKING_TREE__` sentinel no longer leaks to `/content-at`. HistoryPanel now funnels both From and To through a `hashForApi()` helper that maps the sentinel / empty string to `null` before the fetch; the server's `content-at` handler short-circuits to the working-tree read. The previous fix only scrubbed the To side (the From-as-working-tree case was still broken because `fromHash || null` treated the sentinel string as truthy). Also dropped the server's "malformed hash" log line from WARNING → DEBUG — a 400 response is operator-actionable; the WARNING was itself a client-side bug indicator.

- [x] **15** *(1.6.0-dev.15)* — Restore confirmation replaced with a proper shadcn `Dialog` — title "Restore {short_hash}?", contextual body copy that differs depending on whether the working tree is clean or dirty (plain revert commit vs "your uncommitted changes will be overwritten"), Cancel / destructive-Restore footer. CLAUDE.md gained a new Design Judgment rule under the shadcn/ui section: **"No native JavaScript modals — ever"** — no `window.alert` / `window.confirm` / `window.prompt`, use shadcn `Dialog` for confirmations, `Sheet` for panels, `sonner` for toasts. Origin reference in the rule so future developers know why.

- [x] **16** *(1.6.0-dev.15)* — Per-device uncommitted-changes indicator + commit action. Combined UX: **yellow "Uncommitted" pill** sits next to the device name on every Devices-tab row that has a dirty YAML, serving as both indicator AND drill-in — clicking it opens the History panel where the user can review the diff and commit from there. Hamburger menu gains a **"Commit changes…"** item that's only rendered when the row is dirty; opens a proper shadcn Dialog with an optional message field (defaults to `save: <file> (manual)` server-side), Cancel / Commit footer. Server side: new `git_versioning.dirty_paths()` helper runs one bulk `git status --porcelain` and returns the set of dirty relpaths; `/ui/api/targets` merges a per-target `has_uncommitted_changes` boolean into every row payload, so the 1 Hz SWR poll keeps the indicator fresh. Type: new `Target.has_uncommitted_changes?: boolean` in `src/types/index.ts`.

- [x] **17** *(1.6.0-dev.17)* — Settings drawer intercepts the auto-commit on → off flip when any targets are dirty. Proper shadcn Dialog offers three outcomes: **Commit N and turn off** (primary; loops `POST /ui/api/files/{f}/commit` per dirty target via `Promise.all`, then flips the toggle), **Turn off anyway** (flips without committing), **Cancel** (leaves the toggle on). The dialog body shows the list of dirty filenames as chips (first 8, plus "…and N more" if the list is longer). Commits use the manual-commit endpoint's default message (`save: <file> (manual)`). Failed commits are reported in a single summary toast. Dirty-target list comes from App.tsx's existing `/ui/api/targets` SWR data — no extra endpoint call.

- [x] **18** *(1.6.0-dev.17)* — Queue tab now has a **Config** column showing the short git hash (`job.config_hash.slice(0, 7)`) of what was compiled, with a tooltip revealing the full 40-char SHA. Dash for pre-AV.7 jobs and jobs enqueued when the config dir wasn't a git repo. Rendered in a muted mono font so it doesn't visually compete with the other sortable columns, but still always visible — this is the key that links Queue rows to the History panel's "Diff since compile" shortcut.

- [x] **19** *(1.6.0-dev.17)* — Fresh-install auto-commit default now depends on whether a git repo was already there. `init_repo()` returns `True` when Fleet initialised a brand-new repo (no repo before), `False` when the directory was already a git repo. `init_settings()` takes an optional `fresh_repo_init` parameter; on first-boot seed it uses that to pick the default: **fresh Fleet-init → `auto_commit_on_save = True`** (the dataclass default, the Pat-no-git case where Fleet owns history), **pre-existing repo → `auto_commit_on_save = False`** (Pat-with-git case where Fleet shouldn't spray `save: <file>` commits into the user's curated log). Existing installs are unaffected — the override only applies when `/data/settings.json` doesn't exist yet. `main.py` reordered so `init_repo` runs before `init_settings` in `create_app`, sync (no event loop yet), and the return value flows into the settings seed.

- [x] **#20** *(1.6.0-dev.20)* — Queue tab's Commit column (the short hash) is now a button. Click opens the History panel (via `App.tsx`'s existing `historyTarget` + a new `historyFromHash` state) pre-set to `From = job.config_hash, To = Current`, matching the "Diff since compile" shortcut in the log modal. Same entry point two different places, so the user doesn't have to dig into the log modal just to see what's changed since a given compile.

- [x] **#21** *(1.6.0-dev.20)* — Settings drawer's dirty-files interception now fires on auto-commit **off → on** (prior #17 had it backwards: on → off). The new direction is the one that actually matters — turning auto-commit ON means all future saves commit, but existing uncommitted work needs an explicit decision. Dialog copy: "Turn on auto-commit?" with primary action "Commit N and turn on", secondary "Turn on anyway", tertiary Cancel.

- [x] **#22** *(1.6.0-dev.20)* — History drawer commit rows now show the absolute date/time stacked below the "3h ago" relative time (smaller, dimmed) so the user doesn't have to hover to see when a commit actually landed. ISO datetime stays on the tooltip for full precision.

- [x] **#23** *(1.6.0-dev.20)* — Clicking the earliest (parentless) commit in the history list now diffs it against itself — From = To = that commit — which renders as "no differences". Prior behavior set From to `undefined` and the diff editor fell back to `Current`, showing the user a stale "everything changed since the first commit" diff. The row-click handler now checks `entries[idx+1]?.hash` and uses the commit's own hash on both sides when no parent exists.

- [x] **#24** *(1.6.0-dev.20)* — Save in the editor now prompts for an optional commit message when auto-commit is on. New shadcn Dialog: placeholder shows the default subject (`save: <file>`), Enter to confirm, Escape to cancel. Message flows through a new optional `commit_message` field on `POST /ui/api/targets/{f}/content`; server passes it to `commit_file()` which uses it as the commit subject when non-blank, else falls back to the auto-generated form. Fleet-initiated writes (pin / schedule / meta / rename / delete) still auto-generate — they pass no message.

- [x] **#25** *(1.6.0-dev.20)* — Editor gains a **Save & Commit** button when auto-commit is off. Opens the same message dialog as #24; on confirm, saves and then calls the existing `/ui/api/files/{f}/commit` endpoint with the message. Success toast shows the short hash; a no-op commit (file matched HEAD) toasts as "Saved (no changes to commit)". Hidden when auto-commit is on — the plain Save button already handles the commit via #24.

- [x] **#26** *(1.6.0-dev.20)* — Queue tab column header renamed from "Config" to "Commit". The cell already renders a commit hash; "Config" was ambiguous alongside the HA-action / schedule columns.

- [x] **#27** *(1.6.0-dev.20)* — Jobs enqueued via the Home Assistant service action (`esphome_fleet.compile`) now render in the Queue's **Triggered** column with a distinct house icon + "HA action" label, distinct from user-triggered and scheduler-triggered runs. New `Job.ha_action: bool` field set server-side in `/ui/api/compile` when the caller's `ha_user.name == "esphome_fleet_integration"` — round-trips through `to_dict` / `from_dict` so the flag survives queue persistence. UI accessor updated so the Triggered column's sort grouping treats HA-action / scheduled / user as distinct buckets.

 - [ ] 28 -  on hass-4, there's always a particular commit that shows up as the oldest one in the git history, even when I create a new file. I think there's a bug related to that. That's probably the last commit that was done to the repo before we "imported" it into our version management mechanism. The commit is 28e0c8b. 

 - [ ] 29 - an audit of the user interface to clarify terminology. Since now we are talking about two versions, the ESPHome version and the version of the config, let's make sure we're explicit, ideally without being too cluttery in the user interface. One place I'm noticing this is the hamburger menu. 

 - [x] **30 (partial — fix direction #1 landed)** *(1.6.0-dev.19)* — Standalone-Docker installs no longer stall on the "Installing ESPHome…" banner. Three changes in `ha-addon/server/main.py`:
   1. `_pick_latest_stable_version(versions)` helper — picks the newest entry matching `^\d+(\.\d+)*$` from PyPI's newest-first list, skipping betas/rcs/devs.
   2. Startup no longer writes the `"installing"` / `"unknown"` sentinels into `_selected_esphome_version`. Prevents the string `"installing"` from leaking into UI payloads and compile-job stamps. Real versions are still cached as before.
   3. `_install_esphome_background()` falls back to the PyPI picker when `_get_installed_esphome_version()` returns a sentinel AND there is no `SUPERVISOR_TOKEN`. HA add-on installs are unchanged — the Supervisor branch returns immediately so `pypi_version_refresher` resolves the HA ESPHome add-on's version as before.

   Follow-up work still open: fix direction #2 (UI selector when both Supervisor and PyPI fail — the banner still reads "Installing…" in that degenerate case) and #3 (teach `set_esphome_version` itself to reject sentinels rather than relying on every caller to filter). Tests: 4 new unit tests in `tests/test_main.py` covering the picker (latest-stable, empty list, all pre-release, short `X.Y` versions). Original bug trace retained below for #2/#3 context.

   **Symptom.** Fresh install of the server container outside Home Assistant (plain `docker run`, no Supervisor) used to render the "Installing ESPHome…" banner indefinitely. No error, no progress — the UI just sat there.


   **Root cause trace.**
   1. `scanner._get_installed_esphome_version()` runs at startup. After SE.1 the server image no longer bundles `esphome`, so `importlib.metadata.version("esphome")` raises; the `_esphome_ready` event is not yet set; the function falls into the final branch and returns the literal string `"installing"` (`scanner.py:237-240`).
   2. `main.py:1207-1208` passes that sentinel straight into `set_esphome_version("installing")`, and the subsequent `_install_esphome_background` short-circuits because `target in ("unknown", "installing")` (`main.py:1220-1222`). So nothing is ever installed.
   3. The 30-second `pypi_version_refresher` loop calls `_fetch_ha_esphome_version`, which checks `os.environ["SUPERVISOR_TOKEN"]` and returns `None` immediately when it's absent (`main.py:836-839`). There is no `SUPERVISOR_TOKEN` under plain Docker — it's only injected by the HA Supervisor when running as an add-on.
   4. `_esphome_ready` never fires and `_esphome_install_failed` never flips, so the `/ui/api/server-info` handler (`ui_api.py:424-430`) keeps returning `esphome_install_status = "installing"` forever. The banner is tautologically correct: "installing" = "we don't know what to install".

   **What the user asked — "does it need to install ESPHome? I already have it running in another container."** Yes, currently it does: the server uses its own in-process ESPHome venv to resolve `!secret`, list components, and validate configs. That other container is their compile worker — different concern, different install. Worth calling out in the eventual docs update, but the stuck-forever banner is the blocker.

   **Fix direction (preliminary — needs a proper scope decision).** Three complementary pieces:
   1. **Detect the "no Supervisor + no bundled ESPHome" case at startup** and switch to a PyPI-derived default: fetch the latest stable version from PyPI (`_fetch_pypi_versions` already exists), seed it into `set_esphome_version`, and let `_install_esphome_background` run normally. The existing `pypi_version_refresher` loop can continue to take over once Supervisor is available (no behavior change for HA add-on users).
   2. **Surface a selector when auto-detect fails.** If the PyPI fetch itself fails (offline, rate-limited, etc.) — switch the banner from "Installing…" to "Select an ESPHome version to install" with the existing `/ui/api/esphome-versions` dropdown. Gives the user an explicit action instead of a spinner. Matches the "disable, don't fail" Design Judgment rule.
   3. **Never pass the sentinel string as a version.** `main.py:1208` calling `set_esphome_version("installing")` pollutes the selected-version state and leaks into UI payloads (Queue rows, server-info, compile job records). The sentinel should be treated as "no version selected yet" everywhere — either gate the `set_esphome_version` call on the returned value not being `"installing"`/`"unknown"`, or teach `set_esphome_version` itself to reject sentinels.

   **Not in scope for this bug:** making the server share the user's *other* ESPHome container's install. That's a much bigger architectural change (requires RPC / shared volume / shelling out across containers) and the PyPI fallback solves the symptom cheaply.

   **Repro + confirmation steps (for whoever implements):** run `docker run --rm -p 8765:8765 ghcr.io/weirded/esphome-dist-server:latest` with no mounted `/data`, confirm the banner sticks; add temporary `logger.info` around `_get_installed_esphome_version`'s final branch and observe `"installing"` returned; then verify the fix path by setting a dummy `ESPHOME_DEFAULT_VERSION` or by checking the PyPI-fetch-and-install path completes and `_esphome_ready` fires.