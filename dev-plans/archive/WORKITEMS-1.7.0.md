# Work Items — 1.7.0

Theme: **heffneil-inspired device-management polish + fleet tags & routing.** Adds three pieces of per-device UX inspired by `heffneil/esphome-enhanced-dashboard` (`dev-plans/EXTERNAL_REVIEW_heffneil_enhanced_dashboard.md` — mark-inactive to skip polling on known-offline devices, ping diagnostic, install-to-specific-address for recovery flows), ships user-managed tags on devices + workers plus a global K8s-flavored rule engine that decides which workers can claim which jobs, and continues the ESPHome-delegation cleanup. Renamed from 1.6.3 to 1.7.0 because the scope crosses the patch/minor line — these are user-visible features, not hardening.

The Gold-tier-flip work that 1.6.2 pivoted away from originally rode along here too. In practice the tags + routing piece consumed the bandwidth, so the tier-flip plus its dependent QS / HT / CI / SD items moved to **`dev-plans/WORKITEMS-1.7.1.md`** mid-cycle. The four QS / HT / CI items that landed before the slide (HT.5, HT.6, CI.3, CI.5) plus QS.G10's WONTFIX disposition stay below as the historical record of what shipped under 1.7.0 dev tags.

Read first, in order: `dev-plans/EXTERNAL_REVIEW_heffneil_enhanced_dashboard.md` (the source of the four DM.* items below), `dev-plans/archive/WORKITEMS-1.6.2.md` (so you can see what the pivot landed before this release picked up).

Scope rule: every workitem either (a) delegates more hand-rolled code to ESPHome, (b) polishes UX debt, or (c) ships user-visible fleet-management capability (DM.*, RC.1, TG.*). Quality-scale rule lifts and TEST-AUDIT blind-spot closures live in 1.7.1.

---

## EH — ESPHome-delegation (carryover record)

Audit on 2026-04-22 of `ha-addon/server/` + `ha-addon/client/` looking for places we re-implement functionality ESPHome ships as a library (`esphome.*`) or a CLI (`esphome <subcommand>`). EH.2 landed in 1.6.2-dev.11 along the #84 fix (ESPHome's full validator became the primary resolver). The three remaining items (EH.1 firmware-artifact discovery via `esphome idedata`, EH.3 magic-string → `esphome.const` sweep, EH.4 version-detection subprocess removal) moved to `dev-plans/WORKITEMS-1.7.1.md` — 1.7.0's bandwidth went to tags + routing and the heffneil-inspired DM items, and the EH cleanup is delegation polish that doesn't move the needle for end users in 1.7.0's theme.

---

## QS — Quality Scale (carryover record)

Open QS items moved to `dev-plans/WORKITEMS-1.7.1.md`. Closed-in-1.7.0 entry retained for traceability:

- [x] ~~**QS.G10**~~ WONTFIX *(attempted 1.7.0-dev.27, reverted dev.28)* — the workitem's premise (set `"homeassistant": "2024.11.0"` in `manifest.json`) was wrong: that key is only allowed in **core** integration manifests. Custom-integration manifests reject it ("Invalid manifest: extra keys not allowed @ data['homeassistant']" — failed Hassfest in dev.27). There's no manifest-level field to declare a minimum HA version for a custom integration; the constraint is documented in DOCS.md and enforced at runtime via API-shape checks. The "document the minimum in DOCS.md" half folds into QS.G3 in 1.7.1's known-limitations sweep.

---

## HT — Honest testing (carryover record)

Open HT items moved to `dev-plans/WORKITEMS-1.7.1.md`. Closed-in-1.7.0 entries retained for traceability:

- [x] **HT.5** *(1.7.0-dev.27)* — `tests/test_mdns_advertiser.py` landed with 7 tests. Covers: happy-path register/unregister against a mocked `AsyncZeroconf` (asserts ServiceInfo carries the right `addresses` / `port` / `properties` / `server`); `_primary_ipv4()` returning None → register skipped, `stop()` remains a no-op (PR #80's fix); `socket.gethostname()` returning `'localhost'` → `server=None` (PR #80's fallback); `stop()` before `start()` is a safe no-op; `start()` twice — pinned as "non-idempotent, two register calls" so a future caller-side bug surfaces here rather than silently double-advertising; register-time exception → `_info` clears so `stop()` stays safe; unregister-time exception is swallowed. All 7 pass locally.

- [x] **HT.6** *(1.7.0-dev.29)* — `ha-addon/ui/e2e/connect-worker-modal.spec.ts` landed with 4 specs. Renders the modal, walks each format tab (bash / powershell / compose) via a `commandFor(page, format)` helper, then asserts the load-bearing shape per branch: bash + powershell carry `--network host`, `-e SERVER_URL=…`, and `-v esphome-versions:/esphome-versions`; compose carries `network_mode: host`, the `SERVER_URL=` env line, and the `esphome-versions:/esphome-versions` named-volume mount with no `docker run` prefix. A fourth spec asserts the live SERVER_URL value (`http://localhost:8765` from the mocked `serverInfo` fixture) appears verbatim across every format. All 4 pass locally.

---

## CI — Automate the catches (carryover record)

Open CI items (CI.4 hassfest quality-scale gate, CI.6 coverage ratchet) moved to `dev-plans/WORKITEMS-1.7.1.md`. CI.1 (Dockerfile buildx smoke) and CI.2 (AppArmor profile syntax + load smoke) stayed in `WORKITEMS-1.6.2.md` as open items. Closed-in-1.7.0 entries retained for traceability:

- [x] **CI.3** *(1.7.0-dev.29)* — `compile-test.yml` now matrices on `esphome_version: ["2026.4.0", "2026.4.2"]` for both `compile-client` (16 targets) and `compile-server` (4 targets). The pinned floor (`2026.4.0` — same as `MIN_ESPHOME_VERSION` in `constants.py`, kept in sync via a workflow-level comment) anchors reproducibility; the latest-stable axis catches upstream API regressions of the kind from 1.5 ("2026.4.0 reshaped the API"). PlatformIO cache key now keys on `${{ matrix.esphome_version }}` so the two axes don't poison each other's cache, and the per-step name surfaces the matrix combo (`Compile esp32-idf on ESPHome 2026.4.2`) so a red square in the GitHub UI tells the user which axis broke at a glance. The hardcoded `env.ESPHOME_VERSION` is gone — bumping the latest-stable axis is now a one-line edit to the matrix.

- [x] **CI.5** *(1.7.0-dev.27)* — PY-10b landed. `scripts/check-invariants.sh` now counts `@pytest.mark.skip` decorators vs `def test_` / `async def test_` declarations across every non-`_logic` `tests/test_integration_*.py` file; fails CI if the ratio crosses 50 %. Current state: 0 skips across 11 honest tests in `test_integration_reauth_flow.py` + `test_integration_reconfigure_flow.py` + `test_integration_setup.py` → ratio is 0 %. Documented in `CLAUDE.md` § Enforced Invariants alongside PY-10. Total invariant count went 16 → 17.

---

## UD — UX debt carried from 1.6.1 review (minor polish)

Three items surfaced in `dev-plans/UX_REVIEW-1.6.1.md` §5 as "defer to 1.7." They're small; folding them into 1.7.0 prevents "defer" from becoming "forgotten." Two additional Devices-tab columns join them.

- [x] **UD.1** *(1.7.0-dev.27)* — Per-source explanatory tooltip on the address-source label under the Devices-tab IP cell. New `formatAddressSourceTooltip` helper mirrored across `useDeviceColumns.tsx` (managed-row IP) and `DevicesTab.tsx` (unmanaged-row IP), returning a sentence per source ("Detected via ARP scan of the local network — mDNS came up empty…", "From wifi.use_address in the device YAML — overrides hostname-based discovery", etc.). Replaces the old `Address source: <raw_enum>` title that surfaced the API string. The `ha_configured` suffix on the unmanaged row picks up the new sentence too.

- [x] **UD.2** *(1.7.0-dev.48)* — Worker-selection column now renders a short label below the Tailwind `xl:` breakpoint (1280 px) and the long label at ≥1280 px. `formatSelectionReason` (`utils/selectionReason.ts`) gains a `shortLabel` field per case (`Pinned to worker → Pinned`, `Only worker online → Only online`, `Only eligible by tag → Only eligible`, `Least busy worker → Least busy`, `Fastest worker available → Fastest`); the cell wraps both spans in `hidden xl:inline` / `xl:hidden` so the visible form swaps purely via CSS — no JS resize listener, no React state. Same change applied to the matching column in `QueueHistoryDialog` so the two surfaces don't drift. Tooltip retains the long context regardless of viewport. Tests: 4 specs in `ha-addon/ui/e2e/worker-selection-responsive.spec.ts` cover both viewports, the Pinned variant, and the always-on tooltip — protect against future regressions where someone reverts to a single label or moves the breakpoint. Origin: UX_REVIEW-1.6.1 UX.2 — long form ("Fastest worker available") overflowed the cell on a standard 13" laptop.

- [x] **UD.3** *(1.7.0-dev.27)* — Reconfigure form's submit button now reads "Save changes" via the per-step `"submit"` translation key (added to `strings.json` + `translations/en.json`'s `reconfigure_confirm` block) instead of HA's stock "Submit". Per the UX_REVIEW-1.6.1 §4.4 disposition. Other steps (`user`, `discovery_confirm`, `reauth_confirm`) keep HA's default for now — QS.G5's translation pass will sweep them.

- [x] **UD.4** SUBSUMED-BY-#23 *(1.7.0-dev.18 / closed in dev.27)* — Bluetooth-proxy column landed earlier as part of bug #23's "two new optional Devices-tab columns" patch. The `BLE proxy` column at `useDeviceColumns.tsx:452` renders `Active` (success-green) / `Passive` / `—` (off) with the same column-picker toggle the workitem specced. Sourced from `Target.bluetooth_proxy` (`'off' | 'passive' | 'active'`) populated by `scanner._extract_metadata`'s `bluetooth_proxy:` block detection. The `EH.3` pairing note (use `CONF_BLUETOOTH_PROXY` once the magic-string sweep lands) folds into EH.3's text — no separate work here.

- [x] **UD.5** *(1.7.0-dev.49)* — Existing #23 "ESP" column on the Devices tab is now a stacked two-line cell renamed "Platform": chip family on the primary line (`ESP32`, `ESP32-S3`, `ESP8266`, `RP2040`, `Host`) and PlatformIO board on a smaller muted secondary line (`esp32dev`, `esp32-s3-devkitm-1`, `d1_mini`, `rpipico`, …). Server side: `scanner._extract_metadata` reads `board` from inside `esp32:` / `esp8266:` / `rp2040:` blocks (Host has no board concept); `board` field added to the metadata dict default + `/ui/api/targets` payload (both managed + archived rows). Defaults to `None` when the YAML failed to resolve or doesn't pick a platform. Sort key remains the bare chip-family string so ESP32 / ESP32-S3 / ESP8266 group naturally; board comes along within each chip family. Tooltip text widened to "Chip family + PlatformIO board from the resolved YAML: <chip> on <board>" when both are present. Tests: 6 new in `tests/test_scanner.py` cover board extraction from each chip block, the ESP32-S3 variant + board pair, the no-board fallback, and the Host-no-board case. `ha-addon/ui/e2e/platform-column.spec.ts` (4 specs) covers the renamed header, the two-line render for ESP32-S3 + ESP8266 rows, and the muted em-dash fallback when neither field is set. The future "read from `storage.json` post-compile" path is deferred — the YAML round-trip already covers the practical case (bug #23's resolution path runs ESPHome's full validator, so the values are authoritative); a `storage.json` source-of-truth would only matter for diagnosing build-time discrepancies, which can be filed separately if it ever bites.

---

## DM — Device management (heffneil-inspired)

Three items lifted from `dev-plans/EXTERNAL_REVIEW_heffneil_enhanced_dashboard.md` after auditing `heffneil/esphome-enhanced-dashboard` for features we don't have. Each is scoped tight enough to land alongside the Honest-Gold work without crowding it. None of them depend on the side-panel UX rework heffneil uses to surface them — we keep our existing Devices-tab + hamburger affordance and add these as new entries / dialogs. Side-panel is filed for later (`WORKITEMS-1.8.md` or `1.9.md`) since it's a much bigger UX shift.

(A fourth item — sidecar-JSON tag storage — was scoped here originally and dropped after grilling: tags already live cleanly in the `# esphome-fleet:` YAML comment block via `read_device_meta` / `write_device_meta` and are parsed with `yaml.safe_load`, not regex. The "no YAML churn" benefit only paid off if pin/schedule moved with them, which is a much bigger migration than the heffneil scope justifies. Heffneil's tag-UX polish — filter pills + editor tag-pool — remains a candidate in the heffneil review's suggestion table, not a committed item; the single-dict union it relied on works fine over the existing in-memory device snapshot, no storage migration needed.)

- [x] **DM.1 Archived devices visible in-tab.** *(1.7.0-dev.44)* — Replace the current "archived devices live behind a separate `ArchivedDevicesList` page" UX with a single in-tab toggle. Devices-tab column-picker dropdown gets a new `Show archived devices` entry at the bottom (separator above), persisted to localStorage as `devices-show-archived`; when on, archived rows render in the same table at `opacity-50`, sorted into a second group below active rows by `archived_at` desc. Action menu on archived rows: only `Unarchive` + `Permanently delete` (two-step `<Dialog>` confirm matching the existing `ArchivedDevicesList` flow); all other actions hidden. Backend: `/ui/api/devices` always returns archived rows with `archived: boolean` set on every row; new `scanner.scan_archived(config_dir)` helper reads `/config/esphome/.archive/` and the API endpoint merges active + archived. The poller / scheduler / routing engine / queue continue to see only active targets — archived is display-only. `delete_target`'s archive path (`ui_api.py:2585-2615`) explicitly evicts the device from `device_poller._devices` after the `git mv` (mirrors the eviction at `rename_target` `ui_api.py:2776-2779`) so an archived-while-online device freezes immediately at last_seen=now rather than staying "online" for 4h until TTL prunes it. Retire the standalone `ArchivedDevicesList.tsx` component, its Settings-drawer surface (`SettingsDrawer.tsx:314` — replace inline with a one-line pointer to "Devices tab → column picker → Show archived"), and the Devices-tab toolbar archive entry-point (#62). Existing `/ui/api/archive*` endpoints (GET list, restore, delete-from-archive) stay as backing handlers for one release as a soft-deprecate, then retire. Subsumes the originally-spec'd `Mark Inactive` flag (no new YAML field, no sidecar JSON) — Archive is the inactive state, since the only meaningful difference between them was "where the file lives," which is an implementation detail rather than a user feature; the trade-off accepted here is that compiling an archived device requires Unarchive → Compile → re-Archive (3 clicks for a rare action) rather than working in place. Tests: `tests/test_scanner.py` round-trip for `scan_archived`; `/ui/api/devices` shape test asserting the merged `archived` field on every row; poller eviction test asserting `_devices` no longer contains the device after the archive POST returns; `ha-addon/ui/e2e/archived-toggle.spec.ts` covering the column-picker entry, archived-row opacity + reduced action menu, Unarchive flipping back to active, and Permanently-delete closing the row entirely.
- [x] **DM.2** *(1.7.0-dev.50)* — ICMP ping diagnostic. New `POST /ui/api/targets/{filename}/ping` endpoint in `ha-addon/server/ui_api.py` runs `icmplib.async_ping(host, count=10, interval=0.2, timeout=2, privileged=False)` against `device_poller.resolve_ota_address(dev.name)` — same helper the OTA path + #18 fix use, so the ping target matches what an OTA would hit. Returns a flat dict with `is_alive`, `packets_sent`, `packets_received`, `packet_loss`, `min_rtt`, `avg_rtt`, `max_rtt`, `jitter`, plus `target`, `address`, `ran_at`. 404 on unknown target or no resolved address; 503 when the poller isn't attached; 500 on icmplib failure. URL convention follows the existing `/ui/api/targets/{filename}/...` pattern (the bug spec's `/ui/api/devices/{filename}/ping` would have introduced a new top-level path; consistency wins). **No new dependencies, no image bump, no capability changes** — `icmplib>=3.0` is already pinned for the device_poller's existing `_ping_device` fallback, `privileged=False` runs as unprivileged ICMP via `net.ipv4.ping_group_range` which both the HA add-on and standalone Docker containers inherit. UI: new `PingDeviceModal` (shadcn `Dialog`) wired into the Devices-tab hamburger via the Device group ("Ping device…" between `Copy API Key` and the separator). Plumbed through `useDeviceColumns` → `ActionsCell` → `DevicesTab` → `App.tsx` (state owned in `App` so the modal is hoisted out of the row cell — same pattern as the rendered-config modal). Modal shows a status dot + "Reachable" / "No response" header, address, then a compact result table; footer carries `Copy as text` (plain-text block for paste-into-bug-report), `Run again` (re-fires the POST; spinner during in-flight), and `Close`. Worst-case wall time for an unreachable host is `(count-1)*interval + timeout` ≈ 3.8 s — batch UX, no streaming. Hidden behind the archived-row early return (the device isn't pollable when the YAML lives in `.archive/`). Tests: 5 in `tests/test_ui_api.py` cover the 503 (no poller), 404 (unknown target), 404 (no resolved address), happy-path stats round-trip with a mocked `icmplib.async_ping`, and the 500 path when icmplib raises; 3 mocked Playwright specs in `ha-addon/ui/e2e/ping-device.spec.ts` cover the menu entry, the modal renders + result table, and Run-again re-fires the request. e2e-hass-4 smoke deferred — the unit tests cover the wire shape and the prod-smoke suite already exercises icmplib via the poller's liveness fallback path. Heffneil ref: `web_server.py:1052-1163` (handler shape), `template:1460-1510` (modal shape).
- [x] **DM.3** *(1.7.0-dev.51)* — Install-to-specific-address. New `InstallToAddressModal` (shadcn `Dialog`) wired into the Devices-tab hamburger Device group, immediately after `Ping device…`. Input pre-fills with the device's `ip_address` from the cached Targets payload (the value the user already sees in the Devices-tab IP column — same source `device_poller.resolve_ota_address` reads from); subtext reads `Auto-resolved: <addr> — edit to override` or "No auto-resolved address — enter the device's IP or hostname" when the pre-fill is empty. Client-side validation: `^[a-zA-Z0-9.\-_:]+$`, non-empty trim, length ≤ 253; the Install button stays disabled with an inline error until valid. Editing the pre-filled value flips the button into a two-step confirm (warning panel cites the override target / address pair); leaving it unchanged is single-step. Backend: `/ui/api/compile` accepts an optional `address` body field (`ha-addon/server/ui_api.py:start_compile`); when present, must accompany a single-element `targets` array (multi-target + address → 400 `address override requires exactly one target`); strings > 253 chars → 400 `address too long`; whitespace-only → silent fall-through to the auto-resolved value (so an accidental space in the input doesn't 400 out the form). Override stamps `ota_addresses[selected[0]]` so it flows through the same `queue.enqueue(..., ota_address=...)` plumbing the rename auto-recompile (#65) already uses. **No protocol/worker changes** — `Job.ota_address` + worker's `--device <addr>` invocation already exist; the bug spec's "new protocol field" was overstated. Pin-version / routing-rule / worker-pinning behaviour unchanged from the regular Compile path. Plumbing: `onInstallToAddress` threads through `useDeviceColumns` → `ActionsCell` → `DevicesTab` → `App.tsx` (state owned in App.tsx so SWR polls don't slam the modal — same pattern as DM.2). Hidden on archived rows (the early-return menu shape doesn't render the Device group). Tests: 4 in `tests/test_ui_api.py` cover the address override stamping `Job.ota_address`, the multi-target 400, the length-bound 400, and the whitespace-falls-through case; 4 mocked Playwright specs in `ha-addon/ui/e2e/install-to-address.spec.ts` cover the menu entry, the pre-fill + single-step Install POST body, the edit + two-step confirm flow, and the invalid-address disabled-button + inline error. e2e-hass-4 smoke deferred — the protocol path is exercised by every regular compile in the prod-smoke suite. Heffneil ref: `template:1360-1383`.

---

## RC — Rendered configuration view

- [x] **RC.1** *(1.7.0-dev.36)* — read-only "View rendered config" modal landed. Backend: new `GET /ui/api/targets/{filename}/rendered-config` runs `<venv>/bin/esphome config <abs-path>` via the same lazy-installed binary discovery + pinned-version `VersionManager` path the `/ui/api/validate` endpoint uses, captures stdout, returns `{success, output, cached, skipped?}`. `secrets.yaml` short-circuits to a friendly "no device config to render here" message (same shape `/validate` uses) so the menu item doesn't error out on an opt-in click. Cache: module-level `OrderedDict` keyed by `(filename, file mtime ns, secrets.yaml mtime ns)` — any save to either file flips the key automatically, so reopening the modal during one edit session is subprocess-free, and a `!secret` swap re-renders even when the device YAML is byte-identical. Successful AND failed renders both cache (a known-bad YAML doesn't burn a fresh subprocess on every retry); FIFO eviction at 32 entries. The endpoint never logs the rendered body — only the byte count — because the output carries plaintext `!secret` values; a regression test (`test_rendered_config_logs_do_not_leak_output`) pins that invariant. UI: new `RenderedConfigModal` (shadcn `Dialog`) shows the YAML in a read-only Monaco editor (`readOnly` + `domReadOnly`, line numbers, no minimap, `wordWrap: 'on'`, `fontSize: 12`); header carries the always-on "This view contains the values of any `!secret` references — copy with care" notice; footer has Copy + Download .yaml + Close buttons (Copy/Download disable on the failure path so the user doesn't accidentally save an error blob as a YAML). Failure path mirrors the validate panel: red banner with `esphome config reported an error` + the raw stdout text in a monospace block. Entry points: per-row hamburger menu item between `Config history…` and `Commit changes…` on the Devices tab (`onViewRenderedConfig` plumbed through `useDeviceColumns` → `ActionsCell` → `DeviceContextMenu`), plus a `View rendered` button in the EditorModal toolbar between `History` and `Validate` (hidden for `secrets.yaml` and the `.pending.*` create-stub files). Tests: `tests/test_rendered_config.py` (9 tests) covers the success + validator-error paths, the 404 for unknown targets, the secrets.yaml short-circuit (no subprocess), cache hit, mtime-bust on the device YAML, mtime-bust on `secrets.yaml`, failure-path caching, and the no-leak logging guarantee. `ha-addon/ui/e2e/rendered-config.spec.ts` (4 tests) covers the hamburger entry being present, the success render (asserts the resolved YAML appears in the Monaco editor + the secret-warning header is shown), the failure shape (red panel, no editor, Copy/Download disabled), and the Copy-to-clipboard round-trip (Chromium-only since the clipboard permission is browser-specific). `e2e-hass-4/rendered-config.spec.ts` smoke against `cyd-office-info` deferred for now — the mocked Playwright spec covers the UI contract, and a real `esphome config` invocation is on the smoke path implicitly via `/ui/api/validate`. Filing the e2e-hass-4 spec as a follow-up if the production smoke surface needs explicit coverage.

---

## TG — Tags & rule-based job routing

User-managed device tags + worker tags + a global rule engine that decides which workers can claim which jobs. Required-only this release; the rule schema reserves `severity`/`weight` so preferred-with-weight can land later without migration. Design grilled end-to-end on 2026-04-26; the choices below were settled there.

Storage at a glance:
- **Device tags:** existing YAML comment block (`# esphome-fleet:` → `tags: "office,production"`). Unchanged.
- **Per-device additive rules:** new optional `routing_extra: [...]` in the same comment block (same shape as global rules minus `id`).
- **Worker tags:** new `/data/worker-tags.json`, keyed by worker identity (hostname → fallback `worker_id`). Server-side wins after first registration.
- **Global rules:** new `/data/routing-rules.json`, shape `{"version": 1, "rules": [{id, name, severity: "required", device_match: [{op, tags}], worker_match: [{op, tags}]}]}`.

Rule shape: tags are plain strings (no key=value structure; colon-as-convention only — `os:windows` is one string, the system never parses the colon). Operators per clause: `all_of`, `any_of`, `none_of`. Clauses ANDed within each side; sides ANDed within a rule; rules ANDed across the global+per-device list (any single failing rule disqualifies the worker). `effective_rules = global_rules + device.routing_extra` — strictly additive, K8s-honest. To exempt a device from a rule, change the global rule or remove the device's tag; there is no per-device exemption mechanism.

Severity: only `required` accepted by the API in 1.7.0 (`preferred` rejected with 400). Required rules that eliminate every online worker move the job to a new `BLOCKED` state (distinct from `PENDING`) with the rule name surfaced. `BLOCKED` flips back to `PENDING` automatically when the worker pool changes (worker register/deregister/heartbeat) or rules change.

### Backend

- [x] **TG.1** *(1.7.0-dev.2)* — **Worker tag persistence + env-var seed.** Landed: `ha-addon/server/worker_tags.py` (JSON store at `/data/worker-tags.json`, schema `{"version": 1, "tags": {<identity>: [...]}}`), additive `tags`/`overwrite_tags` fields on `RegisterRequest` (PY-6: server + client `protocol.py` byte-identical), `Worker.tags` on the registry record + `set_tags` method, server-side-wins-after-first-registration with `WORKER_TAGS_OVERWRITE` clobber, worker reads `WORKER_TAGS=foo,bar` and `WORKER_TAGS_OVERWRITE=1` from env. Tests: `tests/test_worker_tags.py` (16) for store semantics — first-time seed, persistence across instances, server-side-wins, overwrite clobbers, normalisation (trim/dropempty/dedupe), corrupt-file recovery. `tests/test_registry.py` (+7) for in-memory plumbing. `tests/test_api.py` (+3, `_enable_socket`) for end-to-end seed/win/overwrite via `/api/v1/workers/register`. Identity = hostname; falls back to `client_id` when hostname is empty (the rare-collision case is filed for follow-up — primary key by hostname carries 99% of fleets).

- [x] **TG.2** *(1.7.0-dev.11)* — routing-rule storage + evaluator landed in `ha-addon/server/routing.py`. `Clause` (op + tags) and `Rule` (id, name, severity, device_match, worker_match) dataclasses; pure functions `evaluate_clause` / `evaluate_rule` / `is_eligible`; `RoutingRuleStore` for `/data/routing-rules.json` CRUD with strict validation (severity must be `"required"` in 1.7.0; empty clause tags + unknown operators rejected at create / update / load time; corrupt or unknown-schema files load empty rather than poisoning the eligibility hot path). Conditional rule semantics: a rule fires *when* the device matches `device_match`; if it fires, the worker must match `worker_match` — a non-applicable rule never disqualifies a worker. `is_eligible` ANDs every rule. 27 tests in `tests/test_routing_rules.py` cover each operator (`all_of` / `any_of` / `none_of`), compound clauses ANDed within a side, conditional pass/fail across both sides, multi-rule AND, store CRUD + persistence, validation rejection paths, and additive composition with `routing_extra` (the per-device rule list, plumbed in via TG.4 and TG.5's YAML-comment round-trip). `main.py` now constructs the store at app startup; the API endpoints (TG.4) and the scheduler integration (TG.3) consume it.

- [x] **TG.3** *(1.7.0-dev.14 → dev.17)* — BLOCKED job state + routing-rule wiring fully landed. Data model + `claim_next` skip + `JobQueue.re_evaluate_routing` shipped in dev.14. Wiring landed in dev.17 via the new `routing_eligibility` module: `re_evaluate_routing(app)` builds the eligibility callback (online workers + their tags + per-device `routing_extra` from `read_device_meta`) and calls into the queue's sweep, broadcasts `queue_changed` on any state change so the UI updates without waiting for the 1Hz SWR poll. `fire_and_forget(app)` is the sync-callable shim used at every trigger site: worker register/deregister (`api.py`), worker tag edit (`ui_api.py /workers/{id}/tags`), rule create/update/delete (the three `/ui/api/routing-rules` endpoints), and every `queue.enqueue` site (`/ui/api/compile`, the rename auto-recompile, and both APScheduler callbacks in `scheduler.py`). A defensive 30-s `routing_watchdog` task starts in `on_startup` as a backstop for missed triggers (e.g. workers going offline without deregistering — there's no event for that, only the heartbeat-threshold check). Cheap-path optimisation: `fire_and_forget` skips the spawn entirely when the rule store is empty AND the queue has no BLOCKED jobs / stale `blocked_reason` — the rule-free common case pays nothing. `routing.find_blocking_rule` surfaces the *first applicable rule* that no online worker satisfies as the `blocked_reason` shown in the Queue tooltip; `_summarize_clauses` produces the `"all of [kitchen] AND none of [dev]"` text that fills `summary`. Tests: 7 new in `tests/test_routing_eligibility.py` (no-rules no-op, terminal-skip, PENDING→BLOCKED on missing eligible worker, BLOCKED→PENDING on eligible worker registration, idempotent on steady state, per-device `routing_extra` honoured, offline workers ignored), plus 4 added in `tests/test_routing_rules.py` (`_summarize_clauses` per op, `find_blocking_rule` happy path / first-applicable surfacing / non-applicable rules skipped / no-rules eligible).
    - *Partial in 1.7.0-dev.14 — data model + queue plumbing:* `JobState.BLOCKED` enum value lands; `Job.blocked_reason: Optional[dict]` field; `to_dict` / `from_dict` round-trip the new field (tolerant load — old queue.json files have no `blocked_reason` and load as `None`). `claim_next` explicitly skips BLOCKED jobs (only PENDING is claimable). New `JobQueue.re_evaluate_routing(check_eligibility)` sweep walks PENDING + BLOCKED jobs and flips state via the caller-supplied `(eligible, reason) = check_eligibility(job)` predicate; idempotent (returns 0 changed on a steady-state second pass) so a watchdog tick is safe. 10 unit tests in `tests/test_block_state.py` cover serialisation round-trip, claim_next BLOCKED-skip, both transition directions, terminal-state skip, idempotency, stale-reason refresh, and clearing leftover blocked_reason on a PENDING job. Caller-side wiring (rule-store + registry + scanner integration to actually drive the predicate; `claim_next` per-worker eligibility filter; the four trigger sites) lives in a follow-up turn — the queue side is correctness-tested and ready for it.

- [x] **TG.4** *(1.7.0-dev.4 → dev.19)* — REST API for rules + worker tags fully landed. All five endpoints in `ui_api.py` (worker tags POST in dev.4; routing-rule GET/POST/PUT/DELETE in dev.12) with the in-memory rule cache + `routing_eligibility.fire_and_forget` re-eval pass wired at every mutation site (TG.3, dev.17). `routing_extra` round-trips through the existing `POST /ui/api/targets/{filename}/meta` (verified by `tests/test_scanner.py::test_write_device_meta_routing_extra_round_trip`). Tag matching for `q` lives client-side in `DevicesTab`/`WorkersTab` (`matchesFilter(filter, …, t.tags)` — neither endpoint takes a `q=` parameter today; the UI loads the full list and filters in-browser, which is the right call at fleet sizes that fit in a browser table). Each create/update/delete rule endpoint broadcasts a `routing_rules_changed` WebSocket event so the UI updates without a 30-s poll.
  - *Original spec for reference:* New endpoints, all bodies validated through pydantic models in `protocol.py` per PY-6:
  - `GET /ui/api/routing-rules` — list all global rules.
  - `POST /ui/api/routing-rules` — create. Body validated; `id` slug auto-generated from `name` if absent, validated unique.
  - `PUT /ui/api/routing-rules/{id}` — update.
  - `DELETE /ui/api/routing-rules/{id}` — delete.
  - `POST /ui/api/workers/{worker_id}/tags` — set worker tags (overwrites the entry in `/data/worker-tags.json`).

  `POST /ui/api/targets/{filename}/meta` (existing) accepts arbitrary keys; verify `routing_extra` round-trips through `read_device_meta`/`write_device_meta` without mangling. Search behavior: `/ui/api/devices` and `/ui/api/workers` extend their existing `q` matching to also match against tag strings (substring, case-insensitive — same shape as the existing name/comment match). All four create/update/delete rule endpoints invalidate the in-memory rule cache and trigger a re-eval pass over PENDING+BLOCKED jobs.
    - *Partial in 1.7.0-dev.4:* `POST /ui/api/workers/{client_id}/tags` landed (body `{tags: [str]}`; persists through `WorkerTagStore.set_tags` + `registry.set_tags`; broadcasts `workers_changed`; the search box already extends to match worker tags after dev.2). Rule endpoints + `routing_extra` round-trip + the device-search tag match still to come once TG.2 lands.
    - *Partial in 1.7.0-dev.12:* the four routing-rule endpoints landed: `GET / POST /ui/api/routing-rules`, `PUT / DELETE /ui/api/routing-rules/{id}`. The POST handler auto-slugs `id` from `name` when omitted (lowercase ASCII alphanum + dashes, runs collapsed, leading/trailing dashes trimmed); duplicate ids → 400. Severity validated to `"required"` only — anything else → 400 with the same shape. Each create/update/delete broadcasts a `routing_rules_changed` WebSocket event so the UI updates without a 30-s poll. Per-device `routing_extra` round-trips through the existing `POST /ui/api/targets/{filename}/meta` endpoint with no code changes — `read_device_meta`/`write_device_meta` already handle nested lists via `yaml.safe_load`/`yaml.dump`; verified by `tests/test_scanner.py::test_write_device_meta_routing_extra_round_trip`. UI uses light dict shapes rather than pydantic models (the PY-6 invariant scopes pydantic models to `/api/v1/*` worker endpoints; UI endpoints already use plain dicts everywhere). Re-evaluation of PENDING+BLOCKED jobs lands with TG.3.

### UI

- [x] **TG.5** *(1.7.0-dev.2 → dev.15)* — Devices tab tags fully landed. Tags column at position 2 (after Device, per #16) with click-to-edit chip pills + autocomplete dropdown (dev.7+). Above-table filter pill bar with usage counts (`kitchen (2)`) and OR-logic across selections, persisted to `devices-tag-filter` in localStorage, hidden when the fleet has no tags (dev.15). Search box matches tag substring-case-insensitively (dev.2). Bulk tag editor on multi-row select (dev.9). Empty-cell rendering blank (no em-dash). Lucide icon dropped to save space (#15, dev.8). Inline chip-input editor on click (dev.7) — chip-input opens the shared `TagsEditDialog` rather than auto-save-on-blur per the simpler "click → modal" UX. The whole TG.5 spec sentence is now in code.
    - *Partial in 1.7.0-dev.2 → dev.4:* chip-pill rendering + click-to-edit dialog landed (column shows tags from the YAML metadata comment via the existing `tags` string round-trip in `read_device_meta`/`write_device_meta`; default visible; column-visibility menu entry; search box now also matches tags substring-case-insensitively; clicking the cell opens a shadcn `Dialog` with a comma-separated text input that POSTs through the existing `/ui/api/targets/{filename}/meta` endpoint). Click-to-filter, the chip-input autocomplete editor, and the above-table filter-pill bar still to come.

- [x] **TG.6** *(1.7.0-dev.2 → dev.15)* — Workers tab tags fully landed. Mirror of TG.5: tags column with inline edit + autocomplete + chip-input + same color hash, header tooltip explaining the `WORKER_TAGS` env-seed semantics, Routing rules… button opening the TG.8 rules editor, search box matches tags, above-table filter pill bar with localStorage-persisted selection (`workers-tag-filter`). Workers-tab bulk-select tag edit deferred (would need TanStack row-selection plumbing).
    - *Partial in 1.7.0-dev.2 → dev.4:* chip-pill rendering + click-to-edit dialog landed (`Worker.tags` surfaces through `/ui/api/workers`; column between `Version` and `Slots`; header tooltip explains seed semantics; clicking the cell opens the shared `TagsEditDialog` and saves through the new `POST /ui/api/workers/{id}/tags` endpoint — see TG.4 partial below). Search box now also matches tags substring-case-insensitively. Filter pills, autocomplete pool, and the `Routing rules…` button still to come (TG.4 rule endpoints + TG.8).
    - *Partial in 1.7.0-dev.13:* `Routing rules…` button now lands in the Workers toolbar (next to `+ Connect Worker`, before `Actions`); opens the new `RoutingRulesModal` (TG.8). Above-table filter-pill bar still pending — every other piece of TG.6's spec is now in place.

- [x] **TG.7** *(1.7.0-dev.13)* — NewDeviceModal + ConnectWorkerModal tag inputs. Both modals gain a comma-separated `Tags` text field. ConnectWorkerModal: the bash / powershell / compose snippets gain `-e WORKER_TAGS=…` (compose: `      - WORKER_TAGS=…`) only when the field is non-empty — the flag is omitted entirely when empty so the docker run stays clean for users who don't care about tags yet. NewDeviceModal: after `createTarget` succeeds, a non-empty tag list is persisted via `updateTargetMeta(target, {tags: ...})` (the existing meta endpoint, which writes the YAML metadata comment block). Tag-write failures are reported via toast but don't fail the create — the YAML is already on disk. Plain text input rather than a chip-input + autocomplete pool — the spec called for autocomplete but for two short-lived modals the comma-separated string keeps the form simple; the chip-input editor on the Devices/Workers tabs (TG.5/TG.6, dev.7) handles the richer flow afterwards.

- [x] **TG.8** *(1.7.0-dev.13)* — Routing rules editor. New `RoutingRulesModal` (list view + delete) wraps a `RoutingRuleBuilder` form (create + edit). List view shows Name + summary of each clause set + Edit/Delete actions, with the "No routing rules — all workers eligible" empty state and a `+ Add rule` CTA. Builder: Name auto-slugs the ID (editable until the user types in the ID field; immutable on edit since the server preserves the original id on update); no severity dropdown (always `required`); "When device has" and "Worker must" each render a list of clause rows with `[All of / Any of / None of]` + chip-input drawing autocomplete from the device pool / worker pool respectively; `+ Add clause` appends, `−` per-row removes (disabled on the last clause); live-preview footer counts "**N of M devices** would be constrained, and **K of L online workers** are eligible to build for them" — recomputed on every clause/tag keystroke via the new client-side mirror of the server evaluator (`utils/routing.ts`). Save fires `createRoutingRule` / `updateRoutingRule` from the new API client wrappers; errors surface inline. Toasts on every CRUD via `sonner`. Modal opened from the Workers toolbar's `Routing rules…` button (TG.6).

- [x] **TG.9** *(1.7.0-dev.17)* — Queue BLOCKED state visibility. New `blocked` variant in `utils/jobState.ts` (`bg-[#3f1d1d] text-[#fb7185]` — distinct red-orange from PENDING's grey). `getJobBadge` adds the BLOCKED branch; `isJobInProgress` now treats BLOCKED as in-progress (it's a Pending-with-a-reason that flips back the moment an eligible worker arrives, not a terminal state — counting it as Failed would mis-bucket it on every Queue summary). `QueueTab` renders the BLOCKED badge as a `<button>` with a tooltip surfacing `blocked_reason.rule_name` + `summary`, and an `onClick` that fires the lifted `onOpenRoutingRule(rule_id)` callback. The `RoutingRulesModal` is now hoisted out of `WorkersTab` to `App.tsx` so a single instance serves both surfaces; the modal's new `initialEditRuleId` prop deep-links straight into edit-mode for the rule that fired (gracefully falls back to list-mode if the rule was deleted between the badge click and modal mount). The `Job` type in `types/index.ts` gains the matching optional `blocked_reason: {rule_id, rule_name, summary} | null` field. Cancel affordance unchanged — BLOCKED jobs follow the same row-select + Cancel flow as PENDING (intentional: the user can dismiss a stuck job without first fixing the rule). `STATE_ORDER.blocked = 1.5` keeps blocked rows adjacent to pending in the default sort.

### Tests

- [x] **TG.10** *(1.7.0-dev.19 → dev.20)* — Mocked + real-HA e2e landed. dev.20 patches the real-HA helper to read existing tags via `GET /ui/api/targets` (the `/meta` endpoint is POST-only on the server; the dev.19 spec assumed a GET that returned 405 on hass-4 + haos-pve). `ha-addon/ui/e2e/routing-rules.spec.ts` covers the four mocked surfaces: rule-builder happy path (open modal, fill name, type clause tags, save, confirm the new rule lands in the list view), inline tag edit on Devices and Workers tabs (open `TagsEditDialog` from the cell, add a tag, save, confirm the per-target / per-worker POST body), Devices-tab filter pills (click `kitchen (2)` pill → table narrows to two kitchen-tagged rows; click again → restores), and the BLOCKED badge (seeded job-009 in `state: blocked` with `blocked_reason` set; aria-label cites the rule name; clicking the badge opens `RoutingRulesModal` deep-linked into edit mode for the rule that fired — TG.9). Fixtures gain a `routingRulesSeed` + mutable working copy with mock POST/PUT/DELETE handlers that mirror the server's CRUD (auto-slug id from name, in-place splice on delete, reset between specs so the bulk-tags spec's mutations don't leak into routing-rules). `ha-addon/ui/e2e-hass-4/routing-rules.spec.ts` runs the full round-trip against a live HA: tag the device with `routing-test`, POST a rule whose worker side requires the un-reachable `arm9000` tag, enqueue a compile, assert it lands in `BLOCKED` with `blocked_reason.rule_id` pointing at the rule, DELETE the rule, watch the job leave BLOCKED and run to `success`. Cleanup hook is best-effort (deletes the rule + drops the test tag even on a partial failure) so a re-run starts clean. Tagged `@requires-ha` so the standalone target skips it (the local worker pool needs to be predictable for the eligibility flip to land deterministically). Side-effect: `tests/e2e/tags-modal-layout.spec.ts:54` was updated to use `dialog.locator('input').first()` instead of `input[placeholder*="Type a tag"]` — the placeholder is suppressed when the seeded worker has tags (which TG.10 requires for the autocomplete pool), so the old selector was brittle.

### Deferred (intentionally NOT 1.7.0)

- **Auto-detected tags** (system-derived `os:linux` / `os:macos` / `os:windows` from worker host, `network:thread` / `chip:esp32` / `framework:esp-idf` from `scanner._collect_metadata`'s existing fields). Schema and editor stay user-managed in 1.7.0; auto-detection lights up as additive read-only system tags later without breaking compat. Filed for a future release.
- **`preferred` severity with `weight`** and a scoring engine. Schema reserves the fields; API rejects them today. Lights up only when a real use case at scale demonstrates pull+FCFS-among-eligible doing the wrong thing. At 1–3 workers, preferred is structurally weak — it almost always degenerates to "the eligible worker that polled first took it."
- **Per-device tag-pool panel inside the editor** (heffneil's `template:1541-1577`). Folded into TG.5/TG.7's chip-input autocomplete instead — same affordance, less visual real estate.
- **"Affected devices" preview in the rule editor** that lists actual filenames (vs. just a count). Defer until rule debugging needs it.
- **HA Repairs issue creation for `BLOCKED` jobs.** Queue UI surfaces the state plenty for a single-operator fleet; a Repairs entry would be redundant noise.

---

## DQ — Disk quota & LRU eviction on workers

Worker hosts (especially the in-add-on local worker on a Pi) accumulate disk state under `/esphome-versions/`: ESPHome venvs (~250 MB each), per-slot PlatformIO toolchains (`pio-slot-N/`, ~600 MB–1 GB each), per-target compile caches (`cache/<stem>/`, 50–400 MB), and per-slot/per-target working dirs (`slots/<w>/<stem>/`). Today's bounds are loose — `MAX_ESPHOME_VERSIONS=3` count cap on the venvs (the smallest contributor) and a soft `MIN_FREE_DISK_PCT=10` floor on host disk. The fat dirs (toolchains + target caches) have no upper bound and grow unboundedly as the fleet covers more targets and frameworks.

Replace those knobs with a single byte budget across the whole `/esphome-versions/` tree, evicted LRU between jobs. Policy: keep exactly one ESPHome venv (the most recently used); spend the rest of the budget on build caches; evict the oldest things first when the budget is exceeded. Configurable as a fleet-wide default in server settings (10 GiB), with a per-worker override surfaced in the "Add worker" dialog and the worker row's hamburger menu. Surface `disk: X / Y GiB` per worker in the UI.

Eviction policy (cheapest → most expensive to recreate):

1. **Orphan slot dirs** (`slots/N/` + `pio-slot-N/` for `N >= MAX_PARALLEL_JOBS`) — pruned unconditionally first; can never be in flight.
2. **Stale ESPHome venvs** — collapse to 1 (most recently used). ~1–3 min to re-pip-install.
3. **Per-target caches** (`cache/<stem>/` + every `slots/*/<stem>/` for the same stem, evicted as a unit) — mtime-LRU. 3–5 min cold rebuild.
4. **PlatformIO toolchains** (whole `pio-slot-N/` dirs) — last resort, mtime-LRU. 5–10 min re-extract.

Pinning (must-not-evict): the venv, `slots/<w>/<stem>/`, `pio-slot-N/`, and `cache/<stem>/` of any in-flight job. The cache is already fcntl-locked during rsync (implicit pin); the rest needs an in-memory active set tracked by the run-loop.

Triggers: (a) worker startup before the poll loop starts, (b) end of every job in the same code path as `_sync_slot_into_cache`, (c) inside `ensure_version()` before a fresh `pip install` (replaces the existing eviction call), (d) after a heartbeat brings a new lower `set_disk_quota_bytes`. No background timer — idle workers do nothing.

The `MIN_FREE_DISK_PCT=10` host-disk floor is kept as a separate emergency override: if host disk drops below 10% free regardless of our usage, evict beyond what the quota mandates until host recovers (workers share the disk with HA core, Docker images, logs).

LRU bookkeeping: dir mtimes already maintained today — `cache/<stem>/` is touched by `_sync_slot_into_cache` on every successful compile; `version_manager` already `move_to_end`s on use; `pio-slot-N/` mtime gets a one-line `os.utime` bump at job end.

### Backend

- [x] **DQ.1** *(1.7.0-dev.67)* — Added `default_worker_disk_quota_bytes` (default 10 GiB) to `AppSettings`, with `_validate_int_range(1 GiB, 1 TiB)` validator. PATCH-able via the existing `/ui/api/settings` endpoint without a server restart.
- [x] **DQ.2** *(1.7.0-dev.67)* — New `ha-addon/server/worker_disk_quotas.py` mirroring the `worker_tags.py` shape (hostname/client_id identity, schema-versioned JSON, atomic write+rename, malformed-file recovery). Tests in `tests/test_worker_disk_quotas.py` cover round-trip, server-side-wins on re-registration, explicit-null persistence, and garbage-value drop-through.
- [x] **DQ.3** *(1.7.0-dev.67)* — `Worker.disk_quota_bytes: Optional[int]` plus `Worker.effective_disk_quota_bytes(default)` helper. `to_dict` emits `disk_quota_override_bytes` (the persisted override, may be null) so the UI can drive the dialog's radio without a second fetch.
- [x] **DQ.4** *(1.7.0-dev.67)* — Register handler seeds the persistent store from `RegisterRequest.disk_quota_bytes` on the first registration for the worker's identity (subsequent registrations are server-side-wins, mirroring TG.1). Heartbeat handler pushes the effective quota (override ?? `default_worker_disk_quota_bytes`) on every response — UI edits propagate within one heartbeat (≤10 s) without a worker restart.
- [x] **DQ.5** *(1.7.0-dev.67)* — `POST /ui/api/workers/{id}/disk-quota` accepts `{disk_quota_bytes: int | null}` (null clears the override; ≥1 GiB / ≤1 TiB enforced). `GET /ui/api/workers` adds `disk_quota_bytes` (effective) + `disk_quota_override_bytes` (persisted, nullable) + `default_worker_disk_quota_bytes` (fleet default) per row. `GET /ui/api/server-info` also surfaces the fleet default for the ConnectWorkerModal radio label.

### Protocol

Additive only — no `PROTOCOL_VERSION` bump. Both `ha-addon/server/protocol.py` and `ha-addon/client/protocol.py` (byte-identical, enforced by `tests/test_protocol.py::test_server_and_client_protocol_files_are_identical`):

- [x] **DQ.6** *(1.7.0-dev.67)* — `RegisterRequest.disk_quota_bytes`, `HeartbeatResponse.set_disk_quota_bytes`, `SystemInfo.disk_usage_bytes` / `disk_quota_bytes` / `last_eviction_freed_bytes` — all `Optional[int]`, additive only (no `PROTOCOL_VERSION` bump). Server + client copies stay byte-identical (`tests/test_protocol.py::test_server_and_client_protocol_files_are_identical` enforces). New `tests/test_protocol.py` cases pin the round-trip + the `extra="ignore"` forward-compat for older peers.

### Client

- [x] **DQ.7** *(1.7.0-dev.67)* — `ha-addon/client/disk_quota.py` engine: `compute_usage` (per-category byte breakdown), `prune_orphans(base, max_slots)` (unconditional), `enforce_quota(base, quota_bytes, *, pinned)` (mtime-LRU across stale venvs → per-target caches → pio-slots; per-target evicted as a unit so the cache lock semantics stay consistent), `host_disk_floor(base, min_free_pct, *, pinned)` (emergency override, same eviction order). `ActiveJobSet` provides refcounted pinning so two parallel jobs on the same target both protect it. When only pinned items remain over quota, the engine logs a warning and returns rather than panicking — the next post-job sweep catches up once pins drop.
- [x] **DQ.8** *(1.7.0-dev.67)* — `ha-addon/client/version_manager.py`: module-level `MAX_ESPHOME_VERSIONS` hard-set to `1` (the byte-bound disk-quota engine is the authoritative cache cap now). `MAX_ESPHOME_VERSIONS` env var is a no-op with a one-time warning log on startup when set ≠ 1. The `max_versions` constructor kwarg is preserved so existing test rigs continue to work.
- [x] **DQ.9** *(1.7.0-dev.67)* — `ha-addon/client/client.py` integration: thread-safe `_current_disk_quota_bytes` cell (boot env default → server-pushed value wins), `_active_job_set` pinning, `_run_disk_quota_sweep()` helper called at three trigger points (startup with `prune_orphans_first=True`, post-job same code path as `_sync_slot_into_cache`, heartbeat-driven sweep when the server lowers the quota mid-life). `_build_system_info()` enriches every `SystemInfo` with `disk_usage_bytes` / `disk_quota_bytes` / `last_eviction_freed_bytes` so register and heartbeat both surface the same view. New `WORKER_DISK_QUOTA_GB` env var (integer GiB → bytes on the wire). Per-sweep log line: `disk-quota[label]: X.Y / Z.0 GiB — evicted: ...`.

### UI

- [x] **DQ.10** *(1.7.0-dev.67)* — `ConnectWorkerModal.tsx`: new "Disk Quota" radio field (default = "Use fleet default (X GiB)" reading from `serverInfo.default_worker_disk_quota_bytes`, custom = integer-GiB input ≥1 / ≤1024). Custom mode bakes `-e WORKER_DISK_QUOTA_GB=<n>` (and the matching `      - WORKER_DISK_QUOTA_GB=<n>` for compose) into the rendered docker command; default mode emits nothing so the worker registers with `disk_quota_bytes=None` and the server's fleet default flows in via heartbeat.
- [x] **DQ.11** *(1.7.0-dev.67)* — Workers tab worker-info cell gains a `Quota: 2.1 / 10 GiB` line (yellow >80 %, red >95 %) sourced from `system_info.disk_usage_bytes` + `system_info.disk_quota_bytes`. Actions hamburger gains "Set disk quota…" backed by a new `SetDiskQuotaDialog` (radio "Use fleet default" vs "Custom + integer GiB"). Dialog state is lifted to `WorkersTab` to avoid the SWR-poll-tears-down-row issue (#2 / #71 class). `setWorkerDiskQuota(id, bytes | null)` lives in `src/api/client.ts`; `App.tsx::handleSetDiskQuota` shows the appropriate toast + mutates the SWR cache.
- [x] **DQ.12** — Settings UI form for the fleet-wide default: deferred per plan. The field is editable today via `PATCH /ui/api/settings`; a React form lands when the broader settings drawer gets a build-cache section.

### Tests

- [x] **DQ.13** *(1.7.0-dev.67)* — `tests/test_disk_quota_logic.py` (23 cases): per-category usage accounting, orphan-slot pruning, eviction ORDER across all four categories, per-target unit eviction, pinning honored at every layer, idempotency on steady-state second pass, sweep on quota lowered mid-life, host-disk floor with patched `os.statvfs`, `ActiveJobSet` refcounting + thread safety, **and the pinned-only-exceeds-quota → warn + no-op case I flagged during plan review**.
- [x] **DQ.14** *(1.7.0-dev.67)* — `tests/test_settings.py` extended for `default_worker_disk_quota_bytes`: dataclass default = 10 GiB, validator accepts in-range, rejects below floor / zero / negative / above ceiling, accepts exactly at floor + ceiling.
- [x] **DQ.15** *(1.7.0-dev.67)* — `tests/test_worker_disk_quotas.py` (13 cases) covers seed-on-first / server-side-wins / persisted-explicit-null round-trip / malformed-file recovery / unknown-schema-version / garbage-value drop-through / authoritative `set_quota` UI path.
- [x] **DQ.16** *(1.7.0-dev.67)* — `tests/test_protocol.py` byte-identical assertion still holds; new round-trip cases for `RegisterRequest.disk_quota_bytes`, `HeartbeatResponse.set_disk_quota_bytes`, and the three `SystemInfo` fields plus default-omitted-is-None coverage.
- [x] **DQ.17** *(1.7.0-dev.67)* — `connect-worker-modal.spec.ts` extended: default mode emits no `WORKER_DISK_QUOTA_GB` across bash/powershell/compose; custom mode bakes `WORKER_DISK_QUOTA_GB=5` into all three formats.
- [x] **DQ.18** *(1.7.0-dev.67)* — `workers.spec.ts` extended: row surfaces "Quota: 2.1 / 10 GiB" from `system_info`; "Set disk quota…" hamburger item opens the dialog; saving Custom POSTs `{disk_quota_bytes: 5*1024**3}`; saving "Use fleet default" POSTs `{disk_quota_bytes: null}`. All 193 mocked Playwright tests pass.

### Out of scope

- **Per-target quotas** — one global pool, by design.
- **Background timer for cleanup** — jobs trigger eviction; idle workers do nothing.
- **Quota enforcement mid-compile** — between jobs only; interrupting a live build to free disk is worse than failing the build cleanly.
- **Server-pushed config changes for *other* knobs** — this only adds `set_disk_quota_bytes`; broader server-pushed config is a separate concern.
- **Migration tooling for existing `MAX_ESPHOME_VERSIONS` users** — env var becomes a no-op with a one-time warning log.

---

## SD — Scope discipline (moved)

SD.2 (release-blocker gate) and SD.3 (TEST-AUDIT production) moved to `dev-plans/WORKITEMS-1.7.1.md` — both pre-tag gates were specifically scoped to the Honest Gold tier-flip and only make sense alongside the QS / TP / HT / CI work that now lives in 1.7.1.

---

## Open Bugs & Tweaks

### Carried forward from 1.6.2

*(Any post-tag regression against `v1.6.2` lands here as a numbered bug once 1.6.2 ships.)*

### New in 1.7.0

- [ ] **#228** — On at least one of the user's machines, the Devices-tab dropdown menus (per-row hamburger and/or the bulk-Upgrade dropdown in the toolbar) close immediately on `mouseup` and never let an item be selected. Symptom: click trigger opens the menu, release the mouse, menu disappears before any `<DropdownMenuItem>`'s click fires. Other machines on the same build are unaffected. Code-side, the per-row hamburger has lived behind a parent-controlled `open`/`onOpenChange` since #2 / QS.16 (`DevicesTab.tsx:265` → `useDeviceColumns.tsx:701` → `DeviceContextMenu.tsx:113-114`/`164-165`), and the toolbar dropdowns at `DevicesTab.tsx:580/600/676` are uncontrolled (Base UI's own internal state) — both should be impervious to the SWR-poll re-render that took down #2 / #71 historically. Suspect surface area: (a) browser-specific event handling around Base UI's pointer-down/up sequence inside the HA Ingress iframe, (b) a stale Vite/HMR cached build on the affected machine, or (c) something we shipped recently in the Base-UI dropdown wrapper that interacts badly with the iframe focus model. Investigation needed: which machine + browser version + did a hard reload (Cmd-Shift-R) restore behaviour; if reproducible after a clean SPA reload, capture a Playwright trace and look for the close event before the click on a menuitem.

- [x] **#229** *(1.7.0-dev.72)* — `Upgrade All Online` on the Devices tab queued 95 jobs but only 68 actually compiled (matching the user's real fleet count). The 27-job gap was the count of `.archive/*.yaml` rows that `/ui/api/targets` merges into the same payload for the in-tab "Show archived devices" surface (DM.1, `ui_api.py:944`). Archived rows ship with `online=null` / `needs_update=null` / `config_modified=null`, and the bulk-action filter at `DevicesTab.tsx:622` was `t.online !== false` — `null` passes that test, so every archived target got stuffed into the body of `POST /ui/api/compile`. Server-side, `ui_api.start_compile` (`ui_api.py:2417-2418`) silently filtered them out via `valid = set(scan_configs(...))` (which only globs top-level `*.yaml`, never `.archive/`), so the user saw an optimistic 95-count toast but the queue only ever held the 68 valid ones. `Upgrade All` had the same leak via no predicate at all; `Upgrade Outdated` and `Upgrade Changed` were accidentally OK because their predicates (`needs_update`, `hasDriftedConfig`) read fields that archived rows null-out. Fix: every bulk-Upgrade predicate in `DevicesTab.tsx` (`Upgrade All`, `Upgrade All Online`, `Upgrade Outdated`, `Upgrade Changed`) now leads with `!t.archived`, so the toast count matches what the queue actually receives and a future schema tweak that nulled out fewer fields can't reintroduce the leak. Two new regression specs in `e2e/bulk-upgrade.spec.ts` mock the targets API to include archived rows and assert (a) the modal title reflects the active count only, and (b) `POST /ui/api/compile` carries no `.archive/*.yaml` paths.

- [ ] **#230** — Compile failures on AI-MacBook-Pro continue post-#227. Worker self-reports `client_version=1.7.0` (the abortive release commit, lacking #227's two-pass wipe — see `bump back to 1.7.0-dev.71` revert) and `image_version=7` < `MIN_IMAGE_VERSION=8`, so the server's source-code auto-update is gated off and the worker is stranded on stale code. AI-MAC also reports `disk_usage_bytes=10.99 GB > disk_quota_bytes=10.74 GB` (102 % of quota) with `last_eviction_freed_bytes=0`, so the LRU is starved (every candidate is pinned by an active compile). Fresh failure pattern from the last hour (49/125 = 42 % failure rate, all on `pio-slot-{1,2}`): `FileNotFoundError: '/esphome-versions/pio-slot-1/.cache/downloads/tmpz11q9jio'` (PIO download tmpfile vanished mid-fetch); `Permission denied: '/esphome-versions/pio-slot-1/penv/bin/uv'`; `OSError: ... certifi/cacert.pem ... invalid path`; `Missing ninja package directory '/esphome-versions/pio-slot-1/packages/tool-ninja'` *immediately after* the ninja-installed line; `Missing toolchain directory 'None'`; `RuntimeError: at level 0, expected 1 entry, got ['package.json', 'riscv32-esp-elf']` (idf_tools extracting on top of half-extracted state); `CMake Error ... Detecting C compiler ABI info - failed`. Two threads to investigate independently: **(a)** unblock the auto-update path — either bump AI-MAC's local checkout to develop manually or revisit `MIN_IMAGE_VERSION` semantics for non-Docker workers (the gating exists because in-place `.py` updates can't fix a stale image, but a native-Python install isn't an "image" — that gating may be over-broad); **(b)** investigate whether the new error class survives once #227 lands on AI-MAC, and whether the over-quota state is starving the wipe path (no slots can be evicted while pinned, then the wipe-and-retry has to fight for the same disk).

- [x] **#225** *(1.7.0-dev.68)* — `parallel compile: garage-door-big pinned to local-worker` (in `e2e-hass-4/cyd-office-info.spec.ts:296`) fast-failed in 2.1s on **haos-pve** and **standalone-pve** in the post-#104 (dependabot batch) test-matrix run, while passing on **hass-4** in 47.4s. Root cause: `garage-door-big.yaml` does `packages: common: !include includes/common.yaml`, but `scripts/haos/seed-fleet.sh` and `scripts/standalone/seed-fleet.sh` defaulted `FLEET_TARGETS` to `cyd-world-clock.yaml garage-door-big.yaml .common.yaml secrets.yaml fonts images` — no `includes/` directory. So on the seeded targets, `garage-door-big.yaml` resolved against a missing include and the validator rejected the bundle before the worker ever received the job. hass-4 worked because it has the full `/usr/share/hassio/homeassistant/esphome/` source including `includes/`. Pre-existing since the test landed (`e7eb8417`, 2026-04-09); the dependabot bumps just made it visible because this was the first three-target matrix run that exercised the spec end-to-end. Fix: add `includes` to the default `FLEET_TARGETS` list in both seed scripts (one-word change in each), with an inline comment pointing to this bug for the next reader. **Verified on dev.68** — fresh three-target matrix run had `parallel compile: garage-door-big pinned to local-worker` ✓ on hass-4 (17.9s), standalone-pve (27.1s), and haos-pve (17.1s). Final tallies: hass-4 21/24, haos-pve 21/21, standalone-pve 11/11 (vs. dev.67 baseline of 21/24, 20/21, 10/11 with the spec failing on the latter two).

- [x] **#119** *(1.7.0-dev.43)* — every compile on hass-4 was failing with `FileNotFoundError: '/data/esphome-versions/2026.4.3/bin/python'` from `scanner.create_bundle`'s subprocess. Smoking-gun timeline from `/data/local-worker.stderr.log`: at 2026-04-28 19:02:19 UTC the embedded local-worker logged "Server requested build cache clean — worker is idle, clearing immediately", then "Removed pio-slot-1 / cache / slots / 2026.4.2 / 2026.4.3" — including the venvs the server was actively using. Root cause: `ha-addon/server/main.py:1679` spawns the embedded local-worker with `ESPHOME_VERSIONS_DIR=/data/esphome-versions` so it shares the server's lazy-installed venv directory; the worker's `_clean_build_cache()` blindly `shutil.rmtree`'d every subdir; the server's `_server_esphome_bin` kept pointing at the now-deleted `/data/esphome-versions/2026.4.3/bin/esphome`, and `_venv_python()` resolved to a missing path on every subsequent bundle attempt. Until the add-on restarted (which would re-trigger `ensure_esphome_installed`), every job failed at claim time. Fix: `_clean_build_cache()` now skips any directory containing `bin/esphome` (preserves venvs while still wiping `builds/`, `slots/`, `cache/`, `pio-slot-*/`, `platformio/`). Venv lifecycle is already bounded by `MAX_ESPHOME_VERSIONS` LRU eviction in `VersionManager`, so leaving them in place isn't a leak. Two new tests in `tests/test_client.py`: `test_clean_build_cache_preserves_esphome_venvs` (regression — venvs survive, build caches gone) and `test_clean_build_cache_removes_all_when_no_venvs` (no venvs present → every subdir wiped, behaviour preserved for that path). The Clean Cache button is now safe to click on the embedded local-worker.

- [x] **#118** *(1.7.0-dev.42)* — `e2e/settings.spec.ts › auto-commit toggle flips and persists after reopen` flaked CI on commit `61ba22b`. Recent develop runs had 165 passed; this run got 164 + 1 fail with `strict mode violation: getByRole('button', { name: 'Settings' }) resolved to 2 elements`. Root cause: after `page.keyboard.press('Escape')` closes the Settings drawer, the drawer's "Close settings" close button (aria-label="Close settings") is still in the DOM during the close animation, and `getByRole('button', { name: 'Settings' })` is a substring match — so both the gear trigger AND the in-flight close button match. Whichever side wins depends on animation timing, which differs between local and CI runners. Fix: pass `exact: true` to the second click so only the gear trigger (aria-label="Settings", exact) matches, regardless of whether the close animation has finished. Inline comment explains the substring-match trap so the next reader doesn't reintroduce it. No production code change.

- [x] **#117** *(1.7.0-dev.41)* — `tests/test_api.py` was patching `api.create_bundle` with `unittest.mock.patch(...)` but bug #111's fix renamed the call site to `await create_bundle_async(...)` (imported from `scanner`). The 6 affected tests (claim-job-* + pinned-job-*) all began raising `AttributeError: <module 'api'> does not have the attribute 'create_bundle'` at patch time, turning the CI `test` job red on every push since `a373491`. Fix: switch the patches to `patch("api.create_bundle_async", new=AsyncMock(return_value=_make_test_bundle()))` so the awaited mock yields the bundle bytes the same way the original sync `create_bundle` did. No production code changed; the test surface follows the rename.

- [x] **#116** *(1.7.0-dev.40)* — Rendered-config modal had two visible defects: (a) the Monaco editor was capped at `height="60vh"` inside a `dialog-lg` (which sizes to `100dvh - 6rem`), so the YAML pane stopped halfway down the modal and the bottom half rendered as empty black space below the last line of YAML; (b) ESPHome wraps `!secret 'name'` references in ANSI conceal SGR codes (`\x1b[8m...\x1b[28m`) so terminals can mask them, and Monaco renders the literal escape bytes as garbage text — the user saw `\033[8m!secret 'wifi_ssid'\033[28m` instead of a clean `!secret 'wifi_ssid'`. Fix: `RenderedConfigModal.tsx` wraps the `<Editor>` in a `flex-1 min-h-0` flex child and sets `height="100%"` + `automaticLayout: true` so the editor stretches to fill whatever vertical space `dialog-lg` gives it; outer body container also got `flex flex-col` so the wrapper participates in the layout. Server-side, `ui_api.get_rendered_config` strips ANSI SGR sequences (`\x1b\[[0-9;?]*[A-Za-z]`) from both the success-stdout and the failure-stderr+stdout paths before returning / caching, so the modal only ever sees plain text. Regex catches the conceal pair (`[8m`/`[28m`) plus any colour codes that might leak in from `_LOGGER` chatter on the failure path. Verified live against `Office Speakers` on hass-4: pre-fix the modal had ~50% empty space + literal `\033[8m` markers; post-fix the editor fills the modal and `!secret` references render cleanly.

- [x] **#112** *(1.7.0-dev.38)* — Bundle-creation failure logs surface ESPHome's `INFO` / `WARNING` chatter as if it were the error, burying the actual cause. Symptom (live, from #111's investigation): the kauf-plug failures showed `Including a single package under \`packages:\` is deprecated...` as the headline of the captured stderr, with the real `AssertionError` / `Could not find components folder` buried below — and the deprecation message is a known upstream false positive (PR #15605, fixed post-2026.4.3) that has nothing to do with the actual failure. Same shape applies to any bundle failure: `INFO ESPHome 2026.4.3`, `INFO Reading configuration...`, `INFO Updating https://github.com/...`, plus any module-level deprecation `_LOGGER.warning` decorate the error message users read in the Queue-tab Log modal. Root cause: `scanner._BUNDLE_SUBPROCESS_SCRIPT` runs ESPHome's validator in a subprocess and our `create_bundle` captures stderr verbatim into the `RuntimeError` text — but ESPHome's `_LOGGER` (esphome.* namespace) writes its own status chatter to that same stderr. The script already emits exactly the structured error we want (`validation errors (1 total): <msg>` on exit 3, or a Python traceback on uncaught exceptions); ESPHome's logger noise is pure decoration that confuses users and reviewers. Fix: silence the `esphome` logger inside the bundle subprocess (NullHandler + propagate=False + level above CRITICAL) so only our explicit stderr writes — and Python's default uncaught-exception traceback — reach the captured stream. Keeps `proc.stderr` clean and on-topic. **Verification**: invoked `_BUNDLE_SUBPROCESS_SCRIPT` directly against `tests/fixtures/esphome_configs/device1.yaml` without the silencing — ESPHome leaks `The minimum WiFi authentication mode (wifi -> min_auth_mode) is not set...` (a real upstream warning that was silently riding along with every successful bundle pre-fix). With the silencing, stderr is empty on success. New regression test (`tests/test_scanner.py::test_bundle_subprocess_stderr_does_not_leak_esphome_logger_chatter`) runs the script subprocess against the fixture and asserts `proc.stderr` is empty.

- [x] **#111** *(1.7.0-dev.37)* — concurrent batch-job claims trigger a `git clone` race in server-side bundle creation when multiple jobs reference the same `packages:` / `external_components:` git repo with cold caches. Symptom (live on hass-4): a 7-job batch of kauf-plug-{2..9}.yaml — all referencing `KaufHA/PLF12` + `KaufHA/common` — produced 5 different error messages on the cold-cache run (`AssertionError` from `loader.py:229 (assert "." not in domain)` for one target, `Could not find components folder for source` for three, `kauf-plug.yaml does not exist in repository` for one, and the 2026.4.3 false-deprecation warning as decoration on top), and 7/7 succeeded on a warm-cache rerun with no other change. Same YAMLs, same code, same ESPHome — different errors per run pinned this as a non-deterministic race, not an upstream regression. Root cause: `scanner.create_bundle` was dispatched via `loop.run_in_executor(None, …)` from `api.py`'s `claim_next` handler, six bundle subprocesses ran concurrently, and each called ESPHome's lock-free `git.clone_or_update` (`esphome/git.py:96`) into the **same** `<config_dir>/.esphome/<domain>/<sha8>/` directory. The losers observed a partial-state tree at whichever validation step they reached first. ESPHome's `clone_or_update` assumes single-process invocation; the CLI honours that, our parallel-claim server didn't. Fix: new `scanner.create_bundle_async` wraps the executor dispatch in a module-level `asyncio.Lock` so only one bundle subprocess is in flight at a time. `api.py`'s job-claim handler switched from `loop.run_in_executor(None, create_bundle, …)` to `await create_bundle_async(…)`. Bundle creation is fast (0.1–3 s warm / cold), so server-wide serialisation costs at most N×bundle_time of latency on a batch claim, well under the timeout budget. Three regression tests in `tests/test_scanner_bundle_concurrency.py`: serialisation contract (max 1 in-flight invocation across `asyncio.gather` of 3), exception propagation, and lock-release after failure (so a broken YAML doesn't strand subsequent bundles). Without the lock the test deterministically observes max_in_flight=3 (concurrent); with the lock it observes max_in_flight=1. **Cold-cache reproduction on hass-4 pre-fix**: wiped the three KaufHA-related sha8 dirs at `/config/esphome/.esphome/packages/{c5646bd4,a0579e33}` + `external_components/bbec5a25`, batch-enqueued all 7 kauf-plug-*.yaml via `/ui/api/compile` (run `562f88ae`) — got 7/7 fail with five distinct error messages (`Could not find components folder for source` × 5, `kauf-plf12.yaml does not exist in repository`, plus the deprecation-warning red herring on a sixth) — and a back-to-back rerun of the same 7 (run `a58ae442`) succeeded 7/7 with the cache now warm. **Production-batch validation post-fix**: a real user-triggered 65-job upgrade run (`e396ac98`, all targets across the hass-4 fleet) ran under the lock and 64/65 jobs succeeded; the 1 fail was unrelated to bundle creation (worker-side esptool environment break — see #114, not race-related).

- [x] **1** *(1.7.0-dev.18)* — `fmtEpochRelative` now floors the diff itself rather than only flooring `Date.now()/1000`. `archived_at` comes from `os.stat().st_mtime` (Python float), so the previous `Math.floor(now) - epoch` left a fractional remainder that templated into the sub-60s bucket as `4.327s ago`. Fix: `Math.floor(now - epoch)` produces clean integer-second strings ("4s ago", "12s ago"). Inline comment in `utils/format.ts` documents the gotcha so the next reader doesn't reintroduce it.

- [x] **2** *(1.7.0-dev.16)* — invalidate the `archived-configs` SWR key from `App.handleDeleteDevice` whenever the user archives a device (`archive=true`). The Devices toolbar's "Restore from archive" button reads `archiveEmpty` straight off that subscription, so the toggle now flips immediately after the first archive instead of waiting for a manual reload. Skip the invalidation on hard-delete (`archive=false`) since the archive list isn't affected. Routed through `useSWRConfig().mutate('archived-configs')` so any future caller of `deleteTarget` picks it up automatically.

- [x] **3** *(1.7.0-dev.10)* — Archive as a first-order hamburger action. New `Archive` menu item between `Commit changes…` and `Delete` (no confirmation modal — archived configs are restorable from Settings → Archived devices). Clicking archives the target immediately via `onDelete(target, archive=true)`; a toast confirms with the restore-path hint. `onArchive` plumbs through `DeviceContextMenu` → `ActionsCell` → `useDeviceColumns` → `DevicesTab`.

- [x] **4** *(1.7.0-dev.10)* — defer build-cache clean until the worker is idle. The worker's heartbeat used to call `_clean_build_cache()` immediately when the server set `clean_build_cache=true`; if a job was mid-compile, the wipe truncated the `.esphome/build/<name>/` directory the running `esphome run` was writing into and silently broke the upload. Now the heartbeat sets a `_clean_pending` event when the worker isn't idle, the poll loops skip claiming new jobs while it's set (same `_reregister_needed` / `_update_available` pattern), and the main loop runs the actual `_clean_build_cache()` once `_is_idle()` returns true. If the worker is already idle when the request arrives, the clean still runs immediately so the UX stays snappy.

- [x] **5** *(1.7.0-dev.10)* — date-format picker companion to the existing time-format picker. New `date_format` setting (`'auto' / 'iso' / 'us' / 'eu' / 'long'`); `'auto'` defers to the browser's locale, the named formats force a specific shape regardless. Wired through `setDateFormatPref` in `utils/format.ts` from `App.tsx` on settings load and on every drawer commit; `fmtDateTime` now combines a date-pref-respecting date portion with the existing time-pref-respecting time-of-day. Updated test: `test_settings_as_dict_round_trips` includes `date_format`.

- [x] **6** *(1.7.0-dev.6)* — hash-based tag chip colors. djb2 hash of the tag text picks one of 12 hues; HSL pastel background (60% S / 88% L) + same-hue dark text (60% S / 22% L) is AA-readable on both the dark `[data-theme="dark"]` and light `[data-theme="light"]` surfaces, so we don't need to branch on theme. Same tag string → same color across rows, tabs, and refreshes — `kitchen` looks the same on a device row as on a worker row. Lives in `ha-addon/ui/src/components/ui/tag-chips.tsx`.

- [x] **7** *(1.7.0-dev.7)* — subsumed by **#11**. The chip-input editor in `TagsEditDialog.tsx` shows chips, lets the user add new tags by typing + Enter (or comma), and renders a "Suggestions" row of fleet-wide existing tags (union of every device's `tags` + every worker's `tags`, sorted, deduped, capped at 12) which the user clicks to attach. Substring filter narrows the suggestion row as the user types.

- [x] **8** *(1.7.0-dev.9)* — bulk tag editor for the Devices tab (Workers row-select needs a TanStack-table integration that doesn't exist yet — filed as a follow-up). New `BulkTagsEditDialog` component takes the user-selected targets, shows the *intersection* of their tags as removable chips (click × to mark for bulk-remove), shows the *symmetric-difference* tags ("on some devices only") as read-only italic chips so the user knows what they're not editing, and offers a chip-input "Add to all" with the same fleet-wide autocomplete pool as the single-target dialog. Save emits a `{add, remove}` diff which `DeviceTableActions` applies per-target via `Promise.all` of `updateTargetMeta` calls (preserving each device's unique non-shared tags + bug #9's empty-list-clears-block path). Wired into the existing toolbar Actions dropdown as "Edit Tags…". 3 new specs in `e2e/bulk-tags.spec.ts` cover the open / shared / partial display, bulk-add, and bulk-remove flows. Workers-tab bulk-edit deferred — adding TanStack row-selection to that tab is a separate, larger turn.

- [x] **9** *(1.7.0-dev.7)* — clear-tags strips the YAML key (and the whole metadata comment block when no other keys remain). The Devices-tab dialog now sends `{tags: null}` rather than `{tags: ""}` when the user saves an empty list; `update_target_meta` already pops `null` keys, and `write_device_meta` already strips the whole `# esphome-fleet:` block when the resulting dict is empty (see `test_write_device_meta_removes_block_when_empty`). New regression tests cover the dialog flow specifically: `test_write_device_meta_clearing_only_tags_strips_block` (tags-only meta → block gone) and `test_write_device_meta_clearing_tags_with_other_keys_keeps_block` (mixed meta → block stays minus the tags line).

- [x] **10** ANSWERED — not auto-tagging; not tests. The tags surfaced on hass-4 workers (`kitchen, prod, linux, fast` etc.) were demo POSTs against `POST /ui/api/workers/{id}/tags` to prove the round-trip end-to-end and to give the UI something visible to render. Cleared during 1.7.0-dev.7 setup. Auto-tagging is intentionally deferred (see "Deferred (intentionally NOT 1.7.0)" above) — when it lights up, system-derived tags will be additive read-only chips alongside the user-managed ones.

- [x] **11** *(1.7.0-dev.7)* — chip-input editor with X-to-remove and inline-add. `TagsEditDialog` shows each existing tag as a colored chip with a small × button (uses Lucide `X` inside the chip's white-bg-on-color hover affordance); the trailing free-form input creates a new tag on Enter or comma; Backspace on an empty input removes the last chip; click any suggestion chip in the "Suggestions" row to attach it. `TagChip` (singular) lives next to `TagChips` (plural) in `components/ui/tag-chips.tsx` and accepts an optional `onRemove` (× variant) or `onClick` (suggestion variant). Replaces the dev.4 plain text-input dialog.

- [x] **12** *(1.7.0-dev.7)* — punchier chip palette. Switched from the dev.6 washed-out pastel (60% S / 88% L bg + dark same-hue text) to GitHub-issue-label-style solid chips: saturated mid-tone background `hsl(h, 65%, 45%)` + white text. AA-readable on both `[data-theme="light"]` and `[data-theme="dark"]` surfaces without branching, and reads as the chip's color rather than gray.

- [x] **13** *(1.7.0-dev.18)* — confirmed yes, `last_compile` already comes from the SQLite `job-history.db` (`JobHistoryDAO.last_per_target()` joining on `MAX(finished_at)` per target). The "not populating" symptom was that any target without a server-recorded compile (fresh install, YAML compiled by another install, history evicted) showed "—" even when the device was clearly running compiled firmware whose build time it reports back over the native API. Fix: server falls back to parsing the device's ESPHome `compilation_time` string ("Mar 29 2026, 17:00:00") into epoch seconds when SQLite has nothing for the target, marks the synthesized record with `source: "device"`, and the column renders that with a leading `~` prefix + tooltip ("reported by the device's running firmware, no compile history on this server") so the user knows it's approximate. History-sourced rows carry `source: "history"` and render unchanged.

- [x] **14** *(1.7.0-dev.7)* — background refresh wiping in-progress edits. The `TagsEditDialog`'s seed-from-`initial` `useEffect` previously listed `initial` in its dep array, which fired on every parent SWR poll (1Hz) because the parent passes a fresh array reference each render even when values are unchanged — overwriting the user's typed text mid-edit. Dropped `initial` from the deps so the seed only fires on the open transition (false→true); save still uses the current text state, so the latest typed value is what's persisted.

- [x] **15** *(1.7.0-dev.8)* — dropped the leading Lucide `Tag` icon from `TagChip`. Chip color + text already say "this is a tag"; the icon ate horizontal space in narrow table cells.

- [x] **16** *(1.7.0-dev.8)* — Tags column moved to position 2 (right after Device on Devices tab; right after Hostname on Workers tab). Chip text bumped 10px → 12px, padding 0/1.5px → 0.5/2px, cell `min-width: 120px` so a single chip doesn't squeeze into illegibility. The `+ tags` empty-state placeholder also bumped to 12px so it reads consistently with the populated state.

- [x] **17** *(1.7.0-dev.8)* — modal layout rebuilt around shadcn's structural slots. Title alone in `DialogHeader` (the header is `flex justify-between` and was previously fighting the `DialogDescription` for horizontal space — squashed both); description + chip-input + suggestions live in a padded body section; Cancel + Save anchored in `DialogFooter` (with the dark `--surface2` background that shadcn renders for footers). Floating × close button suppressed (`showCloseButton={false}`) since Cancel is the explicit dismiss path. New regression spec in `e2e/tags-modal-layout.spec.ts` (3 tests: structural slots present, footer below chip-input, dialog fits a 480×600 viewport with footer reachable). 

- [x] **18** *(1.7.0-dev.9)* — Areas column rendering JSON. Root cause: ESPHome's newer schema accepts `area: {name: "...", id: "..."}` (dict) in addition to the original `area: "..."` (string), and `scanner._extract_metadata` was calling `str()` on the dict — `str({...})` produces Python's repr (`"{'name': 'Living Room', 'id': '...'}"`) which surfaced verbatim in the cell. Now both `_extract_metadata` and `_fill_missing_metadata` normalise to the dict's `name` field (falling back to `id`). Two new regression tests in `tests/test_scanner.py`: `test_metadata_extracts_area_from_dict_form` and `test_metadata_extracts_area_from_dict_form_id_fallback`.

- [x] **19** *(1.7.0-dev.9)* — column-picker order invariant. Picker order now matches the table column order exactly (Tags column moved from position 13 in the picker to position 1, mirroring the table's "Device → Tags → Status …" layout). New `e2e/column-picker-order.spec.ts` opens the Devices tab, scrapes the table header text in render order, opens the column-picker, scrapes the *checked* picker labels, and asserts the visible-picker labels appear as a subsequence of the headers in the same order — fails CI if a future column move drifts the two out of sync.

- [x] **20** *(1.7.0-dev.9)* — autocomplete dropdown anchored to the chip-input, replacing the dev.7 "Suggestions" section. Pops below the input as the user types, lists matching fleet-wide tags as colored chips, click-to-add. Only renders when input has non-empty text — dropping the focus-only mode keeps the dialog from hovering chips over the Apply button right after Enter clears the input. `onMouseDown` rather than `onClick` on the dropdown items so the input's `onBlur` (which closes the dropdown immediately) doesn't race with the click handler. Same change applied to the bulk dialog. Both `TagsEditDialog` and `BulkTagsEditDialog`. 

- [x] **21** *(1.7.0-dev.10)* — chip color palette redesigned for visual distinctness. The dev.6 → dev.9 hue list (`[0, 30, 60, 95, 130, 160, 190, 215, 240, 270, 300, 335]` at HSL 65/45) had clusters that read as "the same color" at chip size — `130+160` both green, `215+240` both blue, etc. Replaced with a curated 12-color palette derived from Tailwind's 600 stops (red / orange / amber / lime / green / teal / sky / blue / violet / fuchsia / pink / slate) — each color has a perceptibly different hue + saturation so a row of 4 tags reads as 4 distinct colors at a glance. Plus a same-hue darker border for a faint "frame" that helps separate adjacent same-color chips when the hash maps two tags to the same slot.

Also in **1.7.0-dev.10** (no separate bug entries — diagnosed from dev.9 matrix logs):
- `e2e-hass-4/incremental-build.spec.ts` SPEEDUP_THRESHOLD bumped 1.50 → 2.00 after hass-4 hit ratio=1.70 in dev.9 (cause: other compiles running on local-worker during the test compile #2 — host-load variance, not a cache regression). Docstring updated with the host-by-host ratio table.

- [x] **22** *(1.7.0-dev.16)* — `update_target_meta` derives a per-key commit-message action from the request body. Single-key bodies fan out to specific subjects via the new `meta <key>` / `meta <key> cleared` action keys (e.g. `"Updated device tags"`, `"Cleared device tags"`, `"Pinned ESPHome version"`, `"Updated routing rules"`); multi-key bodies still fall back to the generic `"Updated device metadata"`. `_default_subject` also degrades uncurated `meta <key>` actions to `"Updated <key>"` so a future metadata field landing without a curated entry still reads cleanly. Existing dedicated endpoints (`/pin`, `/schedule`) keep their already-specific messages. New `test_default_subject_specific_meta_keys` covers curated + uncurated + cleared paths.

- [x] **23** *(1.7.0-dev.18)* — two new optional Devices-tab columns: **ESP** (chip family parsed from the resolved YAML — "ESP32", "ESP32-S3", "ESP8266", "RP2040", "Host") and **BLE proxy** (off / passive / active from the `bluetooth_proxy:` block presence + `active:` field). Both default off so existing layouts don't shift; users opt in via the column-picker. Slot in the table order between AP and Schedule, and the picker order is updated to match (bug #19's invariant). Server side: `scanner._extract_metadata` reads `esp32:` (with `variant:` for S2/S3/C3/C6/H2 — rendered as "ESP32-S3" not "ESP32S3" to match Espressif product naming), `esp8266:`, `rp2040:`, `host:`. Bluetooth proxy uses key-presence (not truthiness — bare `bluetooth_proxy:` parses as None but still enables the component, same `web_server` pattern from #74). 6 new tests in `tests/test_scanner.py` cover variant rendering, all chip families, and the three BLE states.

- [x] **24** *(1.7.0-dev.18)* — palette doubled from 12 to 25 colors. The dev.10 (#21) Tailwind-600 set used 12 hues at the same lightness — adjacent slots like red/orange or violet/fuchsia/pink read as "the same color" at chip size when the hash collided. Now: all 17 Tailwind 600 hues (red / orange / amber / yellow / lime / green / emerald / teal / cyan / sky / blue / indigo / violet / purple / fuchsia / pink / rose) cover the full spectrum, plus 7 Tailwind 800-stop darker variants of widely-spaced hues add a second perceptual band so two tags hashing to the same hue can still differ by lightness, plus slate-600 as the neutral. White text reads AA-contrast on every entry. Same djb2 hash so existing tag→color mappings stay stable for hashes that still land in the first 12 slots; the new slots are extra capacity for fleets with more tags.

- [x] **25** *(1.7.0-dev.16)* — chip-input + autocomplete dropdown for the ConnectWorker Tags field. Extracted the chip-input from `TagsEditDialog` into a reusable `TagChipInput` component (`components/ui/tag-chip-input.tsx`) — same UX (Enter/comma to commit, Backspace on empty input drops the last chip, autocomplete dropdown filtered by substring) — and dropped it into `ConnectWorkerModal`'s Tags field. The form still serialises to the comma-joined string `WORKER_TAGS` expects, so the docker invocation output is unchanged. `App.tsx` computes the fleet-wide tag pool inline (union of every device + worker's tags) and passes it via the new `tagSuggestions` prop. `TagsEditDialog` rebased onto the shared component so future chip-input changes apply to both consumers in one place.

- [x] **26** *(1.7.0-dev.16)* — green CI on develop after dev.15. Two failures: (a) `tests/test_ui_api.py::test_get_settings_returns_defaults_on_fresh_boot` was missing `date_format: "auto"` in the asserted dict — the field landed in dev.10 (#5) but the test expectation wasn't updated, so dev.15 went red. (b) the `UI-3 explicit any type` invariant was matching English prose in `WorkersTab.tsx` comments (`"OR-logic: any selected tag matches"`, `"no worker has any tag yet"`) because the regex `:[[:space:]]*any` happens inside human sentences too. Rephrased both comments — the regex itself is intentionally simple and cheap; tightening it to skip line-comments adds risk without much win when the offending phrasing is easy to dodge. 

- [x] **#96** *(1.7.0-dev.22)* — was open as "27" in the local sequence: ConnectWorker dialog autocomplete pool was the union of device + worker tags, which surfaced device tags (`kitchen`, `cosy`, `ratgdo`) as suggestions when configuring a *worker*. Narrowed to worker tags only — same source the routing-rule builder's worker side reads. Single inline change in `App.tsx`'s `tagSuggestions` prop. (Renumbered to #96 to keep the bug index globally monotonic per CLAUDE.md.)

- [x] **#95** *(1.7.0-dev.21)* — routing rules silently bypassed for any device whose rule has at least one eligible worker online. Symptom (reported live against hass-4): rule "Garage Doors on Windows" set `device_match: any_of [ratgdo]` + `worker_match: all_of [windows]`, but `garage-door-big.yaml` ran on macdaddy (macos) and `garage-door-small.yaml` ran on local-worker (debian). Root cause: `JobQueue.claim_next` only filtered out BLOCKED jobs and respected pinning — it never checked per-worker eligibility for PENDING jobs. The fleet-wide `re_evaluate_routing` sweep correctly held a job PENDING when *any* online worker satisfied the rule (OPTIPLEX-7 in this case), but every other worker was free to claim. Fix: new `is_eligible` callback parameter on `claim_next` (`ha-addon/server/job_queue.py`); new `build_claim_eligibility(app, worker_tags)` builder in `ha-addon/server/routing_eligibility.py` returns a closure with a per-call target-meta cache; `api.py`'s `claim_next` handler builds the predicate from the registry's per-worker tag list and passes it through. Pinned jobs bypass the predicate (pinning is the user's explicit override; mismatched pin + rule is a user-visible conflict, not silent stranding). 4 new unit tests in `tests/test_block_state.py` (predicate accepts/rejects/pin-bypass/back-compat-when-omitted); 4 new tests in `tests/test_routing_eligibility.py` (no-rules short-circuit, ineligible vs eligible worker, per-device routing_extra honoured, target-meta cache hit). Bug number jumped to #95 to keep the bug index globally monotonic per CLAUDE.md — the local 1.7.0 sequence (1–26 + the open #27 placeholder) and the archive's pre-existing #28–#94 had collided otherwise.

- [x] **#99** *(1.7.0-dev.24)* — Queue tab's worker-selection reason was misleading when routing rules narrowed eligibility to a single worker. Symptom (live on hass-4 after #98 deployed): garage-door jobs claimed by OPTIPLEX-7 (the only windows-tagged worker) carried `selection_reason="first_available"` ("First to poll"), making it look like OPTIPLEX-7 won a race against equally-eligible peers. Reality: the routing rule disqualified every other worker, so there was no race — OPTIPLEX-7 was the *only* eligible candidate. Fix: track `eligible_other_candidate_count` separately from `other_candidate_count` in api.py's deferral loop (the eligibility filter from #98 already provides the signal). When `other_candidate_count > 0` but `eligible_other_candidate_count` is `0` and we have eligible PENDING work, the reason becomes the new `"only_eligible_worker"` ("Only eligible by tag") — distinct from `"only_online_worker"` ("Only worker online") because *other* workers were online; the rule, not their absence, narrowed the field. UI mapping in `utils/selectionReason.ts` adds the new pill + tooltip; type-doc strings in `types/index.ts` and `job_queue.py` updated. The `"first_available"` label still applies when multiple workers are equally eligible — which now means equally-eligible-after-routing.

- [x] **#98** *(1.7.0-dev.23)* — routing-rule stalemate when the per-worker eligibility check (#95) leaves a higher-perf worker ineligible. Reproduced live against hass-4 after #95 deployed: a `garage-door-big.yaml` (ratgdo-tagged) compile sat PENDING for ~5+ min with no claim. Root cause: api.py's `should_defer` rule 2 ("defer to a worker with higher effective score") didn't know about routing eligibility. OPTIPLEX-7 (windows, the only eligible worker) polled, saw macdaddy/AI-MacBook-Pro had higher perf scores, and deferred. Those macos workers polled, hit the per-worker eligibility check from #95, returned None. Stalemate — no progress until something changes the worker pool. Fix: pre-compute the set of PENDING jobs *I* am eligible for, then in the deferral loop only count an "other" worker as a real alternative when they're eligible for at least one of those same jobs. A faster-but-ineligible worker is no longer treated as a deferral candidate. New regression test in `tests/test_routing_eligibility.py::test_eligibility_isolates_stalemate_when_only_one_worker_qualifies` pins the predicate-side contract; the api.py wiring is exercised by the matrix's live ratgdo round-trip on hass-4.

- [x] **#100** *(1.7.0-dev.25 → dev.26)* — Queue tab Routing column mirrors the Upgrade modal's worker-selection radio so the user can read each job's routing intent off the table. dev.26 reworded the column's leading comment to dodge the `UI-3 explicit any type` invariant's regex (a `: any` pair inside English prose tripped the same grep that bug #26 already documented in dev.16). The existing Worker column shows where the job *landed* (assigned hostname); the new column inserted right after it shows what the user *asked for*: "Any worker" (muted italic) when neither pin nor tag-filter is set, "<Pin> Specific worker" when `pinned_client_id` is set (Pin icon matches the Worker column's existing affordance + the modal's pinned semantics), or `{op-label} <TagChips>` when `worker_tag_filter` is set — same `TagChips` component and djb2-hash colors as the Devices/Workers/RoutingRules surfaces, so a `linux` chip on a Queue row visually matches a `linux` chip on the Workers tab. Op renders lowercase ("all of" / "any of" / "none of") to match the casing used across the Routing-rule builder UI. Tooltip on each variant explains the scheduling implication. Sort key is the canonical `0-any < 1-tag:op:tags < 2-specific` so alphanumeric sorting groups by intent type. Implementation: single column-def in `ha-addon/ui/src/components/QueueTab.tsx` (+`TagChips` import); no API or schema changes — the data already arrives via `Job.pinned_client_id` / `Job.worker_tag_filter` (#97). All 146 mocked Playwright specs still green.

- [x] **#97** *(1.7.0-dev.22)* — was open as "29" in the local sequence: Upgrade modal worker-selection radio with three modes — any worker (default; routing rules + scheduler decide), specific worker (legacy single-worker pin), or tag expression (`all_of`/`any_of`/`none_of` clause against the fleet's worker tag pool). Backend: `Job.worker_tag_filter: dict | None` (shape `{op, tags}` matching a routing-rule clause), serialised through `to_dict`/`from_dict`, plumbed through `JobQueue.enqueue`, and honoured by `routing_eligibility.build_claim_eligibility` — the per-worker predicate that drives the bug #95 claim_next check rejects a worker whose tags don't satisfy the filter (malformed clauses fall through as "no constraint" so a typo doesn't strand a job). API: `/ui/api/compile` accepts `worker_tag_filter` in the request body, validates op + non-empty cleaned tag list before passing through. UI: `RoutingClauseOp` type re-exported on `Job`; `compileTargets` API client takes a 5th `workerTagFilter` arg; `App.handleUpgradeConfirm` plumbs it through and surfaces the chosen filter in the success toast (`(workers any of [linux, macos])`). UpgradeModal's Worker section is now a 3-way radio with conditional sub-controls: the Specific dropdown only appears when "Specific" is selected, the op-dropdown + chip-input clause builder only when "Tag expression" is selected. Confirm button disables until "Specific" has a worker / "Tag expression" has at least one tag. The chip-input pulls autocomplete from the worker-tag pool only (matches RoutingRuleBuilder's worker side). 3 new unit tests in `tests/test_routing_eligibility.py` (filter honoured, every op behaves correctly, malformed filter is permissive). All 146 mocked Playwright specs pass against the rebuilt UI.

- [x] **#101** *(1.7.0-dev.30)* — Queue tab Routing column folded into the existing "Worker selection" column (the two answer related questions: *what* the user asked for vs *why* this worker won — reading them as one cell tells the routing story without bouncing eyes between columns). Top line shows the routing intent — small "📌 specific worker" when `pinned_client_id` is set, `{op-label} <TagChips>` (e.g. `any of [linux]`) when `worker_tag_filter` is set, nothing for the unconstrained default so default rows don't grow a placeholder line. Bottom line keeps the existing selection-reason label (Pinned / Only eligible / Least busy / Fastest / First to poll) with its hover tooltip. Cell falls back to "—" when neither line has content. The standalone Routing column is gone; the Worker column (where the job *landed*) is unchanged. Implementation: single column-def edit in `ha-addon/ui/src/components/QueueTab.tsx`; `formatSelectionReason` + `TagChips` already in scope. All 150 mocked Playwright specs still green.

- [x] **#102** *(1.7.0-dev.30)* — `_device_compile_epoch` was hard-coded to the older `"%b %d %Y, %H:%M:%S"` shape ("Mar 29 2026, 17:00:00") that the dev.18 fix-comment for bug #13 referenced, but `aioesphomeapi` actually reports the build time as `"2026-04-23 06:13:56 -0700"` (ISO with timezone offset). Result: the parser silently returned `None` for every device, the SQLite-history fallback path never produced a `~time` value, and any target without server-side compile history showed a bare "—" in the Devices tab "Last compiled" column even when the device was clearly running compiled firmware. Fix: extracted the parser to a module-level `_parse_device_compile_epoch()` in `ui_api.py`, try the modern format first then the older comma-form for back-compat. Tz-aware parses produce a UTC-correct epoch; tz-naive parses fall back to server-local time as before. Updated the `Device.compilation_time` docstring to use a current-format example. 3 new unit tests in `tests/test_ui_api.py`: modern format with offset round-trips correctly, old format still parses, unknown shapes / `None` / empty / ISO-with-`Z` (unsupported by `%z` in 3.11+) all return `None` without raising.

- [x] **#103** *(1.7.0-dev.30)* — "Commit all uncommitted" entry added to the Devices tab Actions dropdown. When the user has edited YAMLs outside the addon (CLI, file share, another editor) the working tree fills with dirty files; the per-row hamburger commits one at a time which is tedious for a fleet-wide cleanup. The new item filters `targets` for `has_uncommitted_changes`, fans out via `Promise.all(commitFile(t))` (mirroring the pattern already used by SettingsDrawer's auto-commit-onboarding flow), and surfaces a single summary toast — `Committed N`, `Committed N, M failed`, or `No files committed`. Disabled with an explanatory tooltip when there's nothing dirty (`No uncommitted YAML changes in the config directory`); hidden entirely when versioning is off. The label includes a live count `(N)` so the user sees how much is queued before clicking. SWR `onRefresh` runs on completion so the per-row "config changed" indicators clear in the same tick.

- [x] **#104** *(1.7.0-dev.33)* — Workers tab Tags column moved to position 2 (right after Hostname), mirroring the Devices tab's #16 layout. Tags drive routing rules + bulk operations; the eye sees them faster as the second column than buried six positions in. Implementation: reorder `HEADER_ORDER` in `WorkersTab.tsx` and the matching `<td>` cells in both the single-slot and multi-slot row paths (TanStack supplies the headers via the array; cells are hand-written, so the two have to stay in lockstep). Column-defs themselves untouched — the order array is what drives the visible column sequence. All 150 mocked Playwright specs still green; existing `routing-rules.spec.ts` `Tags for build-server-1` selector is position-agnostic so no spec churn.

- [x] **#105** *(1.7.0-dev.33)* — the dev.30 commit for #103 only included the source change in `DeviceTableActions.tsx`; I forgot the matching rebuilt UI bundle under `ha-addon/server/static/assets/`. The Dockerfile only `COPY server/ .`'s the pre-built bundle (no `npm run build` inside the image), so every deploy after dev.30 carried the old bundle that doesn't have the new menu item. Verified by greping the deployed bundle on hass-4 — it has `Commit changes…`, `Edit Tags`, `Schedule Selected`, `Remove Schedule from Selected`, but `Commit all uncommitted` is missing. Fix: rebuild the UI (`npm run build`) and commit the regenerated `static/` files together with the source change going forward. Filing this as a doc note: `dev-plans/RELEASE_CHECKLIST.md` (and the per-turn rhythm) should be loud about "rebuild + commit the static bundle whenever ha-addon/ui/src changes" — it's currently silent.

- [x] **#106** *(1.7.0-dev.34)* — "Commit all uncommitted" now opens a shadcn `Dialog` with an optional commit-message input before fanning out the per-target commits. Same shape as the per-row `Commit changes…` dialog in `App.tsx` (label, placeholder, default-fallback copy) so the two surfaces feel identical. Empty input → `undefined` → server falls back to the default `Manually committed from UI` marker for every file in the batch; non-empty → that subject is applied to every commit so the resulting git history reads as one logical operation. Dialog open + entered-message state owned in `DeviceTableActions`; the existing fan-out loop in `handleCommitAll` is unchanged except for the `message` arg passed through to `commitFile`. Cancel during in-flight commit is suppressed (`onOpenChange` is gated on `commitAllBusy`) so a half-fanned-out batch can't be orphaned.

- [x] **#107** *(1.7.0-dev.34)* — All four bulk-upgrade dropdown items (`Upgrade All` / `Upgrade All Online` / `Upgrade Outdated` / `Upgrade Selected`) now route through the same `UpgradeModal` the per-row Upgrade button uses, so the user picks worker / ESPHome version / action (Upgrade Now, Download, Schedule Recurring, Schedule Once) up-front and the chosen options apply uniformly across the affected device set. Implementation: `App.tsx`'s `upgradeModalTarget` state shape changed from a single `target: string` to `targets: string[]`; new `handleOpenUpgradeModalMany(targets, displayName, defaultMode)` callback opens the modal for a multi-device set; existing `handleOpenUpgradeModal` wraps a single target into a 1-element array. `handleUpgradeConfirm` now POSTs the entire target list in one `compile()` call so the server-side `Queued N device(s)` toast reads correctly. Schedule saves (`onSaveSchedule` / `onSaveOnce`) fan out across the target list via `Promise.all` of `setTargetSchedule` / `setTargetScheduleOnce`. `DevicesTab` gets a new `onUpgradeMany` prop; the four menu items materialise their target lists (`all`, `online` not `false`, `needs_update`, `selectedTargets`) and call into it with a human-readable `displayName` (`"all 12 devices"`, `"5 outdated devices"`, etc.) that surfaces in the modal title and the success toast. Disabled-state on each item now reflects whether its set is non-empty (Upgrade Outdated stays disabled when nothing's outdated; Upgrade Selected stays disabled when no rows are checked). The `UpgradeModal` itself needed no changes — when `t` is `undefined` (multi-target case) the existing `pinnedVersion`/`currentSchedule`/`currentOnce` props go undefined and the modal's `shouldUpdatePin` and `hasExistingSchedule` derivations flip false, suppressing the pin warning + the "Remove existing schedule" button without any new flag. Pin update (`params.updatePin`) is gated on `targets.length` being `1` in `handleUpgradeConfirm` so a multi-device upgrade never touches a per-device pin. Dead `handleCompile` + `onCompile` plumbing removed from `App.tsx` and `DevicesTab` — every entry point now goes through the modal.

- [x] **#108** *(1.7.0-dev.35)* — Every "redo this job" affordance now reads as **Rerun** in the UI. The historical split (`Rerun` for a successful job vs. `Retry` for a failed/timed-out job) collapsed onto the single verb because the distinction never carried meaning beyond colour — re-running a failed job and re-running a successful job both produce a fresh compile against the current YAML. Per-row buttons in the Queue table + the LogModal share a `<Button>` whose `variant` (`success` / `warn`) still flips with the source-job state, so the colour signals "this row failed" without changing the verb. The bulk dropdown trigger and items move from "Retry" / "Retry All Failed" / "Retry Selected" to "Rerun" / "Rerun All Failed" / "Rerun Selected"; toast text shifts from `Retrying X` to `Rerunning X`. Internal symbol names (`retryJobs`, `handleRetryJobs`, `RetryResponse`, `/ui/api/retry`) stay put because none are user-visible. The standalone `EsphomeInstallBanner`'s "Retry the failed install" button stays as-is — that one really *is* a retry of a transient failure, not a rerun of finished work. The `OTA Retry` job-state badge for `ota_only` jobs likewise stays — it names a specific kind of pending work, not a user action. Updated `e2e/queue-extras.spec.ts` "failed job uses Retry" → "failed job also uses Rerun" so the regression guard for the new label moves with the change.

- [x] **#109** *(1.7.0-dev.35)* — Per-row `Rerun` (Queue tab + LogModal) now opens the unified UpgradeModal pre-populated with the original job's worker / version / action / tag-filter so the user can tweak any field before re-submitting, instead of silently re-enqueuing through `/ui/api/retry`. Single-target callers go through the modal; bulk reruns (`Rerun All Failed`, `Rerun Selected`) keep the immediate `/ui/api/retry` path because there's no single set of params to seed a modal with. Implementation: new optional `seed: { pinnedClientId, workerTagFilter, esphomeVersion, action }` prop on `UpgradeModal` initialises `workerMode` (specific → tag → any in priority order), `selectedWorker`, `tagFilterOp`/`tagFilterTags`, `selectedVersion`, and the action radio (`download-now` vs `upgrade-now`). App.tsx's `upgradeModalTarget` state shape gains an optional `seed` field; new `rerunSingleJobViaModal(job: Job)` helper builds the seed from the job's existing fields and opens the modal targeting `[job.target]`. `handleRetryJobs` short-circuits to the modal path when `ids.length` is `1`. The standard `compile()` flow handles stale-job cleanup automatically (its `enqueue` call already removes terminal jobs for the same target), so the original failed/successful row gets pruned the same way `/ui/api/retry` would.

- [x] **#110** *(1.7.0-dev.35)* — Routing-rule conflict detection in the Upgrade modal: when the user picks a `Specific worker` or a `Tag expression` whose chosen workers don't satisfy a global routing rule that fires for the affected target(s), the modal surfaces a warning panel listing the offending rule names. Clicking the now-relabelled `Upgrade & override rules` confirm button sends `bypass_routing_rules: true` to `/ui/api/compile` so the server enqueues the job anyway; the user's pin / tag-filter is still honoured because that's their explicit constraint, not the rule's. Implementation: new `bypass_routing_rules: bool = False` field on the server-side `Job` dataclass (round-tripped through `to_dict` / `from_dict` for queue persistence; tolerant load reads pre-#110 queue.json files as `False`). `JobQueue.enqueue` accepts the flag and stamps it onto new jobs (and forwards it onto follow-up coalesced jobs). `routing_eligibility.re_evaluate_routing`'s `check` callback returns `(True, None)` early when the flag is set so a conflicting job never goes BLOCKED. `routing_eligibility.build_claim_eligibility`'s per-worker predicate likewise returns True early when the flag is set, but still rejects workers that fail the user's `worker_tag_filter` (the user's other explicit constraint). `/ui/api/compile` accepts `bypass_routing_rules: bool` in the body and plumbs it through. Client side: `compile()` API gains an optional 6th `bypassRoutingRules?: boolean` arg; the UpgradeModal fetches active routing rules via SWR (only loaded while the modal is open), computes conflicts client-side using the existing `utils/routing.evaluateClause` mirror, and surfaces a warning panel + relabelled confirm button when a conflict exists. App.tsx materialises per-target tag lists from the affected `Target.tags` strings and passes them as `affectedTargetTags` so the modal can detect device-tag matches without a server round-trip. Tests: 3 new unit tests in `tests/test_routing_eligibility.py` cover bypass-clears-block, bypass-lets-ineligible-worker-claim (with the worker-tag-filter still enforced), and round-trip persistence; 4 new mocked Playwright specs in `e2e/rerun-and-routing-override.spec.ts` cover the rerun-opens-modal flow (#109) plus the conflict-warning + bypass-flag round-trip and the no-conflict-no-warning case (#110). The conflict detector handles both `specific worker` (compare worker tags directly) and `tag expression` (find any online worker that satisfies both the user's filter and the rule's worker_match) modes; the latter falls back to the live worker pool when the user's filter shape isn't a strict superset of the rule's clause. Per-device `routing_extra` from device YAML metadata is honoured server-side via the existing eligibility cache; client-side conflict detection focuses on global rules for now (the common case).


- [x] **#114** *(1.7.0-dev.62)* — covered by **#220**'s expanded #214 self-heal. The fresh repro arrived in a 21-failure home-lab batch on 2026-05-01 (`AI-MacBook-Pro.localdomain` + 3 other workers): the `esptool is not a package` signature shipped alongside `Missing framework-arduinoespressif32-libs package`, `tool-esptoolpy does not appear to be a Python project`, and `/penv/bin/esptool: not found` → `Error 127` — every one of them a different facet of the same broken `pio-slot-N/` tree, none of them caught by #214's narrow cc1-only matcher. The original "force-reinstall would risk other failure modes" disposition holds, but the right action for *all* of these symptoms is the same one #214 already implemented for cc1 — wipe the corrupted subtrees and let PIO re-bootstrap. #220 broadens both the signature pattern set and the wipe target (now includes `penv/`, where `bin/esptool` actually lives), so the next occurrence self-heals on retry without operator intervention.

- [x] **#115** *(1.7.0 — workarounds shipped, upstream tracking only)* — Upstream ESPHome bugs surfaced (and worked around) by #111's investigation, tracked here so future readers know they're known-and-deferred-to-upstream:
  1. **`git.clone_or_update` (`esphome/git.py:96`) is not safe under concurrent invocation.** Multiple processes computing the same `<data_dir>/<domain>/<sha8>/` destination race with no file lock — losers see partial-state trees and surface as `Could not find components folder for source`, `<file> does not exist in repository`, or arbitrary `AssertionError` from incomplete merges. Our #111 fix (server-wide `asyncio.Lock`) avoids the race by serialising bundle subprocesses; the underlying ESPHome assumption is "single-process invocation," which their CLI honours but our parallel-claim server doesn't. Right answer at the upstream level is to add an `fcntl.flock` (or equivalent) inside `clone_or_update`. File as an `esphome/esphome` issue with our concrete repro; our fix doesn't depend on it landing.
  2. **`esphome.core.config._is_target_platform` does not catch `AssertionError`.** It catches `KeyError` and `ImportError`, but `get_component(name, True)` opens with `assert "." not in domain` (`esphome/loader.py:222`). Any caller that passes a dotted name through this path crashes with a stack trace instead of a clean "Platform missing" error. We hit this in #111's race aftermath (a partial packages-pass left `Kauf.PLF12` in the top-level config); the lock fix prevents the partial-merge state, but a future call site could trigger the same assertion. Right answer at the upstream level is to widen the `except` to include `AssertionError`, or to not assert at all — let `_lookup_module` return `None` for invalid module names. File upstream alongside (1).
  3. **The `2026.4.3` "Including a single package under \`packages:\` … is deprecated" warning is a false positive for the dict-of-strings form** (`packages: { Name: github://... }`). Already fixed upstream by PR #15605 (`8790dec13` — "Fix false deprecation warning and wrong error paths in nested packages") but not in `2026.4.3` itself. Auto-resolves on the next ESPHome point release that includes the fix; #112's logger silencing prevents the warning from polluting our captured stderr in the meantime so users never see it from us.

- [x] **#113** *(1.7.0-dev.39)* — Rendered-config modal showed the YAML buried between ESPHome's `INFO` / `WARNING` log lines. "Before" the YAML: `INFO ESPHome 2026.4.3`, `INFO Reading configuration...`, any `WARNING GPIO# is a strapping PIN...` from validation. "After" the YAML: a trailing `INFO Configuration is valid!` line. None of those belong in a "rendered config" view — users want the YAML and only the YAML. Root cause: `ui_api.get_rendered_config` ran `esphome config <yaml>` with `stderr=asyncio.subprocess.STDOUT`, merging the streams, then passed the combined captured bytes through to the modal as `output`. ESPHome's `command_config` (`esphome/__main__.py:672`) writes the rendered YAML to stdout via `safe_print` and writes its `_LOGGER.info(...)` chatter to stderr; the merge collapsed that separation. Fix: capture stdout and stderr separately. On success, `output = stdout` (clean YAML, including the existing `\033[5m...\033[6m` ANSI markers around `password:`/`key:`/`psk:`/`ssid:` values that the modal already renders). On failure, `output = stderr + stdout` because the validation error message lives in stderr and the actionable diagnostic context is what users want there. Cache key + secret-redaction logging contract unchanged. Verified live against `cyd-office-info.yaml` on hass-4 — pre-fix output started with five `INFO`/`WARNING` lines, post-fix output starts with `substitutions:` (the actual rendered YAML's first key).

- [x] 116 *(1.7.0-dev.45)* — `esphome_fleet.compile` and `esphome_fleet.validate` services now accept a `tags` list + optional `tags_op` matcher (`any_of` default; `all_of` / `none_of` available — same vocabulary the routing engine and per-job worker_tag_filter already use). Compile resolves the expression to every matching managed device's YAML filename via the coordinator's `/ui/api/targets` snapshot; validate enforces single-target match (multi-match raises `tags_match_multiple`, zero-match raises `no_targets_match_tags`). Resolution precedence: device picker > tags > targets. Archived rows are skipped. UI surface: new fields exposed in `services.yaml` so the action editor renders `tags` (object) + `tags_op` (select). Tests: `_resolve_tags_to_targets` unit tests cover each operator + archived-skip + token trim + empty-input edge cases; handler tests cover compile multi-match enqueue, validate single-match, multi-match rejection, zero-match rejection, and device-picker-wins precedence; schema tests pin the new optional fields + reject unknown ops.

- [x] 117 *(1.7.0-dev.45)* — Devices-tab Upgrade dropdown gets an `Upgrade Changed` item between `Upgrade Outdated` and the separator. Filters via the existing `hasDriftedConfig(t)` helper — same source-of-truth the per-row "config changed" badge and Upgrade-button color use, so the menu picks exactly the set the user already sees marked. Distinct from `Upgrade Outdated` (firmware-version mismatch); this targets configuration drift (`config_drifted_since_flash || config_modified` fallback). Disabled when no targets carry drift. Modal title pluralises ("1 changed device" / "5 changed devices"). E2E: two new specs in `bulk-upgrade.spec.ts` cover the disabled-empty case and the open-with-drifted-only-count flow.

- [x] **#197** *(1.7.0-dev.46)* — every UI-driven file op must leave the git working tree clean. New `tests/test_git_clean_after_ops.py` drives 12 endpoint scenarios (save / create-staged + save / archive / permanent-delete / archive-restore / delete-from-archive / rename / pin / unpin / meta / schedule-set / schedule-clear / secrets-edit) plus a negative control, drains debounced commits, and asserts `dirty_paths(config_dir)` returns an empty set afterward. While building it, the archive-then-`git status` scenario surfaced a real dangling-files bug: `archive_and_commit` ran `git mv` correctly but its `_staged_paths()` filter used `git diff --cached --name-only`, which collapses a rename to just the destination — so the subsequent `git commit -- <pathspecs>` ran with only the dest path, committing the addition while leaving the staged deletion of the source dangling in the index. Fixed by switching `_staged_paths()` to `--name-status` and parsing the `R100\tsrc\tdst` form so both halves of every rename get included; archive / restore commits now land atomically. New CLAUDE.md invariant **PY-11** documents the rule + points at the test file as the regression net for any future file-mutating endpoint. Origin: user-reported "we leave dangling files open in Git, examples: secrets, archive moves" — same bug class as #94.

- [x] **#198** *(1.7.0-dev.47)* — firmware artifact retention. New `firmware_retention_days` Settings field (default `2`, range `0..3650`, `0` = unlimited) drives the new `firmware_storage.enforce_retention` helper. The existing `firmware_budget_enforcer` task is renamed to `firmware_retention_enforcer` (`main.py`) and now runs two passes per 30-min tick: (1) time-based — delete every `.bin` whose mtime is older than the configured retention window, with only live-queue jobs protected (history-protected binaries are deliberately NOT exempt — that's the whole point); (2) budget-based hard ceiling (the prior `enforce_budget` flow), where both live-queue + history are protected. The Settings drawer's "Disk management" section gains a matching `Firmware retention (days)` IntRow alongside `firmware_cache_max_gb`. Pair fix: `JobHistoryDAO.evict_older_than` now returns the evicted row IDs (was `int`) so the JH retention task in `main.py` can call `delete_firmware(jid)` on each evicted row — pre-fix the binary lingered on disk until the next add-on restart's `reconcile_orphans` sweep, and the JH retention WARNING reported "evicted N rows" while disk usage didn't actually drop. Tests: 6 new in `tests/test_firmware_storage.py` (`enforce_retention` evicts past-cutoff files / protects live-queue jobs / evicts history-protected binaries / no-op on days≤0 / no-op on missing dir / per-variant independence on the same job); 2 updated in `tests/test_job_history.py` for the new `list[str]` return shape; `tests/test_settings.py::test_settings_as_dict_round_trips` + `tests/test_ui_api.py::test_get_settings_returns_defaults_on_fresh_boot` + `tests/test_api_contract.py` updated for the new field. UX: History tab's per-row Download already handles evicted binaries gracefully (`list_variants` returns `[]` → no Download chip rendered) — no UI change needed; users who want a >2-day-old build hit Compile / Rerun, same as after a fresh restore. Pairs with #199.

- [x] **#199** *(1.7.0-dev.46)* — added `firmware` to `ha-addon/config.yaml`'s `backup_exclude:` block alongside `esphome-versions`, with a code comment pointing at this bug. Same reasoning #45 used: firmware binaries are large, weakly-compressible (ESP `.bin` is already compressed → ~30% tar.gz ratio), and **regenerable from the YAML** that's already backed up via the HA core `/config` snapshot path. On hass-4 today the partial-addon backup `ESPHome Distributed Build Server` is **237 MB**, ~100% of which is `/data/firmware/` compressed. Job-history metadata (device, version, timestamp, log, success/fail, selection reason) stays in `/data/job-history.db` (1.7 MB) and continues to be backed up — the user keeps the *record* of every build, just not the artifact. Drops every ESPHome Fleet partial backup from ~237 MB → ~2 MB and shaves the same off every full HA snapshot. Pairs with #198 — once retention caps the on-disk footprint at "≤ 2 days of compiles," the backup-exclude is belt-and-suspenders that also covers users who set retention to 0 (unlimited) or who run a heavy multi-day batch right before a snapshot. Origin: user-reported backup-size investigation 2026-04-29 — `/data/esphome-versions` (14 GB) is already excluded, so firmware/ was the only remaining contributor.

- [x] **#200** *(1.7.0-dev.52)* — `InstallToAddressModal` rebuilt around shadcn's structural slots so it matches the commit-message dialog's layout. Title alone in `DialogHeader` (with the `--surface2` background it's meant to have); the body — description text, OTA-address Label+Input, the auto-resolved hint / invalid-address message, and the override-confirm warning — sits in one `<div className="px-4 py-3 flex flex-col gap-2 text-sm text-[var(--text)]">` block (was previously split across two siblings using `px-1 pb-2` padding that didn't match anything else); Cancel + Install live in a `DialogFooter` (Cancel wrapped in `DialogClose` so Esc / overlay-click stays consistent with the rest of the surface). Dropped the inline `style={{ maxWidth: 480 }}` since `DialogContent` already caps at 480px by default, and dropped `DialogDescription` because the body's leading `<p>` carries the same text in the same style as the commit dialog. No behaviour change; the validation gate, single-step-when-unedited install, and two-step confirm-on-edited-address paths are byte-identical. Existing `e2e/install-to-address.spec.ts` (4 tests) all still green against the rebuilt JSX.

- [x] **#201** *(1.7.0-dev.52)* — tag editing is now disabled for archived devices on every surface that exposed it. Per-row Tags-cell button in `useDeviceColumns.tsx` becomes a read-only `<span>` for archived rows (matching the "—" empty-state instead of "+ tags" so the cell reads as locked, with a tooltip explaining "Unarchive first to edit"); the per-row hamburger never offered Edit Tags so it's already correct (it collapses to Unarchive + Permanently delete). Bulk "Edit Tags…" in `DeviceTableActions` filters its selection set to non-archived rows: when every selected row is archived the menu item disables with the tooltip "Tags can't be edited on archived devices"; mixed selections proceed against the active subset and the success toast appends "(N archived skipped)" so the user knows the archived rows weren't touched. The `BulkTagsEditDialog`'s count + tag-aggregate (common/partial) reflects the editable subset only, so the dialog shows what we'll actually write rather than a misleading intersection that includes tags from rows we're about to skip. Same reasoning the per-row hamburger uses (DM.1 in #3): writes to YAMLs that live in `.archive/` are ignored by the active fleet, so making them appear editable is dishonest UX.

- [x] **#202** *(1.7.0-dev.53)* — CI's `test` job had been red on every push since `tests/test_git_clean_after_ops.py` landed in dev.46 (#197) — `ModuleNotFoundError: No module named 'tests'` at collection time. The file's `from tests.test_ui_api import _UiApp, _make_ui_app, _write_config` only resolves when `tests/` is importable as a package (i.e., has an `__init__.py`), which it isn't — pytest's default `--import-mode=prepend` adds each test's directory to sys.path so siblings are importable without the prefix (every other cross-test import in the suite uses `from test_ui_api import …` accordingly). 1-line fix: drop the `tests.` prefix to match the rest of the suite. Discovered while running `gh run list` after the dev.52 push and seeing the red CI run had already been failing for 7 commits. No production code change.

- [x] **#203** *(1.7.0-dev.54)* — archived devices now retain every attribute the scanner can pull from raw YAML, so the row keeps its tags / area / project / comment / pinned_version / schedule / network / chip / BLE-proxy after archive. The DM.1 archive merge in `ui_api.get_targets` was instantiating archived rows with every attribute hardcoded to `None` / `False` / `"off"`; new `scanner.get_archived_device_metadata(config_dir, filename)` re-reads `<config_dir>/.archive/<filename>` via the existing `read_device_meta` (comment-block path) + `_load_raw_yaml` + `_fill_missing_metadata` (raw YAML path), skipping the expensive `_resolve_esphome_config` because archived files may legitimately reference deleted secrets / packages. Devices-tab `tagPool` in `DevicesTab.tsx` now includes archived rows' tags only when `showArchived` is true (otherwise the pill bar would offer tags that filter no visible row). 4 new unit tests in `tests/test_scanner.py` cover the empty-shape fallback, tags+area+project preservation, schedule preservation, and graceful handling of unparseable YAML.

- [x] ~~**#204**~~ SUPERSEDED-by-#214 *(landed 1.7.0-dev.54 / reverted 1.7.0-dev.59)* — initial fix added a `_strip_quarantine_xattr()` helper hypothesising macOS Gatekeeper was killing PlatformIO's downloaded Xtensa toolchain. The hypothesis is wrong: the affected workers run our published Linux Docker image, so `sys.platform` is not `"darwin"` inside the container and the helper never fires. The xattr code was inert in the actual reproduction environment. Removed in dev.59; real cause + fix tracked under #214.

- [x] **#205** *(1.7.0-dev.54)* — `PingDeviceModal` rebuilt around shadcn's structural slots so it matches the same layout treatment `InstallToAddressModal` got in #200 / `BulkTagsEditDialog` got in #17. Title alone in `DialogHeader`; the spinner / error / result table + RTT stats sit in one `<div className="px-4 py-3 flex flex-col gap-2 text-sm text-[var(--text)]">` body block (was previously `px-1 py-2` which didn't match anything else, and the result table's `text-[var(--text-muted)]` colour leaked onto the data cells); Copy / Run again / Close anchored in `DialogFooter` (Close wrapped in `DialogClose` so Esc / overlay-click stays consistent). Dropped the inline `style={{ maxWidth: 480 }}` since `DialogContent` already caps at 480px. Existing `e2e/ping-device.spec.ts` still passes against the rebuilt JSX.

- [x] **#206** *(1.7.0-dev.54)* — ping diagnostic always failed on HAOS instances. Root cause: HAOS ships kernel default `net.ipv4.ping_group_range = 1 0` (an empty range — unprivileged datagram ICMP is *disabled*), so `icmplib.async_ping(privileged=False)` raised `SocketPermissionError` immediately; the addon container also lacked `CAP_NET_RAW` so the `privileged=True` raw-socket path wasn't available either. Two-part fix: (a) `ha-addon/config.yaml` now declares `privileged: [NET_RAW]` so the container can open raw ICMP sockets (AppArmor profile is already permissive on `network`/`capability`, no change needed), and (b) both ping helpers (`/ui/api/targets/<f>/ping` in `ui_api.py` and `device_poller._ping_device`) try unprivileged datagram ICMP first and fall back to the raw-socket path on `SocketPermissionError`. Standalone Docker deployments without `NET_RAW` keep working as long as the host's `ping_group_range` allows it (the unprivileged path); HAOS now works via the raw-socket fallback. New live-HA spec in `e2e-hass-4/ping-device.spec.ts` with two tests: one POSTs `/ui/api/targets/cyd-office-info.yaml/ping` and asserts the response shape is populated (target / address / packets_sent=10 / numeric RTTs / boolean is_alive — without asserting *whether* the device is reachable, since the device may be off when the smoke runs); the other walks through the UI (hamburger → Ping device → modal opens → result section appears) and asserts the modal does *not* render "Ping failed".

- [x] **#207** *(1.7.0-dev.54)* — archived devices no longer show "Checking…" indefinitely; the Status column renders "Archived" with a muted dot. `useDeviceColumns.tsx` short-circuits the status accessor with `'e-archived'` (sorts past every live state) and the cell renderer with `<StatusDot status="archived" />` before the activeJob / online / null / online-true / online-false fall-through. New `'archived'` member on the `Status` type in `StatusDot.tsx`, with a muted `text-[var(--text-muted)]` colour so the row reads as inert at a glance. New `.dot-archived` class in `theme.css` (grey + 0.55 opacity, no animation — distinguishes archived from offline by being clearly de-emphasised rather than identical-coloured). The lastSeenEl date suffix is omitted on archived rows since the row is inert; the Tags-column archived chip already carries the relative archived-at timestamp.

- [x] **#208** *(1.7.0-dev.54)* — bulk "Archive Selected" / "Unarchive Selected" items added to the Devices Action menu. `DeviceTableActions.tsx` derives `allSelectedActive` (every checked row is non-archived) and `allSelectedArchived` (every checked row is archived) from the selected target objects; "Archive Selected" enables only when `allSelectedActive` is true, "Unarchive Selected" only when `allSelectedArchived`. Mixed selections, all-archived selections (for Archive), and all-active selections (for Unarchive) leave the matching item disabled with an explanatory tooltip — there's no single sensible action for a mixed set and the user shouldn't have to guess which subset would be touched. Both handlers fan out via `Promise.all` and surface one summary toast (per the project's "batch operations get one toast" rule). Wired through the existing `deleteTarget(filename, archive=true)` and `restoreArchivedConfig(filename)` API client functions — no new server endpoint, no schema change.

- [x] **#209** *(1.7.0-dev.55)* — Queue rows now expose only Rerun + Clear inline; everything else (Cancel, View log, Edit YAML, Download firmware variants, plus the full Devices-tab Device-section actions: Live Logs, Compile history, Restart, Copy API Key, Ping device, Install to address) lives in a per-row hamburger menu. The Device section is rendered via a new shared `DeviceMenuSection.tsx` component that both the Devices-tab `DeviceContextMenu` and the Queue-tab `QueueRowMenu` consume — DRY by construction so the two surfaces can't drift. The Config-section actions (Pin/Rename/Duplicate/Config history/Rendered config/Commit/Archive/Delete) deliberately stay Devices-tab-only — Queue rows are compile attempts, not edit surfaces, and config-mutating actions belong where users land to make changes. Hamburger open state is lifted to the QueueTab parent (keyed by job id) so the 1 Hz SWR poll can't tear it down mid-click — same pattern #2 / #71 use. Existing `e2e/compile-download.spec.ts`, `e2e/history-diff-since-compile.spec.ts`, and `e2e/modal-sizing.spec.ts` updated to drive the new menu shape (open hamburger → click "View log" / Download variant items); 187 mocked e2e tests stay green.

- [x] **#210** *(1.7.0-dev.55)* — `selection_reason="only_eligible_worker"` ("Only eligible by tag" in the UI) was firing on jobs that weren't actually tag-targeted. Root cause in `api.py` claim handler: the busy-skip (`if not other_free: continue`) ran *before* the tag-eligibility check, so a fully-booked competitor — eligible by tag, just out of slots — was indistinguishable from a tag-disqualified one. Fix splits the count into `eligible_other_count_any` (tag-eligible regardless of busy state, drives the `only_eligible_worker` reason hint) and `eligible_other_free_count` (eligible AND free, drives the deferral logic). Now a fleet where every other worker is eligible-but-fully-booked falls through to `first_available` rather than misreporting as "Only eligible by tag". Regression test in `tests/test_api.py::test_busy_other_workers_do_not_trigger_only_eligible_reason`.

- [x] **#211** *(1.7.0-dev.55)* — Config-history drawer rows now offer a Download chip when a successful compile at that commit's `config_hash` still has its firmware binary on disk. New `JobHistoryDAO.latest_firmware_by_hash(target, hashes)` returns `{hash: {job_id, firmware_variants}}` for the subset of commits that have a matching binary (newest job wins on duplicate hashes; binaries evicted by retention surface as `firmware_variants=[]` → row dropped). `/ui/api/files/<file>/history` enriches each entry with optional `firmware_job_id` + `firmware_variants`; `HistoryPanel.CommitRow` renders the existing icon-sized `FirmwareDownloadMenu` (.bin / .bin.gz × factory / ota) when those fields are present and lifts its open state to the panel (`downloadMenuOpenHash` keyed by hash) so SWR refreshes don't tear it down. Two new unit tests in `tests/test_job_history.py` cover the happy path (kept binary surfaces, evicted binary drops, failed jobs ignored, unknown hashes absent) and the empty-input short-circuit.

- [x] **#212** *(1.7.0-dev.55)* — archived devices that compose their `esphome:` block via `packages:` / `<<: !include` (e.g. `athom-plug-3.yaml`) once again render their friendly_name in the Archived view. Pre-fix `scanner.get_archived_device_metadata` only ran the raw permissive YAML loader, which treats `!include` / `packages:` as opaque scalars — the `esphome.friendly_name` lived in the included file and was never read. Fix: try `_resolve_esphome_config` first (which handles packages + includes the same way `esphome compile` does — already memoized by mtime, returns None gracefully on broken references) and fall back to the raw loader only when full resolution fails or leaves the friendly_name / area empty. New `tests/test_scanner.py::test_archived_metadata_resolves_friendly_name_via_packages` regression-pins the fix; the existing `test_archived_metadata_unparseable_yaml_falls_back_to_empty` test guarantees the raw fallback still kicks in for legitimately broken archives.

- [x] **#213** *(1.7.0-dev.56)* — Ping result no longer overflows the modal. The `<table>` layout was squishing the muted label cells into ~1 character per line whenever a long RTT triple stretched the value column, and forcing a horizontal scrollbar on narrow viewports. Replaced the table with a `<dl>` grid (`grid-cols-[max-content_minmax(0,1fr)] gap-x-3 gap-y-0.5`); labels carry `whitespace-nowrap` so they always render on one line, the value column takes the remaining width and lets the RTT triple wrap if needed instead of growing. No information lost; existing `e2e/ping-device.spec.ts` (3 tests) stays green against the rewritten layout.

- [x] **#214** *(1.7.0-dev.59 + 1.7.0-dev.60)* — fixed `cc1: posix_spawnp: No such file or directory` compile failures on home-lab Mac-hosted Docker workers. Verified live by firing **Compile All Online** against hass-4 (65 online targets); pre-fix had 3 cc1 failures on `macdaddy.localdomain`'s `pio-slot-1`, post-fix the worker self-heals and the retry succeeds.

  **Two complementary changes**:

  1. **dev.59 — Clean Cache preserves the toolchain home.** `_clean_build_cache()` now preserves anything starting with `pio-slot-` in addition to ESPHome venvs (#119). Pre-fix, every "Clean Cache" click wiped the worker's `PLATFORMIO_CORE_DIR` (~500 MB toolchain) and forced a 5–10 min re-download on the next compile — the failure mode where a parallel job races a half-extracted toolchain was a side-effect, not the steady-state bug. Inert macOS-only `_strip_quarantine_xattr` helper from #204 is removed (it was a no-op inside Linux Docker workers and a misleading red herring in the codebase).

  2. **dev.60 — worker self-heal.** When a compile fails AND the captured log carries the cc1 / posix_spawnp signature (CMake's "Check for working C compiler … broken" + the literal `cc1: posix_spawnp: No such file or directory` line from gcc), the worker now: (a) dumps the toolchain layout to the log via `_log_toolchain_state()` for triage, (b) wipes `pio_dir/packages/` via the new `_wipe_broken_toolchain()` so PlatformIO re-extracts on the next run, and (c) retries the compile in-process before submitting `failed`. The retry path applies to both the OTA `esphome run` and the download-only `esphome compile` flows. PlatformIO does NOT recover on its own — pre-fix, every job that landed on a worker with a broken toolchain failed identically forever; the operator had to ssh and `rm -rf` the toolchain manually. Now the first job to hit the breakage carries a 5–10 min re-download tax and every subsequent job lands on a fresh toolchain.

  Both changes deploy via the existing in-place `.py` source-update path; no IMAGE_VERSION bump per PY-4. Five new tests in `tests/test_client.py`: `test_clean_build_cache_preserves_esphome_venvs_and_pio_slots` + `test_clean_build_cache_removes_unprotected_dirs` cover (1); `test_wipe_broken_toolchain_removes_packages_dir` + `test_wipe_broken_toolchain_noop_when_packages_missing` + `test_wipe_broken_toolchain_swallows_rmtree_failure` cover (2). Live verification: `Compile All Online` against hass-4 with `macdaddy.localdomain` online had `pio-slot-1` broken pre-fix; post-dev.60 the same fleet should complete with zero cc1 failures (operator can re-confirm via the script at `/tmp/compile-online-check.sh`, which fires the run and greps every failed-job log for the cc1 signature).

- [x] **#215** *(1.7.0-dev.57)* — `UpgradeModal` reordered + decluttered. Sections now read top-to-bottom **Action → Worker → Version** (was Worker → Version → Action), so the verb that drives every other decision lands first. The ESPHome-version surface collapses to a two-radio choice — `Current (<defaultEsphomeVersion>)` and `Other` — matching the same shape Worker and Action use. Picking "Other" unfolds the existing search box + scrollable list + "Show betas" checkbox; picking "Current" hides them entirely, removing ~80 vertical pixels of clutter from the common case where the server-default version is fine. Existing `selectedVersion` state plumbing is unchanged (empty string still means "Latest / use default at compile time"); the new `versionMode` state seeds to `'other'` when the rerun-seed or pinned-version pre-selects a non-default value, so a re-opened modal lands on the picker the user previously interacted with. All 187 mocked Playwright tests stay green against the rewritten layout. 

- [x] **#216** *(1.7.0-dev.63)* — covered by #221's rework. Edit YAML moved out of the Queue-tab hamburger's "Job" group and into the shared `DeviceMenuSection` (rendered at the bottom of the Device list — Live Logs / Compile history / Restart / Copy API Key / Ping / Install to address / **Edit YAML**). The Devices-tab hamburger leaves the new optional `onEdit` prop unset because that tab already has an inline Edit button on every row, so DM2's user expectation is preserved on both surfaces.

- [x] ~~**#217**~~ SUPERSEDED-by-#221 *(1.7.0-dev.63)* — refined into #221: "bring back **Log** as the third inline button, but leave **Cancel** in the hamburger." The user's #221 request was the canonical ask; #217 was the broader earlier draft and is closed in favour of the narrower decision. Result: Queue rows now show three inline actions (Rerun · Clear · Log) plus the hamburger; Cancel stays under "Job" in the hamburger because cancellation is destructive and shouldn't be a one-click target on every in-progress row.

- [x] **#218** *(1.7.0-dev.63)* — UpgradeModal horizontal scrollbar fixed. Each radio row in the Action / Worker / Version sections used `whitespace-nowrap` to keep the short label ("Schedule Recurring", "Tag expression", etc.) from wrapping (#78), but the descriptive `<span>` after the label ("— compile only, no OTA; grab the .bin from the Queue tab", "— scheduler picks at compile time", etc.) was forced onto the same line and could exceed the 600 px modal width — the dialog's `overflow-y-auto` then surfaced a horizontal scrollbar at the bottom. Swapped each label's `whitespace-nowrap` for `flex-wrap`: the input + label-word (always short) stay together, and the descriptive text wraps to a second line if the row would otherwise overflow. Inline comment in the JSX explains the gotcha so a future dev doesn't reintroduce `whitespace-nowrap`. No change to spacing in the common case (text fits, layout identical).

- [x] **#219** *(1.7.0-dev.61)* — workers self-pause when their disk fills up. Live observation on hass-4 turned up `docker-pve` at `49 GiB / 50 GiB / 0 B free`, with `PackageException: Could not install package 'platformio/tool-esptoolpy'` plus `Tool Manager: Warning! Package Mirror: [Errno 28] No space left on device` in 20 of 25 failed jobs in the queue. #214's mid-run toolchain self-heal can't help once the filesystem stops accepting writes — the wipe-and-reinstall hits ENOSPC at the same step. Fix: `Worker.health_blocked_reason` (server side, derived from each heartbeat's `system_info.disk_used_pct` with two-threshold hysteresis — enter at 95 %, exit at 90 %, in `ha-addon/server/constants.py:WORKER_DISK_BLOCK_ENTER_PCT` / `WORKER_DISK_BLOCK_EXIT_PCT`). The job-claim path returns 204 for a disk-blocked worker (sibling of the `disabled` short-circuit), and the deferral candidate pool skips it so jobs aren't stranded by being "deferred to a faster worker that won't claim." A new `disk full` badge in the WorkersTab Status column carries the explanation in its `title=` (UI-7-compliant icon-only-button). Belt-and-suspenders preflight in `client._wipe_broken_toolchain` short-circuits the #214 self-heal with one honest log line when free disk is < 1 GiB, so the worker stops looping itself into infinite ENOSPC. Tests: 4 hysteresis cases in `tests/test_registry.py` + a claim-refusal case in `tests/test_api.py` mirroring the existing `disabled` test. No protocol change, no `MIN_IMAGE_VERSION` bump — old workers benefit without redeploy because the disk-pct field has been in `SystemInfo` all along.

- [x] **#220** *(1.7.0-dev.62)* — broadened **#214**'s mid-run self-heal to cover every flavor of corrupted `pio-slot-N/` that surfaces in the home lab, not just `cc1: posix_spawnp ENOENT`. **Fresh repro:** a 21-job batch on hass-4 (2026-05-01) with failures spread across 4 workers (`AI-MacBook-Pro.localdomain` × 16, `docker-optiplex-5` × 3, `macdaddy.localdomain` × 1, `LENOVO-1` × 1) and 4 distinct symptoms — `Missing framework-arduinoespressif32-libs package` (the framework-libs dir is present but only carries metadata files, no library blobs), `tool-esptoolpy does not appear to be a Python project, as neither pyproject.toml nor setup.py are present` (PIO's `pip install -e <tool>` against a half-extracted package), `ModuleNotFoundError: No module named 'esptool.__init__'; 'esptool' is not a package` (#114's signature — esptool half-installed in `penv/.espidf-*/`), and `sh: 1: /esphome-versions/pio-slot-N/penv/bin/esptool: not found` → `Error 127` (penv binary outright missing). Pre-fix #214 only matched cc1, so all four landed in plain `failed` with no retry. **Fix:** new `_BROKEN_PIO_SIGNATURES` regex tuple + `_is_broken_pio_state(log)` helper in `client.py` (one place to add new symptoms, replaces both inline `cc1_signature` checks), and `_wipe_broken_toolchain` now removes both `pio-slot-N/packages/` *and* `pio-slot-N/penv/` so penv-side rot is recovered too. Disk-pressure guard, `dist/` preservation (so re-extract doesn't re-download), and the one-shot in-process retry are all unchanged. Tests in `tests/test_client.py`: 6 parametrized lab-failure log excerpts (cc1, missing framework-libs, not-a-Python-project, ModuleNotFoundError-esptool, penv/bin/...-not-found+Error-127, generic firmware.bin Error 127) all match the new helper; 5 negative cases (clean compile, user component error, OTA error, user C++ error, unrelated `make` Error 127) don't; and the wipe tests now assert both subtrees are removed while `dist/` + `appstate.json` siblings survive. **Closes #114** — the same self-heal mechanism handles its symptom, no upstream-PIO changes needed. **Live verification (2026-05-01):** re-queued the 21 originally-failed targets on the dev.62 workers — 19 passed on first attempt (most landing on the same `AI-MacBook-Pro.localdomain` worker that previously had a 16-fail streak; the in-process self-heal wiped the corrupted slot subtrees and the retry succeeded mid-job, no operator intervention), 2 stragglers (`screek-2a-5` hit a transient `Failed to install Python dependencies into penv` from the espidf-venv bootstrap step + `seplos-bms-1` lost its claimed worker mid-compile after AI-MacBook-Pro briefly went offline 3× in a row); both passed on a single re-queue. Final result 21/21 green vs. 0/21 pre-fix.

- [x] **#221** *(1.7.0-dev.63 + 1.7.0-dev.64)* — Queue-tab row actions reshaped to the user's mental model. Inline buttons are now **Rerun · Clear · Log** (Log was hamburgered between #209 and #221; bringing it back as a peer of Rerun/Clear matches the frequency with which users open logs to investigate failures or successes — burying it behind the hamburger added friction with no real benefit). The hamburger keeps **Cancel** (under "Job", destructive — shouldn't be a one-click action on every in-progress row), the **Download firmware** group when applicable, and the full **Device** group via the shared `DeviceMenuSection`. **Edit YAML** moved out of the "Job" group and into the bottom of the Device group (closes #216) — Edit YAML is conceptually a device-targeted action, and grouping it with Live Logs / Restart / Ping / Install to address makes the hamburger read coherently. `DeviceMenuSection` gained an optional `onEdit` prop so the Devices tab (which has its own inline Edit button) doesn't duplicate the entry. When the row's target was deleted from the config dir, Edit YAML falls back to a "Job" group entry so the user can still open the editor (the file might be restorable from `.archive/`). Existing e2e tests that drove the hamburger ("View log" → click) updated to drive the inline Log button instead; all 188 mocked Playwright specs stay green against the new layout. **dev.64 follow-up:** the live `e2e-hass-4/cyd-office-info.spec.ts` was missed in the dev.63 push (still drove `getByRole('menuitem', {name:'View log'})`); fixed in dev.64. Subsequent hass-4 smoke retries on dev.64 hit an unrelated environmental block — the `cyd-on-mac` routing rule routes cyd-tagged compiles to `macdaddy.localdomain`, which was busy compiling another fleet target during the smoke window — so the `compile runs to completion` step waited the full `COMPILE_BUDGET_MS` budget for an idle macos worker that never freed up. Not a regression in #221; the inline Log button was clicked successfully and the modal opened. Mocked specs (188/188) prove the menu layout end-to-end without depending on a live worker pool.

- [x] **#222** *(1.7.0-dev.63)* — Tag-filter pill bar on the Devices and Workers tabs flipped from OR-logic to AND-logic. Pre-fix, clicking `kitchen` then `prod` showed *every* device tagged kitchen plus *every* device tagged prod (the union); the user's mental model was the intersection — "narrow this to kitchen-AND-prod devices." The new behaviour matches: clicking each successive pill narrows the visible set. Code-side, both `DevicesTab.filteredVisible` and `WorkersTab.filteredWorkers` swapped `tagsOnRow.some(t => selectedSet.has(t))` for `selectedTags.every(t => tagsOnRowSet.has(t))`. `TagFilterBar.tsx` docstring rewritten to call out the AND semantics. New regression spec `e2e/tag-filter-and.spec.ts` clicks `kitchen` (both kitchen-tagged fixtures visible), then clicks `cosy` (only Living Room Sensor — which carries both kitchen + cosy — survives), proving the second click narrowed rather than widened the set.

- [x] **#223** *(1.7.0 — addressed)* — Looks like the client on docker-pve died. Investigated and resolved as part of the disk-pressure self-pause work in #219 + the broadened `pio-slot-N/` self-heal in #220 (the docker-pve worker hit ENOSPC; #219 now blocks claims when free disk < 5 % and surfaces the "disk full" badge so operators see the cause rather than silent failures).

- [x] **#226** *(1.7.0-dev.70)* — Cold-start disk-quota gap: any worker that booted *over* its server-pushed quota stayed over until its next compile. Surfaced live during release-prep DQ verification on hass-4 dev.69: LENOVO-1 at 25.75/10 GiB (+158%), OPTIPLEX-7 31.25/10 GiB (+212%), docker-optiplex-5 27.81/10 GiB (+178%) — all `last_eviction_freed_bytes=0`. Root cause in `_run_disk_quota_sweep` trigger logic: startup sweep returned early because the env-var quota is `None` for every worker except `WORKER_DISK_QUOTA_GB` setters, then the first heartbeat transition `None → 10 GiB` failed the `prev_quota is not None and new_quota < prev_quota` check, so no sweep fired. Fix: in the heartbeat handler (`client.py:1004`), the `prev_quota is None` first-arrival case now fires `_run_disk_quota_sweep(label="initial-quota")` immediately so an over-budget worker self-evicts on its first heartbeat instead of waiting for its first post-job sweep. The lowered-quota path (`elif new_quota < prev_quota`) is unchanged. (beads ef-a5l.)

- [x] **DQ.12** *(1.7.0-dev.70 — pulled forward from "deferred")* — Settings drawer field for the fleet-wide default disk quota. New `IntRow` in the `Disk management` section between `Job log retention (days)` and `Timeouts`, labelled "Worker disk quota — fleet default (GiB)" with help text explaining the byte budget covers ESPHome venvs + PlatformIO toolchains + per-target build caches. Range 1..1024 GiB to match the backend `_validate_int_range(1 GiB, 1 TiB)`. Stored as bytes on the wire (`default_worker_disk_quota_bytes`), exposed as integer GiB in the UI via `Math.round(bytes / 1024**3)` on read and `gib * 1024**3` on commit. Required adding the field to the `AppSettings` TypeScript interface (`src/api/client.ts`) — the backend has surfaced the field since DQ.1 (1.7.0-dev.67), only the UI's narrow type was missing. Pulled forward from "deferred per plan" because shipping a quota system without a UI to change the fleet default is dishonest UX — operators would have to `curl PATCH /ui/api/settings` to retune. (beads ef-qat.)

- [x] **#224** *(1.7.0-dev.65 + 1.7.0-dev.66)* — CI `test` job has been red on every push since 2026-05-01 with `ERROR: Failed to build 'PyYAML' when getting requirements to build wheel` deep inside pip's resolver. Root cause: `aioesphomeapi` 44.23.0 was published 2026-04-30T04:33:44Z, ~50 min after our last green CI. `esphome` 2026.4.3 pins `aioesphomeapi` to 44.16.1 exactly. The CI install step listed both `aioesphomeapi` and `esphome` unconstrained in a single `pip install`, so pip resolved aioesphomeapi to the new latest (44.23.0) first, then had to backtrack through every esphome version trying to find one that accepts it — eventually hitting esphome 2023.6.5 which pins `PyYAML` to 6.0, an sdist-only release whose `setup.py` calls `cython_sources` and fails to build under Python 3.12 / modern Cython (`AttributeError: 'build_ext' object has no attribute 'cython_sources'`). Fix: split the install into two pip passes — `pip install esphome` first so its strict transitive pins win, then `pip install pytest … aioesphomeapi …`; pip without `--upgrade` keeps already-installed packages, so the second pass leaves aioesphomeapi at 44.16.1. Verified locally in a fresh Python 3.12 venv: pre-fix install reproduces the PyYAML 6.0 sdist build failure; post-fix install succeeds with `aioesphomeapi-44.16.1` + `esphome-2026.4.3` + all test extras and zero backtracking. **dev.66 follow-up:** with the install fixed, CI ran the full suite and surfaced one masked-by-the-install-failure regression: `test_claim_job_disk_blocked_worker_returns_204` (added in #219) wraps the first claim attempt (which expects 204) but not the second claim attempt (which expects 200). The success-path claim hits the real `create_bundle_async`, which tries to read `device.yaml` from a `tmp_path` that doesn't contain it, raising `FileNotFoundError` → 500. Fix: wrap the second `ta.get(...)` in the same `with patch("api.create_bundle_async", new=AsyncMock(return_value=_make_test_bundle())):` block the rest of `test_api.py`'s success-path claims use. Verified locally: 69/69 in `test_api.py` pass.