{
  "config": {
    "flow_title": "ESPHome Fleet",
    "step": {
      "user": {
        "title": "Connect to ESPHome Fleet",
        "description": "Enter the base URL of your ESPHome Fleet add-on (for example `http://homeassistant.local:8765`) and the shared token from the add-on's Configuration tab.",
        "data": {
          "base_url": "Base URL",
          "token": "Add-on token"
        }
      },
      "discovery_confirm": {
        "title": "Set up {name}",
        "description": "Home Assistant found an ESPHome Fleet server at {url}. Confirm or edit the URL to finish setup.",
        "data": {
          "base_url": "Base URL",
          "token": "Add-on token"
        }
      },
      "reauth_confirm": {
        "title": "ESPHome Fleet needs a new token",
        "description": "The add-on at {url} is rejecting requests. Re-enter the shared token from Settings → Add-ons → ESPHome Fleet → Configuration.",
        "data": {
          "token": "Add-on token"
        }
      }
    },
    "error": {
      "invalid_url": "Enter a valid http(s) URL.",
      "cannot_connect": "Could not reach the ESPHome Fleet server at that URL. Check the URL, that the add-on is running, and that this host can reach it.",
      "token_required": "The add-on token is required. Copy it from Settings → Add-ons → ESPHome Fleet → Configuration."
    },
    "abort": {
      "already_configured": "This server is already configured.",
      "already_in_progress": "Setup is already in progress for this server via Supervisor discovery.",
      "invalid_discovery_info": "The Supervisor discovery record was missing host or port.",
      "reauth_successful": "The token was updated and the integration reloaded."
    }
  },
  "services": {
    "compile": {
      "name": "Compile",
      "description": "Enqueue an ESPHome firmware compile."
    },
    "cancel": {
      "name": "Cancel",
      "description": "Cancel a queued or running compile job."
    },
    "validate": {
      "name": "Validate",
      "description": "Run ESPHome config validation on a target YAML."
    }
  }
}
